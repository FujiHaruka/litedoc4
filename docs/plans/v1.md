# v1 — 何を満たしたら 1.0 と呼ぶか、そのために何が要るか

起票 2026-08-29。**この文書は棚卸しであって着手の宣言ではない。**
現況はすべてこの日に確認した — 「docs の『未』は腐る。項目を潰す前にまず現況を確認する」
(CLAUDE.md) に従い、下の数字はすべて 2026-08-29 に取り直したもの。

## 1. 出発点 — 今どこに居るか

| | 実測 (2026-08-29) |
|---|---|
| 最新タグ | **`v0.2.0`** (2026-08-29)。§3 A まで完了 |
| 木の版 | `Cargo.toml` の `workspace.package.version` = **`0.2.0`** (リリース無し) |
| 差分 | **221 commit / 339 ファイル / +36,697 −22,754 行** |
| IR schema | 木は **5** (`MIN_SCHEMA_VERSION = 5`、`irSchemaVersion tagged = 5`)。**`v0.1.4` のバイナリは schema 5 を拒否する** (`Cargo.toml` 冒頭の記録) |
| CI | main で緑 (2026-08-28、`ci.yml` 4 ジョブ / `ci-action.yml` / `ci-lake.yml`) |
| `cargo test --workspace` | 564 passed / 0 failed / 22 ignored (2026-08-28 の handoff、EXIT=0 をパイプ無しで取得) |
| open PR / open issue | **0 / 0** |
| star | **0** — **このツールは作者以外にまだ一度も使われていない** |

### 起票時に効いていた不整合 — README が「出していない製品」を書いていた【§3 A で解消】

`v0.1.4` のツリーには `watch.rs` / `cli.rs` / `queries.rs` / `stages.rs` / `ledger.rs` /
`deps_docs.rs` / `httpd.rs` が**無く**、`crates/litedoc4-render/web/` (TypeScript) も無い。
つまり **README が節を割いて説明している次のものは、どのリリースにも入っていない**:

- `litedoc4 watch` + ローカルサーバ
- `litedoc4.toml` (`title` / `index`)
- docstring の数式 → MathML
- `Used by` / `sorry` の可視化 / 属性の構造化 / 生成宣言のグルーピング
- `--deps-docs-map` (依存リンク先の選択)

**それでいて README は `@v0.1.4` に固定しろと案内していた。**
README の言うとおりに固定した利用者は、README に書いてある機能を持たないバイナリを手にする。
**これは v1 の話ではなく、その日壊れていた** — だから B〜D を待たずに `v0.2.0` を出した (§3 A)。

---

## 2. Approach — v1 の定義を「約束とその番人」に置く

v0.1 は「**ここまでを通した線**」だった (ゲート A/B の決着だけが根拠で、未検証項目は残した)。
v1 を同じ形で引くと「機能が増えた線」にしかならない。**そうしない。**

> **v1 = 作者以外が、作者の助けなしに、使い続けられる。**

「使える」ではなく「**使い続けられる**」に置くのが要点で、そこから 4 つの約束が出る。
**約束はそれを守るゲートを伴うものだけ書く** — 守れない約束を README に書くのは、
このリポジトリが 2026-08-17 以来ずっと潰してきた「見ていないのに緑」と同じ形だから。

| | 約束 | それを守るもの |
|---|---|---|
| **P1** | **出したものが README と一致する** | リリース手順そのもの。§3 A |
| **P2** | **1.x の間に何が動かないかが宣言されている** | 宣言が先。§3 B |
| **P3** | **その宣言は一度きりの実測ではなくゲートが守る** | §3 C |
| **P4** | **外から使われる経路が一度は通っている** | §3 D |

**この分け方を選ぶ理由**: A〜D はどれも「機能を足す」話ではない。
v1 に足りないものは機能ではなく、**機能が来年も同じ意味を持ち続ける仕組み**だから。
doc-gen4 の要望 8 件は 2026-08-22 に全件決着しており、**範囲外にしたもの**
(型検索 / LeanInk 的ソース閲覧 / 依存込み全文検索 / Verso 追随) は v1 でも範囲外のまま。

**撤退ライン**: §3 B で「約束を書けるものが 1 つも無い」に着地したら、それは
v1 を名乗る条件が揃っていないということなので、**版を 0.2.0 のまま出して v1 を先送りする**。
番号を先に決めて中身を合わせにいかない。

---

## 3. 項目

### A — 出荷の穴 (今日すでに壊れている) — **完了 2026-08-29**

**`v0.2.0` を出した。** 順序は A2 → A3 → A1 → A4。

- [x] **A2. タグを打つ前に、未実走の 4 ワークフローを `workflow_dispatch` で回した。**
      `release` (dry-run) / `ci-extractor-portability` (X3/X4/X5) /
      `ci-browser-windows` (U3) / `ci-placement` (same / split / report) — **4 本とも緑**
      【実測 2026-08-29、run 33219655542 / 33219658989 / 33219657381 / 33219660703】。
      **`.github/actions/setup-node` を入れた `575bb5a` (2026-08-28) 以降、この 4 本の実走は
      これが最初**
- [x] **A3. 固定先を `v0.2.0` に向けた** (`README` 3 箇所 + Status、`action.yml`、
      `lakefile.lean`、`ci-action.yml` の `released` job)。
      **併せて「Lean 4.31.x〜4.33.x」を実測した 3 版 (4.31.0 / 4.32.2 / 4.33.0) に狭めた** —
      `v4.33.1` が既に出ており、`4.33.x` は測っていない版を含んでいた
- [x] **A1. `v0.2.0` を出した。** `release.yml` 4 ジョブ緑、アセット 3 点
      (musl / darwin / `checksums.txt`)、`latest` も `v0.2.0`【実測、run 33220996042】。
      **released 経路を実バイナリで検証済み** — `ci-action` の
      `uses:@v0.2.0 — the released binary, not a cargo build` が
      `binary-source = release` を主張して緑、`ci-lake` の download 経路も緑
      【実測、run 33221141180 / 33221142721】
- [x] **A4. リポジトリのメタデータ。** description から **"— design stage"** を落とし、
      homepage に live example、topics 7 件
- [x] **リリースノートは自動生成 (`--generate-notes`) を捨てて最終状態だけ書いた** —
      221 commit の一覧は CLAUDE.md「user-facing は最終状態だけを書く」に反する。
      **`release.yml` は今も `--generate-notes` なので、次のリリースでも同じ手当てが要る**
      (直すなら workflow 側)

### B — 1.x の約束を決める (決めないと守れない)

**どれも「決める」であって「作る」ではない。** 決めた結果 README に 1 行増えるか、
「これは内部です」と書くだけで済むものが多い。

- [x] **B1. 公開するサブコマンドの線引き — 決着 2026-08-29。**
      判定規則は**「利用者が使うなら残す、使わないなら隠す」**【ユーザー判断】。
      **測った結果、利用者経路が呼ぶのは `build` 1 つだけ**だった —
      `action.yml` と `lakefile.lean` の `docs` script はどちらも `build` しか起動せず、
      README が走らせ方を書いているのは `build` と `watch` の 2 つ、
      残る 12 個 (`incremental` `modules` `links` `extract` `site` `render` `global`
      `ledger` `ownership` `merge` `impact` `prune`) を呼ぶのは
      `tools/*-gate.sh` とテストだけ【実測 2026-08-29】。
      **したがって隠した**: 引数無し / `--help` / `-h` は新設の `SUMMARY`
      (`build` と `watch` の synopsis + 12 個を名前だけ列挙 + `--help-all` への案内)、
      **全文は `--help-all` と各サブコマンド自身の `--help`**。
      **どれも消していない** — 消せばゲートが落ちる。
      **2 つとも一度落としてから通した**: `prune` を `SUMMARY` から抜くと
      `the_summary_names_every_subcommand_…` が名指しで落ち、
      `--frobnicate` を混ぜると `every_documented_flag_is_parsed` が
      `USAGE names flags no parser accepts: ["--frobnicate"]` で落ちる
      (**`SUMMARY` を `documented()` の走査対象に足したので、front door の文言も
      パーサ照合の中に入った**)
- [x] **B2/B3. 約束を決めて書き、番人を付けた — 完了 2026-08-29。**
      **固定するのは「外から見えるもの」だけ**【ユーザー判断】 — action の入出力 (10+4)、
      `litedoc4.toml` のキー (2)、`build` / `watch` のフラグ (16 / 13)、
      サイトのページパスとアンカー。**IR schema / ledger / `.lidx` は内部**
      (利用者は 1 つ pin するだけで抽出器とバイナリが同じ木から来る)。
      台帳は **`tools/public-surface.txt`**、番人は **`tools/public-surface-gate.sh`**
      (`ci.yml` の `test` ジョブが回す。バイナリもツールチェインも対象も要らない)。
      **一方向にしたのは意図的** — 足すのは誰も壊さない、消す / 改名が壊す。
      `action.yml` だけは両方向 (データ対データなので只)。
      **4 通りで落として通した**: 入力を消す / 台帳に無い入力が増える /
      約束したフラグが USAGE から消える / config のフィールドが改名される
- [x] **B4. Lean 版の方針 = C1 の台帳。** `tools/lean-toolchains.txt` に載っている版が
      「主張する版」であり、**載っていない版は名指しで落ちる**

### C — 約束を守るゲート (今は一度きりの実測しかない)

- [x] **C1. Lean 複数版のゲート — 完了 2026-08-29。**
      **着手前の実測で、想定より悪かった**: v4.31.0 / v4.32.2 は e2e が通るが、
      **v4.33.0 では `tools/e2e-micro.sh` 自体が落ちた** — GATE 8 が
      `implicit_reducible` を直書きしていて、Lean が改名した先を知らなかった
      (ローカル 3 版、実測)。**「3 版で建つ」は本当だったが「3 版でゲートが通る」は嘘だった。**
      直し方は版で分岐させることではなく **台帳** (`tools/lean-toolchains.txt`) —
      「この toolchain で Lean はこう綴る」を 1 行ずつ書き、
      **行の無い toolchain は名指しで落ちる**。
      新設: `.github/workflows/ci-lean-versions.yml` (マトリクスは**台帳から読む**ので
      2 つのリストが食い違いようがない) と `tools/lean-versions-gate.sh`
      (記録された改名だけを正規化して**それ以外の相違を残す**。無視リストにしない)。
      **v4.33.1 は落ちて値を持って帰った** — `UNMEASURED` の行は
      「Lean はここで `instance_reducible` と綴った。列 2 に書いて回し直せ」と言って落ちる。
      **結果 (実測 2026-08-29、run 33232645330)**: 4 版すべて e2e 緑、
      **12 の IR ファイルで一致** (記録された改名を当てたあとは相違ゼロ)。
      `leanVersion` が leg の toolchain と一致することも検査する —
      **同じ toolchain を 4 回入れたマトリクスが「一致」と報告する形**を塞ぐため
- [ ] **C2. `build-gate.sh` を実物で回す。** **「機材が無い」ではなかった** —
      `setup-clone.sh` は **APFS の clonefile (`cp -Rc`)** なので複製は CoW で、
      12 GB の `.lake` を二重に持つ必要はない。要るのは `REF_IR` / `REF_MODULES` /
      `.lidx` の生成
- [ ] **C3. README の性能表を現行コードで確認する。** (実行中)
- [x] **C4. CLAUDE.md の「8 ワークフロー」→ 7。** 直した (実測: `setup-node` を持つのは 7 本)

### D — 外から使われる経路 (v1 の意味そのもの)

- [x] **D1. R5「他人のリポジトリから `uses:` される」— 保留は腐っていた。既に起きている。**
      **`FujiHaruka/information-theory` (= 計測対象 `../lean-projects`) の
      `.github/workflows/docs.yml` が `uses: FujiHaruka/litedoc4@v0.1.4` を実行し、
      緑で Pages にデプロイしている**【実測 2026-08-29、run 32735626768 (main) /
      32726595468 (tag `v1.0.1`)、いずれも 2026-08-24】。
      **これは R5 が「覆われていない」と名指しした経路そのもの** —
      `GITHUB_WORKSPACE` が litedoc4 ではない木で、`lib: InformationTheory` /
      `lake-build: false` を渡し、`hashFiles()` も相対パスも `--lib` の見つけ方も
      その木の上で動いている。**検証用リポジトリを新設する話は最初から要らなかった。**
      CLAUDE.md「docs の『未』は腐る。項目を潰す前にまず現況を確認する」の再演で、
      **保留と書いた時点で既に実走していた**

  **dogfooding ワークフローについて【提案の評価】**: **R5 の穴は塞がらない。**
  litedoc4 の中に置くワークフローでは `GITHUB_WORKSPACE` が litedoc4 自身の
  checkout になり、それは `ci-action.yml` の `published` job が既に覆っている側。
  **そして litedoc4 は自分自身をドキュメント化できない** — `lakefile.lean` が持つのは
  `lean_exe extract` だけで `lean_lib` が無く、**自前の `docs` script が
  「declares no `lean_lib`, so there is nothing to document」で拒否する**【実測】。
  **別の価値ならある**: `e2e/micro` のサイトを HEAD から litedoc4 自身の Pages に出せば、
  **常に最新の実例**が持てる (micro は数式の失敗・scoped notation・BMP 外・`sorry` など
  「対象が持たない形」を構成として持つ)。**R5 の代わりではなく、D3 として別立て**

- [x] **D3. 実例サイト 2 本 — 完了 2026-08-29。両方やった**【ユーザー判断】。
      (a) 対象リポジトリ `information-theory` の `docs.yml` の pin を `v0.2.0` に上げ、
      Pages を再デプロイした (run 33232584431 緑)。**実測で確認**:
      `InformationTheory/Fano/Core.html` の `Used by` は **0 → 21 件**。
      (b) **litedoc4 自身の Pages** を有効にし、`.github/workflows/pages.yml` が
      `e2e/micro` を **HEAD から毎 push で**出すようにした →
      <https://fujiharuka.github.io/litedoc4/>。`tools/e2e-micro.sh` を通してから
      公開するので、**自分のゲートに落ちたサイトが serve されることはない**。
      **実測**: `Micro/Math.html` に MathML が **3 件**。
      対象は 5,079 docstring 中 3 span しか数式を持たないので、
      **数式を見せられるのはこちらの実例だけ**

### E — 「やらない」と書くもの (書かないと未検証と区別がつかない)

決着済みなので**作業ではない**。v1 の README に**明示する**かどうかだけが残っている。

| | 状態 |
|---|---|
| 検索は自パッケージのみ | 設計 (§5.6)。README に既出 |
| 非 GitHub forge | 意図的な拒否。`--source-url` が答え |
| プレビューモード / テーマ機構 / 型検索 / 依存込み全文検索 | 範囲外 (approach.md §9、feature-sweep §1) |
| 小 RAM Linux での実走 | **やらない**【決定 2026-08-22】。外挿で答えが出ている。再検討の入口は「olean 5.7 GB の全部が要るとは限らない」だけ |
| Windows / Intel macOS / Linux arm64 のバイナリ | 現在ソースビルド。**うち Linux/arm64 は要確認** — `release.yml` は「arm Linux ランナーがここに無い」と書いているが、**public リポジトリ向けの arm64 ランナーは現在ある可能性が高い**。1 ジョブの `workflow_dispatch` で決まる。CLAUDE.md「機材が無いのではなく持っている機材を使っていなかった」の再演を疑う対象 |

---

## 4. 判断が要るもの【ユーザー】

1. ~~**版番号**~~ — **決着。`v0.2.0` を先に出した** (2026-08-29)。`1.0.0` は B〜D のあと
2. ~~**D1 の保留を解くか**~~ — **決着。解くまでもなく既に起きていた** (§3 D1)
3. ~~**B1 の線引き**~~ — **決着。隠した** (§3 B1)
4. **D3 — 実例サイトの再生成** — 計測対象リポジトリの `docs.yml` の pin を上げてよいか
