# What must carry an attribution, and what that attribution must say.
#
# `docs/provenance.md` is the SoT for *which* third-party work is in this tree
# and *why* each obligation applies. This file is the machine-checkable half:
# one line per claim that document makes about a file, so that the claim cannot
# quietly stop being true. `tools/provenance-gate.sh` reads it.
#
# The obligations became live on 2026-08-16, when the repository went public and
# Apache-2.0 §4 fired (provenance.md §4). Every one of them was paid before that
# — this gate is what keeps them paid.
#
# Format: <path> : <substring that must appear somewhere in the file>
# A file may have several lines; all of them must hold.

# --- the two documents §4(a) and §4(b) are satisfied by ------------------
LICENSE : Apache License
LICENSE : Version 2.0, January 2004
NOTICE : doc-gen4
NOTICE : md4c
NOTICE : MD4Lean
NOTICE : Unicode Basic
NOTICE : V8
NOTICE : math-core
NOTICE : Böving
# §4(b) at the level of the work, not just the file (provenance.md §1.1).
#
# The string is English because README.md was rewritten for users on 2026-08-17
# and the notice moved language with it ("litedoc4 is a derivative work of
# doc-gen4"). **The notice itself never went away** — this line is not being
# relaxed to make the gate pass, it is being pointed at the same sentence in the
# language that sentence is now written in. The gate was red for four commits
# because nothing tied the two together.
README.md : derivative work
NOTICE : has been changed

# --- doc-gen4 (Apache-2.0): the files §4(b)(c) names -----------------------
extractor/Extract.lean : Böving
extractor/Extract.lean : Apache
crates/litedoc4-render/assets/style.css : Böving
crates/litedoc4-render/assets/style.css : Apache 2.0
crates/litedoc4-md/src/html.rs : doc-gen4
crates/litedoc4-md/src/escape.rs : doc-gen4
crates/litedoc4-render/src/code.rs : doc-gen4
crates/litedoc4-render/src/whitespace.rs : doc-gen4
crates/litedoc4-render/src/autolink.rs : doc-gen4
# The prototype renderer transcribes the five files above into Lean. The notice
# is on the whole file rather than per passage because the transcription is not
# split along their boundaries; `Böving` and not `doc-gen4`, because the file
# argues about doc-gen4's output throughout and the bare word would be matched by
# any of those sentences.
benchmarks/lean-prototype/Render.lean : Böving
benchmarks/lean-prototype/Render.lean : Apache-2.0
# The product-side transcription of the same five, split along their boundaries,
# so each module names the one Rust file it transcribes. `Böving` for the same
# reason as above: these files discuss doc-gen4 throughout.
#
# There is no row for `crates/litedoc4-md/src/math.rs`: it is math-core's work
# and not doc-gen4's, and the Lean side converts through MathML4Lean rather than
# transcribing it. What `Md/Html.lean` carries is `math_into`, which is html.rs.
src/Litedoc4/Md/Escape.lean : Böving
src/Litedoc4/Md/Escape.lean : Apache-2.0
src/Litedoc4/Md/Html.lean : Böving
src/Litedoc4/Md/Html.lean : Apache-2.0
src/Litedoc4/Render/Autolink.lean : Böving
src/Litedoc4/Render/Autolink.lean : Apache-2.0
src/Litedoc4/Render/Code.lean : Böving
src/Litedoc4/Render/Code.lean : Apache-2.0
src/Litedoc4/Render/Whitespace.lean : Böving
src/Litedoc4/Render/Whitespace.lean : Apache-2.0

# --- the other third-party works (provenance.md §7) -----------------------
# md4c is in the tree three times, byte-identical (provenance.md §7). Each copy
# is redistributed on its own — Lake builds `vendor/md4c/` from the package
# directory and never reaches into `crates/` — so each carries the MIT notice.
crates/litedoc4-md/vendor/md4c/LICENSE.md : MIT
crates/litedoc4-md/vendor/md4c/PROVENANCE.md : md4c
vendor/md4c/LICENSE.md : MIT
vendor/md4c/PROVENANCE.md : md4c
benchmarks/lean-prototype/vendor/md4c/LICENSE.md : MIT
benchmarks/lean-prototype/vendor/md4c/PROVENANCE.md : md4c
crates/litedoc4-md/src/parse.rs : MD4Lean
# The Lean transcription of that transliteration. Both claims, and neither is
# the word "MD4Lean": the file names MD4Lean a dozen times while arguing why it
# is not a dependency, so a gate looking for that word would be satisfied by the
# wrong sentence — the same trap `style.css` carries below.
src/Litedoc4/Md.lean : Jz Pan
src/Litedoc4/Md.lean : MIT
benchmarks/lean-prototype/Md.lean : Jz Pan
benchmarks/lean-prototype/Md.lean : MIT
crates/litedoc4-md/src/ffi.rs : md4c
crates/litedoc4-md/src/gc.rs : Unicode
# The Lean transcription of that table. `Dorais` and not `Unicode`, for the
# reason the two lines above this file's `MD4Lean` rows give: the word Unicode
# appears wherever the table is discussed.
src/Litedoc4/Md/Gc.lean : Dorais
src/Litedoc4/Md/Gc.lean : Unicode
crates/litedoc4-global/src/v8_gc.rs : V8
# The same shape one language over: `str::to_lowercase`'s answers, enumerated.
# Two claims, because two works are behind the table — Rust's mapping and
# Unicode's data — and a row for only one of them would go on passing after the
# other's name was dropped. Neither string is a bare `Rust` or a bare `Unicode`:
# the file's own prose says both words while explaining what it holds.
src/Litedoc4/Lower.lean : The Rust Project Developers
src/Litedoc4/Lower.lean : Unicode®, Inc.
# `str::to_lowercase` and not `The Rust Project Developers`: that name is already
# in NOTICE for the `unicode-ident` crate, so a row matching it would be
# satisfied by a paragraph about a different work.
NOTICE : str::to_lowercase
# math-core (MIT): the CSS its MathML needs, reproduced in §15b of the sheet.
# Two claims, because the file already carries doc-gen4's name and a gate that
# only looked for "MIT" would be satisfied by the wrong paragraph.
crates/litedoc4-render/assets/style.css : math-core
crates/litedoc4-render/assets/style.css : Charles Edward Gagnon
crates/litedoc4-md/src/math.rs : math-core

# --- generated fixtures whose origin has to stay recoverable --------------
# These are program output, not copied source, but "the record of provenance is
# required just the same — losing track of how to regenerate it does more real
# harm" (provenance.md §7).
crates/litedoc4-md/tests/data/PROVENANCE.md : doc-gen4
crates/litedoc4-render/tests/data/PROVENANCE.md : experiments-frozen
