# What this package says about its own site: the title and the index page.
#
# It is also what `tools/config-gate.sh` compares — with no file at all the four
# commands that write HTML agree trivially, and a gate that can only pass is not
# a gate.
title = "litedoc4 sample"
index = "docs/index.md"
