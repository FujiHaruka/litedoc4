# The corpus gate's inventory: every #[ignore]d test in the workspace.
#
# `tools/corpus-gate.sh --verify-list` (which CI runs) fails when this file and
# cargo disagree in either direction — a test that quietly stops being in the
# gate, and a gate that names a test nobody wrote, are the same bug seen from
# two ends. Adding an ignored test means adding a line here, on purpose.
#
# A test belongs here when it reads something outside the repository: the
# measurement target, its doc-gen4 tree, a generated IR tree, or one of the
# `--full` recordings.
#
# The name is `<cargo test target>::<test>`, because two names are each carried
# by two tests: `the_corpus_matches_the_prototype` (impact, merge) and
# `the_whole_corpus_matches_the_prototype` (autolink, fragment). This used to say
# `ledger, impact, merge`, and there has never been a `ledger` one — the
# arithmetic in the next line only ever worked with two (measured 2026-09-02).
# Bare names collapse the inventory from 21 entries to 19 (measured 2026-08-23).
# A trailing `# …` says what the test needs.
#
# Doctests are **not** in here, so `cargo test --workspace` reports one more
# ignored test than this file has entries: `crates/litedoc4/src/cli.rs`'s
# ```ignore``` sketch is an illustration, not a test with an input it cannot
# reach (measured 2026-08-23). The gate does refuse in both directions — deleting a
# line here fails with `ignored but not in the inventory`, checked by deleting
# one.

## corpus — runs when the target and its derivatives are on this machine

base_ir::reads_every_module_of_the_target_package     # LITEDOC4_BASE_IR
base_ir::astral_binders_slice_correctly               # LITEDOC4_BASE_IR
link_index_fixture::reads_the_dependency_closure_of_the_target_package  # LITEDOC4_LINK_INDEX; `litedoc4 extract --link-index`
pages::pages_carry_the_doc_gen4_trees_declarations    # LITEDOC4_DOCGEN4_TREE + LITEDOC4_IR
global::artifacts_match_the_reference                 # LITEDOC4_REFERENCE_GLOBAL + LITEDOC4_IR
global::corpus_facts_match_the_prototype              # LITEDOC4_IR
state_and_delta::the_seven_states_agree_over_the_real_corpus  # LITEDOC4_IR
state_and_delta::the_delta_is_the_prototypes_delta            # LITEDOC4_IR + LITEDOC4_REFERENCE_GLOBAL
state_and_delta::the_curated_tokens_reach_what_the_corpus_does_not  # LITEDOC4_IR
merge::the_corpus_matches_the_prototype               # LITEDOC4_BASE_IR + LITEDOC4_MERGE_FIXTURES
merge::the_dependency_slices_are_the_from_scratch_bytes        # same
litedoc4::packages::tests::every_root_matches_doc_gen4s_own_blob_urls   # LITEDOC4_TARGET + LITEDOC4_DOCGEN4_TREE
litedoc4::packages::tests::a_line_range_is_the_anchor_doc_gen4_writes   # same
litedoc4::packages::tests::every_lidx_entry_matches_doc_gen4s_declaration_urls  # LITEDOC4_LINK_INDEX + LITEDOC4_DECL_URLS; the latter from benchmarks/tools/extract-decl-source-urls.sh

## frozen — no regenerator in HEAD; the generator is at tag `experiments-frozen`
#
# `fixtures/render/PROVENANCE.md` states it plainly: "There is
# no regenerator in this tree any more." Everything below needs something only
# the removed prototype could write, so running it can only ever panic. They stay
# in the inventory — they are real tests and they will run again if somebody
# restores the generator — but the gate does not attempt them, because a
# permanently red gate is one nobody reads.
#
# **Two kinds, and the second was only recognised on 2026-08-16**:
#
#   1. `--full` recordings the prototype's renderer made (5.6 / 29 / 77 MB).
#   2. **Trees the prototype wrote, which `/private/tmp` has since emptied** —
#      `m1/ref-pages` (its 432 rendered pages) and `w7h/base-state` (its state
#      file). The **directories are still there and empty**, so these four read
#      as "no input" rather than as a disagreement about bytes; the committed
#      fixtures they compare against are fine. Regenerating the input needs the
#      renderer at the tag, which is what puts them here.

autolink::the_whole_corpus_matches_the_prototype      # LITEDOC4_AUTOLINK_FULL (5.6 MB)
fragment::the_whole_corpus_matches_the_prototype      # LITEDOC4_FRAGMENT_FULL (29 MB)
page_parts::the_whole_corpus_carries_the_prototypes_content  # LITEDOC4_PAGE_PARTS_FULL (77 MB)
ref_pages::every_docstring_in_the_reference_pages_is_reproduced  # LITEDOC4_REF_PAGES = m1/ref-pages, emptied
pages::pages_carry_the_reference_trees_content        # LITEDOC4_REFERENCE_PAGES = m1/ref-pages, emptied
impact::the_corpus_matches_the_prototype              # LITEDOC4_PAGES = m1/ref-pages, emptied
state_and_delta::the_state_file_is_the_prototypes_bytes  # LITEDOC4_PROTOTYPE_STATE = w7h/base-state, emptied
