# The entrance refusals of `litedoc4`, frozen.
#
# Every row is one command line the program must refuse, the exit code it must
# refuse with, and the whole of what it must say. `tools/refusal-gate.sh` reads
# this file and nothing else, which is what lets it go on asking the question
# now that `crates/` and the Rust oracle are gone.
#
# **Minted from the Rust `litedoc4` at tag `rust-frozen`**, except for the
# `@case` and `argv` lines, which were written by hand — the mint filled in
# `exit` and the body and could therefore never invent a case or quietly drop
# one. **There is no minter any more**: a row that has to change is changed by
# hand, by someone who has read why it moved, because re-minting from the thing
# under test would replace the answer with the question.
#
# FORMAT
#   @usage         the `--help-all` block, stored once. Every exit-2 refusal
#                  ends with it, so the bodies below say `<usage>` in its place
#   @case <name>   a name that does not move; it is what a failure is reported by
#   argv <words>   split the way a shell splits it (`""` is an empty argument)
#   exit <n>       the exit code. Never 0 — this file is refusals
#   | <text>       one line of stderr. A bare `|` is an empty line, and a
#                  trailing `\s` is one trailing space (there is one, in
#                  `build-jobs-empty`, and an editor that trims it would
#                  otherwise change the expectation without saying so)
#
# `<dir>` is the gate's working directory, which is a different absolute path on
# every run. Three cases name it, and they are the only three here that look at
# the filesystem at all: `<dir>/root` and `<dir>/out` are two empty directories
# the gate makes, and `R` is a name that is deliberately not there.

@usage
| usage: litedoc4 build  --root <repo> --out <dir> [--link-index <file>]
|                        [--lib <Name>]... [--source-url <url>] [--full]
|                        [--deps-docs-url <Root>=<url>]...
|                        [--deps-docs-index <Root>=<url|path>]...
|                        (--extractor-bin <path> [--lake <path>] [--jobs <n>]
|                         | --extractor <program> [--extractor-arg <arg>]...)
|                        [--mode self|referrers|importers|all] [--max-rounds <n>]
|                        [--timings <file>]
|        litedoc4 incremental --ir <dir> --pages <dir> --ledger <file> --work <dir>
|                        --modules <file> --source-url <url> --link-index <file>
|                        --state <dir> [--make-link-index] [--root <repo>]
|                        [--deps-docs-map <file>]
|                        (--extractor <program> [--extractor-arg <arg>]...
|                         | --serve --extractor-bin <path> --target <repo>
|                           [--lake <path>] [--jobs <n>])
|                        [--mode self|referrers|importers|all] [--max-rounds <n>]
|                        [--timings <file>]
|        litedoc4 watch  --root <repo> --out <dir> [--port <n>] [--interval <ms>]
|                        [--lib <Name>]... [--source-url <url>]
|                        (--extractor-bin <path> [--lake <path>] [--jobs <n>]
|                         | --extractor <program> [--extractor-arg <arg>]...)
|                        [--mode self|referrers|importers|all] [--max-rounds <n>]
|        litedoc4 modules --root <repo> [--lib <Name>]... [--out <file>]
|        litedoc4 extract --modules <file> --ir-dir <dir> --timings <file>
|                        [--extractor-bin <path>] [--target <repo>] [--lake <path>]
|                        [--events <file>] [--jobs <n>]
|                        [--link-index <file> [--link-index-omit <file>]
|                         [--link-index-key <token>]]
|        litedoc4 site   --ir <dir> --out <dir> --source-url <url>
|                        (--link-index <file> | --no-link-index)
|                        [--root <repo>] [--lake <path>] [--deps-docs-map <file>]
|                        [--state <dir>] [--timings <file>]
|        litedoc4 render --ir <dir> --pages <dir> --source-url <url>
|                        (--link-index <file> | --no-link-index)
|                        [--root <repo>] [--lake <path>] [--deps-docs-map <file>]
|                        [--only <Module>]... [--only-from <file>]
|        litedoc4 global --ir <dir> --out <dir> [--state <dir>] [--root <repo>]
|                        [--before <map.json>] [--print-set <file>]
|                        [--delta-json <file>] [--timings <file>]
|        litedoc4 ledger build --modules <file> --target <repo> --out <ledger.json>
|                        [--algorithm sha256|lake] [--concurrency <n>]
|                        [--ir <dir>] [--source-url <url>] [--link-index <file>]
|                        [--root <repo>] [--lake <path>] [--deps-docs-map <file>]
|                        [--timings <file>]
|        litedoc4 ledger check --ledger <ledger.json> [--modules <file>]
|                        [--algorithm sha256|lake] [--concurrency <n>] [--ir <dir>]
|                        [--source-url <url>] [--link-index <file>]
|                        [--root <repo>] [--lake <path>] [--deps-docs-map <file>]
|                        [--changed-out <file>]
|                        [--removed-out <file>] [--render-all-out <file>]
|                        [--timings <file>]
|        litedoc4 ledger touch --ledger <ledger.json> --module <Module> [--out <file>]
|        litedoc4 ownership --base <ir> [--inc <ir>] [--removed <file>]
|                        [--exclude <file>] [--print-set <file>] [--json <file>]
|        litedoc4 merge --base <ir> [--inc <ir>] [--out <ir>] [--remove <file>]
|                        [--modules <file>] [--changed-out <file>] [--timings <file>]
|        litedoc4 merge --verify <ir> --against <ir>
|        litedoc4 impact --ir <dir> [--changed <Module>]... [--changed-file <file>]
|                        [--mode self|referrers|importers|all] [--census <file>]
|                        [--print-set <file>] [--json <file>]
|        litedoc4 prune --pages <dir> [--remove <file>] [--ir <dir>] [--dry-run]
|                        [--json <file>]
|        litedoc4 links  --root <repo> [--lake <path>] [--link-index <file>]
|                        [--deps-docs-map <file>] [--out <file>]
|
|   --root         (`build`, `modules`) the Lean package: the sources are globbed
|                  under it, its oleans are hashed, `lake env` runs inside it, and
|                  for `build` its git HEAD is where --source-url comes from.
|                  (`site`, `render`, `global`, `incremental`, `ledger`)
|                  optional, and for two things. One is the dependency link map:
|                  with it, every link into a dependency is that package's
|                  version-pinned GitHub blob URL, read out of its
|                  lake-manifest.json plus `lean --githash`; without it
|                  those links stay relative to pages this site does not write.
|                  The other is <root>/litedoc4.toml, which sets the site's title
|                  and the Markdown on its index page — the same file for every
|                  command, so the four that write HTML cannot disagree about
|                  what the package is called. It is **not** --target: that names the
|                  tree whose oleans are hashed. A ledger and the run it licenses
|                  have to agree about this flag, or the render key moves and
|                  every page is re-rendered. `build` has one by construction, so
|                  its sites always carry the links.
|   --out          (`build`) the directory this command owns: <out>/site is the
|                  site, <out>/{ir,state,work} the caches, <out>/ledger.json the
|                  ledger. Required, with no default — <root>/.lake/build/doc is
|                  doc-gen4's own output tree — and it may not be inside --root
|   --port         (`watch`) the port the site is served on (default 8484). A
|                  port that is taken is refused by name, never moved to the next
|                  free one: an address that changes between runs leaves the tab
|                  you already have open pointing at nothing.
|   --interval     (`watch`) how often the loop asks the ledger, in milliseconds
|                  (default 1000, minimum 100). `watch` **does not run `lake
|                  build`** — run that in another window and it notices the oleans
|                  it writes. It acts only after one quiet interval, so a build
|                  still writing oleans is never extracted mid-flight, and it says
|                  so while it waits.
|   --full         (`build`) regenerate everything, ignoring what is under --out.
|                  The escape hatch for an input no ledger key covers. The
|                  dependency map is not one: its bytes are in renderKey, so a
|                  map that moved re-renders on its own.
|   --ir           an IR tree written by the extractor (schema 5)
|   --ir-dir       (`extract`) where the extractor writes that tree. Required,
|                  with no default
|   --pages        where the pages go; directories are created
|   --source-url   https://host/owner/repo/blob/<40-hex-rev>. `incremental`
|                  checks the 40 hex digits; `render` and `site` do not.
|   --link-index   the dependency closure's name -> module map (.lidx). Its
|                  SHA-256 is part of renderKey: a map that moved re-renders every
|                  page (150 of the target's 432 change bytes).
|                  For `build` it is optional — left out, the map is this
|                  command's own <out>/link-index.lidx, written by the extractor
|                  from the environment it imported anyway. Given, it is an
|                  input and is never written to. For `extract` it asks the
|                  extractor to write one.
|   --deps-docs-url  (`build`) <Root>=<url>: link that dependency's declarations
|                  at the documentation site it already publishes, e.g.
|                  Mathlib=https://leanprover-community.github.io/mathlib4_docs.
|                  Repeatable, off by default, and **verified**: a name that site's
|                  declaration table holds gets the docs page, one it does not
|                  keeps the version-pinned source, and a table that will not read
|                  sends the whole root to the source. There is no fallback on a
|                  404 — a build cannot see one. A <Root> that is not a dependency
|                  of --root is exit 3.
|   --deps-docs-index  (`build`) <Root>=<url|path>: where that site's declaration
|                  table is. Default <url>/declarations/declaration-data.bmp. A
|                  local path is read as a file, which is how a run with no
|                  outbound network uses this.
|   --deps-docs-map  (`site`, `render`, `incremental`, `ledger`, `links`) the
|                  resolved map `build` wrote under <out>/work. It carries the base
|                  URL and the verified names, so the commands that render cannot
|                  disagree about the links (the reason there is no --title).
|                  Nothing here reads a table or the network.
|                  It is an input to renderKey, so `ledger build` and `ledger
|                  check` need it for the same reason --root and --link-index are
|                  theirs: without it they compute a different key from the one the
|                  run that wrote the pages recorded, and then report a changed or
|                  unchanged key for a reason that is not true.
|   --link-index-omit  (`extract`, with --link-index) the modules whose own
|                  declaration groups are left out of that map, one name per
|                  line — normally the package's own module list. The renderer
|                  answers those names out of the IR before it reads the map, so
|                  the site is byte-identical; what changes is that the map stops
|                  moving when the package is edited, and with it renderKey.
|                  Module names still appear in the map's `@` section.
|                  `incremental --serve` passes its own --modules here.
|   --link-index-key  (`extract`, with --link-index) an opaque token standing for
|                  everything about that map the extractor cannot see: the oleans
|                  behind the imported modules, and the omit list's bytes. With
|                  it, a map whose sidecar <file>.key holds the same token and
|                  whose `@` section still matches the environment is left
|                  untouched — no scan, no write (1.20-1.81 s of a 6.2 s one-module
|                  incremental build on the measurement target). Anything less than
|                  a full match rewrites both. `build` and `incremental --serve`
|                  compute their own; here it is passed through verbatim.
|   --make-link-index  (`incremental --serve`) the resident extractor writes
|                  --link-index instead of reading it
|   --work         (`incremental`) the round's scratch directory. Everything in
|                  it is a diagnostic: the pipeline writes it and reads none of
|                  it back.
|   --extractor    (`incremental`) the extraction program, called as
|                  `<program> [<extractor-arg>...] --modules <list>
|                  --ir-dir <dir> --timings <file>`. No default, and the seam is
|                  what lets the pipeline be tested without Lean. Exclusive with
|                  --serve.
|   --extractor-arg  one argument for it, before those three; repeatable
|   --serve        (`incremental`) one resident Lean environment for the whole
|                  run instead of one process per round: imported at the first
|                  round that extracts, released on the way out of the loop.
|                  There is no --serve-dir — a server this run did not start is
|                  one whose olean generation it cannot vouch for.
|   --max-rounds   how many extract/ownership/merge rounds may run (default 5).
|                  Reaching it with modules still stale is exit 5.
|   --root         (`modules`) the repository the sources are globbed under
|   --lib          (`build`, `modules`) a library root: <Name>.lean and <Name>/;
|                  repeatable. Left out, the names come from <root>/lakefile.toml's
|                  [[lean_lib]] blocks; a lakefile.lean is refused by name, because
|                  reading it honestly means elaborating it with Lake
|   --extractor-bin  (`extract`, `incremental --serve`) the Lean extractor built
|                  by extractor/build.sh, or $EXTRACT_BIN. No default: it is built
|                  against the target's toolchain, so a baked-in path would be
|                  right on one machine
|   --target       (`extract`, `incremental --serve`) the Lean package to run
|                  inside, or $TARGET_REPO. It is opened read-only: an --ir-dir
|                  under it is refused, and its oleans are the generation every
|                  resident request is checked against
|   --lake         (`extract`, `incremental --serve`) the lake executable, or
|                  $LAKE (default: `lake`). Also (`build`, `site`, `render`,
|                  `ledger`) where the `lean` that answers `--githash` is looked
|                  for: its **sibling**, so `--lake ~/.elan/bin/lake` means
|                  `~/.elan/bin/lean`. That revision is Lean core's, the one
|                  dependency the manifest does not pin
|   --events       (`extract`) the extractor's phase events JSONL. Defaults to
|                  <timings without .json>-events.jsonl, which is what
|                  `incremental` relies on: it passes only --timings
|   --jobs         (`extract`, `incremental --serve`) extractor threads
|                  (default 1). It is the resident server's start-up
|                  configuration, so there is no per-request job count
|   --only         render only this module; repeatable
|   --only-from    render only the modules named in this file, one per line.
|                  An empty file renders nothing.
|   --out          the site root the six whole-package artifacts go under — for
|                  `site` the module pages go under it too — or the ledger file
|                  `ledger build` writes
|   --state        directory holding the contentHash cache (global-state.json).
|                  Without it every module is read: the from-scratch build.
|   --before       a previous declarations/name-map.json. Turns the delta on.
|   --print-set    the modules to re-render, one per line. An affected set that
|                  came out empty is an empty file, not a blank line.
|   --delta-json   the delta's diagnostic summary
|   --timings      one JSON line of counts and durations
|   --modules      the module list, one name per line; # comments are skipped.
|                  `ledger check` without it re-reads the ledger's own list and
|                  cannot see a module that appeared or vanished since `build`.
|                  For `merge` it is the order the merged index.json's modules
|                  come out in — a from-scratch extraction's, which is the order
|                  the extractor was handed the list in. A list that names other
|                  modules than the merged tree holds is exit 3, not a guess.
|   --target       the repository whose .lake/build/lib/lean holds the oleans.
|                  **Not** where the dependency link map comes from — that is
|                  --root, even for `ledger`, where on a real package the two name
|                  the same directory but on a hashed tree with no package behind
|                  it only one of them exists
|   --ledger       a ledger.json written by `ledger build`. `incremental` reads
|                  it and never rewrites it; `build` writes it back, after the
|                  last step that could fail.
|   --algorithm  sha256 hashes the olean bytes; lake reads the <file>.hash Lake
|                  already wrote. Defaults to sha256, and for `check` to the
|                  ledger's own.
|   --concurrency  olean reads in flight (default 1). The ledger's bytes do not
|                  depend on it.
|   --changed-out  the modules to re-extract, one per line
|   --removed-out  the modules that no longer have an olean, one per line
|   --render-all-out  why every page has to be re-rendered, one reason per line.
|                  Empty means the render set follows from the IR diff as usual.
|   --module       the module `ledger touch` invalidates
|   --base         the IR as it was before this round
|   --inc          the partial extraction's IR tree. Absent is a real case: a pure
|                  deletion re-extracts nothing.
|   --removed      modules that no longer exist, one per line (`ownership`)
|   --remove       the same list, spelled as the prototype spells it for `merge`
|   --exclude      modules already scheduled for re-extraction, one per line.
|                  They are fresh by definition and are never reported.
|   --verify       compare two IR trees; --against names the second
|   --changed      a module that changed; repeatable (`impact`)
|   --changed-file the same list in a file, one name per line
|   --mode         which modules the change reaches: self, referrers (direct),
|                  importers (the sound transitive bound, the default), or all —
|                  which is valid with an empty changed set and is what a moved
|                  render key selects
|   --census       a per-module TSV of |IMPORTERS| / |REFERRERS| / declarations
|   --pages        (`prune`) the page tree; nothing outside it is ever deleted
|   --dry-run      report what would be deleted and delete nothing
|
|

@case unknown-subcommand
argv frobnicate
exit 2
| litedoc4: unknown subcommand `frobnicate`
|
| <usage>

@case build-unknown-flag
argv build --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case watch-unknown-flag
argv watch --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case incremental-unknown-flag
argv incremental --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case modules-unknown-flag
argv modules --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case links-unknown-flag
argv links --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case extract-unknown-flag
argv extract --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case site-unknown-flag
argv site --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case render-unknown-flag
argv render --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case global-unknown-flag
argv global --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case ledger-unknown-subcommand-flag
argv ledger --frobnicate
exit 2
| litedoc4: unknown `ledger` subcommand `--frobnicate`
|
| <usage>

@case ledger-build-unknown-flag
argv ledger build --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case ownership-unknown-flag
argv ownership --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case merge-unknown-flag
argv merge --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case impact-unknown-flag
argv impact --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case prune-unknown-flag
argv prune --frobnicate
exit 2
| litedoc4: unknown argument `--frobnicate`
|
| <usage>

@case site-out-needs-a-value
argv site --out
exit 2
| litedoc4: --out needs a value
|
| <usage>

@case build-jobs-not-a-number
argv build --jobs four
exit 2
| litedoc4: --jobs wants a number, not four
|
| <usage>

@case build-jobs-empty
argv build --jobs ""
exit 2
| litedoc4: --jobs wants a number, not\s
|
| <usage>

@case build-jobs-fractional
argv build --jobs 4.5
exit 2
| litedoc4: --jobs wants a number, not 4.5
|
| <usage>

@case build-jobs-negative
argv build --jobs -1
exit 2
| litedoc4: --jobs wants a number, not -1
|
| <usage>

@case build-jobs-needs-a-value
argv build --jobs
exit 2
| litedoc4: --jobs needs a value
|
| <usage>

@case site-colour
argv site --colour
exit 2
| litedoc4: unknown argument `--colour`
|
| <usage>

@case site-no-link-index-choice
argv site --ir A --out B --source-url U
exit 2
| litedoc4: pass --link-index <file>, or --no-link-index to say so on purpose: without the dependency map 150 of the target package's 432 pages change bytes
|
| <usage>

@case site-both-link-index
argv site --ir A --out B --source-url U --link-index L --no-link-index
exit 2
| litedoc4: pass --link-index <file>, or --no-link-index to say so on purpose: without the dependency map 150 of the target package's 432 pages change bytes
|
| <usage>

@case site-only
argv site --only M
exit 2
| litedoc4: --only is not a `site` flag: full generation renders every module, which is what makes it full. Use `litedoc4 render --only ...` for a subset
|
| <usage>

@case site-only-from
argv site --only-from F
exit 2
| litedoc4: --only-from is not a `site` flag: full generation renders every module, which is what makes it full. Use `litedoc4 render --only-from ...` for a subset
|
| <usage>

@case site-before
argv site --before D
exit 2
| litedoc4: --before is not a `site` flag: the map delta names the pages an incremental round has to re-render, and this command re-renders all of them. Use `litedoc4 global --before ...`
|
| <usage>

@case site-print-set
argv site --print-set F
exit 2
| litedoc4: --print-set is not a `site` flag: the map delta names the pages an incremental round has to re-render, and this command re-renders all of them. Use `litedoc4 global --print-set ...`
|
| <usage>

@case site-delta-json
argv site --delta-json F
exit 2
| litedoc4: --delta-json is not a `site` flag: the map delta names the pages an incremental round has to re-render, and this command re-renders all of them. Use `litedoc4 global --delta-json ...`
|
| <usage>

@case site-pages
argv site --pages P
exit 2
| litedoc4: `site` writes the pages and the six whole-package artifacts into one tree: name it with --out
|
| <usage>

@case site-ir-required
argv site
exit 2
| litedoc4: --ir is required
|
| <usage>

@case site-out-required
argv site --ir A
exit 2
| litedoc4: --out is required
|
| <usage>

@case site-source-url-required
argv site --ir A --out B --link-index L
exit 2
| litedoc4: --source-url is required: doc-gen4 reads it from lake plus git, and it is not in the IR
|
| <usage>

@case render-ir-required
argv render
exit 2
| litedoc4: --ir is required
|
| <usage>

@case render-pages-required
argv render --ir A
exit 2
| litedoc4: --pages is required
|
| <usage>

@case render-source-url-required
argv render --ir A --pages P --link-index L
exit 2
| litedoc4: --source-url is required: doc-gen4 reads it from lake plus git, and it is not in the IR
|
| <usage>

@case global-ir-required
argv global
exit 2
| litedoc4: --ir is required
|
| <usage>

@case global-out-required
argv global --ir A
exit 2
| litedoc4: --out is required
|
| <usage>

@case ledger-needs-a-subcommand
argv ledger
exit 2
| litedoc4: ledger needs a subcommand: build, check or touch
|
| <usage>

@case ledger-unknown-subcommand
argv ledger frob
exit 2
| litedoc4: unknown `ledger` subcommand `frob`
|
| <usage>

@case ledger-build-needs-three
argv ledger build
exit 2
| litedoc4: ledger build needs --modules <file>, --target <repo> and --out <ledger.json>
|
| <usage>

@case ledger-check-needs-ledger
argv ledger check
exit 2
| litedoc4: ledger check needs --ledger <ledger.json>
|
| <usage>

@case ledger-touch-needs-two
argv ledger touch
exit 2
| litedoc4: ledger touch needs --ledger <ledger.json> and --module <Module>
|
| <usage>

@case ledger-build-foreign-flag
argv ledger build --ledger L
exit 2
| litedoc4: --ledger is not a flag of `ledger build`: it belongs to `ledger check` / `ledger touch`
|
| <usage>

@case links-root-required
argv links
exit 2
| litedoc4: --root <repo> is required
|
| <usage>

@case links-link-index-needs-a-value
argv links --link-index
exit 2
| litedoc4: --link-index needs a value
|
| <usage>

@case ownership-bare
argv ownership
exit 2
| litedoc4: ownership needs --base <ir> and at least one of --inc <ir> / --removed <file>
|
| <usage>

@case ownership-base-alone
argv ownership --base A
exit 2
| litedoc4: ownership needs --base <ir> and at least one of --inc <ir> / --removed <file>
|
| <usage>

@case merge-bare
argv merge
exit 2
| litedoc4: merge needs --base <ir> and at least one of --inc <ir> / --remove <file>
|
| <usage>

@case merge-verify-needs-against
argv merge --verify A
exit 2
| litedoc4: merge --verify <ir> needs --against <ir>
|
| <usage>

@case impact-ir-required
argv impact
exit 2
| litedoc4: --ir is required
|
| <usage>

@case prune-bare
argv prune
exit 2
| litedoc4: prune needs --pages <dir> and at least one of --remove <file> / --ir <dir>
|
| <usage>

@case prune-pages-alone
argv prune --pages P
exit 2
| litedoc4: prune needs --pages <dir> and at least one of --remove <file> / --ir <dir>
|
| <usage>

@case incremental-ir-required
argv incremental
exit 2
| litedoc4: --ir is required
|
| <usage>

@case incremental-pages-required
argv incremental --ir A
exit 2
| litedoc4: --pages is required
|
| <usage>

@case incremental-ledger-required
argv incremental --ir A --pages P
exit 2
| litedoc4: --ledger is required
|
| <usage>

@case incremental-work-required
argv incremental --ir A --pages P --ledger L
exit 2
| litedoc4: --work is required
|
| <usage>

@case incremental-modules-required
argv incremental --ir A --pages P --ledger L --work W
exit 2
| litedoc4: --modules is required: without the current module list `check` re-reads the ledger's own and cannot see a module that appeared or vanished. `litedoc4 modules` writes it
|
| <usage>

@case incremental-source-url-required
argv incremental --ir A --pages P --ledger L --work W --modules M
exit 2
| litedoc4: --source-url is required: doc-gen4 reads it from lake plus git, and it is not in the IR
|
| <usage>

@case incremental-source-url-no-blob
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/x
exit 2
| litedoc4: --source-url has no `/blob/` segment: https://example.invalid/x
|   the acceptance oracle normalises `/blob/[0-9a-f]{40}/` and nothing else, so with a tag or a branch name here every page keeps its revision in the compared bytes and the score drops 3.1103 points with no diagnostic (measured)
|
| <usage>

@case incremental-source-url-not-hex
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/main/x
exit 2
| litedoc4: --source-url must carry a 40-digit lower-case hex revision after `/blob/`, not `main` (4 character(s))
|   the acceptance oracle normalises `/blob/[0-9a-f]{40}/` and nothing else, so with a tag or a branch name here every page keeps its revision in the compared bytes and the score drops 3.1103 points with no diagnostic (measured)
|
| <usage>

@case incremental-link-index-required
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/0123456789abcdef0123456789abcdef01234567/x
exit 2
| litedoc4: --link-index <file> is required, and there is no --no-link-index here: without the dependency map 150 of the target package's 432 pages change bytes
|
| <usage>

@case incremental-state-required
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/0123456789abcdef0123456789abcdef01234567/x --link-index L
exit 2
| litedoc4: --state <dir> is required: the whole-package derivation is always the cached one, and the map delta it feeds the renderer needs a cache to compare against. The previous run — full generation with `litedoc4 site --state`, or the last incremental round — is what leaves it behind
|
| <usage>

@case incremental-l3-1
argv incremental --l3-1 off
exit 2
| litedoc4: --l3-1 is not a pipeline flag: `off` was the ablation that measured L3-1's contribution and it produces a wrong site (a referring module keeps an IR naming the module a declaration used to live in). Ownership always runs
|
| <usage>

@case incremental-global
argv incremental --global old
exit 2
| litedoc4: --global is not a pipeline flag: `old` was stage 5's two-process derivation, kept only as the control of stage 7h's A/B. The product is always the cached one, which is why --state is required
|
| <usage>

@case incremental-serve-dir
argv incremental --serve-dir D
exit 2
| litedoc4: --serve-dir is not offered: `--serve` starts a server this run owns, and a server it does not own is one whose olean generation it cannot vouch for. Correctness comes from that generation and never from the round number — a server imported before the edit returns the pre-edit owner of every name that moved, and then no round is safe, including round 2 (measured, stage 6a)
|
| <usage>

@case incremental-serve-from
argv incremental --serve-from 2
exit 2
| litedoc4: --serve-from is not offered: it chose which rounds a server the caller owns was allowed to answer, and stage 6a measured that the round number is not what makes a round safe. With `--serve` the server is started inside this run, so every round is served and every round is checked against the same olean generation
|
| <usage>

@case incremental-count-reads
argv incremental --count-reads
exit 2
| litedoc4: --count-reads is a measurement tool, not a product flag: it wraps every stage to count IR reads and makes the timings meaningless
|
| <usage>

@case incremental-module
argv incremental --module M
exit 2
| litedoc4: --module is not a pipeline flag: the prototype's is a label that goes straight into the timings record and is read by nothing. A harness that needs one adds it to the line it appends
|
| <usage>

@case incremental-no-link-index
argv incremental --no-link-index
exit 2
| litedoc4: --no-link-index is not an incremental flag: a round re-renders a subset, so a page rendered without the map is indistinguishable from one that was not re-rendered at all — without the dependency map 150 of the target package's 432 pages change bytes
|
| <usage>

@case extract-serve
argv extract --serve
exit 2
| litedoc4: --serve is not an `extract` flag: residency is `litedoc4 incremental --serve`. A server that answers one request and stops is this command with a protocol in front of it — the environment is still imported once per extraction — so the only caller it can pay off for is the round loop, which owns the server for the whole run. `--serve-dir` is not offered anywhere: a server this process did not start is one whose olean generation it cannot vouch for, and that is where correctness comes from (measured)
|
| <usage>

@case extract-serve-dir
argv extract --serve-dir D
exit 2
| litedoc4: --serve-dir is not an `extract` flag: residency is `litedoc4 incremental --serve`. A server that answers one request and stops is this command with a protocol in front of it — the environment is still imported once per extraction — so the only caller it can pay off for is the round loop, which owns the server for the whole run. `--serve-dir` is not offered anywhere: a server this process did not start is one whose olean generation it cannot vouch for, and that is where correctness comes from (measured)
|
| <usage>

@case extract-serve-from
argv extract --serve-from 2
exit 2
| litedoc4: --serve-from is not an `extract` flag: residency is `litedoc4 incremental --serve`. A server that answers one request and stops is this command with a protocol in front of it — the environment is still imported once per extraction — so the only caller it can pay off for is the round loop, which owns the server for the whole run. `--serve-dir` is not offered anywhere: a server this process did not start is one whose olean generation it cannot vouch for, and that is where correctness comes from (measured)
|
| <usage>

@case extract-write-ir
argv extract --write-ir
exit 2
| litedoc4: --write-ir is not a flag here: it is always on. Those four are what "IR schema 5" means, and an IR written without one of them parses and renders wrongly rather than failing
|
| <usage>

@case extract-tagged-code
argv extract --tagged-code
exit 2
| litedoc4: --tagged-code is not a flag here: it is always on. Those four are what "IR schema 5" means, and an IR written without one of them parses and renders wrongly rather than failing
|
| <usage>

@case extract-equations
argv extract --equations
exit 2
| litedoc4: --equations is not a flag here: it is always on. Those four are what "IR schema 5" means, and an IR written without one of them parses and renders wrongly rather than failing
|
| <usage>

@case extract-refs
argv extract --refs
exit 2
| litedoc4: --refs is not a flag here: it is always on. Those four are what "IR schema 5" means, and an IR written without one of them parses and renders wrongly rather than failing
|
| <usage>

@case extract-no-attrs
argv extract --no-attrs
exit 2
| litedoc4: --no-attrs is an ablation, not a product flag: it subtracts one of three extractor additions so its cost can be measured, and the resulting index.json carries an `ablations` list precisely because the tree is not renderable
|
| <usage>

@case extract-decl-profile
argv extract --decl-profile
exit 2
| litedoc4: --decl-profile is a measurement or inspection flag of the extractor, not a product one. Run `extractor/build/extract` directly for it — the command line is in `Extract.lean`'s header
|
| <usage>

@case extract-modules-required
argv extract
exit 2
| litedoc4: --modules <file> is required: the module list to extract, one name per line
|
| <usage>

@case extract-ir-dir-required
argv extract --modules M
exit 2
| litedoc4: --ir-dir <dir> is required and has no default: an IR tree written somewhere the caller did not name is worse than none
|
| <usage>

@case extract-timings-required
argv extract --modules M --ir-dir D
exit 2
| litedoc4: --timings <file> is required: it is the extractor's phase timers folded into one JSON object, and `litedoc4 incremental` merges it into the run's record
|
| <usage>

@case extract-link-index-omit-alone
argv extract --modules M --ir-dir D --timings T --link-index-omit O
exit 2
| litedoc4: --link-index-omit without --link-index does nothing: it names the modules whose declaration groups are left out of the map, and no map is being written
|
| <usage>

@case extract-link-index-key-alone
argv extract --modules M --ir-dir D --timings T --link-index-key K
exit 2
| litedoc4: --link-index-key without --link-index does nothing: it is the token that lets the extractor leave an already-correct map alone, and no map is being written or read
|
| <usage>

@case extract-extractor-bin-required
argv extract --modules M --ir-dir D --timings T
exit 2
| litedoc4: --extractor-bin <path> is required (or EXTRACT_BIN): the Lean extractor built by `extractor/build.sh`, which is 171 MB and is therefore not committed. There is no default — the binary is built against the target's toolchain, so a path baked in here would be right on exactly one machine
|
| <usage>

@case build-root-required
argv build
exit 2
| litedoc4: --root <repo> is required: the Lean package to document
|
| <usage>

@case build-out-required
argv build --root R
exit 2
| litedoc4: --out <dir> is required and has no default: it is where the site, the IR, the cache and the ledger go. The obvious default would be <root>/.lake/build/doc, which is doc-gen4's own output tree — a default that overwrites another tool's output is a data-loss bug with a friendly face
|
| <usage>

@case build-port
argv build --root R --out O --port 8080
exit 2
| litedoc4: --port is a `watch` flag: `build` writes a site and exits, so there is nothing left running to serve it. `litedoc4 watch --root … --out … --port <n>` is the one that serves
|
| <usage>

@case build-interval
argv build --root R --out O --interval 500
exit 2
| litedoc4: --interval is a `watch` flag: it is how often the loop asks the ledger, and `build` asks once
|
| <usage>

@case build-deps-docs-map
argv build --root R --out O --deps-docs-map M
exit 2
| litedoc4: --deps-docs-map is not a `build` flag: this command resolves the documentation map itself, from --deps-docs-url, and writes it under <out>/work for `litedoc4 site` and `litedoc4 render` to read. A build that rendered against somebody else's resolved map would record its own map's digest in the ledger
|
| <usage>

@case build-ir
argv build --root R --out O --ir I
exit 2
| litedoc4: --ir is not a `build` flag: this command owns the layout under --out (<out>/ir, <out>/site, <out>/state, <out>/work, <out>/ledger.json) so that a second run can find what the first one left. Name the pieces yourself with `litedoc4 incremental`
|
| <usage>

@case build-pages
argv build --root R --out O --pages P
exit 2
| litedoc4: --pages is not a `build` flag: this command owns the layout under --out (<out>/ir, <out>/site, <out>/state, <out>/work, <out>/ledger.json) so that a second run can find what the first one left. Name the pieces yourself with `litedoc4 incremental`
|
| <usage>

@case build-ledger
argv build --root R --out O --ledger L
exit 2
| litedoc4: --ledger is not a `build` flag: this command owns the layout under --out (<out>/ir, <out>/site, <out>/state, <out>/work, <out>/ledger.json) so that a second run can find what the first one left. Name the pieces yourself with `litedoc4 incremental`
|
| <usage>

@case build-work
argv build --root R --out O --work W
exit 2
| litedoc4: --work is not a `build` flag: this command owns the layout under --out (<out>/ir, <out>/site, <out>/state, <out>/work, <out>/ledger.json) so that a second run can find what the first one left. Name the pieces yourself with `litedoc4 incremental`
|
| <usage>

@case build-state
argv build --root R --out O --state S
exit 2
| litedoc4: --state is not a `build` flag: this command owns the layout under --out (<out>/ir, <out>/site, <out>/state, <out>/work, <out>/ledger.json) so that a second run can find what the first one left. Name the pieces yourself with `litedoc4 incremental`
|
| <usage>

@case build-modules
argv build --root R --out O --modules M
exit 2
| litedoc4: --modules is not a `build` flag: the list is the source glob over the libraries (`litedoc4 modules`), and the same list has to reach detect, the extractor and merge or the merged index.json comes out in an order a from-scratch run would not have written. Choose the libraries with --lib
|
| <usage>

@case build-target
argv build --root R --out O --target T
exit 2
| litedoc4: --target is not a `build` flag: the package being documented is --root, and it is the same repository the sources are globbed from, the oleans are hashed in and `lake env` runs inside
|
| <usage>

@case build-no-link-index
argv build --root R --out O --no-link-index
exit 2
| litedoc4: --no-link-index is not a `build` flag: without the dependency map 150 of the target package's 432 pages change bytes, and a build command whose ordinary output is silently wrong on a third of its pages is not worth having. `litedoc4 render --no-link-index` still says it on purpose
|
| <usage>

@case build-serve
argv build --root R --out O --serve
exit 2
| litedoc4: --serve is not a `build` flag: with --extractor-bin this command *is* the resident path — one Lean environment for the whole run, started here and released after the last round. There is nothing to switch on, and a server this run did not start is one whose olean generation it cannot vouch for
|
| <usage>

@case build-serve-dir
argv build --root R --out O --serve-dir D
exit 2
| litedoc4: --serve-dir is not a `build` flag: with --extractor-bin this command *is* the resident path — one Lean environment for the whole run, started here and released after the last round. There is nothing to switch on, and a server this run did not start is one whose olean generation it cannot vouch for
|
| <usage>

@case build-serve-from
argv build --root R --out O --serve-from 2
exit 2
| litedoc4: --serve-from is not a `build` flag: with --extractor-bin this command *is* the resident path — one Lean environment for the whole run, started here and released after the last round. There is nothing to switch on, and a server this run did not start is one whose olean generation it cannot vouch for
|
| <usage>

@case build-max-rounds-zero
argv build --root R --out O --max-rounds 0
exit 2
| litedoc4: --max-rounds must be at least 1: round 1 is where deletions are folded in
|
| <usage>

@case build-jobs-zero
argv build --root R --out O --jobs 0
exit 2
| litedoc4: --jobs must be at least 1
|
| <usage>

@case build-extractor-needs-link-index
argv build --root R --out O --extractor P
exit 2
| litedoc4: --extractor <program> needs --link-index <file>: the dependency map is written by the Lean extractor out of the environment it imports, and --extractor names a program whose interface is `--modules --ir-dir --timings` and nothing else. Either pass a map (`litedoc4 extract --link-index <file>` writes one) or use --extractor-bin, where this command owns the extractor and derives the map itself. without the dependency map 150 of the target package's 432 pages change bytes
|
| <usage>

@case build-extractor-and-extractor-bin
argv build --root R --out O --extractor P --extractor-bin Q --link-index L
exit 2
| litedoc4: --extractor-bin and --extractor are exclusive: one names the Lean extractor this run keeps resident, the other names a program to call once per round. How that program finds its own binary is its own business
|
| <usage>

@case build-extractor-and-lake
argv build --root R --out O --extractor P --lake Q --link-index L
exit 2
| litedoc4: --lake and --extractor are exclusive: one names the Lean extractor this run keeps resident, the other names a program to call once per round. How that program finds its own binary is its own business
|
| <usage>

@case build-extractor-jobs
argv build --root R --out O --extractor P --link-index L --jobs 2
exit 2
| litedoc4: --jobs is a flag of the resident path: a resident extractor fixes its job count at start-up. Behind --extractor, pass it through with `--extractor-arg --jobs --extractor-arg <n>`
|
| <usage>

@case build-bad-mode
argv build --root R --out O --mode sideways
exit 2
| litedoc4: --mode takes self|referrers|importers|all, not `sideways`
|
| <usage>

@case build-mode-needs-a-value
argv build --root R --out O --mode
exit 2
| litedoc4: --mode needs a value
|
| <usage>

@case watch-port-not-a-number
argv watch --root root --out out --port banana
exit 2
| litedoc4: --port wants a number 1-65535, not `banana`
|
| <usage>

@case watch-port-out-of-range
argv watch --root root --out out --port 70000
exit 2
| litedoc4: --port wants a number 1-65535, not `70000`
|
| <usage>

@case watch-port-zero
argv watch --root root --out out --port 0
exit 2
| litedoc4: --port 0 asks the kernel for whatever is free, which is an address nobody can type. Name the port, or leave the flag out for the default
|
| <usage>

@case watch-interval-below-floor
argv watch --root root --out out --interval 10
exit 2
| litedoc4: --interval 10 is below 100 ms: a pass reads every olean of the package, which on the 422-module measurement target takes 2.25 s (measured, benchmarks/results/purelean-watch-2026-08-31.txt), so a shorter one spends a core asking a question whose last answer is still current
|
| <usage>

@case watch-interval-not-a-number
argv watch --root root --out out --interval soon
exit 2
| litedoc4: --interval wants a whole number of milliseconds, not `soon`
|
| <usage>

@case watch-full
argv watch --root root --out out --full
exit 2
| litedoc4: --full is not a `watch` flag: it means "regenerate everything, ignoring what is under --out", and a loop that did that every pass would never do anything else. Run `litedoc4 build --full` once, then start watching
|
| <usage>

@case watch-deps-docs-url
argv watch --root root --out out --deps-docs-url Foo=https://example.invalid
exit 2
| litedoc4: --deps-docs-url is not a `watch` flag: it resolves a dependency's declaration table over the network, once, against the IR tree of that run. A loop would either re-fetch 5.7 MB on every rebuild or serve pages resolved against an IR tree that has moved since. Use `litedoc4 build` for a site with documentation links
|
| <usage>

@case watch-deps-docs-index
argv watch --root root --out out --deps-docs-index F
exit 2
| litedoc4: --deps-docs-index is not a `watch` flag: it resolves a dependency's declaration table over the network, once, against the IR tree of that run. A loop would either re-fetch 5.7 MB on every rebuild or serve pages resolved against an IR tree that has moved since. Use `litedoc4 build` for a site with documentation links
|
| <usage>

@case watch-out-inside-root
argv watch --root root --out root/site
exit 3
| litedoc4: --out <dir>/root/site is inside --root <dir>/root: the package being documented is opened read-only and nothing is ever written into it — `litedoc4 extract` refuses an --ir-dir there for the same reason. Copy <out>/site into the repository afterwards if that is where the pages belong
|

@case build-out-inside-root
argv build --root root --out root/site
exit 3
| litedoc4: --out <dir>/root/site is inside --root <dir>/root: the package being documented is opened read-only and nothing is ever written into it — `litedoc4 extract` refuses an --ir-dir there for the same reason. Copy <out>/site into the repository afterwards if that is where the pages belong
|

@case incremental-serve-and-extractor
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/0123456789abcdef0123456789abcdef01234567/x --link-index L --state S --serve --extractor P
exit 2
| litedoc4: --serve and --extractor are exclusive: one names a program to run once per round, the other says this run owns a Lean environment for all of them. `--serve` is the resident path and it uses --extractor-bin, not a wrapper
|
| <usage>

@case incremental-make-link-index-needs-serve
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/0123456789abcdef0123456789abcdef01234567/x --link-index L --state S --make-link-index
exit 2
| litedoc4: --make-link-index is a flag of --serve: the dependency map is written by the Lean extractor out of the environment it imported for the extraction, and --serve is the path where this command spells that command line. Behind --extractor, the program is the one that decides — `litedoc4 extract --link-index <file>` writes it — and --link-index here names the file it wrote
|
| <usage>

@case incremental-extractor-bin-needs-serve
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/0123456789abcdef0123456789abcdef01234567/x --link-index L --state S --extractor-bin B --extractor P
exit 2
| litedoc4: --extractor-bin is a flag of --serve: without it the extraction is whatever --extractor names, and how that program finds its binary is its own business (`litedoc4 extract` takes --extractor-bin through --extractor-arg)
|
| <usage>

@case incremental-target-needs-serve
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/0123456789abcdef0123456789abcdef01234567/x --link-index L --state S --target T --extractor P
exit 2
| litedoc4: --target is a flag of --serve: without it the extraction is whatever --extractor names, and how that program finds its binary is its own business (`litedoc4 extract` takes --target through --extractor-arg)
|
| <usage>

@case incremental-lake-needs-serve
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/0123456789abcdef0123456789abcdef01234567/x --link-index L --state S --lake K --extractor P
exit 2
| litedoc4: --lake is a flag of --serve: without it the extraction is whatever --extractor names, and how that program finds its binary is its own business (`litedoc4 extract` takes --lake through --extractor-arg)
|
| <usage>

@case incremental-jobs-needs-serve
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/0123456789abcdef0123456789abcdef01234567/x --link-index L --state S --jobs 2 --extractor P
exit 2
| litedoc4: --jobs is a flag of --serve: parallelism is the extractor's, and a resident one fixes it at start-up. Behind --extractor, pass it through with `--extractor-arg --jobs --extractor-arg <n>`
|
| <usage>

@case incremental-jobs-zero
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/0123456789abcdef0123456789abcdef01234567/x --link-index L --state S --jobs 0 --serve
exit 2
| litedoc4: --jobs must be at least 1
|
| <usage>

@case incremental-needs-extractor-or-serve
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/0123456789abcdef0123456789abcdef01234567/x --link-index L --state S
exit 2
| litedoc4: one of --extractor <program> and --serve is required, and neither has a default: --extractor is called as `<program> [<extractor-arg>…] --modules <list> --ir-dir <dir> --timings <file>`, which is `litedoc4 extract`'s interface; --serve starts one resident Lean environment for the whole run and needs --extractor-bin and --target
|
| <usage>

@case incremental-bad-mode
argv incremental --ir A --pages P --ledger L --work W --modules M --source-url https://example.invalid/blob/0123456789abcdef0123456789abcdef01234567/x --link-index L --state S --serve --mode sideways
exit 2
| litedoc4: --mode takes self|referrers|importers|all, not `sideways`
|
| <usage>

@case modules-root-required
argv modules
exit 2
| litedoc4: --root <repo> is required
|
| <usage>

@case modules-no-lakefile
argv modules --root R
exit 3
| litedoc4: no lakefile.toml and no lakefile.lean under R: --root names a Lake package, and the library names come from its lakefile. Pass --lib <Name> to name them yourself
|

@case links-out-needs-a-value
argv links --out
exit 2
| litedoc4: --out needs a value
|
| <usage>

@case extract-jobs-zero
argv extract --modules M --ir-dir D --timings T --jobs 0
exit 2
| litedoc4: --jobs must be at least 1
|
| <usage>

@case ledger-check-foreign-flag
argv ledger check --modules M --target T
exit 2
| litedoc4: --target is not a flag of `ledger check`: it belongs to `ledger build`
|
| <usage>

@case ledger-touch-foreign-flag
argv ledger touch --ledger L --module M --target T
exit 2
| litedoc4: --target is not a flag of `ledger touch`: it belongs to `ledger build`
|
| <usage>
