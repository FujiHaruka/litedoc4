[package]
name = "litedoc4-testutil"
license.workspace = true
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
publish.workspace = true

# Deliberately none, and not only "none yet". The helpers here exist because
# fifteen copies of the same thirty lines were cheaper to fold than to keep
# equal — not because a dependency was hard to add. See src/lib.rs.
[dependencies]

[lints]
workspace = true
