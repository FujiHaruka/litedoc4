# 検証ログ

[`approach.md`](approach.md) §7 の検証段階の結果を記録する。

> **2026-08-24 に docs の大半を削除した**【決定、ユーザー判断】 — 実装計画・実装ログ・
> 完遂した計画文書 19 本。**本文の数字は書き換えていない**が、**それらへのリンクは外した**
> (辿れない先を指さないため)。読むなら git 履歴。生ログは `benchmarks/results/` に残っている。

**この文書が数字の SoT。** 計画側の予測と食い違ったら、計画を直す (逆をやらない)。
各行の数字には CLAUDE.md「計測の誠実性」の 4 ラベル (実測 / 外挿 / 仮定 / 理論値) を付ける。

> **この文書に出てくる `experiments/...` は、すべて tag `experiments-frozen` の中を指す。**
> プロトタイプは 2026-08-16 に HEAD から撤去した (→ README「撤去したプロトタイプ」)。
> `git show experiments-frozen:experiments/stage4c/coverage.ts` の形で読める。
> **本文は 1 文字も書き換えていない** — 数字も、その出所として書かれたパスも当時のまま。
> **例外**: `.gitignore` されていた成果物 (`experiments/stage*/build/` の Lean バイナリ) は
> **tag にも入っていない**。それを出所とする数字は、いま再現する手段が無い。

> **Every `crates/...` in this document points inside tag `rust-frozen`.**
> The Rust half was removed from HEAD in M10; read it with
> `git show rust-frozen:crates/litedoc4-render/src/autolink.rs`. **The body is not
> rewritten** — the numbers, and the paths written down as where they came from,
> stay as they were measured.

## 状態

| # | 検証すること | 判断基準 | 状態 |
|---|---|---|---|
| 1 | 1 プロセスで対象パッケージをまとめて抽出できる | 時間が再現し、カスタム構文を含むプロジェクトでも動く | **完了** ((a) (b) とも) |
| 2 | 走査をやめ、モジュールから宣言を引ける | §6.1 の目標時間に到達する | **完了** — 11.82s (目標 14.5s) |
| 3 | 依存パッケージの IR なしでリンクが解決できる | Mathlib へのリンクが正しく張れる。必要な写像の実サイズが分かる | **完了** ((a) (b) とも、348/432 モジュールの範囲) |
| 4 | IR から HTML を生成でき、再生成に Lean が要らない | Lean を起動せず HTML を作り直せる | **完了** — 増分 1 で出力範囲を確定し**参照集合では署名内リンクを作れない**と判定、増分 2 で位置つきタグを追加 (抽出 13.71s / 書き込み 0.549s / 読み出し 0.100s)、増分 3・4 で生成器を実装し **432 ページを warm 0.82s / cold 0.93s** で実測 (byte 再現率 63.6%) |
| CI | cold の前提の検証と、cold を縮める打ち手 | 打ち手を 1 つ以上実測し、cold 合計を出す | **完了。ただし結論が 2 度動いた**。**打ち手 1 (展開直後なら warm) は否定** (macOS 実測 → `ubuntu-latest` でも `cache get` 直後の常駐率 60.4% で追認)。**2026-08-10 (2 コア / 7.75 GiB)**: `importModules` は同じジョブ **2.610s** / 別ジョブ **20.4〜89.2s = 8〜34 倍**【前者 n=1、両側は別ワークフロー・別ランナー実体、importModules 単体、抽出器はプロトタイプ (schema 3)】。**2026-08-16 (4 コア / 15.6 GiB、製品で n=5)**: **8〜34 倍は再現せず、split/same は 1.08〜1.58 倍**。**陽性対照 (同じランナーで `drop_caches`) だけが 5.2〜11.9 倍**なので、**機構は生きていて「別ジョブ = cold」という前提が偽**だった (→ 本ログ「CI 軸 (2026-08-16)」/ approach.md §3)。**打ち手 2 (先読み) は macOS で成立** (importModules 13.573 → **6.923s**、major fault 121,386 → **26,329**)、**Linux はランナー実体で割れる** (−4% ↔ −23〜−44%)、**現ランナーでは fault が 3 桁以下で先読みする相手が居ない** |
| 5 | 変更したモジュールだけ再処理できる | 無変更で再実行してもほぼ即時。stale ページが残らない | **完了** — 増分 1 で 1 モジュール変更を実測 **3.30〜4.39s** (フル 17.07s の 3.9〜5.2 分の 1)。**理論値 0.03s は否定**。増分 2 =【判断点 3】は **stale が残るため否定** (5 命題すべて破れた) → §5.5 を 3 層に見直し、増分 4〜7 で全層を実装して**本物の移動と削除を `lake build` ごと回しフルビルドと byte 一致**。**`lake build` が端から端までの 74〜98%** |
| 6 | 環境を常駐させて再抽出できる | §6.5 の 0.03 秒オーダーに到達する | **完了** — 要求あたり **0.035〜0.088s**、IR は単発と byte 一致。段階 6a で配線して**編集をまたいだサーバは誤答すると実測** (439 ページ中 1)。`lake build` 後に立てて全ラウンドを任せると **7.940 → 6.168s (−22%)、byte 一致** |
| 7 | 完成品の値を出す (出力範囲の差という留保を外す) | byte 再現率を上げ、意味解析 10.58s を割り、§7 の ◐ を閉じる | **完了** — 7a〜7c で byte 再現率 **63.587% → 99.506% = 到達上限** (完全一致ページ 34 → 304 / 348)、7d で意味解析を並列化 (10.79 → 3.34s、IR は byte 一致)、7g で常駐を本配線 + 600 要求ソーク、7e で構文近似は「アウトライン」と確定、7h で L3-3 を増分化 (−0.327s、439/439 byte 一致) |
| 8 | rev をバイトから外した形が実際に動くか | 注入が `jump-src.js` の `DOMContentLoaded` ハンドラより前に書き換え終える (**ブラウザ実測**) | **完了** — Chrome 151 で 10 ケース × 5 回。**満たし方は 3 通りあり、外すのは「リスナ内で注入し登録が後」のときだけ**。書き換えは最悪 **0.80 ms / ページ** (**本数ではなく要素数に効く**)。**キャッシュは鮮度ヘッダ次第** (無しだと 10/10 取りに来る) |

## 記録済みの結果

### 段階 1 — 前提の実測 (2026-08-09)

doc-gen4 に「複数モジュールを 1 プロセスで抽出する」コマンドを足して計測した。

- **実測**: 432 モジュールを 1 プロセスで 47.4 秒 (逐次プロセスは約 1,084 秒、23 倍)。
  ログ → `benchmarks/results/batch.jsonl`、レポート → `benchmarks/doc-gen4-report.md`

これは **doc-gen4 の機構での実測**であって、litedoc4 自身の抽出器での再現ではない。
段階 1 を満たすには (a) 自前の抽出器で同等の時間が出ること、
(b) カスタム構文・`Notation`・独自属性を持つプロジェクトでも壊れないこと、の 2 つが要る。

**この節の 47.4 秒 / 12.9 秒は下の「再現」で page cache のアーティファクトと判明した。**
数字は記録として残すが、床の議論には使わないこと。

### 段階 1 — 自前抽出器での再現 (2026-08-09)

**計測条件**: Apple M1 / 8 コア / 16 GB、Lean・Mathlib・doc-gen4 すべて v4.31.0
(mathlib `fabf563a7c` / doc-gen4 `0bc516c`)、`LEAN_NUM_THREADS` 未設定 (Lean 既定)、
olean ビルド済み、並列度 1 (単一プロセス)。

計測プログラムは `experiments/stage1/`。`import Lean` だけに依存する単一ファイルで、
432 モジュールを 1 回の `importModules` でロードし (import closure 6,021 モジュール)、
宣言を **索引方式** (`moduleData[i].constNames`) と **走査方式** (env 全定数) の
両方で列挙して**結果が一致することを検証**する。pretty print は一切しない。
ネイティブコンパイル済みバイナリで計測 (インタプリタ実行ではない)。

#### 結果

| | cold 寄り (1 回目) | warm 安定 (5 回、run4–8) |
|---|---:|---:|
| `importModules` | 10.49s | **2.36–2.50s** |
| 索引方式の列挙 | 0.0022s | 0.0022s |
| 走査方式の列挙 | 0.630s | 0.62s |
| プログラム合計 | 11.13s | 3.08–3.14s |
| プロセス壁時計 | 14.67s | 3.07–3.23s |
| peak RSS | 3.27 GB | 3.27 GB |

すべて**実測**。ログ → `benchmarks/results/stage1-extract*.jsonl`、
計測条件 → 同ディレクトリの `*-summary.txt`。

- **実測**: 宣言は 5,123 件。索引方式は 8,849 件を列挙、走査方式は 490,171 定数を走査して
  8,824 件が該当 (該当率 1.80%)。**両方式の宣言集合は完全一致** (`agree: true`)。
- **実測**: 索引方式 0.0022s vs 走査方式 0.62s = **286 倍**。ただしこれは
  1 バッチプロセスあたりの差で、現行が 432 回払う分ではない。

#### 判明したこと 1 — 「環境ロード 12.9 秒の床」は page cache のアーティファクト

warm 実行を 5 回連続で取ると `importModules` は 2.36–2.50 秒に収束し、
**壁時計 ≈ CPU 時間** (run7: 3.14s wall / 3.11s user+sys) になる。cold 寄りの実行では
壁時計 14.67s に対し CPU は 4.01s しかなく、**差はすべて olean の mmap ページイン待ち**。

これが litedoc4 側だけの現象でないことを確かめるため、**同じ warm 状態で doc-gen4 の
`batch` を測り直した** (2 回、再現性あり):

| phase | 初回計測 `batch.jsonl` | warm 再計測 `batch-rerun*.jsonl` |
|---|---:|---:|
| `load.importModules` | 12.910s | **2.601s / 2.518s** |
| `process.getAllModuleDocs` | 16.37s | 16.636s / 16.486s |
| `process.constantLoop` | 15.99s | 11.575s / 10.746s |
| └ うち `ofConstant` | 11.54s | 11.216s / 10.354s |
| `batch.updateModuleDb` | 0.639s | 0.676s / 0.660s |
| **`batch.total`** | **45.93s** | **31.50s / 30.43s** |

すべて**実測**。doc-gen4 の `importModules` も 2.6 秒に落ちた。
litedoc4 の 2.44 秒とほぼ同じで、**同じ API を同じ引数で呼んでいる以上これは当然**。

したがって:

- **環境ロードの床は CPU で約 2.5 秒**であり、12.9 秒ではない。
  12.9 秒は「page cache に olean が乗っていない状態」の値。
- **cold と warm で 5 倍違う** (2.4s ↔ 12.3s、`stage1-extract-3th` が cold 側の実測)。
  計測条件に page cache 状態を書いていない数字は読めない。
- 初回起動には `stage1.total` の外側に **3.5 秒**ある (98 MB の静的リンクバイナリの
  ページイン)。2 回目以降は 0.07 秒。~~**CI は毎回この cold 側を払う。**~~
  (**後の訂正**: この断定は根拠が無く、後の「CI 軸 — cold の前提の検証」で**仮定**に格下げした。
  macOS では `cache get` 直後でも cold のままだと実測したが、**Linux ランナーは未測定**。)

#### 判明したこと 1b — 方式A / 方式B を warm 同士で測り直すと 34 倍

上の再計測で方式B だけが warm になったので、**方式A (モジュールごと 1 プロセス) も
同じ warm 状態で 432 モジュール全数を測り直した** (以前は 112 モジュールの実測からの換算)。

| | warm 実測 | うち `importModules` |
|---|---:|---:|
| 方式A (432 プロセス) | **1,076s** (壁時計) / 1,022.6s (計装合計) | 908.6s (**88.8%**) |
| 方式B (1 プロセス) | **31.9s** (壁時計) / 31.50s (計装合計) | 2.6s |

**34 倍** (壁時計同士 1,076 / 31.9)。ログ → `benchmarks/results/serial-warm.jsonl`。

- **方式A は page cache 状態にほとんど影響されない**: 別セッションの旧計測
  (112 モジュールからの換算) が 1,084 秒、今回の全数実測が 1,076 秒。
  プロセスごとに import をやり直す以上、cache が効いても 1 回分しか得しないため。
  → **旧レポートのフルビルド外挿値はそのまま使える。**
    (**leg 9 の訂正**: この行は当時「33,034 秒」と書いていたが、**その値は出所を辿れない**。
    レポート §4.6.1 の実測 13,611 秒 / 3,590 モジュール = 3.79 s/モジュールを
    8,600 モジュールに伸ばすと **約 32,600 秒 = 9.1 時間**で、レポート本文もそう書いている。
    生ログ `docs/doc-gen-bench/raw/full-build.jsonl` (対象リポジトリ) を再集計しても
    `single.total` の合計は 13,611.5 秒 / n=3,590 で一致する。
    `approach.md` §6.4(b) と `README.md` を 32,600 秒 / 約 2,240× に訂正した。)
    (**2026-08-17 の追記**: この 13,611 秒は**計装の CPU 合計**であり、外挿した 32,600 秒 =
    9.1 時間も **CPU 時間**。**方式A と違ってこのビルドは並列で走っている**ので、
    壁時計はこれより小さい (レポート §4.6「全体の wall clock は測れていない」)。
    分母の 14.97 秒は壁時計なので、**(b) の倍率は単位が揃っていない** →
    `approach.md` §6.4(b) に注記、`README.md` からは 9.1 時間の数字を落とした。
    上の「外挿値はそのまま使える」は **CPU 時間としては**成立する、という意味に読むこと。)
- 逆に**方式B は cache 状態に強く影響される**ので、旧 23 倍は
  「cold な方式B」対「warm な方式A」を比べていたことになる。**過小評価**だった。
- 1 プロセスあたりの `importModules` は 2.10 秒 (平均)。432 モジュールぶんの
  closure をまとめて読む方式B が 2.6 秒。**まとめて読んでも 1 個ぶんと大差ない** —
  closure が Mathlib に支配されているため。これがバッチ化が効く理由の全部。
- 走査は方式A で延べ 194,143,507 定数 / 該当 8,849 (**0.0046%**)、
  方式B で 490,171 / 8,824 (**1.80%**)。同じ 0.032% 系の数字でも方式で 2 桁違う。

#### 判明したこと 2 — 「走査 4.45 秒」も走査のコストではなかった

`approach.md` §6.1 の「走査 (該当外) 4.45s」は `constantLoop` 15.99s − `ofConstant` 11.54s の
**導出値**だった。warm 再計測では同じ差が **0.36 秒**になる。つまり 4.45 秒の大半は
走査そのものではなく cold cache のページインだった。

素の compiled IO で同じ 490,171 定数を舐めると 0.62 秒 (上表)。
**結論 (索引方式にすれば消える) は変わらない**が、行の名前と大きさは誤っていた。

#### 判明したこと 3 — 索引方式は宣言の「所有モジュール」を一意に決めない

索引方式の列挙数 8,849 と走査方式の該当数 8,824 に 25 件の差がある。
複数モジュールの olean に同名で焼かれる自動生成補題 (`Foo.eq_1`、`Foo.congr_simp`) で、
`const2ModIdx` は最初に定義したモジュールしか記録しないため走査は 1 回しか見ない。

- **実測**: 25 件、うち 5 件は内部名でない (`typeCount.congr_simp` 等)。
  帰属先はすべて対象パッケージ内で、依存側に漏れるものは無い。
- カバレッジではなく**ページ配置**の問題。索引方式は doc-gen4 が暗黙に持つ
  「最初に定義したモジュールが勝つ」規則を、明示的に決め直す必要がある (→ §8 の問い)。
- **2026-08-18 に製品の経路で測り直した — 現在は 21 件で、出力には出ない**【実測 →
  [`../benchmarks/results/duplicate-owners-2026-08-18.txt`](../benchmarks/results/duplicate-owners-2026-08-18.txt)】。
  規則は**製品では 2 本ある** (IR 側は `seen` の first-wins、link index 側は `const2ModIdx`) が、
  **どちらを消しても出力は 1 バイトも動かない** — 21 件は blacklist で先に落ちる。
  **出ない理由は blacklist であって、2 本が一致しているからではない。**

#### 判断

**(a) は満たした。(b) は未検証。段階 2 に進んでよいが、段階 1 は閉じない。**

- (a) 自前の抽出器で同等の時間が出るか → **満たす**。同一 warm 条件で
  litedoc4 2.44s / doc-gen4 2.60s。走査数 490,171・該当数 8,824 も完全一致しており、
  両者が同じ環境・同じワークロードを見ていることが確認できた。
- (b) カスタム構文・`Notation`・独自属性で壊れないか → **未検証**。
  この最小抽出器は pretty print をしないので、カスタム構文が絡む経路を通っていない。
  `importModules (loadExts := true)` + `enableInitializersExecution` で
  環境拡張の初期化までは走っているが、それは (b) の一部でしかない。

  ただし**別の計測対象を用意する必要は無い**ことは確認した。現行の対象は
  Mathlib の `notation3` による scoped notation を持つ
  (`InformationTheory/Shannon/TypedRV.lean` の `H(μ; X)` / `I(μ; X ; Y)` / `D(μ; X ∥ Y)`、
  `InformationTheory/Asymptotic.lean` の `≐`)。**pretty print を入れた段階 2 が
  そのまま (b) の検証になる。**

`stage1.total` (3.1 秒) を `batch.total` (30.4 秒) と比べていない。
本抽出器は意味解析をしないので、比較できるのは `importModules` だけ。

#### `approach.md` に反映した箇所

- §2 の「決定的だった 1 つの実験」を warm 同士の全数実測に差し替え (23 倍 → 34 倍)
- §3 「12.9 秒が床」→ warm 約 2.5 秒 / cold 10〜13 秒に訂正
- §5.1 常駐の優先度を下げた / §5.2 走査排除の価値を「時間」から「構造」に置き換え
- §6.1 の表を warm 実測で再構成 (合計 45.9s → 31.5s、最適化後 25.1s → 14.5s)
- §6.3 (26 秒 → 16 秒) / §6.4 (43 倍 → 74 倍、1,251 倍 → 約 2,000 倍) / §6.5 / §6.6
- §8 に「宣言の所有モジュールをどう決めるか」「意味解析を減らせるか」を追加
- `README.md` の理論値表、`benchmarks/README.md` の計測条件、
  `benchmarks/doc-gen4-report.md` の冒頭に訂正注記

### 段階 2 — 意味解析の第 1 増分 / 段階 1 (b) の決着 (2026-08-09)

**計測条件**: 段階 1 と同一 (M1 / 8 コア / 16 GB、すべて v4.31.0、並列度 1、warm)。
計測プログラムは `experiments/stage2/`。索引方式で引いた宣言に対し、型の pretty print・
docstring・宣言種別・定義位置を取り出す。equation 生成はフラグで切り替え。
ログ → `benchmarks/results/stage2-*.jsonl`、条件 → `stage2-*-summary.txt`。

#### 同じ仕事をしていることの確認 (時間を比べる前提)

doc-gen4 の blacklist を転写して適用し、同一 432 モジュールに対する doc-gen4 の DB と
突き合わせた。すべて**実測**:

| | litedoc4 | doc-gen4 DB |
|---|---:|---:|
| 宣言 | **4,750** | **4,750** |
| 種別内訳 (theorem/def/instance/structure/ctor/opaque) | 3742/842/91/37/37/1 | 同一 |
| docstring | 3,394 | 3,394 |
| equation lemma (生成 ON 時) | 863 | 863 |
| structure field / parent | 156 / 1 | 156 / 1 |

段階 1 の 5,123 は粗いフィルタ後の数で、doc-gen4 が実際に `DocInfo` を作るのは 4,750 件。

#### 時間 (warm、litedoc4 5 回 / doc-gen4 3 回)

**doc-gen4 側も同じ日・同じ warm 状態で測り直した。**段階 1 の 11.22 秒が
equation 生成 ON の値だったか記録が無かったため、両側で ON/OFF を固定して測っている。

| | litedoc4 | doc-gen4 | 差 |
|---|---:|---:|---:|
| 意味解析 (equations ON) | 9.30s | 10.51s | −11% |
| 意味解析 (equations OFF) | 8.15s | 9.20s | −11% |
| 意味解析 (OFF + tagCodeInfos) | 8.56s | 9.20s | −7% |
| `importModules` | 2.49s | 2.49s | — |
| peak RSS | 3.28–3.37 GB | — | |

**この −11% を「速くなった」と読んではいけない。** litedoc4 側は属性収集・instance の
className/typeNames・`sorried`・`renderTagged` を省いている。省いた分の一部
(`tagCodeInfos`) を戻すと差は −7% に縮む。**結論は「意味解析は本質的コストであり、
再実装しても減らない」** — `approach.md` §6.6 の予測どおり。

- **実測**: `ofConstant` の 11.22 秒 (段階 1 で引用した値) は 3 回中の**最大値**だった。
  中央値は 10.51 秒。§6.1 の意味解析の行はわずかに悲観側だった。
- **実測**: equation 生成のコストは意味解析の 10% (0.97s / 863 本)。切り替え可能にする
  価値はあるが、支配項ではない。
- **実測**: 例外・失敗は全 15 回の実行を通じて **0 件**。この対象は定理中心なので
  ヒートビート上限に当たらない (レポート §4.7 の「Lean core では出る」と整合)。

#### 段階 1 (b) の決着 — カスタム構文は壊れない。ただし既定では**使われない**

生の出力は `benchmarks/results/stage2-notation-samples.txt` に残した。

```
                          既定 (doc-gen4 と同一)          --open 指定時
DotEq.refl              : DotEq a a                     a ≐ a
entropy_nonneg_rv       : 0 ≤ entropy μ X               0 ≤ H(μ; X)
mutualInfo_comm_rv      : mutualInfo μ X Y = ...        I(μ; X ; Y) = I(μ; Y ; X)
klDivRV_def             : klDivRV μ X Y = ...           D(μ; X ∥ Y) = ...
```

- **実測**: 既定モードの出力は doc-gen4 の HTML と**一字一句一致**。両モードとも
  例外・空署名は 0 件。**「壊れない」は確認できた。**
- **実測**: `scoped notation` の delaborator は `ScopedEnvExtension` にあり、
  import しただけでは**未活性**。`Core.Context.openDecls` に入れるだけでは効かず、
  **`Lean.activateScoped` で環境側を活性化する必要がある**。doc-gen4 はどちらも
  やっていないので、**パッケージ自身の記法が doc に一切出ていない**。
  litedoc4 は出せる。コストは意味解析 +6% (8.15s → 8.67s)。4,750 件中 294 件が変わる。
- **実測**: `H(μ; X | Y)` は両モードとも出ない。notation が `Shannon.condEntropy`
  (`@[reducible]` な `MeasureFano.condEntropy` の別名) に対して定義されており、
  elaborate 後の項には `MeasureFano.condEntropy` しか残らないため delaborator が
  一致しない。該当 34 件。**対象プロジェクト側の宣言のしかたの性質で、抽出器の欠陥ではない。**

#### 判断

**段階 1 は完了。段階 2 に進む。ただし残りはほぼ 1 項目に集中している。**

| | §6.1 最適化後 | stage2 実測 |
|---|---:|---:|
| 環境ロード | 2.60s | 2.49s |
| タクティク収集 | 0.04s (**仮定**) | **未実装** (doc-gen4 実測 16.7s) |
| 走査 → 索引 | ~0s | 0.002s |
| 意味解析 | 11.22s | 9.30s (eq ON) |
| 書き込み | 0.68s | 0.06s (JSONL dump、IR ではない) → 段階 4 準備で **0.198s 実測**に置換 |
| **合計** | **14.5s (理論値)** | **11.8s + タクティク収集** |

残っているのは (a) モジュール docs / タクティク収集、(b) 本物の IR 永続化、
(c) 省いた属性・instance 索引・`renderTagged`。

**(a) が warm でも batch 全体の最大項** (16.7s、全体の 54%。意味解析 10.5s より大きい)。
§6.1 の「0.04 秒に畳める」は**仮定のままで未検証**であり、14.5 秒に届くかは
ほぼこれ 1 つで決まる。これが次の一手。

#### 計測で 1 回踏み外しかけた点 (記録)

doc-gen4 は `DocGen4/Load.lean` で pp オプション (`pp.tagAppFns` / `pp.funBinderTypes` /
`debug.skipKernelTC` / `Elab.async=false`) を設定している。これを入れずに測ると
出力が変わり (`fun n =>` 対 `fun (n : ℕ) =>`)、pp が 7.27s → 7.96s と **9.5% 速く見えた**。
条件を揃えて測り直した。**「自分に有利な数字が出たときほど疑う」が実際に効いた箇所。**

### 段階 2 — 完了 (2026-08-09)

**計測条件**: 段階 1・段階 2 第 1 増分と同一 (M1 / 8 コア / 16 GB、すべて v4.31.0、
並列度 1、warm)。ログ → `benchmarks/results/stage2-full-eq-{1..6}.jsonl`、
条件 → `stage2-full-eq-summary.txt`。

#### `getAllModuleDocs` の 16.7 秒の正体

doc-gen4 の処理を `experiments/stage2` 内で再現し (`--tactics-emulate`)、
**16.913 秒**を実測 (doc-gen4 の 16.64 秒に対し 1.6% 以内。再現は忠実)。内訳:

| | 時間 | |
|---|---:|---|
| 432 回の `allTacticDocs` 呼び出し | 3.759s (22%) | 1 回 8.7 ms。タクティク表の作り直し |
| doc-gen4 のフィルタループ | **13.153s (78%)** | 432 × 421 回、1 回 **72 µs** |

72 µs は「2 回のハッシュ参照」に対して異常なので切り分けた (`--tactics-probe`、
421 タクティクを 5 通りで舐める):

```
d.internalName に触るだけ                 0.000s
  + env.getModuleIdxFor?                  0.000s
  + env.header.moduleNames[modIdx]!       0.151s   <- 全部これ
  同じ処理、moduleNames をループ外に出す    0.000s   <- 直し方
```

**`EnvironmentHeader.moduleNames` はフィールドではなく `def`**
(`Lean/Environment.lean:182` — `header.modules.map (·.module)`)。
**呼ぶたびに 6,021 要素の配列を作り直す。**
doc-gen4 の `collectTactics` (`DocGen4/Process/Analyze.lean:140`) は
これをモジュール × タクティクごとに呼んでいる。

- **`approach.md` §5.2 の説明「`collectTactics` の O(モジュール数 × 全タクティク数)」は
  形は合っているが、支配項を取り違えていた。** §5.2 が指す「モジュールごとに全タクティクを
  列挙し直す」分は 22% でしかない。実際の計算量は
  **O(対象モジュール数 × タクティク数 × import closure のモジュール数)**
  = 432 × 421 × 6,021 ≈ 1.1 × 10⁹ で、**第 3 の因子が呼び出し側から見えない**。
- **この対象パッケージはタクティクを 1 つも定義していない。**
  doc-gen4 はバッチ全体の 54% (16.7 秒) を、空の結果を作るために使っている。

#### 出力の一致確認 (時間を比べる前提)

`benchmarks/tools/compare-modules.py` で doc-gen4 の DB と突き合わせた。
**件数だけでなく集合として一致** (`benchmarks/results/stage2-modcmp.txt`):

| | 432 モジュール (対象) | 573 モジュール (対象 + タクティク定義元 141) |
|---|---|---|
| modules | 432 = 432 | 573 = 573 |
| module_imports | 3,503 = 3,503 | 4,096 = 4,096 |
| module_docs (mod, line, text) | 1,515 = 1,515 | 1,740 = 1,740 |
| tactics | 0 = 0 | **421 = 421** |

対象パッケージのタクティクが 0 件では検証にならないので、
**環境内の 421 タクティクを定義する 141 モジュールを足したリストでも測り直した。**

#### 時間 (warm、5 回の中央値)

| phase | litedoc4 実測 | §6.1 最適化後 |
|---|---:|---:|
| `importModules` | 2.512s | 2.60s |
| 索引 | 0.002s | ~0s |
| モジュール docs + タクティク | **0.011s** | 0.04s (**仮定**) |
| 意味解析 (equations ON) | 9.261s | 11.22s |
| IR 書き込み | 未実装 (→ 段階 4 準備で **0.198s 実測**) | 0.68s |
| **合計** | **11.82s** | **14.5s (理論値)** |

壁時計 11.82–11.97s に対し user+sys 12.04–12.17s → warm。収束幅 ±0.05s。
peak RSS 3.29–3.30 GB (前増分から不変)。

**遅延評価で仕事を先送りしていないかの確認**: Lean は構造体フィールドを遅延構築するので、
`--dump-modules` で全フィールドを強制し JSON 直列化まで行った値も測った —
**0.009s (432 モジュール) / 0.015s (573 モジュール、421 タクティク全件)**。
最悪でも 0.02s で、仮定の 0.04s の半分。

#### 判断

**段階 2 は判断基準を満たした。到達した。**

11.82 秒 (目標 14.5 秒)。未実装の IR 書き込みに doc-gen4 の実測 0.68 秒を代入しても
**12.50 秒**で目標内 (**この代入は段階 4 準備で不要になった** — 実測 0.198 秒)。§6.1 のどの行も仮定より悪化していない:

- タクティク収集 0.04s (仮定) → **0.011s 実測** (doc-gen4 比 1,500 分の 1)
- 意味解析 11.22s → 9.261s (11.22 は doc-gen4 3 回の最大値。中央値 10.51s)
- 環境ロード 2.60s → 2.512s

**ただし litedoc4 側はまだ属性収集・instance 索引・`renderTagged`・本物の IR 永続化を
出していない。** これらを入れると 13 秒前後になる見込みで、目標内には収まるが、
**12.50 秒を「完成品の値」として引用しないこと。**

#### 計測で 1 回踏み外しかけた点 (記録)

最初の probe が `let _ := getTacticExtensions env tac` を使っており、Lean では
**強制されないので何も測っていなかった** (実際 38 ms のところ 7 ms と出た)。
転写したコードではなく実物の関数を直接測って発覚。前増分の pp オプション取り違えと同種で、
**どちらも「都合の良い方向」に外れていた。**

### 段階 3 — 増分 1 / 署名が参照する定数の列挙 (2026-08-09)

**計測条件**: Apple M1 / 8 コア / 16 GB、Lean・Mathlib・doc-gen4 すべて v4.31.0、
`LEAN_NUM_THREADS` 未設定 (Lean 既定)、olean ビルド済み、並列度 1 (単一プロセス)、
**warm** (壁時計 ≒ user+sys を全 run で確認)。対象 432 モジュール / 4,750 宣言。
5 構成を同一セッションで各 6 回連続実行し、run 1 を cold として捨て **runs 2–6 の中央値**を取る。
ログ → `benchmarks/results/stage3-{noeq,noeq-tag,noeq-refs,eq,eq-refs}-{1..6}.jsonl`、
条件は同名の `*-summary.txt`。

`experiments/stage3/` は `stage2` のコピーに `--refs`（doc-gen4 の `renderTagged` が
`.const` タグを張る名前の回収）を足したもの。`--refs` OFF での `--dump` 出力は
stage2 とバイト一致する (`"refs":[]` の追加を除く) ので、下の baseline は段階 2 と同じ仕事。

#### 参照定数の実サイズ — 需要側の下限 (すべて実測)

| | |
|---|---:|
| 出現数 (equations ON) | 143,121 |
| 出現数 (equations OFF) | 130,898 |
| 1 宣言あたり平均 (ON) | 30.1 |
| **ユニーク** | **1,446** |
| うち自パッケージ由来 | 913 |
| **うち依存側 (Mathlib ほか)** | **533** |
| モジュールを解決できなかったもの | 0 |

一意集合 → `benchmarks/results/stage3-refs.jsonl` (1 行 1 定数、定義モジュール付き)。

**依存側 533 件は、doc-gen4 が配る写像 `declaration-data.bmp` の 0.21%** (分母 258,760 宣言 /
37,481,374 バイト、**実測** — 同ファイルを直接パースして数えた)。
段階 3 の「下限が上限より 2 桁小さいか」(→ 必要な分だけ引く設計が成立するか) は
**2 桁どころか 2.7 桁小さい**。

ただし (1) これは*この対象パッケージ*の需要であって他パッケージに外挿していない、
(2) 供給側がこの 533 件を実際に解決できるかはまだ照合していない (増分 2)、
(3) 分母の bmp は下に書く 348 モジュールの部分ビルドと同じビルドの産物。

#### 追加コスト (すべて実測、`stage3.analyze` の中央値)

| 構成 | `stage3.analyze` | baseline 比 |
|---|---:|---:|
| equations OFF, baseline | 8.058s | — |
| equations OFF, `--tag` | 8.510s | +0.452s |
| equations OFF, `--refs` | 8.612s | **+0.554s** |
| equations ON, baseline | 9.175s | — |
| equations ON, `--refs` | 9.675s | **+0.500s** |

`stage3.total` は 11.775s → 12.259s (**+0.484s**)。
コレクタ自体 (`refUs`、`ppUs`+`eqUs` の**内数**) は 0.468s。

**段階 2 の `--tag` +0.405s との整合**: 同一セッションで測り直した `--tag` は +0.452s。
段階 2 の値は別セッションなので直接は比べられないが、同じ桁で整合。
`--refs` はそれより 0.10s 高い — `tagCodeInfos` を通さない代わりに
全タグを歩いて名前を積むぶん。

#### doc-gen4 の HTML との照合 (実測)

`benchmarks/tools/compare-links.ts` で、doc-gen4 が実際に出力した HTML の
**署名ブロック内のリンク**だけを取り出して突き合わせた
(`div.decl_header` / `ul.equations` / `li.structure_field`、宣言自身への自己リンクは除外)。
出力 → `benchmarks/results/stage3-linkcmp.txt`。

| | 増分 1 時点 | **訂正後 (増分 3)** |
|---|---:|---:|
| HTML のリンク先 ユニーク | 1,154 | **1,161** |
| litedoc4 の参照定数 ユニーク | 1,446 | 1,446 |
| **HTML にあって litedoc4 に無い** | **0** | **0** |
| litedoc4 にあって HTML でリンクになっていない | 292 | 285 |

> **訂正 (増分 3 で判明)。** 増分 1 の `li.structure_field` の正規表現が
> **属性順に依存していた** — doc-gen4 の `Output/Structure.lean:fieldToHtml` は
> direct field を `<li id={name} class="structure_field">` で出すため、
> この corpus の field ブロック **153 本のうち 4 本しか拾えていなかった** (実測)。
> `div.structure_field_info` を直接ブロックにする形に直し、突き合わせ対象が
> **広がった**うえで `missing 0` は変わらなかった。増分 1 の結論は否定されず、
> 母集団が増えたぶん強くなった。**増分 1 の数字は `--legacy-blocks` で
> バイト一致で再現できる** (`benchmarks/results/stage3-urlcmp-legacy.txt`)。
>
> 教訓: doc-gen4 の HTML は同じ論理要素でも分岐ごとに属性順が違う。
> 正規表現でのブロック照合は**「一致した件数」ではなく「母集団に対する割合」で
> 検算する** — でないと今回のように静かに 97% を取りこぼす。

**取りこぼし 0。** doc-gen4 の HTML は解決できた分だけがリンクなので
(`name2ModIdx` に無い名前は `<span class="fn">` になる)、包含 `HTML ⊆ litedoc4` が
期待される向きで、それが成立している。逆向きの 285 件の分類は増分 3。

**重大な制約 — 突き合わせ先が 432 モジュール中 348 しかない。**
`.lake/build/doc/InformationTheory` の HTML は 348 ページ (3,477 宣言) で、
対象の 432 モジュールのうち **84 が欠けている** (欠落一覧は
モジュール表と `find` の差分で再現できる)。フルビルドを 42% で打ち切った影響。
**上の 0 件・1,161 件はこの 348 モジュールの範囲での実測**であり、432 全体の一致率ではない。

#### 判断

**増分 1 は通った。段階 3 の判断基準 (b)「必要な写像の実サイズが分かる」の需要側は出た。**
判断基準 (a)「Mathlib へのリンクが正しく張れる」は、
*doc-gen4 が張ったリンクを 1 本も取りこぼしていない*ところまで (348 モジュール範囲)。
供給側 (`declaration-data.bmp` が需要を満たすか) と URL の一致は増分 2・3。

段階 3 全体の判断はまだ出していない。

#### 計画から変わった点

- 突き合わせツールは `compare-links.py` ではなく **`compare-links.ts`** にした。
  このリポジトリのツールは `analyze.ts` を含め TS で、Python の前例が無いため。
- 増分 3 の「1 ファイル 146 リンクのうち何本が署名由来か」は、
  ファイル単位ではなくブロック単位で解けることが分かった (上のセレクタ)。
  ただし **structure のフィールドは親のブロック内にレンダリングされる**ので、
  「宣言 → HTML ブロック」の対応は親子関係込みで解く必要がある。

### 段階 3 — 増分 2 / 供給側の照合と写像の実サイズ (2026-08-09)

**計測条件**: 機材・バージョンは増分 1 と同じ。ロード時間は Deno 2.7.14 / V8 で
`readTextFile` + `JSON.parse` を **7 回、run 1 を捨てて warm 6 回**の中央値。
ツール → `benchmarks/tools/map-size.ts`、集計 → `benchmarks/results/stage3-mapsize.txt`
(機械可読は同名 `.json`)。**入力の bmp は 42% で打ち切ったビルドの産物**であることに注意。

#### 需要 ↔ 供給のカバレッジ (すべて実測)

| 集合 | 総数 | bmp にある | 欠落 | カバレッジ |
|---|---:|---:|---:|---:|
| 参照定数 全体 | 1,446 | 1,309 | 137 | 90.53% |
| **依存側** | **533** | **530** | **3** | **99.44%** |
| 自パッケージ | 913 | 779 | 134 | 85.32% |

**依存側で解決できない 3 件は `Eq.rec` / `List.recOn` / `Nat.rec`** — すべて再帰子で、
doc-gen4 の `isBlackListed` (`isRec` / `isAuxRecursor`) が実ページを作らないもの。
出現数で見ると依存側 128,526 出現のうち未解決は **5 出現 (0.004%)**。

自パッケージ側の 134 件は写像の問題ではない (自分の IR から引く側)。内訳は
**121 件が「この部分ビルドの bmp にモジュールが無い」**、9 件が `_private.` 名、
4 件が `._proof_N` 形。原因は排他ではなく 3 件が重複。

**別経路での裏取り** (有利な数字を疑うため):
`own` フラグをモジュール表から再計算 → 1,446 件中不一致 0。
定義モジュールが litedoc4 の `const2ModIdx` と bmp で **1,309/1,309 一致**。
依存側ヒット **530 件全数**について `docLink` のファイル実在と `id="<name>"` アンカー実在を確認 → 530/530。

**doc-gen4 は dead link を出している。** `name2ModIdx = env.const2ModIdx` (環境の全定数) を
見てリンクを張るので、ページが無い `Eq.rec` などにも `href` を出す (実測 3 本)。
**写像に載っている名前だけリンクする方式は、この点で doc-gen4 より厳密に良い。**
ただし今回名前だけで判別できたのは相手が再帰子だからで、`isMatcher` / `isProjFn` の
意味論部分が名前から一般に復元できるわけではない。

#### 写像の実サイズ (すべて実測、gzip は実圧縮)

| スライス | 形 | 件数 | raw | gzip |
|---|---|---:|---:|---:|
| 全体 (上限) | `docLink`+`kind` | 258,760 | 34,269,816 B | 2,663,668 B |
| 依存側のみ | `docLink`+`kind` | 254,763 | 33,403,709 B | 2,603,753 B |
| **参照分 (下限)** | `docLink`+`kind` | **530** | **52,985 B** | **7,885 B** |
| 参照分 | 名前 → モジュール名の 2 段 | 530 | 26,970 B | 5,453 B |

ファイル全体 (`instances` 等を含む) は 37,481,374 B / gzip 3,127,535 B。

**上限 34.3 MB に対して下限 53 KB — 647 倍の開き** (分母は `declarations` の raw バイト数)。
2 段正規化すると 27 KB (1,270 倍)。**「必要な分だけ引く」設計は成立する。**

#### ロード時間 (実測、warm 6 回の中央値)

| 変種 | bytes | 中央値 | 収束幅 | 環境ロードの床 2.512s 比 |
|---|---:|---:|---|---:|
| bmp 全体 | 37,481,374 | 0.2068s | 0.196–0.233 | **8.2%** |
| `declarations` 全体 | 34,269,816 | 0.2007s | 0.196–0.232 | 8.0% |
| 参照分 (530) | 52,985 | 0.0003s | 0.0002–0.0003 | 0.01% |

別プロセスで 1 read だけする形でも測り直し、同じ値 (壁時計 0.22–0.24s に対し
user+sys 0.20–0.23s で warm)。**これは Deno/V8 での値**であり、実装言語は未決なので
1 ランタイムの値でしかない。**cold は未計測** (page cache を落とすのに sudo が要り、事前決定 #6 で使わない)。

#### `kind` 列は要るか → **リンクには要らない**

リンク生成 (`Output/Base.lean` の `renderedCodeToHtmlAux` / `declNameToLink`) は
`name2ModIdx` だけを見て `kind` を読まない。`kind` の唯一の読み手は
`static/declaration-data.js` の `allowedKinds`、つまり**検索ページの種別フィルタ**。
→ **写像は 3 列ではなく 2 列 (名前, モジュール) で足りる。**
検索 UI を作るなら要るので、そのときは結論が変わる。

#### 判断

**増分 2 は通った。段階 3 の判断基準 (b) は満たした。**
需要側 (増分 1) と供給側 (増分 2) の両方が出て、**下限が上限より 2.8 桁小さい**ことが実測で確定。
撤退ライン (削除済の `plans/stage3.md` §4。読むなら git) の 4 本のうち 3 本は当たらなかった:

- blacklist が復元できない → 当たらず (依存側の未ヒットは再帰子 3 件のみ、名前で判別可)
- 解決に環境が要る → 当たらず (533 中 530 が名前一致だけで解決)
- ロード時間が床と同等以上 → 当たらず (床の 8%、参照分なら 0.01%)

**残り 1 本「公開ドキュメントに bmp 相当があるか」は未検証。**
ネットワーク不使用 (事前決定 #5) のため上流の公開物を確認していない。
`approach.md` §8 の答えはここが未決のまま。**成立性は崩れない**が、
「写像を誰が持つか」の答えは「自分で作る」側に倒れる可能性が残る。

段階 3 全体の判断は増分 3 (URL 生成と正しさの照合) の後。

### 段階 3 — 増分 3 / URL 生成と正しさの照合 (2026-08-10)

**計測条件**: 機材・バージョンは増分 1 と同じ。時間は測っていない (増分 3 の成果物は
一致率であって秒ではない)。ツール → `benchmarks/tools/compare-links.ts` (`--decls` 系)、
出力 → `benchmarks/results/stage3-urlcmp.txt` / `stage3-urlcmp-mismatches.jsonl`。
入力の宣言ダンプは 7.3 MB あるのでコミットしていない。再現コマンドは
`experiments/stage3/README.md`。**範囲はここでも 348/432 モジュール。**

#### URL 規則 (実装)

`DocGen4/Output/Base.lean` の `getRoot` / `moduleNameToLink` / `declNameToLink` を読み、
HTML で確認した規則:

```
href = "../" * (ページのモジュール成分数 - 1) + "./"
     + 対象定数の定義モジュール名の "." → "/" + ".html#" + 定数名
```

**IR には絶対的な識別子 (モジュール名 + 宣言名) を持ち、相対化は出力時**にする
(計画 §5.6 の境界と一致)。実測でプレフィクスは深さ 2〜6 まで行使されている
(`.././` 27 宣言 〜 `../../../../.././` 77 宣言)。

#### 照合の母集団 (実測)

ダンプの 4,750 宣言のうち、HTML に `div.decl` として存在する **3,477 が母集団**。
除外の内訳は「親に畳まれた member 190」「ページの無いモジュール 1,083」。
**「ページはあるが `div.decl` が無い」0 / 「`div.decl` はあるが dump に無い」0** —
つまり 348 モジュールの範囲では宣言の集合が完全に一致している。

| ペア (宣言, 参照定数) | 件数 |
|---|---:|
| HTML 側 | 39,298 (`<a>` 出現 76,318) |
| litedoc4 側 | 40,936 |
| **共通** | **39,298 (HTML の 100%、litedoc4 の 96.0%)** |

#### href 文字列の完全一致 (実測、共通ペア)

| 方式 | 一致 |
|---|---:|
| A. env 由来 (`getModuleIdxFor?` + 規則) | 39,298 / 39,298 = **100.000%** |
| **B. 写像 (bmp `docLink`) 由来** | 39,295 / 39,298 = **99.992%** (未一致 3) |

**A の 100% を独立な証拠として扱わない。** doc-gen4 の `name2ModIdx` は
`env.const2ModIdx` そのもの (`DocGen4/Process/Analyze.lean:243`) で、litedoc4 が
`getModuleIdxFor?` で読んだ写像と**同一入力**。A が検証しているのはパス規則だけで、
モジュール解決は両者で共有している。**主張は B で述べる。**

**別経路での裏取り** (有利な数字を疑うため): ツールと無関係のスクリプトで、HTML 内の
宣言リンク形の href を全部 (署名ブロックに限定せず、docstring・import 一覧も含む)
取り出し、bmp の `docLink` を深さプレフィクスで相対化した文字列と比較 →
**13,309 件中 13,307 一致 / 差異 0 / bmp 欠落 2**。ブロック抽出ロジックを一切通さない
経路でも規則はバイト一致する。

未一致 3 件は `Eq.rec` ×1 / `List.recOn` ×2 で、いずれも**再帰子で bmp に無い**。
プレフィクス違い・パス違い・アンカー違いは A・B とも **0 件**。

#### HTML にあって litedoc4 に無いペア: **0 件**

#### litedoc4 にあって HTML でリンクになっていないペア: 1,638 件 (分類)

| 件数 | 分類 |
|---:|---|
| 1,063 | `<a>` が全く無い (依存側、bmp にあり) |
| 340 | `<a>` が全く無い (自パッケージ、bmp にあり) |
| 226 | HTML には出ているが**宣言自身への名前リンク**なので本ツールが意図的に落とした分 (差ではない) |
| 8 | private 名 (`_private.…`) |
| 1 | `<a>` が無い (依存側、**bmp に無い** = `Nat.rec`) |

doc-gen4 側で `<a>` が出ない主因は `renderedCodeToHtmlAux` の `innerHasAnchor`
(内側が既にアンカーなら外側の `.const` を包まない) だが、**これは HTML に痕跡を残さない**
ので分類として立てていない (「同じブロックに祖先アンカーが在る」は 266 件あるが、
無関係な参照でも起きるので証拠にならない — 示唆止まりと明記)。

#### dead link のリスク (撤退ライン判定、実測)

生成 URL の指すページを実際に開き `id=` の有無で判定:

| 母集団 | live | dead |
|---|---:|---:|
| litedoc4 が張りうる全ペア | 40,924 | **12 (11 名)** |
| うち doc-gen4 も張った分 | 39,295 | 3 (2 名) |
| うち doc-gen4 が張らなかった分 | 1,629 | 9 (9 名) |

dead 11 名の内訳: **依存側 3 名 = `Eq.rec` / `List.recOn` / `Nat.rec`** (すべて bmp に無い)、
自パッケージ 8 名 (すべて `_private.`)。母集団が参照する依存側 469 名のうち
**bmp に無いのはこの 3 名だけ**。

**`Eq.rec` と `List.recOn` は doc-gen4 自身が href を出しており、`Init/Prelude.html` は
存在するが `id="Eq.rec"` が無い** (手で確認) — **doc-gen4 の既存の dead link**。
doc-gen4 のソースにも `RenderedCode.lean:250` に `TODO replicate blacklist logic` がある。
方式 B はこの 3 件について**リンクを出さない**ので、**litedoc4 の方が正しい。**

#### 判断

**増分 3 は通った。段階 3 の判断基準 (a)「Mathlib へのリンクが正しく張れる」は
348/432 モジュールの範囲で満たした。主張は方式 B (写像だけ) で述べる:**

- 同一ペアでの URL 文字列一致 **99.992% (39,295/39,298)**。残り 3 は
  「解決に環境が要る」のではなく、**そもそもリンクしてはいけない先**
- **HTML にあって litedoc4 に無いペアは 0** — doc-gen4 が張ったリンクを取りこぼしていない
- 撤退ライン **「依存側の blacklist が写像から復元できない」は否定された。**
  bmp からの欠落が blacklist そのもの (ページを与えない名前は索引にも入らない)

留保 (誇大にしないため):

1. **範囲は 348/432 モジュール。** 残り 84 は未検証 (フルビルド 42% 打ち切りの影響)
2. **方式 A の 100% は独立な検証ではない** (上記)。写像だけでやる主張なら B を引く
3. **private 名 8 件は素朴に URL を出すと dead。** litedoc4 側に
   「`_private.` を除外するか親に落とす」処理が要る — 小さいが実在する欠落
4. **docstring 内のリンク** (`Output/DocString.lean` 経路) は増分 1 から一貫して範囲外

### 段階 3 — 総括 (2026-08-10)

**段階 3 は通った。** `approach.md` §7 の 3 行目「依存パッケージの IR なしでリンクが解決できる」
の判断基準を、348/432 モジュールの範囲で両方満たした。

| 判断基準 | 結果 |
|---|---|
| (a) Mathlib へのリンクが正しく張れる | **達成** — 写像だけで href 一致 99.992%、取りこぼし 0 |
| (b) 必要な写像の実サイズが分かる | **達成** — 下限 53 KB / 上限 34.3 MB (647 倍) |

撤退ライン (削除済の `plans/stage3.md` §4。読むなら git) 4 本のうち **3 本は否定**、1 本は未検証:

| 撤退ライン | 判定 |
|---|---|
| 依存側の blacklist が写像から復元できない | **否定** (欠落 = blacklist そのもの。litedoc4 の方が dead link を出さない) |
| 参照定数の解決に環境が要る | **否定** (依存 533 中 530 が名前一致だけで解決) |
| 写像のロード時間が環境ロードの床と同等以上 | **否定** (床の 8%、参照分なら 0.01%) |
| 公開ドキュメントに bmp 相当が存在するか | **未検証** (ネットワーク不使用、事前決定 #5) |

**この段階で設計に足すべきこと** (`approach.md` に反映済み):

- IR は絶対識別子 (モジュール名 + 宣言名) を持ち、**相対化は出力時**
- 写像は 2 列 (名前, モジュール)。`kind` は検索 UI を作るときだけ要る
- **`_private.` 名の扱いが未決** — 素朴に URL を出すと dead (実測 8 名)。自分の IR 側の問題
- **docstring 内のリンクは段階 1〜3 で一貫して範囲外**。別経路 (`Output/DocString.lean`) なので
  署名側とは別の仕組みになる

**引き継ぐ制約**: すべての一致率は **348/432 モジュール**の範囲。残り 84 モジュールを
含めるには doc-gen4 の HTML 生成をやり直す必要があり、それ自体が高い (フルビルドは 42% で打ち切り)。

### 段階 4 準備 — IR 永続化 (2026-08-10)

**計測条件**: Apple M1 / 8 コア / 16 GB、Lean・Mathlib・doc-gen4 すべて v4.31.0、
`LEAN_NUM_THREADS` 未設定、olean ビルド済み、並列度 1 (単一プロセス)、
**warm** (壁時計 ≒ user+sys を全 run で確認、peak RSS 3.06 GB)。
対象 432 モジュール / 4,750 宣言、構成は `--equations --refs`。
同一セッションで 6 系列 (IR OFF / IR ON をブロック・交互・上書きの 3 通り + stage3 の対照) を
各 6 回、run 1 を cold として捨て **runs 2–6 の中央値**。
読み出しは Deno 2.7.14 / V8 14.7。
実装は `experiments/stage4/` (stage3 のコピー + IR ライタ。stage1–3 は不変)。
ログ → `benchmarks/results/stage4-{noir,ir,alt-noir,alt-ir,irkeep,s3ref}-{1..6}.jsonl`、
条件は同名の `*-summary.txt`。読み出し → `benchmarks/results/stage4-readir.{txt,json}`。

#### まず「0.68s」の出所を辿った — 取り違えは無いが、注釈が 3 つ要る

§6.1 の「現行 doc-gen4 (warm) 書き込み 0.68s」は **432 モジュール全数の実測**で、
比例縮小でも単位の取り違えでもない。出所は `batch.updateModuleDb`:

| ログ | 値 | 状態 |
|---|---:|---|
| `benchmarks/results/batch-rerun.jsonl` | 0.676s | warm 再計測 1 |
| `benchmarks/results/batch-rerun-2nd.jsonl` | 0.660s | warm 再計測 2 |
| `benchmarks/results/batch.jsonl` | 0.639s | 初回 (cold セッション) |

いずれも `modules: 432` の 1 レコード、つまり 432 モジュールを 1 回の
`updateModuleDb` 呼び出しで書いた値。0.68 は warm 2 回の大きい方 (0.676) の丸め。

注釈:

1. **`doc-gen4-report.md` §4.6.1 の「DB 書き込み 68s (0.5%)」は別の数字。**
   打ち切ったフルビルド (8,600 中 3,590 モジュール = 42%) の `single.updateModuleDb`
   を 3,590 プロセス分合計した **67.7s** (実測、生ログは対象リポジトリの
   `docs/doc-gen-bench/raw/full-build.jsonl`)。1 モジュール 18.9 ms。
   **0.68s の 100 倍ではなく、モジュール数も経路も違う。**
2. **doc-gen4 の実運用経路では同じ 432 モジュールに 2.173s かかる** —
   `benchmarks/results/serial-warm.jsonl` の `single.updateModuleDb` 432 件の合計 (実測)。
   0.68s は本プロジェクトが計装で足した `batch` コマンドの値で、
   **モジュールごと 1 プロセスの現行実装が実際に払う額の 3.2 分の 1**。
   §6.1 の doc-gen4 列は方式B (バッチ) の内訳なので列内では整合しているが、
   「現行実装の書き込みコスト」として引用すると過小になる。
3. **warm は 2 回しか取られていない。** このリポジトリの基準 (5 回以上) を満たしていない
   唯一の行。cold 側 0.639s が warm より*速い*ことから、この行が page cache に
   ほぼ依存しないことは分かる (書き込みなので当然)。

**doc-gen4 の「書き込み」が含むもの**: `DocGen4/DB.lean:494` の `updateModuleDb`、
すなわち **SQLite への宣言の書き込みだけ**。HTML の書き込み (4.05s) と
検索用 JSON の書き込み (1.71s) は `fromDb` 側で、この行には入っていない
(レポート §4.4、Lean core 2,394 モジュールでの実測)。

#### 比較可能な範囲 — 「解析結果の永続化」だけ。倍率は出さない

| | doc-gen4 `updateModuleDb` | litedoc4 の IR |
|---|---|---|
| 書き先 | 既に 6,072 モジュールが入った 453 MB の SQLite に `deleteModule` + insert | 新規 JSON 436 ファイル |
| 署名 | `RenderedCode = TaggedText Tag` のバイナリ blob。**位置つきで全タグを保持** | プレーンテキスト + `(モジュール, 名前)` の**集合** |
| 他に書くもの | 属性、instance の `className`/`typeNames`、`sorried`、再帰子の内部名 | モジュール内容ハッシュ、依存側の写像スライス |
| 同じ 432 モジュールでのサイズ | 22.18 MB (`bench.db`) / 22.68 MB (`bench-a.db`) — 索引と空きページ込み | 8.74 MB |

**同じ仕事ではないので 0.68s と 0.198s の比を書かない。** 言えるのは
「どちらも約 12〜31 秒の実行の中で 1 秒未満であり、ここは支配項ではない」まで。

#### IR の形 (段階 3 の結論を反映)

```
<irDir>/index.json                   索引: モジュール一覧 + ハッシュ + ファイルパス + Lean/スキーマ版
<irDir>/modules/<Module.Name>.json   モジュール 1 個 = 1 ファイル (§5.4)
<irDir>/deps/<Package>.json          依存側の写像スライス (名前 → モジュール名の 2 列)
```

- **絶対識別子のみ**。参照は `(定義モジュール, 名前)` のペアで、**URL は相対も絶対も持たない**
  (相対化は出力時 = §5.6)
- 依存スライスは 2 列。`kind` は持たない (段階 3 増分 2 の結論)
- `schemaVersion` はモジュールファイルの本体に入っているので、**スキーマを上げると全ハッシュが変わる**
- ハッシュは Lean の `String.hash` の 16 桁 hex = **64 bit・非暗号学的**。
  Lean core v4.31.0 に digest が無いため。432 モジュールなら衝突確率 ~5e-15 で
  変更検出には十分だが、改竄検知には使えない (leg 7 で再検討)

#### IR 書き込み (すべて実測、書き込み有効な 10 run の中央値)

| | 秒 | 範囲 |
|---|---:|---|
| 直列化 (`Json` 構築 + `compress`) | 0.147 | 0.146–0.157 |
| **内容ハッシュ (432 回)** | **0.0023** | 0.0023–0.0024 |
| `IO.FS.writeFile` × 436 | 0.046 | 0.041–0.050 |
| **`stage4.writeIR`** | **0.198** | **0.193–0.211** |

既存ツリーへの上書き (`stage4-irkeep-*`) でも 0.196s [0.186–0.199]。
**新規作成と上書きは同じコスト。**

書き込みの 4 分の 3 は直列化 (8.6 MB のテキスト生成) で、
実際の `write(2)` は 4 分の 1。**ハッシュは書き込みの 1.2%・実行全体の 0.02%** なので、
§5.5 が「モジュール単位ハッシュを単一の真実にする」ことのコストは事実上ゼロ。

| 書いたもの | |
|---|---:|
| ファイル数 | 436 (モジュール 432 + 依存写像 3 + 索引 1) |
| バイト数 | **8,743,395 (8.34 MiB)** — モジュール 8,627,735 / 依存 27,208 / 索引 88,452 |
| 宣言 | 4,750 |
| (宣言, 参照) ペア (重複排除後) | 56,552 |
| 依存写像の件数 | **533** (Mathlib 401 / Init 131 / Lean 1) |
| モジュールを解決できなかった参照 | 0 |

533 は段階 3 増分 1 の依存側ユニーク数と完全一致。
**IR 本体はコミットしない** (8.34 MiB、再生成可能)。書き先はスクラッチ領域で、
対象リポジトリ `/Users/haruka/dev/lean-projects` には一切書いていない。

#### 読み出し — Lean を起動せずに読める (実測)

`benchmarks/tools/read-ir.ts` (Deno)。7 回、run 1 を捨てる。

| | 秒 |
|---|---:|
| in-process、runs 2–7 の中央値 | **0.0563** (0.0548–0.0643) |
| 1 read = 1 プロセス、runs 2–6 の中央値 | **0.0588** (0.0588–0.0590) |

**環境ロードの床 2.5s の 2.3%**、148 MiB/s。プロセスを毎回起こす形でも 4% 以内で一致
(V8 のヒープが温まっているだけではない)。**これは Deno/V8 での値**であり、
実装言語は未決 (§5.6) なので 1 ランタイムの上限を測ったにすぎない。

読み出しは IR の整合チェックにもなっていて、**すべての件数が過去の段階の独立な実測と一致**した:

| 読み出し | 一致先 |
|---|---|
| 宣言 4,750 | 段階 2 (= doc-gen4 の DB) |
| ユニーク `(モジュール, 名前)` 参照 1,446 | 段階 3 増分 1 |
| 依存写像 533 | 段階 3 増分 2 |
| equation 863 / モジュール docstring 1,515 | 段階 2 (= doc-gen4 の DB) |
| structure メンバ 194 | 段階 2 の field 156 + parent 1 + ctor 37 |
| 直接 import 3,935 | 段階 2 の 3,503 **+ 432** (下記) |

#### ベースライン同一性 (段階 2・3 との比較可能性)

- `--write-ir` OFF の stage4 と stage3 の `--dump` / `--dump-modules` / `--dump-refs` が
  **バイト一致** (10 モジュールの smoke、`--refs` ON/OFF の両方)
- `--write-ir` ON でも `--dump` はバイト一致 → **IR 書き込みは純粋な追加**
- IR は決定的 (別ディレクトリへの 2 回の書き込みが `diff -r` で一致)
- **同一セッションで stage3 を 6 回測り直した** (`stage4-s3ref-*`): `analyze` 9.808s /
  `total` 12.506s に対し stage4 の IR OFF が 9.804s / 12.466s。
  `produced` 4,750 と `refOccurrences` 143,121 も両者一致 —
  **同じ時間なだけでなく同じ仕事をしている。**

#### 予測と食い違った点 / 踏み外しかけた点

**1. ハッシュのタイマーが最初 0 µs を返していた (記録)。**
`let h := hashHex text` の消費者はタイマーの外 (索引エントリ構築) にしかないので、
コンパイラが呼び出しをタイマーの後ろに沈め、**8.6 MB に対して 34 µs = 253 GB/s** と出た。
落とせない分岐 (`if h.length != 16 then throw …`) で強制して 2.3 ms になった。
段階 2 の `--tactics-probe`、段階 3 の `refUs` と同型で、**3 回とも低い側 = 都合の良い側に外れた。**
なお `writeIR` の**合計は両方とも正しかった** (0.192s → 0.194s)。
タイマーが飛ばした分を後段の `writeFile` が払っていたため、内訳だけが誤っていた。
検算の型: 直列化 + ハッシュ + 書き込みは「8.6 MB ÷ 妥当なバイト単価」に収まること。

**2. `stage4.total` の差から書き込みコストを読んではいけない。**

| 系列 | `importModules` | `total` |
|---|---:|---:|
| IR OFF (ブロック) | 2.597 [2.551–2.714] | 12.466 [12.411–12.700] |
| IR OFF (交互) | 2.628 [2.548–2.696] | 12.685 [12.472–12.769] |
| IR ON・毎回削除 (ブロック) | 3.169 [2.610–4.429] | 13.447 [12.790–14.677] |
| IR ON・毎回削除 (交互) | 3.230 [2.563–5.852] | 13.251 [12.536–15.963] |
| IR ON・上書き | 2.816 [2.659–3.712] | 12.888 [12.638–14.017] |

`total` は最大 1.0s 動くのに `writeIR` はどの系列でも 0.196〜0.198s。
超過分は **`importModules`、つまり書き込みが始まる前に終わるフェーズ**に出ている。
効いているのは実行の前後のファイル操作で、**436 ファイルの削除が最も高く**、
上書きだけの系列はベースラインの半分まで戻る。2.59 GB の olean を mmap で
ページインするワークロードの隣で数千回のファイル操作をすると見える、ということ
(バイト数は ci-residency 節の実測。以前ここに書いていた「3 GB」は peak RSS からの流用だった)。
→ **書き込みコストはフェーズタイマーの 0.198s。合計の差は書き込みに帰属できない。**

最初のブロック配置 (IR OFF を 6 回 → IR ON を 6 回) では
**`--write-ir` が触れないはずの `analyze` に +0.23s が出た**ので、交互配置で測り直した。
交互配置では逆向き (IR OFF 側が +0.17s) になり、run 間ノイズと確定。

**3. import が 432 件多い。** `ModuleOut.imports` は
`header.moduleData[i].imports` の生の値で、全モジュールで `Init` が 2 回入る。
IR の import は 3,935 件、doc-gen4 の `module_imports` は 3,503 件、差はちょうど 432。
段階 2 の突き合わせは集合ベースだったので影響なし。重複除去は出力側の仕事。

**4. 参照の「位置」は IR に入っていない (leg 5 への申し送り)。**
`refs` は宣言ごとの `(モジュール, 名前)` の**集合**で、印字された署名のどのオフセットに
現れたかは落としている。doc-gen4 は保持している (`name_info.type` が
`RenderedCode` のバイナリ blob)。署名中の `Real.log` をその位置で `<a>` にする
レンダラは位置を要求する。**leg 5 が「集合で足りるか」を判定する。**
足りなければ IR は太り、この計測はやり直しになる — その場合の再計測は退行ではない。

#### 判断

**§6.1 の「書き込み 0.68s = 仮定」は実測 0.198s に置き換わった。**
段階 4 (「IR から HTML を生成でき、再生成に Lean が要らない」) の**前提**である
「Lean を起動せずに IR を読める」も実測で確認した (0.0563s、床の 2.3%)。
段階 4 本体の判断基準 (HTML を Lean 無しで作り直せる) は leg 5 で出す。

`approach.md` に反映した箇所:

- §6.1 の表を段階 4 の構成 (equations + 参照定数 + IR 書き込み) で組み直し、
  書き込み行を実測に、合計を **12.73s 実測**に
- §6.3 の抽出行 (12.5s = 実測 + 仮定) → **12.73s 実測**、合計 約 14 秒は据え置き
- §6.4(a) の 12.5s / 86× → **12.73s / 85×**、「0.68 秒だけが仮定」の但し書きを削除
- §6.4(b) の 13.8s → **14.0s**、約 2,400× は据え置き
- §5.4 に「ハッシュは実測で書き込みの 1.2%」を追記
- `README.md` の理論値表 (11.8s / 86× / 13.8s)

### 段階 4 — 増分 1 / 出力範囲の確定と「参照集合で足りるか」の判定 (2026-08-10)

**計測条件**: 突き合わせ対象はディスク上の doc-gen4 出力
`/Users/haruka/dev/lean-projects/.lake/build/doc/InformationTheory/` (348 ページ /
22,028,728 バイト、2026-08-09T08:40Z 生成、doc-gen4 `0bc516c1` = v4.31.0、
計装パッチ適用済み — パッチは計時ログを足すだけで HTML を変えない)。
IR は `experiments/stage4` が 2026-08-09T15:41Z に書いた schemaVersion 1
(432 モジュール / 4,750 宣言)。集計は `benchmarks/tools/html-inventory.py`
(Python 3.9.6、実行 1.5s)。**すべて実測** — ログ →
`benchmarks/results/stage4-html-inventory.{txt,json}`。時間の計測はこの増分には無い。

**母数の但し書き**: 348 ページはすべて IR 側にモジュールを持つが、逆向きには
**IR の 84 モジュールに HTML ページが無く、うち 77 は宣言を持っている**。
ディスク上の `.lake/build/doc` は 432 モジュールを出し切ったビルドではない
(フルビルド 42% 打ち切りの影響)。以下の割合はすべて「348 ページの範囲」。

#### 結論 — **参照集合では署名内リンクを再構成できない。IR に位置情報が要る。**

`experiments/stage4/README.md` が「leg 5 が集合で足りるかを判定する」と書いた分岐は、
**足りない側に倒れた**。3 つの独立な根拠 (すべて実測)。

**(1) 印字トークンと定数名に字面の関係が無いものが 44.0%**

`div.decl_header` (署名 = binder + 結果型 + `extends`) 内のアンカー 78,065 件の内訳:

| | 件数 | |
|---|---:|---:|
| 宣言へのリンク | 72,421 | 92.8% |
| sort リンク (`foundational_types.html`) | 5,638 | 7.2% |
| モジュールリンク | 6 | 0.0% |

宣言リンク 72,421 件を、アンカーテキストと対象名の字面の関係で分けると:

| | 件数 | |
|---|---:|---:|
| テキスト == 完全名 | 29,042 | 40.1% |
| テキストが完全名の接尾辞 (`Foo.bar` を `bar` と印字) | 11,514 | 15.9% |
| **字面の関係なし (記法)** | **31,865** | **44.0%** |

記法の上位: `ℝ`→`Real` 4,901、`ℕ`→`Nat` 3,381、`*`→`HMul.hMul` 2,081、
`≤`→`LE.le` 2,047、`×`→`Prod` 2,024、`{`/`}`→`Singleton.singleton` 各 629。
どの記号がどの定数かは delaborator が持つ情報で、**名前の集合には無い**。
sort リンク 5,638 件はそもそも `refs` に入らない (`Expr.sort` は定数ではない —
段階 3 の設計判断)。

**(2) 素朴照合の recall は上限でも半分**

IR の平文 (`binders`+`type`) と `refs` 集合だけを使う最強の素朴解法
(トークンが完全名なら link、短縮名が `refs` 中で一意ならその定数へ link) を実装して測った。
**評価は宣言ごとの (テキスト, 対象) 多重集合の比較 — 位置を問わない上限評価**である。

| | |
|---|---:|
| doc-gen4 が出したリンク (正解) | 72,421 |
| 素朴照合が出したリンク | 37,462 |
| 一致 | 37,458 |
| **recall** | **51.7%** |
| precision (位置不問) | 100.0% |
| **宣言単位の完全一致** | **140/3,477 = 4.0%** (うちリンク 0 個の自明例 4) |
| 到達可能な天井 (完全名 + 接尾辞のアンカー) | **56.0%** |

precision 100% は「出したものは当たる」であって「足りている」ではない。
**天井が 56% なので、実装を頑張っても伸びない。**

**(3) 集合では原理的に決まらないケースが実在する**

| | 件数 |
|---|---:|
| 同じ印字トークンが 1 署名内で 2 つ以上の別定数を指す | 352 |
| 1 署名内で同一対象が複数回リンクされる (宣言, 対象) 組 | 15,026 |
| 1 宣言の `refs` 内で短縮名が衝突 | 118 |
| binder 名が `refs` の短縮名と一致 (局所束縛が参照名を隠す) | 14 |

実例: `capacity_image_nonempty` の署名には `Nonempty` が 2 回現れ、1 回目は
`Init.Prelude.Nonempty`、2 回目は (ドット記法で) `Set.Nonempty`。
**`refs` は両方を持っているが、どちらがどちらかを決める情報を持っていない。**
`MeasureTheory.integral` が `∫` と `,` の 2 つの `<a>` に分かれ、間に別のリンクが挟まる例もある
(`Singleton.singleton` の `{`…`}` が 629 対、`Subtype.mk` の `⟨`…`⟩` が 143 対)。

#### 対照 — **あて先の解決は集合で足りている**

紛れないように分けて書く。「どこにリンクを置くか」は足りないが、
**「その名前がどのモジュールの何か」は IR がほぼ完全に持っている**。

348 ページの内部アンカー 80,743 件 / 2,442 種を、IR から作れる名前集合
(宣言名 4,750 + `refs` 1,446 + `members` 194 + `deps/*.json` 533 = 5,300) に当てる:

| 領域 | アンカー | 解決できない |
|---|---:|---:|
| `div.decl_header` | 72,421 | **0** |
| `ul.equations` | 3,278 | **0** |
| その他 (docstring・instances・structure field) | 5,044 | 293 |
| **合計** | **80,743** | **293 (0.36%)** |

**293 件全部が docstring の `<code>` 内 autolink** (`Real.log_prod`, `csInf_le` 等、
署名に出てこない Mathlib 補題)。段階 3 の「写像だけで href 一致 99.992%」と整合し、
署名側の取りこぼしが 0 であることをページ側から独立に確認したことになる。

#### 出力範囲の確定 — ページの 7 割は署名の描画

「doc-gen4 と同じ仕事」を主張するときに何を出す必要があるかを、バイトで固定する。

| 領域 | バイト | 比 |
|---|---:|---:|
| 合計 (348 ページ) | 22,028,728 | 100.0% |
| `head` + `header` + `nav.internal_nav` | 2,065,396 | 9.4% |
| … うち `nav.internal_nav` (宣言ジャンプ表) | 1,445,893 | 6.6% |
| **`div.decl_header` (署名)** | **15,661,530** | **71.1%** |
| `ul.equations` | 497,903 | 2.3% |
| 残り (docstring・instances・`<main>` の枠) | 3,803,899 | 17.3% |

**署名内リンクを落とすと HTML の 71.1% を作っていないことになる**ので、
それを落としたまま生成時間を doc-gen4 と比べると分母が違う
(CLAUDE.md「倍率は分母を明示する」に真正面から当たる)。
したがって段階 4 は**位置情報を足してから時間を測る**。

#### 規則で埋まるもの (IR に追加フィールドは要らない) — 実測で確認

| 項目 | 結果 |
|---|---|
| 宣言とモジュール docstring の並び | `line`,`col` の安定ソートで **348/348 ページ**再現。ただしキーが一意に決まるのは 346/348 |
| Equations の省略規則 | 描画テキスト長 **200 文字以上**の equation を落とし通知 li を足す規則で、省略あり 195/195・なし 3,282/3,282・`ul.equations` 653/653・`li.equation` 個数 3,477/3,477 |
| `render := false` 相当 (IR 3,663 に対し HTML 3,477、差 **186**) | **186/186 が親 `structure` の `members` に載っている名前と完全一致**し、逆に `members` の名前が単独 `div.decl` として出ることは 0 件。**IR が既に正確に決められる** |

`equationLimit` は「equation の本数」ではなく**1 本ごとの描画テキスト長**の閾値である
(当初「200 本超で省略」と誤解していた。総計はどちらでも合うので、規則を取り違えたまま
数字だけ合わせる危険があった)。

#### IR に足りないもの (位置情報以外)

| 足りないもの | 影響 (348 ページ母数) |
|---|---|
| **署名内タグの位置** | アンカー 72,421 件 + `span.fn` 216,838 個。**これが本体** |
| sort タグ (`Type`/`Prop`) | 5,638 アンカー。`refs` には設計上入らない |
| declaration range の終端行 | `gh_link` 3,477 件が `#L<開始>-L<終了>` を作れない (開始行は 3,477/3,477 一致) |
| `noncomputable` / `abbrev` / `unsafe` の修飾 | `definition` 650 件中 **456 件 (70%)** のラベルが誤る。`instance` も 56 中 1 |
| 同一 `(line,col)` のタイブレーク | 2 ページ / 4 宣言 (`Polymatroid.ext`/`ext_iff`、`differentialEntropyExt`/`_def`) |
| 構造体フィールドの binder / docstring / `isDirect` | `structure_field_info` 153 / `structure_field_doc` 114 / `inherited_field` 4 |
| 属性 (`div.attributes`) | 89。段階 2 が意図的に落とした分 (§6.1 の未実装分) |

#### doc-gen4 側の欠陥 (真似しない差分として記録)

`<a href="#top">return to top</a>` が **348/348 ページ**にあるが、`id="top"` を持つ要素は 0。
全ページでこのリンクは死んでいる。段階 3 の `Eq.rec` 3 件と同じ扱い —
**一致率が 100% にならない原因を「litedoc4 の欠陥」と読まないための記録**。

#### 判断

**段階 4 は続行。ただし IR を太らせる。** これは `experiments/stage4/README.md` が
事前に書いた分岐 (「集合で足りなければ IR は太り、この計測はやり直しになる —
その場合の再計測は退行ではない」) の発動であって、設計の否定ではない。

次の増分でやること:

1. 抽出器に**位置つきタグ**を足す (`Widget.TaggedText.prettyTagged` の木は
   `Extract.lean` が既に作っていて、`collectConsts` が名前だけ拾って位置を捨てている。
   捨てずに残す)。既存の数字の再現性のため**新ディレクトリ**に置く
2. 併せて 1 フィールドで済む欠落 (`endLine`、`noncomputable`/`abbrev`/`unsafe`、
   宣言の出現順) を足す
3. **`stage4.writeIR` 0.198s と読み出し 0.056s を測り直す** (IR が太るため)
4. その上で Deno の HTML 生成器を書き、受け入れテストは
   **`decl_header` 内アンカーの (位置, テキスト, href) 三つ組の列比較**で取る
   (集合比較では上の precision 100% のような罠を通してしまう)

`approach.md` に反映した箇所:

- §5.6 の「位置つきのリンクが要ると分かれば IR を太らせる必要がある — 段階 4 の判定項目」
  → **判定済み。要る**
- §8 の「docstring 内のリンクをどう解決するか」に実測 (293 件 / 188 種、
  署名側の取りこぼしは 0) を追記

### 段階 4 — 増分 2 / 位置つきタグの追加と、IR 永続化の再計測 (2026-08-10)

**計測条件**: Apple M1 / 8 コア / 16 GB (Darwin 25.6.0 arm64)、Lean・Mathlib・doc-gen4
すべて v4.31.0、`LEAN_NUM_THREADS` 未設定、olean ビルド済み、並列度 1 (単一プロセス)、
対象 432 モジュール / 4,750 宣言、構成は `--equations --refs` (+ `--tagged-code`)。
読み出しは Deno 2.7.14 / V8 14.7。実装は `experiments/stage4b/` (stage4 のコピー +
位置つきタグ。**stage1〜4 は不変**)。
2 系列 × 6 回を**交互配置**で回し、run 1 を捨てて **runs 2–6 の中央値** [min-max]。
**すべて実測** — ログ → `benchmarks/results/stage4b-alt-{notag,tag}-{1..6}.jsonl`
(条件は同名の `*-summary.txt`)、読み出し → `stage4b-readir-{notag,tag}.{txt,json}`。

**warm 判定**: `notag-1` のみ cold (壁 20.77s / CPU 13.48s、`importModules` 10.54s、
page faults 76,271)。**「run 1 を捨てる」規則で落ちたのであって、cold だったから
捨てたのではない。** 残り 9 run はすべて壁時計 ≒ user+sys (0.98–0.99)、
page faults 372–1,314、peak RSS 3.29–3.33 GB。

#### IR に足したもの

増分 1 の判定 (集合では署名内リンクを作れない) を受けて、断片ごとの
**フラットな pre-order スパン列** `[start, end, kind, name?]` を持たせた
(`kind` 0 = `span.fn` / 1 = 定数 / 2 = sort、オフセットは **UTF-16 コード単位**)。
doc-gen4 の `renderTagged` (`DocGen4/RenderedCode.lean`) の Tag 分類を写している。
併せて宣言範囲の終端行・`noncomputable`/`abbrev`/`unsafe`・モジュール内出現順を追加。

| | schema 1 (タグ無し) | schema 2 (タグ付き) | 比 |
|---|---:|---:|---:|
| IR 総バイト | 8,743,396 | 15,867,059 (15.13 MiB) | **×1.815** |
| スパン | — | 408,804 (定数 142,181 / sort 7,550 / その他 259,073) | |
| 断片 | — | 55,465 | |

`refOccurrences` は両系列とも 143,121 で一致 — **別の walk が同じタグを見ている**ことの確認。
Baseline identity 4 項目 (stage4 との `--dump` バイト一致 / IR バイト一致 /
`--tagged-code` が純粋に加算的 / IR が決定的) はすべて PASS
(`experiments/stage4b/baseline-identity.sh`、再実行可能)。

#### 書き込み — `writeIR` は 0.198s → **0.549s** (×2.77)

| フェーズ | schema 1 | schema 2 | 差 |
|---|---:|---:|---:|
| `importModules` | 2.5518 [2.4964-2.7596] | 2.5224 [2.4809-2.5631] | −0.029 |
| `analyze` | 9.7004 [9.6164-9.7941] | **10.5836** [10.5438-10.7120] | **+0.883** |
| … `ppUs` | 8.5178 | 9.3190 | +0.801 |
| … `eqUs` | 0.9765 | 1.0590 | +0.082 |
| … `refUs` | 0.4638 | 1.2984 | +0.835 |
| **`writeIR`** | **0.1982** [0.1911-0.2245] | **0.5491** [0.5457-0.5826] | **+0.351** |
| … serialize | 0.1475 | 0.4961 | +0.349 |
| … hash | 0.0023 | 0.0035 | +0.001 |
| … write | 0.0475 | 0.0536 | +0.006 |
| `total` | 12.5340 | **13.7088** [13.6169-13.7737] | **+1.175 (+9.4%)** |

**タグ付けの正味は +1.175s (+9.4%)**、うち **生成 +0.883s / 永続化 +0.351s**。
**生成が永続化の 2.5 倍**であり、注目されていた書き込みの方が小さい半分だった。

**`refUs` の +0.835s を「位置情報のコスト」として引用しないこと。** schema 1 の
`refUs` は名前だけの walk、schema 2 は**スパンの walk + 幅の不変条件の検査**
(断片ごとに 1 回の線形走査。タイマーの中にあることを stage4b の README が明記している)。
差は「(スパン walk + 幅検査) − (名前 walk)」であって、何もしない状態との差ではない。

**タイマーの健全性** (0 µs の罠を 3 回踏んでいるため毎回確認する): 下位タイマーは
すべて入力量と一緒に動いた (serialize ×3.36 / hash ×1.52 / write ×1.13)。
毎バイト速度は serialize 59.3 → 32.0 MB/s、write 184 → 296 MB/s、hash 3.88 → 4.59 GB/s
でいずれも妥当。内訳の和がフェーズ値に一致 (0.1973 対 0.1982、0.5532 対 0.5491 —
中央値は別 run 由来なので厳密一致はしない)。

**交互配置は効いた**: `importModules` (フラグが触れないフェーズ) の系列間差は −0.029s。
stage 4 のブロック配置は `analyze` に +0.23s の偏りを作っていた。

**同一セッションの対照**: schema 1 側は `writeIR` 0.1982 / 0.1475 / 0.0023 / 0.0475 で
段階 4 準備の 0.198 / 0.147 / 0.0023 / 0.046 を再現した。**過去の数字と直接並べず、
同一セッション内の差だけを使っている。**

#### 読み出し — 0.056s → **0.100s**。バイト比例より**速い**

| | schema 1 | schema 2 |
|---|---:|---:|
| バイト | 8.34 MiB | 15.13 MiB (×1.815) |
| in-process 中央値 (runs 2-7) | 0.0568 [0.0559-0.0591] | **0.1004** [0.0990-0.1047] (**×1.77**) |
| 別プロセス 1 read ずつ (runs 2-6) | 0.0590 | 0.1042 |
| スループット | 146.73 MiB/s | **150.66 MiB/s** |
| **warm `importModules` の床 (2.5s) 比** | 2.3% | **4.0%** |

**予測が外れた点。** 「小さい配列が大量に増えるので、読み出しはバイト比例より*悪く*なる」
と予測して測ったが、**逆にスループットが 2.7% 上がった** (×1.815 のバイトに対し時間は ×1.77)。
書き込み側は逆で、`Json.compress` の毎バイト速度がほぼ半減している (59.3 → 32.0 MB/s)。
**同じ入れ子の小配列という形が、書き込みでは毎バイト高くつき、読み出しでは高くつかない**
— schema 2 は書き込み側のコストであって読み出し側のコストではない。

スパンを実際に走査するコストは +0.0031s (+3.2%) で、`JSON.parse` が支配項という予測は当たった。
読み出し側が独立に数えた 408,804 スパン / 142,181 定数 / 7,550 sort / 259,073 その他は
書き込み側の記録と一致する (writer ↔ reader の交差検証)。

**`read-ir.ts` の拡張が加算的であることの確認**: 拡張版を **stage 4 の IR そのもの**
(schema 1、未変更) に対して走らせ、コミット済み `stage4-readir.txt` の集計表 12 行が
`diff` でバイト一致することを確認した (時間も 0.0573 / 0.0588 で記録済みの散らばりの内側)。
ログ → `benchmarks/results/stage4b-readir-schema1-check.txt`。

#### 既知の欠落 (実測)

- **定数の出現 143,121 のうち 940 件 (0.66%) に位置が無い** — 構造体メンバの署名の
  *binder* 部分。`Member` が `sig.type` しか持たないので、スパンが指すテキストが存在しない。
  37 個の structure に絞った再実行で差が正確に再現する (1,884 出現 / 944 保存 / 差 940)。
  **レコードの形の問題であってタグ付けの問題ではない。** 増分 1 が数えた
  `structure_field` アンカーは 153 件
- **`splitWhitespaces` の空白復元**: 定数タグが改行をまたぐと doc-gen4 は `"\n   "` を
  `"    "` に潰すが IR は改行のまま持つ。文字数は変わらないのでオフセットは動かないが、
  **HTML のバイト単位の diff では差として出る**

#### 判断

**続行。** IR が ×1.815 に太っても、読み出しは環境ロードの床の **4.0%** にとどまり、
§5.6 の「出力は Lean の外側」という分離は依然として安い。抽出全体は
**12.53s → 13.71s (+9.4%)** で、§6.1 が「未実装分を入れると 13 秒台になる見込み
(目標 14.5 秒には収まる)」と書いていた予測の範囲内 —
ただし**まだ属性・instance 索引は入っていない**ので、これで打ち止めではない。

`approach.md` に反映した箇所:

- §6.1 の表を schema 2 (位置つきタグ) の構成に更新。意味解析 9.90 → **10.58s**、
  書き込み 0.198 → **0.549s**、合計 12.73 → **13.71s** (すべて実測)。
  schema 1 の列を同一セッションの対照として残す
- §6.3 / §6.4(a)(b) / `README.md` の合計と倍率を 13.71s 基準に

### 段階 4 — 増分 3 / IR から署名 HTML を再構成 (2026-08-10)

**問い**: **Lean を一度も起動せずに**、IR (schema 2) だけから doc-gen4 と同じ
`div.decl_header` を作れるか。これが【判断点 2】の正しさ側。**時間は測っていない** (増分 4)。

**計測条件**: 生成・突合とも Deno 2.7.14 / V8 14.7 (macOS 26.6 arm64)。入力は
`experiments/stage4b` の schema 2 IR (432 モジュール / 4,750 宣言 / 15.13 MiB)。
正解は doc-gen4 v4.31.0 が実際に出した HTML (rev `0bc516c1`、生成 2026-08-09T08:40Z)。
実装 → `experiments/stage4c/`、正解抽出 → `benchmarks/tools/decl-header-truth.py`
(条件と検算は `benchmarks/results/stage4-decl-header-truth.txt`)。
生成物 23 MB は再生成可能なのでコミットしていない。**すべて実測。**

#### 母数 — 348 ページ / 3,477 宣言

doc-gen4 の HTML は **432 モジュール中 348 しかない** (フルビルドを 42% で打ち切ったため)。
その 348 モジュールの IR 側 3,663 宣言から、親 structure に畳まれる 186 件を除いた
**3,477** が母数。**この 348/3,663/186/3,477 を IR だけから独立に再現し、
正解側と名前単位で一致した (欠落 0 / 余剰 0)。** HTML の無い 84 モジュール分
1,083 宣言も生成しているが**採点対象外で、何も主張しない**。

#### 結果 — アンカーの列は 3,477/3,477 一致、バイトは 2,705/3,477 一致

比較はすべて**順序を保った列**。増分 1 の素朴照合は多重集合で評価したせいで
precision が 100% に見えていた (実際は位置を無視した上限) ので、今回は集合比較を一切使わない。

| 指標 | 一致 | 母数 | 率 |
|---|---:|---:|---:|
| 宣言単位の完全一致 — `(位置, テキスト, href)` の列 | 3,477 | 3,477 | **100.000%** |
| アンカー単位の一致 (三つ組) | 81,542 | 81,542 | **100.000%** |
| … href だけ違う / 位置だけずれ / 欠落 / 余剰 | 0 | 81,542 | 0% |
| `span.decl_kind` の一致 | 3,477 | 3,477 | 100.000% |
| `span.impl_arg` の個数の一致 | 3,477 | 3,477 | 100.000% |
| 平文の完全一致 | 2,705 | 3,477 | 77.797% |
| **生成マークアップの byte 完全一致** | **2,705** | **3,477** | **77.797%** |
| … 長さは同じで空白文字だけ違う | 772 | 3,477 | 22.203% |
| … 長さが違う / 空白以外が違う | 0 | 3,477 | 0% |

**100% を信用しないための三重化。** 有利な数字ほど疑う規則の適用:

1. **正解側が二重**: `decl-header-truth.py` は独立な 2 経路 (`html.parser` /
   タグ深さ走査 + 正規表現) で同じ 3,477 宣言を解析し、平文も `(href, text)` 列も
   食い違い 0。既存実測 7 項目 (348 / 329 / 3,477 / 78,065 / 72,421 / 5,638 / 6) と全一致
2. **採点器に欠陥注入**: href 破壊 / 1 文字挿入による位置ずれ / アンカー削除 /
   `decl_kind` 書換の 4 種を各 300 宣言に注入し、**300/300 ずつ検出**。
   「0 件試行」を PASS と報告しないガードも入れてある
3. **抽出を挟まない byte 比較**: 生成マークアップを doc-gen4 のページから切り出した
   `div.decl_header` の部分文字列と直接 diff。オーケストレータ側でさらに独立に
   (バランス `<div>` 走査) 再実行し、**doc-gen4 の decl_header 総バイト 15,661,530 が
   §C の実測と一致**することを確認した上で、同じ 2,705 / 772 / 0 / 0 を得た

つまり **生成物は「等価」ではなく大半が「同じバイト列」**であり、残り 772 も
同長の空白置換だけ。だからアンカー表が 100% で平文表が 77.8% になる。

補助の交差検証 (実測): ヘッダ経路が消費した定数スパン **126,717** − 出したアンカー
**101,641** = **25,076** が、レンダラが独立に数えた入れ子アンカー抑制の回数と一致。
sort は 7,354 消費 / 7,354 出力。**定数スパンの 57% が入れ子**なので、
平坦に走査する実装はここで壊れる。

#### 唯一の欠落 — `splitWhitespaces` (IR の情報不足、実測)

| | |
|---|---:|
| 差のある宣言 | 772 / 3,477 (22.203%) |
| 差のある文字 | 1,765 (すべて `"\n"` → `" "`) |
| 正解の平文の総長 (UTF-16 code units) | 1,528,638 |
| 差の割合 | **0.115%** |
| 影響を受けたアンカー | **0** (同長置換なのでオフセットが動かない) |

**schema 2 からは復元できないことを実測で確かめた。** 「childless な定数スパンに接する
空白列をスペース化する」推測を入れると byte 一致は 2,705 → 3,012 になるが、
**407 件直して 100 件壊す** — 改行が定数タグの*外側*にある実例
(`<a>Filter.Tendsto</a>\n  (fun …`) があり、IR は両者を区別できない。
**推測は既定では入れていない。上の数字は忠実な生成の方。**

閉じるには抽出器が定数タグの**トリム前の範囲**も記録すればよい
(`wsTrim` が既に計算して捨てている `front`/`back` の幅 2 つ)。schema 3 の小変更。
**今回は入れない** — アンカーに 0 件しか影響せず、入れると抽出の再計測が必要になるため。
`docs/approach.md` §6.1 の数字を byte 完全一致まで要求する段になったら戻る。

#### この対象が試していない分岐 (実測のカバレッジ)

100% は**この対象に出てくる範囲での 100%**。出現しなかったので**転写しただけで未検証**:

- `decl_kind` は 9 種しか出ない (`theorem` 2,734 / `noncomputable def` 443 / `def` 194 /
  `instance` 55 / `structure` 36 / `abbrev` 12 / `opaque` 1 / `noncomputable abbrev` 1 /
  `noncomputable instance` 1)。**未出現**: `class` / `class inductive` / `inductive` /
  `constructor` / `axiom` / `partial def` / `unsafe *` 系すべて
- `extends` は **348 ページ中 1 例**。親が複数のときの `", "` 区切りは未検証
- 定数のフォールバック連鎖: 直接ヒット 126,711 回 / private 名 → モジュールリンク 6 回。
  `findLinkableParent` と `span.fn` 落ちは**発火 0 回**
- `div.decl.sorried` は 0 件。IR に `sorried` が無いことがここでは無料

**増分 2 の既知の欠落 940 件 (位置の無い定数出現) はここでは 0 アンカーの影響**だった —
`decl_header` は structure メンバの binder を印字しないため。次の増分のメンバ表でだけ効く。

#### 判断

**【判断点 2】の正しさ側は通過。「Lean を起動せずに IR から署名 HTML を再構成できる」
と言える。** 範囲は `div.decl_header` (ページの **71.1%** のバイト、§C 実測)、
条件は上の未検証分岐を転写扱いのまま残していること。

残りは増分 4: ページ全体 (chrome / docstring / equations / instance 一覧 / `gh_link`) を
埋めて **warm の生成時間を測り、`approach.md` §6.2 の「1.28s = 仮定」を置き換える**。
これが 3 軸の「初回」に残った最後の仮定。→ **次節で決着** (warm 0.82s)。

増分 4 前半 (ページ全体 + 領域別バイト会計、**63.6% が byte 完全再現**、
未再現 36.4% を原因別に全額配賦) は本ログに節を立てていない。
**出所は `experiments/stage4c/README.md` の「Increment 4」節**で、
`coverage.ts` が §C の 7 項目を独立に再計算して一致することまで含めてそこにある。

### 段階 4 — 増分 4 / ページ全体の生成時間の実測【判断点 2 の結論】 (2026-08-10)

**問い**: IR (schema 2) から **432 モジュールぶんの HTML ページをディスクに書く**まで、
warm で何秒か。これが `approach.md` §6.2 の「N+1 解消後 **1.28s** = **仮定**」を
置き換える数字であり、3 軸の「初回生成」に残った最後の仮定
(3 軸の実行計画 §3)。

**計測条件**: Apple M1 / 8 コア / 16 GB (Darwin 25.6.0 arm64)、Deno 2.7.14 /
V8 14.7.173.20-rusty、並列度 1 (1 プロセス・worker 無し)、**Lean は一度も起動していない**。
入力 = `experiments/stage4b` の schema 2 IR (432 モジュール / 4,750 宣言 / 15,867,059 バイト)、
出力 = **432 ファイル / 29,671,173 バイト**を scratch に書く (計測対象リポジトリには書いていない)。
1 run = 1 プロセス。壁時計は µs 精度の monotonic clock、CPU 時間 / peak RSS / page fault は
`/usr/bin/time -l` (macOS の `real` は 2 桁しか出ないので壁時計はこちらを使わない)。
**すべて実測** — ログ → `benchmarks/results/stage4c-render-*.{jsonl,txt}`、
条件つきサマリ → `stage4c-render-summary.txt`。再現 → `experiments/stage4c/time-render.sh`。

**warm 判定**: page faults が cold 148 → warm 3 に落ちて以降動かない。
**`(user+sys)/壁` は 1.3 前後で 1 を超える** — これは cold の印ではなく、V8 と tokio の
補助スレッドが並行に走っている印。`CLAUDE.md` の「壁時計 ≒ user+sys なら warm」は
単一スレッド前提の判定なので、ここでは **page fault と cold/warm の壁時計差の両方**で判定した。

#### cold と warm

| | 壁時計 | user+sys | (user+sys)/壁 | peak RSS | page faults | run |
|---|---:|---:|---:|---:|---:|---:|
| **cold** (セッション最初の 1 run) | **0.9331** | 1.1600 | 1.243 | 222 MB | 148 | 1 |
| **warm** (runs 2–6 の中央値 [min-max]) | **0.8239** [0.8229–0.8360] | 1.1100 [1.0900–1.1200] | 1.35 | 219 MB | 3 | 5 |
| warm・20 run 系列 (runs 2–20、収束確認) | 0.8368 [0.8176–0.9155] | 1.1300 [1.0800–1.2000] | | | 3 | 19 |

**cold / warm = 1.13。olean の mmap のような 5 倍の振れは無い。**
ただしこの cold は「IR と出力先ディレクトリが cold」であって「機械が cold」ではない —
deno のバイナリと transpile キャッシュは直前の `deno check` で既に warm。
sudo を使わない方針 (計画 §6 事前決定 #6) なのでページキャッシュは破棄していない。

20 run 系列で分布を見ると **bimodal** で、4/19 run が描画側だけ約 9% 遅い
(`renderHeaders` 0.214 → 0.260)。6 run では引きが偏りうるので長い系列を併記している。

#### 分解 (warm、runs 2–6 の中央値 [min-max])

| フェーズ | 秒 | 壁時計比 |
|---|---:|---:|
| deno の起動 (空スクリプトの壁時計、**別計測**) | 0.0262 [0.0260–0.0273] | 3.2% |
| IR の読み出し (`readTextFile` + `JSON.parse`) | 0.1297 [0.1287–0.1322] | 15.7% |
| 名前写像・抑制集合の構築 | 0.0089 [0.0088–0.0091] | 1.1% |
| 描画: `decl_header` (4,560 宣言) | 0.2131 [0.2115–0.2196] | 25.9% |
| 描画: ページ本体 (432 ページ) | 0.2494 [0.2480–0.2497] | 30.3% |
| … うち docstring 描画 (**内数**) | 0.1780 [0.1762–0.1781] | 21.6% |
| 描画: 文字列の実体化 (rope の平坦化) | 0.0484 [0.0480–0.0490] | 5.9% |
| 書き出し (`mkdir` + `writeTextFile` ×432) | 0.1341 [0.1311–0.1356] | 16.3% |
| **内訳の和** | **0.7839** [0.7832–0.7954] | 95.1% |
| プロセス内合計 (`timeOrigin` → 終了) | 0.7859 [0.7853–0.7974] | 95.4% |
| プロセスの起動と後始末 (壁 − プロセス内合計) | 0.0380 [0.0376–0.0386] | 4.6% |
| **壁時計** | **0.8239** [0.8229–0.8360] | 100% |

**内訳の和はフェーズ値と合う**: 0.7839 対 0.7859 で差 0.0020 秒 (0.25%)。
スループットは全体 36.0 MB/s、描画のみ 58.1 MB/s、書き出しのみ 221.2 MB/s、
IR 読み出し 121.6 MB/s。1 ページ 1.91 ms / 1 宣言 181 µs。

**deno の起動は引き算で推定せず、空スクリプトを同条件で 6 回測った** (0.0262s)。
`performance.timeOrigin` は exec ではなく Deno の初期化の途中に置かれるので、
プロセス内で読める値 (0.0016s) を起動時間として引用してはいけない。

#### タイマーの健全性

**(a) rope の罠は実在した。** `pageHtml` は `+=` で組むので V8 は cons string を返し、
実体化は `writeTextFile` まで遅延する。実体化を描画側で強制しないと
「描画が速く書き出しが遅い」という**嘘の分解**が出る。対照実験 (実測):

| | flatten | write | 壁時計 |
|---|---:|---:|---:|
| 既定 (描画側で実体化) | 0.0484 | 0.1341 | 0.8239 |
| `--no-flatten-probe` | 0.0000 | 0.1827 | 0.8374 |
| 差 | **−0.0484** | **+0.0486** | +0.0134 |

**費用は消えずに移動する** (−0.0484 対 +0.0486)。合計は 20 run 系列の散らばりの内側。
段階 4 増分 2 の「0 µs タイマー」と同じ型の罠 (値の消費者がタイマーの外にある) で、
**今回は踏まなかったことを対照で示した**。

**(b) 全タイマーが入力量と一緒に動く** (108 / 216 / 324 / 432 モジュール、各 9 run):

| モジュール | 宣言 | IR バイト | read IR | index | render hdr | render page | … docstring | flatten | write | 壁 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 108 | 906 | 2,695,289 | 0.0276 | 0.0028 | 0.0419 | 0.0616 | 0.0441 | 0.0088 | 0.0302 | 0.2149 |
| 216 | 1,902 | 5,846,337 | 0.0526 | 0.0046 | 0.0784 | 0.1089 | 0.0799 | 0.0182 | 0.0604 | 0.3632 |
| 324 | 3,286 | 9,984,674 | 0.0807 | 0.0068 | 0.1307 | 0.1789 | 0.1316 | 0.0308 | 0.0912 | 0.5616 |
| 432 | 4,560 | 15,778,518 | 0.1304 | 0.0094 | 0.2142 | 0.2510 | 0.1783 | 0.0485 | 0.1302 | 0.8270 |

**0 を返しているタイマーは無い。** モジュール数は入力量の代理として悪い
(後ろのモジュールほど大きい) ので、倍率は宣言数 ×5.03 / IR バイト ×5.85 と並べて読む:
read IR ×4.72 / render hdr ×5.11 / render page ×4.07 / write ×4.31 / flatten ×5.49。
壁時計だけ ×3.85 と低いのは起動 0.026 秒 + 後始末が固定費だから —
これを引くと ×5.05 で宣言数の ×5.03 に一致する。単位あたりの速度も安定
(read IR 97→121 MB/s、render hdr 46→47 µs/宣言、write 172→226 MB/s)。

**(c) 計装が生成物を動かしていない。** 計装前 (HEAD) の `render.ts` と突き合わせて
5 項目すべて PASS: 432 ページが byte 一致 / 2 回の `--pages` が一致 / 増分 3 の
`--out` JSONL が byte 一致 / `--pages` の有無で JSONL が動かない /
`--stats` の `## decl_header path` ブロックが文字一致。出力の実サイズ 29,671,173 バイトも
増分 4 前半の独立計数と一致。

#### IR 読み出しは既存値を再現するか — **する。ただし消費者が違えば値も違う**

| | 秒 | 出所 |
|---|---:|---|
| 増分 2 の schema 2 中央値 (コミット済み) | 0.1004 | `stage4b-readir-tag.txt` |
| 同・別プロセス 1 read ずつ | 0.1042 | 同上 |
| **今回の再実行** (in-process runs 2–7) | **0.1014** | `stage4c-readir-recheck.txt` |
| **今回の再実行** (別プロセス runs 2–6) | **0.1049** | 同上 |
| `render.ts` の read-IR フェーズ | **0.1297** | 上の分解表 |

**既存値は再現した。** `render.ts` が 0.130 秒と高いのは cold でも計測ミスでもなく、
**IR をメモリに保持するから** — `read-ir.ts` は 1 モジュール数えたら捨てるが、
レンダラは全部持ち続ける必要がある。保持の有無だけを変えた対照 (各 7 run、
`stage4c-read-retain.txt`) で **0.0973 → 0.1167 秒 (+0.0194、+20%)**。差はこれで説明し尽くされる。
**「IR 読み出しは 0.100 秒」は streaming する消費者にだけ言える数字。**

#### doc-gen4 側との比較 — 分母と作業範囲を必ず併記する

**doc-gen4 の warm 取り直しはやっていない。** `fromDb` を再実行すると `.lake/build/doc` の
798 MB を上書きし、**増分 3・4 の突き合わせ先である 348 ページの正解データが壊れる**ため。
代わりに既存ログを辿った結果、**§6.2 の 44.52 秒には page cache 状態が実は付いていた**:

| | `fromDb.total` (計装) | lake の壁時計 (§4.6.3) | ログ |
|---|---:|---:|---|
| 1 回目 | **44.516s** | 46.25s | `fromdb-6k.jsonl` |
| 2 回目 (DB も出力も無変更) | **42.311s** | 43.91s | `fromdb-6k-2nd.jsonl` |
| 差 | **−5.0%** | −5.1% | |

**§6.2 の 44.52 秒と §4.6.3 の 46.25 秒は同じ 1 回目**で、単に内側と外側の計測。
2 回目は 433 MB の DB を直前に読み切った後なので**構成上 warm**。
**差 5.0% は §6.1 の olean mmap の 5 倍とは桁が違う** — このフェーズは page cache 感受性を
持たない、と読める。litedoc4 側の同種ワークロードの cold/warm 差 13% も同じ桁で、これを支持する。
**ただし n=2 で CPU 時間も page fault も記録が無いので、「5% 以内」以上のことは言えない。**
warm 側を厳密に主張するには 5 run 以上の測り直しが要り、それは上の理由で今はできない。

比例縮小 (6,072 → 432 モジュール、**外挿**):

| | 6,072 モジュール | 432 に比例縮小 |
|---|---:|---:|
| doc-gen4 `fromDb.total` 1 回目 | 44.516s | **3.17s** |
| doc-gen4 `fromDb.total` 2 回目 | 42.311s | **3.01s** |
| うち `htmlOutputResultsParallel` のみ (モジュールに比例する部分) | 39.781s | 2.83s |

**この比例縮小は外挿であり、しかも上振れ側に偏っている** —
`loadLinkingContext` (2.20s) と `htmlOutputIndex` (1.07s) はモジュール数に比例しない
サイト全体の仕事なのに、比例縮小はそれも 432/6,072 に割ってしまう。

**倍率は 3.8 倍** (3.17s ÷ 0.8239s)。ただし**両者の作業範囲は違う**:

| | doc-gen4 | litedoc4 (今回) |
|---|---|---|
| モジュールページ | 6,072 (→432 に縮小) | 432 |
| ページのバイト再現率 | 100% (定義上) | **63.6%** (増分 4 前半の実測) |
| `declaration-data-*.bmp` / `backrefs-*.json` | 出す | **出さない** |
| `search.html` / `index.html` / `navbar.html` / 静的資産 | 出す | **出さない** |
| SQLite の読み出し | ある | 無い (IR は JSON) |

**この 3.8 倍を「同じ仕事量で 3.8 倍」と書いてはいけない。**

#### 判断 — **【判断点 2】通過。仮定 1.28s は外れ、実測はその 0.64 倍**

| | 秒 | ラベル |
|---|---:|---|
| §6.2 の旧行「N+1 解消後」 | 1.28 | 仮定 |
| **実測 (warm)** | **0.8239** | 実測 |
| 実測 (cold) | 0.9331 | 実測 |

**仮定は 1.55 倍の過大見積もりだった。有利な向きに外れたので、根拠を 3 つ残す**:
(1) 出力は 432 ファイル / 29,671,173 バイトがディスク上に実在し、計装前の生成器と byte 一致。
(2) 壁時計はプロセスの外から測っており、フェーズの和 (0.784) との差 0.040 秒も
起動・後始末として説明がついている。(3) 分解の各項が入力量と一緒に動く。

**ただしこの 0.82 秒は完成版の下限である。** ページのバイトの 36.4% がまだ byte 再現
できておらず、検索索引も静的資産も作っていない。**下限とラベルすること。**

未実装分でどれだけ増えうるか (**外挿**、根拠 = 現行 docstring 項の実測のみ):
いま docstring 描画は **0.178 秒 = 壁時計の 21.6%**。未実装で重いのは CommonMark
(md4c 相当) と autolink 索引で、どちらもこの項に乗る。**本物の CommonMark がこの項を
3 倍にしても +0.36 秒、合計 1.2 秒前後**。md4c の実速度は測っていないので、
この 3 倍という係数自体に根拠は無い — **「数秒に化けることは無さそうだ」以上の主張はしない。**
検索索引を作るのは未実装分の埋め合わせではなく**このフェーズに足す新しい仕事**なので、
別に測り直しが要る。

`approach.md` に反映した箇所:

- §6.2 を全面差し替え。表題を 431 → **432 モジュール**、旧「1.28s 仮定」を
  **warm 0.82s / cold 0.93s の実測**に。doc-gen4 側は 1 回目 3.17s / 2 回目 3.01s の
  **外挿**として両方載せ、page cache の宿題への回答 (差 5.0%) を書いた
- §6.3 の HTML 生成 1.3s (仮定) → **0.82s (実測)**、合計 約 15 秒 → **14.53s**
- §6.4(b) の分母 15.0s → **14.53s**、倍率 約 2,200× → **約 2,270×**。
  「分母に仮定が入っている」という注記を「仮定は無い。残るのは出力範囲の差」に書き換え、
  同じ作業範囲で比べた 3.8 倍を併記
- §6.6 の「N+1 解消の効き方」を「HTML 生成の未実装分」に差し替え (仮定は消えたが下限は残る)
- §7 の検証表 4 行目に ✅
- `README.md` の理論値表 15.0s → 14.5s / 2,200× → 2,270×、
  「HTML 生成はまだ理論値」を実測済みに。3 軸の実行計画の 3 軸表と leg 表

### CI 軸 — cold の前提の検証 / 「展開直後なら warm」の否定 (2026-08-10)

**問い**: `approach.md` §3 の「**CI (常に cold)**」には**ラベルが無く、ログも無かった**。
CI では `lake exe cache get` が olean を展開した直後に doc 生成が走るので、
**展開直後なら page cache に載っていて cold にならない**かもしれない
(削除済の `plans/three-axes.md` §5 打ち手 1。読むなら git)。正なら CI 軸の前提そのものが変わる。

**計測条件**: Apple M1 / 8 コア / **16 GiB** (Darwin 25.6.0 arm64)、page size 16,384 B、
Lean・Mathlib v4.31.0、leantar 0.1.19 (`--jobs` 既定 = 8)、extract は 1 プロセス
(`LEAN_NUM_THREADS` unset)、ディスク空き 12 GiB 以上を各点の前に確認。
**対象リポジトリには 1 バイトも書いていない** — `lake exe cache get` / `lake update` は
実行せず、`leantar -x -C <scratch>` を直接呼んで scratch に展開した。
cold は `olean-evict` (msync MS_INVALIDATE、sudo 不要) で作る。
ログ → `benchmarks/results/ci-warmstart-*`、条件つきサマリ →
`benchmarks/results/ci-warmstart-summary.txt`。再現 → `benchmarks/tools/measure-warmstart.sh`。

**この機材の headroom** (= free + speculative + purgeable + inactive、実測、各ステップで記録):
計測中 **3.11–5.48 GiB**。compressor が **6.38–7.11 GiB** を占有している。
16 GiB のうち page cache に回せるのは 2〜3 割。

#### (a) 書き込み直後の常駐率 — payload を 0.25 → 7.34 GiB で振る (実測、6 点 × 3 回)

olean のコピーを `tar` で scratch に書き、**書き終わった直後に** `mincore(2)` で常駐を数える。

| payload GiB | **書いたページの常駐 %** | 常駐 GiB (中央値) | payload/headroom | **同じ瞬間の「読んだ」ページの常駐 %** |
|---:|---:|---:|---:|---:|
| 0.25 | 94.0 | 0.235 | 0.07 | 96 |
| 0.50 | 45.1 | 0.226 | 0.13 | 86 |
| 1.00 | 23.7 | 0.237 | 0.26 | 81 |
| 2.00 | 11.7 | 0.233 | 0.48 | 73 |
| 4.00 | 5.0 | 0.198 | 0.96 | 37 |
| 7.34 | 2.6 | 0.193 | 1.77 | 16 |

**曲線は headroom で決まっていない。** 常駐した**絶対量**が payload に依らず 0.15–0.24 GiB で
一定で、率はその割り算にすぎない。**headroom は一度も律速していない** —
7.34 GiB を書いた直後でも 3.26–4.10 GiB 空いていた。
そして**同じ瞬間に読んだページは 6.3 倍よく残っている** (2 GiB 点で 73% 対 11.7%)。
容量ではなく**読みと書きで保持方針が違う**。

対照 (`dd if=/dev/zero`、書込速度を 11〜16 倍にする、実測 3 点 × 3 回): 常駐は
0.250 / 0.309 / 0.302 GiB で**速度に依らない**。つまり残るのは「直近 N 秒」ではなく
**「末尾 N バイト」**。ファイル数 (1 個 vs 18,092 個) にも依らない。

#### (b) 本物の `leantar` 展開直後 (CI の実経路、実測 3 回 × 2 系列)

現行 rev の ltar クラスタ **8,516 個 / 410.70 MiB** を展開 → **110,708 ファイル / 6.123 GiB**、
うち olean 3 種 **25,548 ファイル / 5.167 GiB**。展開 12.78–13.65 秒。

| 系列 (展開前 headroom) | 展開直後の olean 常駐 |
|---|---|
| headroom 4.70–4.75 GiB | **17.23 / 19.46 / 20.19 %** (0.89–1.04 GiB) |
| headroom 3.34–3.72 GiB | **4.72 / 7.62 / 8.27 %** (0.24–0.41 GiB) |

2 系列の差はファイル集合ではない (同じ 24,507 ファイルに絞り直しても 17.4–20.1%)。
**残る量は headroom に応じて動くが、payload には比例しない。**

#### (c) 展開直後の `importModules` は速いか — 秒で答える (実測)

`LEAN_PATH` の 17 エントリのうち **mathlib の 1 つだけを scratch の展開先に差し替え**、
残りは対象リポジトリのまま。3 変種で変えているのは **Mathlib の暖機状態だけ**。
全 run で `declaration sets agree (5123 declarations)` — 仕事量は同一。

| 変種 | Mathlib の状態 | **importModules** | 壁時計 | **major fault** |
|---|---|---:|---:|---:|
| **fresh** | leantar が直前に書いた | **14.956 / 15.656** | 15.71 / 16.38 | 119,318 / 119,934 |
| **cold** | 同じ木を退避 | **13.862 / 13.999** | 14.60 / 14.73 | 122,974 / 121,386 |
| **warm** | 直前の run が読んだまま | **2.492–2.587** | 3.21–3.33 | 190–412 |

**fresh は warm ではない (5.9 倍)。cold より速くもない (1.1–1.7 秒遅い)。**
major fault が浮くのは 2–3% だけ。warm 判定は major fault で行っている
(190–412 対 119,318 以上、2 桁半の差)。warm 側が 2.49–2.59 秒に収束して
前増分の warm 中央値 2.538 秒と一致するので、LEAN_PATH を差し替えた計測系自体は健全。

なお **(b) の常駐 20% と (c) の fault 2–3% は釣り合っていない。**
比例して重なるなら 12% 前後浮くはず。理由の候補は「残るのは最後に書かれたページで
読み出し順と無関係」「展開終了から読み出しまでにさらに剥がれる」の 2 つで、**切り分けていない**。
**「20% 常駐していれば 20% 速くなる」は成り立たない**ことだけ記録する。

#### 判断 — **打ち手 1 はこの機材では否定。CI については未検証に格下げ**

- **仮説はこの機材では偽 (実測)。** 展開直後は page cache に載らない。
- **ただし「CI で載らない」の証明にはならない。** 理由は 2 つ、向きが逆:
  1. **機材の非対称性** — この機材は compressor が 6.4–7.1 GiB を占有し実効 headroom 3〜5 GiB。
     `ubuntu-latest` は 16 GB をほぼ専有する。**不利な側で否定が出ても移送できない**
     (逆に「載った」なら単調性で CI にも言えたが、載らなかったので使えない)。
  2. **退避ポリシーの違い** — macOS の UBC と Linux の page cache は
     **「書いたページがいったんキャッシュに入る」点は共通だが、その後が違う**。
     本計測が捉えたのは「入った後すぐ解放される」挙動で、容量ではなくポリシーの結果。
     Linux は writeback 後も clean なまま保持し、解放は reclaim まで待つ設計なので、
     同じ結果になるとは限らない。**この機材に Linux 環境が無く** (docker / colima / lima /
     podman / VM いずれも未インストール、ネットワーク不使用は計画 §6 事前決定 #5)、**測れていない。**
- **容量の側からは Linux CI で仮説が通る余地が残る (外挿)。** cold が実際に読むのは
  **2,588,347,936 B (2.59 GB、実測)** で `ubuntu-latest` の 16 GB の **16%**、
  展開が書く 6.12 GiB でも 38%。**容量は制約にならない。** 残るのはポリシーだけ。
- → **検証項目 (未着手)**: `ubuntu-latest` 上で `cache get` 直後の olean 常駐率を
  `mincore` / `/proc/<pid>/pagemap` で測る。**ここが埋まるまで CI 軸の床は確定しない。**

`approach.md` に反映した箇所:

- §3 の「**CI (常に cold)**」を **「CI: 未検証 (仮定)」**に格下げし、
  macOS 実測で「展開直後 = warm」が否定されたことと、Linux 未測定であることを明記
- §6.1 の「**CI は常に cold 側**」に同じ但し書き
- §7 の検証表に **CI 軸の行**を追加 (状態: 部分的に否定、Linux 未検証)
- §8 の「cold な環境ロードをどう扱うか」に、打ち手 1 が macOS では否定された事実を追記

### CI 軸 — 打ち手 2「先読み」の実測 / **成立** (2026-08-10)

**問い**: 削除済の `plans/three-axes.md` §5 打ち手 2 (読むなら git) —「抽出の開始と並行して olean を舐めて
page cache に載せる」。前増分で **cold の 12.7 秒は 132,343 回の同期 major fault の
直列な待ち行列**であり帯域律速ではないと分かっている (実測)。ならば先に並列で読めば
消せるはず、という仮説。打ち手 1 が否定されたので、**CI 軸に打ち手が入るかはここで決まる。**

**判断基準 (測る前に宣言)**: (1) overlapped の壁時計が cold baseline より短く、
5 回の分布が重ならない (2) major fault が減っている (3) 読むバイト数の増加を払って得か。
**満たさなければ「先読みは効かなかった」と書く**という条件で開始した。

**計測条件**: Apple M1 / 8 コア / **16 GiB** (Darwin 25.6.0 arm64)、page size 16,384 B、
Lean・Mathlib v4.31.0、extract は 1 プロセス (`LEAN_NUM_THREADS` unset)、
先読みの並列度は 1/4/8/16/32、ディスク空き 16 GiB 以上。
**対象リポジトリには 1 バイトも書いていない。** cold は `olean-evict` で作り、
**退避後に 34,304 ファイルの常駐が 0 バイトであることを毎回の系列で検証**している。
extract **162 run** (stage1 146 / stage4b 16) + 先読み単体 36 run。
stage1 の 146 run すべてで `declaration sets agree (5123 declarations)`、
stage4b の 16 run すべてで `produced 4750` — **仕事量は全 run 同一**。
ログ → `benchmarks/results/ci-prefetch-*`、条件つきサマリ →
`benchmarks/results/ci-prefetch-summary.txt`。再現 → `benchmarks/tools/measure-prefetch.sh` /
`olean-prefetch.c` / `import-closure.py` / `summarize-prefetch.py`。

#### (a) 道具の検証 — 「載っていない先読み」で時間を測らないための前提 (実測)

先読み器は 3 方式を持つ: `read(2)` / `madvise(MADV_WILLNEED)` / mmap して 1 ページ 1 バイト触る。
250 MiB の部分集合で **3 方式とも退避後 0% → 先読み後 100.00%** を mincore で確認。
34,304 ファイル全部でも退避後 **0 バイト**、closure 17,199 を先読みして **2,556,988,152 B**。
**`madvise -j1` の所要 4.28 秒は `read -j1` の 4.19 秒と同じ** → この OS の
`MADV_WILLNEED` は**同期**で、経過時間を正味の I/O 時間として読んでよい
(**Linux では非同期**なので移送不可)。

#### (b) 先読み単体 — 前増分の「床」を超えた (実測、3 回)

| 集合 / 並列度 / 方式 | 所要 s | MB/s | cold が必要な 2,588,347,936 B のうち常駐 |
|---|---:|---:|---:|
| oracle j=16 madvise | **1.745** | **2,395** | 78.1–79.4% |
| oracle j=8 madvise | 1.772 | 2,358 | **90.7–93.5%** |
| oracle j=16 read | 1.777 | 2,351 | 69.8–70.0% |
| oracle j=1 read | 4.186 | 998 | 75.6–77.9% |
| blind j=8 madvise | 3.359 | 2,345 | 36.9–37.0% |

前増分の「8 並列 `cat` で 1,841 MB/s」が床だったが、**2,395 MB/s まで出る**。
並列度は 4 で頭打ち (j=1 の 998 → j=4 の 2,120 → j=16 の 2,351)。

#### (c) 本体 — 3 変種の比較 (実測、交互配置 5 回、ブロック Y)

base → read j1 → read j16 → madvise j16 → blind madvise j16 を 1 セットにして 5 周。
`wall` は退避直後から extract 終了まで (`lake env` 込み)、`real` は extract バイナリだけ。

| 変種 | wall s | **importModules s** | extract real s | **major fault** |
|---|---:|---:|---:|---:|
| **baseline (cold)** | 18.03 [17.56–18.35] | **13.573 [13.455–13.834]** | 14.34 | **121,386 [121,206–124,752]** |
| overlapped oracle read j=1 | 11.93 [11.79–12.11] | 8.620 [8.507–8.801] | 9.38 | 52,971 |
| overlapped oracle read j=16 | 13.69 [12.73–15.49] | 10.168 [9.201–11.721] | 10.92 | 69,009 |
| **overlapped oracle madvise j=16** | **10.44 [9.52–11.15]** | **6.923 [6.031–7.555]** | **7.69** | **26,329 [20,446–36,671]** |
| overlapped blind madvise j=16 | 16.39 [15.89–17.58] | 11.369 [10.895–12.468] | 12.15 | 79,306 |
| (参考) warm、同一セッション 6 run | 4.31 | **2.458 [2.395–2.478]** | 3.18 | 276 [190–385] |

**5 本の分布はどの隣接ペアでも重なっていない。** 並列度は別ブロックで交互配置して
**j=8 / 16 / 32 は区別できない**と確認した。`sequential` (先読み完了を待ってから抽出) は
測ったすべての対で overlapped に負け、**差はおおむね先読みの所要時間**だった。

#### (d) 判定 — **3 条件とも満たす。打ち手 2 は成立 (この機材で、実測)**

| 条件 | baseline | 先読みあり (oracle 集合 / madvise / j=16) | 判定 |
|---|---|---|---|
| 壁時計の分布が重ならない | 17.56–18.35 s | **9.52–11.15 s** | **満たす** (中央値 −42.1%) |
| major fault が減る | 121,206–124,752 | **20,446–36,671** | **満たす** (−78.3%) |
| バイト増を払って得か | — | 1.61 倍のバイトで importModules −49.0% | **満たす** |

demand paging の実効レート 2,588,347,936 B ÷ 11.115 s = **232.9 MB/s** に対し
先読みは **2,352–2,395 MB/s** で **10.1 倍**。前増分の「律速は帯域ではなく fault 遅延」の
直接の裏づけになっている。
**blind (LEAN_PATH 配下の olean 全部、3.04 倍のバイト) は成立するが割に合わない** —
importModules は −16.2% にしかならず、oracle/closure の −49.0% の 1/3。

#### (e) **oracle は「実装不可能な上限」ではなかった** (実測)

leg の想定では oracle (cold が実際に触った 17,528 ファイル) は実装不可能な上限だったが、
`.lean` のヘッダから import closure を作ると **432 → 6,021 モジュール**が出て、
これは extract が報告する `loaded modules 6021` と**完全に一致**する。

| | ファイル | バイト | 必要な 2,588,347,936 B のうち |
|---|---:|---:|---:|
| oracle (観測した集合) | 17,528 | 4,178,202,360 | 100% |
| **closure (計算できる集合)** | **17,199** | **4,140,727,992** | **98.80%** |
| oracle のみ | 399 (全部 toolchain) | 55,102,440 | 1.20% |

差の 399 ファイルは計測ハーネスの `lake env` 自身がロードする Lean core の分で、
抽出器の closure ではない。**別ブロックで交互配置して実測したところ、
closure と oracle は区別できない** (importModules 12.808 対 12.039、分布が完全に重なる)。
closure の計算コストは **cold 1.63–1.64 秒 / warm 0.93–0.98 秒** (6,021 ソースの
ヘッダ 82,043,320 B を読む Python 逐次実装)。**解決できた順に先読みへ流せるので、
先読みの 1.75 秒とほぼ重なる。**

#### (f) cold 合計 — **外挿**

warm 合計 14.53 秒 (抽出 + IR 13.71 + HTML 生成 0.82、いずれも既存の実測) に、
今回 stage1 抽出器で実測した cold の追加コストを足す。HTML 生成の cold は実測 0.93 秒。

先読みの条件は **oracle 集合 / madvise / j=16 / overlapped** (ブロック Y)。
実装時は closure 集合になるが、(e) の別ブロックで**両者は区別できない**
— ただしこれは**ブロックをまたぐ推論**。

| | 先読みなし | **先読みあり** |
|---|---:|---:|
| cold の追加コスト (13.573 − 2.458 / 6.923 − 2.458) | +11.115 | **+4.465** |
| 抽出 + IR 書き込み | 24.83 | 18.18 |
| HTML 生成 (cold) | 0.93 | 0.93 |
| **cold 合計** | **25.76 s** | **19.11 s** |
| closure 計算を直列に払った場合 (上限) | — | 20.74 s |

**差 6.65 秒 (−25.8%)。ラベルは外挿** — 足している秒数は stage1 抽出器で測った値で、
前提は「cold の追加コストは importModules に集中する」。**この前提は検証できていない**ので、
実際は**大きい側に外れうる**。`lake env` 自身の cold コスト (+2.56 秒) は
14.53 秒の基準に入っていないのでここでも足していない。

#### (g) 効果の大きさはメモリ regime に依存する (実測)

同じ条件でもブロックによって効き方が違う。**向きは変わらないが大きさは変わる。**

| ブロック | wired GiB | baseline importModules | 先読みあり | 削減 |
|---|---:|---:|---:|---:|
| Y / X | 1.82–2.18 | 13.573 / 13.283 | 6.923 / 6.943 | **−49.0% / −47.7%** |
| W | 3.17–3.39 | 15.703 | 12.039 | **−23.3%** |

wired が増えた原因は特定できている: 次項の stage4b を 16 run 回した後、
wired が 2.08 → 4.09 GiB に上がって戻らなかった。

#### (h) stage4b を直接測ろうとして失敗した (記録)

cold 合計を外挿でなく実測にするため stage4b (完全版、peak RSS 3.33 GB) で
baseline / overlapped を交互配置 5 対 + warm 6 run 測ったが、**分離できなかった**。
**warm のはずの run が major fault を 59,152–226,749 回払っている** (stage1 の warm は 190–385)。
つまり **stage4b の作業集合がこの機材の当日のメモリ状態に収まらず、
自分が読んだ olean を自分で追い出している**。
**これは「先読みが効かない」証拠ではない** — 対照 (warm) 自身が壊れているので
**何も言えない系列**として記録する。前セッションの stage4b warm は major fault 387 回・
13.82 秒だったので、収まるかどうかは機材の当日の状態次第。

#### (i) この計測で分かっていないこと

- **Linux では 1 バイトも測っていない。** ただし打ち手 1 と**向きが逆**で、
  こちらは**不利な機材で出た肯定**。機構 (同期 major fault を並列読みで置き換える) が
  同じなら Linux でも成立する側に寄る — **これは外挿**。
  さらに **`MADV_WILLNEED` は Linux では非同期**なので、
  「read と madvise のどちらが良いか」は**移送できない**。測り直しが要る。
- **stage4b の cold 合計は実測できていない** ((h))。cold 合計は外挿のまま。
- **sequential と overlapped を交互配置で比べていない。**
- **先読みの読み出し順はパス名のソート順**で、extract の読み出し順に合わせていない。
- **madvise が read より残る理由は同定していない。** 観測は「同じ所要時間で常駐が高い」
  「下流の fault も少ない」の 2 点だけ。

`approach.md` に反映した箇所:

- §3 の CI の項に、打ち手 2 が実測で成立したことと cold 合計の 2 つの値を追記
- §6.3 の cold の記述を「抽出が 24 秒前後、合計 25 秒前後」から
  **先読みなし 25.76s / 先読みあり 19.11s (外挿)** に置換
- §7 の CI 行の状態を「打ち手 1 は否定、**打ち手 2 は成立**」に更新
- §8 の「cold な環境ロードをどう扱うか」に先読みの実測結果を追記

### 段階 5 — 増分生成 / 増分 1「ハッシュによる差分検出と再抽出」 (2026-08-10)

**計測条件**: macOS 15.6 (Darwin 25.6.0) / Apple M1 / 16 GB / Lean 4.31.0 /
対象 `/Users/haruka/dev/lean-projects` (432 モジュール、import closure 6,021)。
並列度 1 (差分検出の 1 系列だけ 8 並列)。**すべて同一セッション** (UTC 21:47–22:15)。
抽出器は `experiments/stage4b`、生成器は `experiments/stage4c` を**無改造**で使用。
新規は `experiments/stage5/` の差分検出・影響集合・IR マージのみ。
**warm 判定は major page fault を主指標**にした (生成器は多スレッドで (user+sys)/壁 が
1.3 になるため、この比では判定できない)。
生ログ → `benchmarks/results/stage5-*.jsonl`、
まとめ → `benchmarks/results/stage5-incremental-summary.txt`、
設計判断 → `experiments/stage5/README.md`。

#### (0) 何を偽装したか

対象リポジトリは書き換えられない (`.lake/build/doc` は段階 4 の正解データ、
`.lake/build` の olean は全計測の基盤) ので、**「モジュール M が変更された」という
事実だけ**をハッシュ台帳の M のエントリを無効化して注入した。それ以外はすべて本物で、
抽出器は実際に M の import closure を読み直して M を解析している。

**したがって `lake build` はこの数字に含まれない。** litedoc4 の外だが実際の
編集 → プレビューの臨界パス上にある。測るには対象リポジトリの複製が要るので
**この増分では測らない (未着手の検証項目)**。

もう 1 つの帰結: 実際には何も変わらないので再抽出した IR はバイト一致し、
**正直な再生成対象は空集合**になる (パイプラインは `irChanged: 0` とそう報告している)。
時間を測るためにページ集合を強制した。**再生成の秒数は実在のページ集合に対する実測だが、
その集合は導出ではなく選択である。**

#### (a) 設計判断 — 何をハッシュするか

**入力側と出力側で別のハッシュを使う。** §5.5 の「モジュール単位ハッシュが単一の真実」は
*役割*の話で、役割は 2 つある。1 つの数字では両方を埋められない (問う時点が違う)。

| | 入力側 | 出力側 |
|---|---|---|
| 問い | どのモジュールを**再抽出**するか | どのページを**再生成**するか |
| ハッシュ対象 | モジュールの `.olean` | モジュールの IR JSON |
| 計算するもの | `stage5/ledger.ts` (Lean 不要) | 抽出器 (既に `index.json.contentHash` にある) |

**`.lean` ソースではなく olean を選んだ理由**: 抽出器がモジュールについて見られるのは
`importModules` が返すものだけで、その出所は olean しかない。ソースのハッシュは
両方向に外す — 証明中の空白のように olean に届かない変更を「変更」と報告し、
依存の再ビルドで olean に入った変更を見落とす。**IR のハッシュは遅すぎる** —
それを計算するには、台帳が省こうとしている抽出そのものが要る。

**3 分割 olean**: モジュールシステムで作られたモジュールは
`.olean` / `.olean.server` / `.olean.private` を持つ。**存在するものは全部ハッシュする**
(抽出器は declaration range と docstring = server 側、private 宣言 = private 側を読む)。
**この対象では自パッケージ 432 モジュールは `.olean` 1 個ずつ** (実測)。3 分割は
依存パッケージ側にだけあり、8,360 モジュール中 8,297 が 3 つ持つ (実測)。

**Lake が既に同じハッシュを計算している (実測)。** olean の隣の `<file>.olean.hash` は
`cacheFileHash` → `computeBinFileHash` (`Lake/Build/Common.lean`、v4.31.0 のソースを確認)
すなわち**その olean の内容ハッシュ**。読むのは 432 × 16 B = 6.9 KB。
ただし 64bit 非暗号ハッシュで、かつビルドツールの内部仕様。**SHA-256 を基準にし、
Lake のハッシュは「ただで手に入る代替」として両方測った。**

**台帳の置き場所は IR の `index.json` とは別ファイル。** index は Lean が IR の一部として
書き、台帳は Lean を起動しない側が読み書きするため。分けておくと差分検出が
15.8 MB の IR を parse せずに済み、index.json のバイト再現性 (15,867,059 B) も保てる。

#### (b) 設計判断 — 依存の伝播

**健全な上界は「M を推移的に import しているモジュール全部」。** M を推移的に import
しないモジュールは M が宣言したものを一切観測できない。Lean にそれ以上遠くへ届く経路はない。

**olean 台帳だけではこの上界に届かない。** M の型レベルの内容が変われば import 側も
再ビルドされて olean が動くので台帳が捕まえるが、**2 つの経路が抜ける**:

1. **印字の経路** — `notation` / `syntax` / `@[app_unexpander]` / `@[delab]` / `export` は
   M の環境拡張にあり、**N の olean を一切触らずに N の署名の印字を変えうる**。
2. **所有モジュールの経路** — IR は参照を `(定義モジュール, 名前)` の対で持つ。
   宣言を M から M′ に移すと N の `refs` が古くなる (リンク先が旧ページ) が、
   N の olean は変わらない。

**どちらも塞いでいない。** 1 の表面積だけは実測した (対象の `.lean` を読むだけ、
書き換えていない): **`notation` 宣言は 6 個・2 モジュールだけで全部 `scoped`**
(`InformationTheory.Asymptotic` と `…Shannon.TypedRV`)、
`syntax` / `macro` / `elab` / `declare_syntax_cat` / unexpander / delab / `export` は
**0 個**。抽出器は既定で `--open` を渡さない (計画 §6 事前決定 #4) ので、
**この対象・この構成では印字の経路は不活性**。ただしこれは**対象の性質であって定理ではない**。

**2 つの閉包の実サイズ (実測、`impact.ts` で IR のグラフから)**:

| | 中央値 | 平均 | p90 | 最大 | 0 のモジュール |
|---|---:|---:|---:|---:|---:|
| 推移的 import 元 (自パッケージ内) | 13 (3.0%) | 31.3 (**7.2%**) | 63 | 414 | 1 |
| 直接の参照元 (`refs` 由来) | 0 | 2.6 (**0.60%**) | 7 | 80 | 259 |

参照元 ⊆ import 元 (M の定数を書くには M を import する必要がある)。
健全な上界は平均でパッケージの 7.2%、参照ベースなら 0.60%。
ルートモジュール `InformationTheory` が他 430 個を import しているため、
import 元が 0 のモジュールは 1 個しかない (55 個はルートだけが import している)。

#### (c) 変更するモジュールの選び方

グラフの両端 + 現実的な 1 つ。**両端を測ると幅が出る。**

| | モジュール | 宣言 | import closure | 推移的 import 元 | 直接参照元 |
|---|---|---:|---:|---:|---:|
| hub | `…Meta.EntryPoint` | 1 | **2,259** | **414** (95.8%) | 0 |
| leaf | `…Shannon.Kolmogorov.OmegaNoncomputable` | 52 | 4,258 | 1 (ルートのみ) | 0 |
| mid | `…Shannon.ChannelCoding.Basic` | 32 | 5,267 | 159 | **80** (最大) |

hub は再抽出が最安・再生成が最高、leaf はその逆、mid が現実的な編集。
**1 モジュールの環境ロードはパッケージ全体 (6,021) の 38〜87%** しか小さくならない。

#### (d) 正しさ (時間より先に確認した)

| 検査 | 結果 |
|---|---|
| フル抽出の IR が別セッションで再現するか | **432 ファイル + index + deps が byte 一致** (前セッションの ir-tagged と `diff -r`) |
| 1 モジュールだけ再抽出した IR が、フル抽出の同じモジュールと一致するか | **byte 一致** (hub / leaf / mid) |
| 部分抽出を merge した 432 モジュールの IR がフル抽出と一致するか | **module 432/432 byte 一致、index エントリ一致、deps は写像として 533/533 一致** |
| 部分再生成したページが、432 ページ全部を生成したときと一致するか | **415 / 160 / 81 / 2 / 1 ページすべて byte 一致** |
| 増分を 6 回回した後の IR とページがフルビルドと一致するか | **3 変種すべてで IR 432/432・ページ 432/432 一致** |
| 出力側ハッシュが「変わっていない」と正しく言えるか | **`irChanged: 0`** |

deps だけは JSON のキー順が Lean の HashMap 由来で外から再現できないため、
**バイトではなく写像として**比較している。

**副産物 (実装上の欠陥を 1 つ発見)**: 部分抽出は `deps/*.json` を正しく作れない。
抽出器は「defining module が target list に無ければ依存」と判定するので、
1 モジュールの target list だと自パッケージの他モジュールが依存に化ける
(leaf の 1 モジュール実行はフル実行に存在しない `deps/InformationTheory.json` を書く)。
**増分の性質ではなく抽出器のインタフェース (target list が package list を兼ねている)
の副作用**。merge 側で全モジュールから作り直して潰した (0.161s)。

#### (e) 差分検出 = 増分の下限 (実測)

432 モジュール / 432 olean / 237,909,832 B。Lean は起動しない。
warm 3 系列を交互配置で 8 round、cold 系列はその**後**に単独で 8 run
(evict は次の warm を汚すので混ぜられない)。中央値 [min-max]、run 1 は捨てる。

| 系列 | 壁時計 | うち hash | major fault |
|---|---:|---:|---:|
| `lake` (`.olean.hash` を読む、warm) | **0.0798** [0.0789-0.0838] | 0.0509 | 3 |
| `sha256` 8 並列 (warm) | **0.1173** [0.1121-0.1455] | 0.0875 | 3 |
| `sha256` 逐次 (warm) | 0.2493 [0.2424-0.2519] | 0.2184 | 3 |
| `lake` cold (olean を evict) | 0.0800 [0.0793-0.1377] | 0.0507 | 3 |
| `sha256` 逐次 cold | 0.3819 [0.3773-0.3916] | 0.3506 | 3 |

- **無変更の検出は 0.080s (lake) / 0.117s (sha256 8 並列)。これが増分の下限。**
  deno の起動床 0.026s (段階 4 増分 4 で実測) がそのうち約 1/3。
- **Lake のハッシュ経路は olean の page cache 状態に影響されない** (cold 0.0800 対
  warm 0.0798) — 6.9 KB しか読まないため。
- SHA-256 のスループット warm 1,090 MB/s (逐次) / 2,715 MB/s (8 並列) / cold 678 MB/s。

#### (f) 1 モジュールの再抽出 (実測)

**系列は交互配置していない。これ自体が発見。** 最初に round-robin (前の leg の規則) で
試したところ全系列が cold のままだった — full の作業集合 (olean 6,021 モジュール +
匿名メモリ 2.8 GB) が 1 モジュール側のページを追い出し、6 round 回しても major fault
72,000。**作業集合が 3 倍違う変種の交互配置は「どれも warm にならない」ことを保証するだけ。**
系列ごとに連続実行して各自の warm 定常に入れ、**最後に小さい系列を取り直して
ドリフトを測った** (hub 1.9257 → 1.8438、leaf 3.1613 → 3.0191 — どちらも
**速くなっており**、full による悪化は無い)。

| 系列 | 壁時計 | プロセス内 total | importModules | analyze | writeIR |
|---|---:|---:|---:|---:|---:|
| hub | **1.9257** | 0.8075 | 0.8024 | 0.0003 | 0.0007 |
| leaf | **3.1613** | 1.9962 | 1.9421 | 0.0419 | 0.0037 |
| mid | **4.6490** [3.998-5.863] | 3.0388 | 2.9635 | 0.0610 | 0.0044 |
| full (432) | **16.1919** | 14.6152 | 2.7171 | 11.2839 | 0.5974 |

mid は散らばりが大きい (major fault 715-22,682)。peak RSS 3.0 GB で 16 GB 機の
余裕を使い切りかけているため。**この散らばりごと読むこと。**

**壁時計とプロセス内の差 (1.1-1.6s) はほぼ `lake env`** — 単独で実測すると
`lake env /usr/bin/true` = **0.9618s** [0.9594-0.9626] (warm 6 run、
`stage5-lakeenv.jsonl`)。`approach.md` の 13.71s / 14.5s 系はプロセス内の値なので
**`lake env` はそこに入っていない**。増分ではこの固定費が leaf で 25% を占める。

**同一セッションのフル抽出は 14.615s (プロセス内) で、前セッションの 13.71s と 6.6% 違う。**
内訳も importModules 2.717 対 2.52 / analyze 11.28 対 10.58 と一様に高い。
同じ構成・同じ入力なので**セッション間の差であって効果ではない**。
**倍率はすべて同一セッション内で取っている。**

#### (g) 影響ページ集合の再生成 (実測)

6 系列を交互配置で 6 round、IR は同一。

| 系列 | ページ | 壁時計 | プロセス内 total | うち IR 読み出し |
|---|---:|---:|---:|---:|
| leaf-self | 1 | **0.1952** | 0.1562 | 0.1345 |
| leaf + import 元 | 2 | 0.2044 | 0.1655 | 0.1371 |
| mid + 参照元 | 81 | **0.3494** | 0.3098 | 0.1356 |
| mid + import 元 | 160 | 0.5113 | 0.4718 | 0.1308 |
| hub + import 元 | 415 | 0.8696 | 0.8287 | 0.1335 |
| full | 432 | **0.8796** | 0.8403 | 0.1358 |

**1 ページの再生成は 432 ページの 4.5 分の 1 にしかならない。** 床は
IR 読み出し 0.135 + 名前写像 0.010 + deno 起動 0.026 ≒ **0.17s** で、ページ数に依存しない。
**「Lean の外側の全域索引」が増分の床を作っている** — §5.5 の
「全モジュール横断の後処理も差分から更新できる形にする」が未実施であることの実測値。

#### (h) 増分の合計 (実測) と、増分の律速

1 run = 1 プロセスツリー。中央値 [min-max]、runs 2-6。

| 変更した M | 再生成ページ | **壁時計** | detect | extract | merge | impact | render |
|---|---:|---:|---:|---:|---:|---:|---:|
| hub | 415 | **3.3044** [3.2859-3.4043] | 0.1023 | 1.9537 | 0.1601 | 0.1578 | 0.8623 |
| leaf | 1 | **3.8339** [3.8019-4.0349] | 0.1026 | 3.1175 | 0.1609 | 0.1580 | 0.2203 |
| mid | 81 | **4.3893** [4.3726-4.4329] | 0.1006 | 3.5307 | 0.1613 | 0.1573 | 0.3784 |

同一セッションのフル生成は **17.0715s** (抽出 16.1919 + 生成 0.8796、どちらも壁時計)。
**倍率は 3.9〜5.2 倍**。ただし**仕事量は同じではない** — フルは 432 ページ全部を作り、
増分は 1〜415 ページを書き直し、代わりに detect と merge を余分に払っている。

**律速は環境の取得** (leaf、壁時計 3.8339s の内訳):

| | 秒 | 比 |
|---|---:|---:|
| `importModules` | 1.942 | 50.7% |
| `lake env` | 0.962 | 25.1% |
| exec + 171 MB のバイナリのページイン + 後始末 + スクリプトの糊 | 0.231 | 6.0% |
| **環境の取得と糊 小計** | **3.135** | **81.8%** |
| render / merge / impact / detect (全域索引の再構築が主) | 0.642 | 16.8% |
| **analyze + writeIR + tactics (本来の仕事)** | **0.053** | **1.4%** |

**`approach.md` §6.5 の「ほぼ全部が環境ロード」は向きとしては正しいが、数字が 2 つ外れていた**:

1. **床は 2.5 秒ではない。** 1 モジュールの import closure は 2,259〜5,267 で
   パッケージ全体の 6,021 より小さく、**importModules は 0.80〜2.96s** (モジュールで 3.7 倍違う)。
2. **`lake env` の 0.96 秒が数えられていなかった。** Lean の外だが毎回かかる。

**常駐化しても消えない項がある**: 全域索引の再構築 (render の IR 全読み 0.135 +
名前写像 0.010 + merge の deps 再計算を含む 0.161 + impact のグラフ構築 0.158) で
**0.463s = 12.1%**。

#### (i) 理論値 0.03s は否定された

| | 秒 | 理論値との比 |
|---|---:|---:|
| 理論値 (§6.5、常駐前提) | 0.03 | 1x |
| **実測・最小 (hub)** | **3.3044** | **110x** |
| 実測・最大 (mid) | 4.3893 | 146x |
| 環境ロードを除いた分だけ (leaf: analyze + writeIR + render) | 0.266 | **8.9x** |

0.03s は常駐前提の数字なので使い捨てプロセスの実測と直接は比べられない。
**しかし比べられる部分を切り出しても届かない** — 環境ロードを除いた
「1 モジュール解析 + 1 ページ生成」相当が 0.266s で、差の大半は生成側の
**IR 全読み 0.135s** (§5.5 の未実施分)。**0.03 秒は常駐を仮定しても出ない。**

#### (j) doc-gen4 との比較 (分母を明示する)

| | doc-gen4 (報告書 §4.5、実測) | litedoc4 (実測) |
|---|---|---|
| 無変更 | 2.33s (Lake が全 job を replay、doc-gen4 は 1 プロセスも起動しない) | **0.080s** (台帳照合のみ) |
| 1 モジュール変更 | 5.21s。**ただし HTML が再生成されていない** (stale)。`lake build` を含む | **3.30〜4.39s**。1〜415 ページを実際に書き直す。**`lake build` を含まない** |

**この 2 列は同じ仕事ではないので倍率を書かない。** doc-gen4 側は `lake build` を含み
HTML を更新しておらず、litedoc4 側は `lake build` を含まず HTML を更新している。
無変更側の 2.33 対 0.080 も同様で、Lake の replay はビルドグラフ全体を検証し、
台帳は olean 432 個を比べるだけ。**29 倍を単独で引用しないこと。**

#### (k) 判断

**宣言した判断基準 (計測前) と結論**:

| # | 基準 | 結果 |
|---|---|---|
| 1 | 再抽出した IR がフル抽出と一致する | **満たした** ((d)) |
| 2 | 増分の合計がフル生成より有意に速い | **満たした** — 3.30〜4.39s 対 17.07s、同一セッション、分布は重ならない。ただし **3.9〜5.2 倍**であって 480 倍ではない |
| 3 | 影響ページ集合が正しく閉じている | **この増分では決着しない。** 必要条件 (6 回回してもフルビルドと一致) は満たすが、(b) の 2 経路が塞がっていない。**判断点 3 (段階 5 増分 2) の問い**であり、ここでは「満たした」と書かない |

**段階 5 は増分 2 に進む。**

#### (l) この計測が答えていないこと

1. **`lake build` の時間** ((0))。実際の編集ループの臨界パス上にあるが範囲外。
2. **影響集合の閉包** ((b) の印字の経路と所有モジュールの経路)。増分 2 が攻める。
3. **パッケージ横断の出力**。`render.ts` はモジュールページしか書かない。
   モジュール索引・検索索引 (`declaration-data-*.bmp`)・`backrefs-*` は未実装なので、
   「stale が無い」は **432 ファイルについての主張でしかない**。
4. **削除**。消えたモジュールのページも台帳エントリも残る。何も消していない。
5. **増分の cold**。差分検出だけ cold を取った。1 モジュールの環境ロードが cold で
   いくらになるかは未測定。

`approach.md` に反映した箇所:

- §6.5 の増分の表を**全面的に実測に置換**し、理論値 0.03s の否定を明記
- §3 の「増分」に関する記述と §7 の段階 5 の行を更新
- §8 の「常駐プロセス」の項に、常駐でも残る 0.46 秒 (全域索引) を追記

### 段階 5 — 増分生成 / 増分 2「stale ページが残らないか」【判断点 3】= **否定** (2026-08-10)

**計測条件**: macOS 26.6 (Darwin 25.6.0) / Apple M1 / 16 GB / Lean 4.31.0 / Deno 2.7.14 /
対象 `/Users/haruka/dev/lean-projects` (432 モジュール)。**同一セッション**。
抽出器 `experiments/stage4b`・生成器 `experiments/stage4c`・台帳と影響集合
`experiments/stage5` を**すべて無改造**で使用。新規は `experiments/stage5b/` のみ。
E1/E2/E4/E5 は **Lean を起動しない** (deno と python だけ)。E3 だけが Lean を起動する。
生ログ → `benchmarks/results/stage5b-*.txt` / `*.json` / `*.jsonl.gz`、
まとめ → `benchmarks/results/stage5b-stale-summary.txt`、
判断基準の宣言と設計 → `experiments/stage5b/README.md`。

**この節の数字はほぼすべてバイト比較・集合演算・ファイル列挙**なので、
page cache / 並列度 / warm・cold は結果を動かさない。時間の数字は 2 つしか出さず、
どちらも「その操作が完走した」ことの傍証で、性能の主張には使っていない。

#### (0) 何を偽装したか

対象リポジトリを書き換えられないので 6 種類の偽装を使った。**偽装したのは
「入力が変わった」という事実だけで、プログラムはすべて本物が本物の入力に対して動いている。**

| # | 何を | どうやって |
|---|---|---|
| 1 | git revision の変更 | `render.ts --source-url` の 40 桁 hex を差し替え |
| 2 | 宣言が 1 個増えた | IR の複製に宣言を 1 個足す (`inject-decl.py`) |
| 3 | olean ツリー | 432 モジュールをシンボリックリンクで複製 (実体コピー 0 バイト) |
| 4 | モジュールの削除 / 追加 | 3 のリンクを消す / 足す |
| 5 | 環境キーの変更 | scratch 側の台帳 JSON の `envKey` 記録値を書き換え |
| 6 | 宣言の所有モジュールの移動 | IR の複製で宣言 1 個を M から M′ へ移す (`move-decl.py`) |

**測れていないもの (仮定として明記する)**: 偽装 2・6 に対して「**参照側 N の olean は
変わらない**」。対象を `lake build` し直せないので検証していない。E6 の結論はこの仮定に
依存する部分としない部分があり、(f) で分けて書く。

#### (a) 判断基準 — 計測前に宣言した (`experiments/stage5b/README.md` §1、commit `d8f95df`)

増分 1 の基準 3「影響ページ集合が正しく閉じている (nothing stale, nothing gratuitous)」は
1 文で 5 つの独立な主張を運んでいたので、**5 命題に分解して否定条件と事前予測を先に書き、
実験より先に commit した**。基準は計測後に 1 文字も動かしていない。

| # | 命題 | 否定条件 |
|---|---|---|
| S1 | ページを変える入力はすべて台帳が見ている | `ledger check` が 0 changed のまま 1 ページ以上のバイトが変わる |
| S2 | 影響集合は実際に変わるページを全部含む | ある変更で (真の変更集合) ⊄ (機構の再生成集合) |
| S3 | 1 モジュール再抽出の IR がフル抽出と一致する (**全構成で**) | 一致しない構成が 1 つでもある |
| S4 | 消えたモジュールは何も残さない | ページ / 台帳エントリ / IR が残る、または例外で落ちる |
| S5 | パイプラインが保守する成果物がサイト全体である | サイトに要る成果物で、モジュールに依存し、生成されないものがある |

**判断点 3 は S1〜S5 が全部満たされたときだけ満たす。**

#### (b) 結果 — **5 命題すべてが否定された (実測)**

| # | 判定 | 決め手 |
|---|---|---|
| S1 | **否定** | `--source-url` の rev を変えると **432/432 ページ**のバイトが変わる (rev 文字列 4,992 出現) のに `ledger check` は **0 changed** |
| S2 | **否定** | 宣言 `log` を 1 個注入すると **142 ページ**が変わる。最も広い `--mode importers` でも **stale 3** (hub 注入) / **stale 141** (leaf 注入) |
| S3 | **否定** | `--open` 構成で、単独再抽出 **12,694 B** 対 フル抽出 **12,961 B** で不一致 |
| S4 | **否定** | olean を 1 個消すと `ledger check` が**例外で exit 1**、増やしても不可視、IR から消してもページは残りバイト一致、生きた 4 ページがリンクし続ける |
| S5 | **否定** | 非モジュール成果物 **23/23** が未生成。うち 5 個は全モジュールの関数で **38,581,208 B** |

事前予測は 5 行すべて「refuted」で、**5 行とも予測どおりに否定された**。
行レベルで予測と食い違ったものは無い。食い違ったのは中身のほうで、(g) に列挙する。

#### (c) S1 — モジュールに属さない入力が台帳の外にある

`envKey` は 5 項目しか持たない (`experiments/stage5/ledger.ts:146-157`)。
`--source-url` は `ledger.ts` に 1 度も現れず、値は `incremental.sh:40` のハードコード。
さらに 2 つの実装上の穴が実測で出た:

- **`envChanged` は計算されるが `changed` に入らない** (`ledger.ts:222-224` 対 `231-234`)。
  `envKey` を 3 項目偽装して走らせても **0 changed / exit 0**。
- **実パイプラインでは `irSchemaVersion` と `irGenerator` が比較対象にすらならない** —
  `incremental.sh:75-76` が `check` に `--ir` を渡さないため。

この状態で commit しただけ (olean は 1 バイトも変わらない) だと、
`incremental.sh` は**抽出 0 モジュール・再生成 0 ページで「完了」する**一方、
正しい出力は 432 ページ全部が違う。**doc-gen4 より悪い** — doc-gen4 は
再ビルドされたモジュールのぶんだけは更新する (それが 43 ページ混在の実例、下記 (h))。

#### (d) S2 — ページの依存は import で閉じない (増分 1 の §4 の誤りの訂正)

増分 1 の `experiments/stage5/README.md` §4 は
「**健全な上界は逆向きの推移的 import 閉包**。M を推移的に import しないモジュールは
M が宣言するものを観測できない」と書いた。**これは elaboration については正しく、
生成されるページについては偽**だった。

docstring の autolink は「名前 → 定義モジュール」の**全域写像** (`render.ts` の `known`、
doc-gen4 の `name2ModIdx` に対応) を引く。import と無関係に全域を引くので、
**どこかに宣言が 1 個増えると、それを import していないページのリンクが増える。**

- autolink は **122,403 回試みて 5,080 回しか解決していない** (実測)。
  失敗トークン **8,965 distinct / 117,323 出現**が「宣言が増えたら解決しうる」表面積。
- 宣言名の形をした候補を出現ページ数で並べ**機械的に最大のものを採る**規則を先に決めて
  `log` (138 ページ / 638 回) を選び、増分 1 で選んだ 2 モジュールに注入した。

| 注入先 | 真の変更ページ | `self` | `referrers` | `importers` |
|---|---:|---|---|---|
| leaf (`…OmegaNoncomputable`) | 142 | stale 141 | stale 141 | **stale 141** / gratuitous 1 |
| hub (`…Meta.EntryPoint`) | 142 | stale 141 | stale 141 | **stale 3** / gratuitous **276** |

**`--mode importers` は上界でないだけでなく精度も低い** (取りこぼしつつ 276 ページを無駄に書く)。
stale の実物: `Fano.BinaryJensen` の docstring の `x * log m` の `log` が、
import していない `Meta/EntryPoint.html#log` へのリンクになる (12,625 → 12,690 B)。

**「注入先を選べば閉じる」ではない (導出、実測 2 点に EXACT 一致するモデル)**:
`log` を 432 通りの注入先すべてに広げると `importers` の stale は
min 3 / median 137 / max 141 で、**stale 0 になる注入先は 0/432**。
候補 1,095 × 注入先 432 = 473,040 組のうち **94.4% で取りこぼしが出る**。

#### (e) S3 — 抽出器の構成が IR を変え、単独再抽出はそれを再現できない

`--open` (`Extract.lean:83-84`) は 2 つのことをする — `openDecls` の設定 (`1562`) と
**`Lean.activateScoped`** (`1517-1524`)。フル抽出で `--open` を ON にすると
**263/432 モジュール**の IR が変わる (実測)。内訳は **notation 発火 97 / 名前短縮のみ 166**。

- notation 起因の 97 は「`refs` が notation の頭 6 個のいずれかを名指しするモジュールの集合」と
  **両方向で完全一致**した。
- `IMPORTERS(TypedRV) ∪ IMPORTERS(Asymptotic)` は **15 モジュール**。
  263 のうち **250**、notation 起因 97 のうち **95** がこの上界の外。
  → **IMPORTERS は印字の経路についても上界でない (実測)。**
- **単独再抽出は notation を再現できない**: notation の頭は TypedRV の外で定義されており、
  95 モジュールのうち TypedRV を推移的に import するものは **0**。したがって
  1 モジュールの target list では TypedRV が環境に載らず `activateScoped` が空振りする。
  実測 — 単独 `--open` ON の IR は**フル `--open` ON と不一致** (12,694 対 12,961 B) で、
  **`--open` OFF のフル抽出とバイト一致した** (= `--open` が何の効果も持たなかった)。
- **射程**: 既定構成 (`--open` OFF) では単独再抽出とフル抽出は今回も一致した
  (増分 1 の基準 1 の再現)。否定したのは「**全構成で**一致する」のほう。
- 副産物として、`--open` のもう一方の機構 (名前短縮 166 モジュール) は単独実行でも再現する。
  **「`--open` は増分で壊れる」と広く読むのは誤り** — 壊れるのは notation 側だけ。

ページ側への波及: `--open` OFF/ON で **247/432 ページ**が変わる。
IR 変化 263 に対する差 16 は全件 `equations` のみの差で、`render.ts` の 200 文字上限で落ちるもの。

#### (f) E6 — 所有モジュールの経路は「再生成」ではなく「再抽出」の問題だった

増分 1 §4.2 の主張「宣言を M から M′ に移すと参照側 N の IR が古くなる」を、
最も参照の多い宣言 `InformationTheory.Shannon.entropy` (49 モジュールが参照) で実測した
(移動先は「宣言 1 個以上を持つモジュールのうち最小」という規則を先に決め、
M を import する側 (down) と しない側 (far) の 2 通り)。

- 真の変更ページ **50**。`--mode self` では **stale 48**、
  `referrers` は **stale 0 / gratuitous 1**、`importers` は stale 0 / gratuitous 212〜365。
- **リンクの壊れ方は (a) 型**: 参照側 48 ページ・**171 本**のリンクが
  `Bridge.html#…entropy` を指し続け、**そのアンカーは `Bridge.html` から消えている**。
  「古いが生きている」ではなく **404 相当**。原因は `render.ts:561` の
  `refs.get(name) ?? known.get(name)` — 宣言ごとの `refs` が全域写像より優先される。
- **仮定に依存しない結論**: `--mode` をどれだけ広げても**直るページは 0**。
  `incremental.sh:81-83` は**再抽出**を台帳の変更集合に限っており、`--mode` は
  描画対象しか決めない。**`referrers` の stale 0 は「描画集合としては正しい」だけで、
  1 ページも直さない。** 打ち手は「影響集合を広げる」ではなく
  「**台帳の変更集合から再抽出集合を導くときに、名前の所有権の差分を足す**」側にある。
- **仮定に依存する部分**: 参照側 N の olean が実際は変わるなら台帳が N を検出して直る。
  対象を再ビルドできないので**この仮定は検証されていない**。

なお **base とパイプラインの出力をバイト比較しても、この壊れは見えない** (差は M と M′ の
2 ページだけ)。増分 1 の「6 回回してもフルビルドと一致」という必要条件は、
**この種の stale に対して感度が無い**。

#### (g) S4 / S5 — 削除と、サイトの器

- **削除**: olean が 1 個消えると `ledger check` は `no olean for …` で**例外**
  (`ledger.ts:121`、exit 1)。`incremental.sh` は `set -euo pipefail` なので
  **パイプライン全体がその場で止まる**。新規モジュールは台帳のモジュール一覧が
  `build` 時に凍結される (`ledger.ts:174`) ため**不可視**。IR とページの側には
  削除経路が無く (`Deno.remove` は `merge-ir.ts:208` の deps 掃除 1 箇所のみ)、
  消したモジュールのページは**バイト一致で残り**、生きた 4 ページがそこへリンクし続ける。
  **残り 431 ページのバイトは 1 つも変わらない** — `render.ts:1357` が `refs` からも
  `known` を埋めるため、消えたモジュールへのリンクは生成され続ける。
- **サイトの器**: doc-gen4 の出力 6,095 ファイル / 759,121,622 B のうち
  **モジュールページ以外は 23 ファイル / 38,637,158 B**。
  そのうち **5 個 (38,581,208 B) が全モジュールの関数** —
  `declarations/declaration-data.bmp` 37,481,374 B / `navbar.html` 646,438 B /
  `tactics.html` 451,823 B / `references.html` / `references.bib`。
  **`render.ts` はこの 23 個を 1 つも生成していない**のに、生成したページはそのうち
  12 個を参照している (出現 12,538 回)。**自パッケージのモジュールページへのリンク切れは 0** で、
  432 ページの内部整合は取れている。依存パッケージへのリンク切れ 96,061 件は
  **生成範囲の差であって stale ではない** (混ぜない)。
  doc-gen4 は per-module の `.bmp` を読み直して統合し (`Output.lean:236-243`)、
  navbar は既存 HTML の走査で作り直す (`Output.lean:357-383`) — **増分の設計は既にそこにある。**

#### (h) 43 ページ混在の実例の機構が特定できた

`.lake/build/doc` に **git rev が 2 つ混在** (`573793b…` 305 ページ / `5e38aec…` 43 ページ)
していた件は、原因が確定した (コードと mtime の実測):

- rev は `lakefile.lean:110-119` の `git rev-parse HEAD` から `srcUri` facet を経て
  SQLite の `modules.source_url` に入る。**`srcUri` は trace に入っていない**まま
  `buildFileUnlessUpToDate'` の内側にあるので (`doc-gen4/lakefile.lean:277-283`)、
  マーカーが最新なら HEAD が動いても DB は更新されない。
- マーカー `.lake/build/doc-data/<M>.doc` の mtime は 2 群に**完全に分離**していて
  (stale 43 群 17:03:14–17:25:26、他 305 群 17:26:47–17:38:33)、HTML は全 348 が同じ 3 秒窓。
  **ビルド中に commit が入った**ことによる時間的分割で、サブツリーの構造とは無関係。

**これは「ハッシュ差分では原理的に捕まらない stale」ではなく、「グローバル入力が
trace に入っていない stale」だった** (増分 1 の handoff での見立てを訂正する)。
litedoc4 側は同じ入力を `--source-url` として持っているが `envKey` に入れていないので、
**同じ穴を、より広く (432/432 ページ) 開けている** ((c))。

#### (i) 判断 — **判断点 3 は満たされない = 計画 §3 の outcome (B)**

3 軸の実行計画 §3 は「判断基準に届かないときに条件を緩めて良い数字を出しに行くのは
失敗」と書いている。**5 命題すべてが否定されたので、判断点 3 は満たさない。**
段階 5 は「stale が残らない」を主張できる状態に無い。

ただし**これは増分生成という方向の否定ではない**。否定されたのは
「**モジュール単位ハッシュを単一の真実とすれば増分が閉じる**」という §5.5 の構造であって、
増分の速度 (増分 1、3.30〜4.39s 対 17.07s) も、再抽出 IR の一致 (既定構成) も生きている。
→ `approach.md` §5.5 に**見直し案**を書いた (3 層構造への差し替え)。

#### (j) この計測が答えていないこと

1. ~~**参照側 N の olean が実際に変わるか** ((0) の仮定)~~ → **増分 3 で決着 — 変わらない (実測)。**
   対象の複製 (APFS clonefile) で `lake build` を回して確認した。**L3-1 は必須で確定。**
2. **class a の全域索引を増分更新したときの時間**。実装が無いので規模 (38.6 MB) しか出していない。
3. **依存パッケージのページをどう扱うか** — 生成範囲の決定は判断点 3 の外。
4. **doc-gen4 が同じ 5 命題を満たすか**。E5 で読んだのは「何を作るか」だけ。
5. **`--open` を既定にしたときの正しさ全般**。今回測ったのは IR の一致と波及規模だけで、
   出力が doc-gen4 と比較可能かは見ていない (計画 §6 事前決定 #4 で既定 OFF のまま)。

### 段階 5 — 増分生成 / 増分 3「参照側モジュールの olean は実際に変わるか」 (2026-08-10)

増分 2 の「この計測が答えていないこと」(j)-1 を潰した節。**対象リポジトリの複製で
実際に `lake build` を回した**ので、増分 1・2 で仮定のままだった土台が実測に置き換わる。

判断基準は**計測前に commit した** (`04a5b2d`、`experiments/stage5c/README.md`)。
全数字の出所は `benchmarks/results/stage5c-olean-propagation-summary.txt`、
生ログは `benchmarks/results/stage5c-lake-build-times.jsonl`。

#### (a) 計測条件と、制約をどう外したか

Apple M1 / 16 GiB / macOS (Darwin 25.6.0) / Lean 4.31.0 / Mathlib v4.31.0 /
Lake 5.0.0-src+68218e8。並列度は Lake の既定。

CLAUDE.md の「対象リポジトリを書き換えない」を守ったまま `lake build` を回すために、
**APFS の clonefile (`cp -Rc`) で複製を作った**。COW なので実消費ディスクは 0
(12 GB の複製後も `df` の空きは 16 GiB のまま)、**原本は一切書き換わっていない**。
複製直後に `lake build --no-build` が `All targets up-to-date (3779 jobs)` を返すことを
確認してから開始した。→ 増分 2 (j)-1 が要求していた「対象の複製」はこの方法で足りる。

対象: **A** = `Shannon.GaussianPDFVarianceDerivative`、
**C** = `Shannon.FisherDeBruijnGaussian` (A の `isHeatTimeDerivHyp_gaussian` を 2 か所で参照)。
A の直接 importer は C と root だけ。**root (`InformationTheory`) は 431 モジュールを
全部 import している**ので、どのモジュールを変えても root は必ず再ビルドされる
(パッケージ内 import グラフで葉は root ただ 1 つ、実測)。

#### (b) 判定 — P3 が偽なので **L3-1 は必須で確定**

| # | 命題 | 事前予測 | 結果 |
|---|---|---|---|
| P1 | olean の生成は決定的 | 真 | **真** (パスを固定すれば。再ビルド 4 回すべて byte 一致) |
| P2 | 未参照の宣言を A に足しても C の olean は不変 | 真 | **真** (C の sha256 完全一致。mtime は変わる = 再ビルドはされている) |
| **P3** | **宣言が A→X へ移動すると参照側 C の olean が変わる** | **偽** | **偽** (C は 1 byte も変わらない) |

P3 の実験: A の中身を丸ごと新モジュール X へ移し、A を `import X` だけの shim にした。
**C のソースは 1 byte も変えていない**。宣言の完全名は不変 (namespace はファイルパスと独立)。
`lake build` 後の olean 差分は **added 1 (X) / content changed 1 (A のみ)** で、
**C と root は「再ビルドされたが byte 一致」**。Lake の `<file>.olean.hash` と sha256 は
全ステップで完全に一致した (食い違い 0) ので、**ハッシュの精度の問題ではなく、
参照側モジュールに変化そのものが存在しない**という構造の問題。

- 宣言の**改名**は参照側の olean を変える (名前が式に埋まっている。P2 の対偶)
- 宣言の**移動**は参照側の olean を変えない

→ `approach.md` §5.5 の L3-1「所有権が変わった名前を参照するモジュールを再抽出集合に足す」を
**仮定から実測に裏づけられた必須項目へ格上げ**した。
同時に P2 が真だったので、**再抽出集合を import 閉包へ広げる必要は無い**ことも実測で確定した
(広げるべきは出力側 = ページであって入力側ではない。増分 2 の結論と一致)。

#### (c) `lake build` の時間 — 増分の臨界パスの 74〜79% 【実測】

増分 1 が測った 3.30〜4.39 秒の**手前に必ず乗る**のに、これまで未測定だった数字。

| | 再ビルド数 | 壁時計 | CPU (user+sys) | major fault |
|---|---:|---|---|---|
| 無変更 (n=5) | 0 | **2.33〜3.39s** (中央値 2.40s) | 3.15〜3.35s | 233〜489 |
| 1 モジュール変更 (n=8) | 3 | **12.40〜26.89s** | **11.55〜14.18s** | 29,176〜167,377 |
| 同上・fan-in 大 (n=2) | **127** | **237.24 / 242.72s** | 1,083〜1,150s (並列 4.0〜4.8×) | 275,347 / 555,347 |

**壁時計は 2.2 倍動くが CPU 時間は安定**していて、差はそのまま major fault の数に対応する。
系列を繰り返すと warm 化し、**収束後の warm 値は 12.40〜12.73 秒**。peak RSS は 3.25 GB
(無変更は 0.74 GB)。1 回だけ測っていたら 26.89 秒を「1 モジュール変更のコスト」として
記録していたところで、CLAUDE.md「1 回だけ測った数字を信じない」がそのまま効いた事例。

端から端まで (warm):

| | 時間 | 割合 |
|---|---|---|
| `lake build` (1 モジュール変更、直接 importer 2) | 12.40〜12.73s | **74〜79%** |
| `lake build` (同、直接 importer 54 / 推移 126 → 127 モジュール再ビルド) | **237.24〜242.72s** | **98.2%** |
| doc 側の増分 (増分 1) | 3.30〜4.39s | 1.8〜26% |

**1 モジュールの変更でも fan-in で 19 倍 (12.4 → 237〜243 秒) 動く。** 再ビルドされたのは 127 個
(= 推移 importer 126 + 自分。import グラフの予測と完全一致)、1 モジュールあたり
min 2.4s / median 6.3s / max 34.0s。
**doc 側をゼロにしても端から端では 12.4 秒より速くならない。** 無変更なら
`lake build` 2.40s + 無変更検出 0.080s = **2.48s**。
→ `approach.md` §6.5 に「増分の分母は doc 生成だけではない」を追記した。
**§6.5 の 3.30〜4.39 秒を「増分の時間」として単独で引用しないこと。**

#### (d) 副次的に出た 2 つ (どちらも §5.5 の設計に直接効く)

1. **olean にソースの絶対パスが 429/432 モジュールで埋まっている**【実測】。
   原本 (パス 31 文字) と複製 (107 文字) で同じソースをビルドすると C の olean が
   5,968 → 6,048 B (+80 = 差 76 文字を 8 byte 境界に丸めた値) になり、sha256 も
   Lake のハッシュも変わる。正体は **Mathlib の style linter のログを持つ環境拡張
   `lintLogExt`** で、警告文 (`Copyright too short!` など) に絶対パスが入ったまま
   olean に保存されている。パスを含まない 3 件 (root / `Meta.EntryPoint` /
   `Shannon.WynerZiv.Operational`) は `lintLogExt` のエントリも持たず、**429 と 429 が
   完全一致**するので原因はこれで確定。Mathlib 自身の olean にはパス文字列が 0 個。
   fan-in 実験がこれを 124 モジュール規模で再確認した — 撤回して baseline と同一ソースに
   戻したのに 124 個が byte 不一致で、**サイズ差は +72 と +80 の 2 値しかない**
   (パス長差 76 を 8 byte 境界に丸めた値)。**P1 は破れていない。破れているのは
   「パスをまたいだ再現性」。**
   → **olean の内容ハッシュはチェックアウト先のパスに依存する。** CI のワークスペースパスが
   開発機と違うだけで 429/432 が「変更あり」になり IR キャッシュが全滅する。
   Lean の olean 形式の性質ではなく**警告を出したままにしていることの帰結**なので、
   L1 に「リンタ構成」を入れるだけでは足りず、**キャッシュの鍵をパス非依存にする**
   (または警告を消す) 必要がある。
2. **Lake は孤児 olean を消さない**【実測】。X のソースを消して `lake build` しても
   X の olean は残った。同じ理由で、現行ソースが 432 モジュールしかないのに `.lake` の
   olean は 1,090 個あり、**659 個が現行ソースに対応しない**。
   → §5.5 の L2 改善案「モジュール一覧をディスク走査で作る」は、走査先を `.lake/build` に
   すると幽霊を 659 個拾う。**列挙元は Lake のライブラリ target (ソースの glob)**。
   増分 2 の S4 (消えたモジュールがページを残す) は doc 側だけの問題ではなく、
   **Lake 自身が同じ構造を持っている**。

#### (d2) ledger の穴 3 つを塞いだ — 数字は動かなかった【実測】

増分 2 が見つけた実装の穴を塞ぎ、**同一セッションで before → after → before を測り直した**
(2 つ目の before はドリフト検査)。出所は `benchmarks/results/stage5c-ledger-fix-summary.txt`、
生ログは `stage5c-ledger-fix.jsonl` (3 ブロック × 3 変種 × 6 回)。

| 直したもの | 何が起きていたか |
|---|---|
| L1 を変更集合に効かせる | `envChanged` を計算して捨てていた (S1 の正体)。鍵の比較も**和集合**にした — 片側にしか無い鍵を無視していたので `--ir` を渡し忘れると IR スキーマの変化が黙って消えた |
| `incremental.sh` に `--ir` | 上の穴の実発火経路 |
| モジュール一覧の凍結を外す | `check --modules` で現在の一覧を読み直し `added` / `removed` を報告。olean が無いときに**例外を投げるのをやめた** (S4 で `check` が exit 1 で死んでいた原因)。`incremental.sh` は削除を見たら exit 4 で止まる — 削除経路は未実装なので、黙って古いページを残すより落ちるほうがよい |

| 変種 | detect (before1 / after / before2) | total |
|---|---|---|
| leaf-self | 0.101 / **0.103** / 0.102 | 3.603 / **3.600** / 3.630 |
| mid-referrers | 0.101 / **0.102** / 0.102 | 4.183 / **4.186** / 4.179 |
| hub-importers | 0.101 / **0.103** / 0.102 | 3.175 / **3.154** / 3.149 |

**detect の増分は +0.001〜0.002 秒 (1〜2%) で before どうしのドリフトと同じ大きさ、
total は 3 ブロックで区別できない。穴を塞ぐのは無料だった。** 出力も 3 ブロックで一致
(changed=1 / irChanged=0 / pagesRendered=1・81・415)。

**この値で増分 1 の 3.30〜4.39 秒を置き換えない。** 今回のセッションは 4〜6% 速い
(その間に複製上で 127 モジュールのビルドを何度も回して page cache が入れ替わっている)。
今回の値の意味は「**別セッションでも 4〜6% 以内で再現する**」という再現性の確認。

**新しく起きること (未計測)**: グローバル鍵が変わると 432 モジュール全部が再抽出集合に入る。
正しいが粗い — `--source-url` の変更は再抽出ではなく**再レンダリングだけで足りる**はずで、
**鍵を「再抽出を要求する鍵」と「再レンダリングだけを要求する鍵」に分けるのが次**
(→ `approach.md` §5.5 L1)。

#### (e) この計測が答えていないこと

1. **fan-in の分布**。測ったのは両端 (直接 importer 2 と 54) の 2 点だけで、
   実際の編集がどのあたりに落ちるかは分かっていない。**中央値の変更コストは未測定。**
2. **P1〜P3 は n=1 のモジュール対 (A, C) でしか見ていない**。決定性は 4 回確認したが、
   「移動しても参照側が変わらない」を別の宣言・別の種類 (instance / simp 補題 /
   `abbrev`) で確かめてはいない。**instance は特に怪しい** — インスタンス解決の結果が
   参照側の項に焼き込まれるので、移動が伝播する可能性が残っている。
3. **Linux での再現**。`lintLogExt` のパス埋め込みも olean の決定性も macOS でしか見ていない。
4. **参照側の IR が実際に腐ることの直接観測**。P3 は「olean が変わらない」までで、
   そこから先 (IR の `refs` が古い所有者を指し続ける) は増分 2 の実測に依拠している。

### 段階 5 — 増分生成 / 増分 4「L1 の鍵を 2 種類に分ける」 (2026-08-10)

増分 2 で否定された S1 の**残り半分**を潰した節。段階 5c は「`envChanged` を計算して
捨てていた」ほうを直したが、**鍵の中身には `--source-url` が最後まで入っていなかった**
(`envKey` は 5 項目で、どれも source-url ではない)。つまり S1 の否定はここまで生きていた。

判断基準は**計測前に commit した** (`0b68b7e`、`experiments/stage5d/README.md` の C1〜C7)。
全数字の出所は `benchmarks/results/stage5d-keysplit.txt` (生ログ `stage5d-keysplit.jsonl`) と
`stage5d-detect-cost.txt` (生ログ `stage5d-detect-cost.jsonl`)。

#### (a) なぜ「入れる / 入れない」の二択が両方まずいか

`--source-url` は 40 桁の git revision を含むので、**コミットのたびに変わる**。
増分ビルドが走るのもコミットのたびなので、この入力は「たまに変わる設定」ではなく
**毎回変わる入力**である。

- 鍵に**入れない** → `check` は 0 changed、しかし 432/432 ページが stale (= S1)。
- 単一の鍵に**入れる** → 毎コミットで 432 モジュール全再抽出。Lean が見ることすら
  できない入力 (IR に source URL は入っていない) のために Lean を起動する。

第 3 の答えが**血中半径の違う 2 つの鍵**。振り分けの基準は「出力を変えるか」ではなく
(両方変える)、「**IR を変えうるか** = 答えるのに Lean が要るか」。

| 鍵 | 中身 | 変わったとき |
|---|---|---|
| `extractKey` | toolchain / manifest / 抽出器 id / IR schema / IR generator | 432 全再抽出。どのページが stale かは従来どおり IR 差分が決める |
| `renderKey` | 生成器 id / `--source-url` | 再抽出**ゼロ**、再レンダリング**全部** |

#### (b) 判定 — C1〜C6 は予測どおり、C7 は真だが予測値が外れた

| # | 命題 | 事前予測 | 結果 |
|---|---|---|---|
| C1 | source-url だけ変えると `reExtract 0` + render-all 報告 | 真 | **真** (`renderKey:sourceUrl` 1 行) |
| C2 | その実行で Lean が起動しない | 真 | **真** (extract 0.033s = 分岐不通過、抽出器イベント無し) |
| C3 | 432/432 ページのバイトが実際に変わる | 真 | **真** (旧 rev を残すページ **0**、新 rev を持つページ **432**) |
| C4 | IR は前後で byte 一致 | 真 | **真** (`diff -r` が無出力) |
| C5 | extract 鍵の変更は 432 再抽出、render-all は空 | 真 | **真** |
| C6 | 分割の detect コストはドリフト以下 | 真 | **真** (**+0.3 ms** 対 ドリフト 6.8 ms) |
| C7 | 単一鍵より安い | 真, 25〜30 s 節約 | **真。ただし予測値は外れ — 実際は 17.05 s** |

#### (c) C7 — 分割前後の対照【実測】

「単一鍵だったら source-url 変更が何を要求したか」は**反実仮想を作って測った** —
extract 鍵を偽装 + `--mode all` = 「432 再抽出 + 432 再レンダリング」。
5 回ずつ**交互**実行、run 1 破棄、同一セッション・同一暖機状態。

| | split (source-url 変更) | onekey (単一鍵の反実仮想) |
|---|---:|---:|
| 壁時計 | **1.3414 [1.3359–1.5036]** | **18.3923 [17.2356–21.2470]** |
| user+sys | 1.5200 | 17.3300 |
| major fault | 190 | 19,565 [5,586–53,544] |
| peak RSS | 218.5 MB | 3,297 MB |
| extract | 0.0281 (不実行) | 16.7658 [15.5857–19.5784] |
| render | 0.8652 | 0.9245 |
| 再抽出モジュール | **0** | 432 |
| 生成ページ | 432 | 432 |

**節約は 17.05 秒 / 13.7 倍。分母は「1 コミット = revision が変わる」1 回分。**

**予測 25〜30 s が外れた理由**: 27 秒は `extract-once.sh` 単独の既存値で、この系列の
432 モジュール再抽出は 16.77 秒だった。**同じ計測ではない** (前者は単独・cold 寄り、
後者は render と交互で page cache が動き続ける)。**onekey 系列は収束していない** —
major fault が 5,586〜53,544 と 1 桁動き、壁時計も 4 秒幅がある (メモリ律速の署名)。
**17.05 秒はこのセッションの中央値どうしの差であって、27 秒の置き換えではない。
両者を並べて引用しない。**

**detect 列は対照になっていない** (split 0.1441 対 onekey 0.1035)。onekey は各 run の
直前に台帳を作り直すので 432 個の `.olean.hash` が暖まっている。**分割の真のコストは
C6 の +0.3 ms** — 同一ワークロードに対し 2 つの**版**を交互に当てて測った。

#### (d) 節約以外に確定したこと — 「影響集合の mode」は実運用でほぼ効かない

revision は**ページのバイトに 4,992 か所出る**ので、コミットのたびに 432 ページ全部が
無効になる。したがって:

1. `--mode self|referrers|importers` は、**実際のコミットの revision 成分に対しては
   常に `all`** に潰れる。mode が決めるのは「モジュール変更が追加で stale にするページ」
   だけで、**レンダリング量を決めるものではなくなった。**
2. ただしその量は元々小さい — **432 ページ全部で 0.87 秒**。
   分割が 13.7 倍になるのは、**高いのはレンダリングではなく抽出のほうだった**から。

**訂正 (2026-08-10)。** ここには当初「大半は IR 全読みの固定費」と書いていたが、**生ログと
合っていなかった**。`benchmarks/results/stage5d-keysplit.jsonl` の `stage5d-split` の段別
タイマー (run 2、中央値の run) は次のとおり:

| | 秒 | 割合 (プロセス内 total 0.7996 に対して) |
|---|---:|---:|
| 固定費 (preMain 0.002 + readIr 0.133 + indexBuild 0.009) | **0.144** | **18%** |
| ページ数に比例する分 (renderHeaders 0.2136 + renderPage 0.2508 + flatten 0.0492 + write 0.1398) | **0.653** | **82%** |
| deno 起動など (サブプロセス壁時計 0.8653 − プロセス内 0.7996) | 0.066 | — |

**固定費が大半を占めるのは 1 ページだけ再生成する場合** — §(g) の `leaf-self` が
壁時計 0.1952 秒中 床 0.17 秒 = 87%。**その記述が 432 ページのケースに横滑りしていた。**
効くのは下の 3 で、**0.87 秒のうち消えるのは 82% の側**なので取り分は小さくない。

#### (e) この計測が答えていないこと

1. **実コミットの合計は測っていない。** split の 1.34 秒は「source-url だけが変わった」
   場合で、実際のコミットは**モジュールも変わる**。合計 = 1.34 秒 + 変更モジュールの抽出
   (+ `lake build`、これが臨界パスの 74〜98% であることは増分 3 の実測)。
2. **`--source-url` 以外の renderKey 項目は空。** `--ws-heuristic` は approach.md が挙げて
   いるが `render.ts` には該当フラグが無い。今の renderKey は生成器 id と sourceUrl の 2 項目。
3. **rev をページのバイトに埋めない設計は検討も計測もしていない。** 相対リンク + 配信時
   注入なら、コミットで無効になるページは **0** になり 0.87 秒も消える。§5.6 の出力契約の
   変更であって台帳の話ではない (→ approach.md §8 の未解決の問い)。

### 段階 5 — 増分生成 / 増分 5「L3-1 を実装して本物の移動で検証する」 (2026-08-10)

増分 3 で「必須」と確定した L3-1 を実装し、**複製上で本物の移動リファクタ + `lake build`**
を回して、フルビルドとの **byte 一致**で判定した節。判断基準は**計測前に commit**
(`ec646a3`、`experiments/stage5e/README.md` の D1〜D5)。
出所は `benchmarks/results/stage5e-ownership.txt`。

#### (a) 判定オラクルを「stale 0 と報告されること」にしない

報告を出すのは検証対象と同じコードなので、報告は証拠にならない。
**判定はフルビルドのページ木とのバイト一致**にした。3 本の木を作って比べる:
L2 だけの増分 / L2+L3-1 の増分 / 移動後の状態からの全抽出 (= オラクル)。
両増分とも**実物の `incremental.sh`** を通す (`--l3-1 on|off`)。

#### (b) 結果

| # | 命題 | 事前予測 | 結果 |
|---|---|---|---|
| D1 | 参照側は L2 の変更集合に入らない | 真 | **部分的に否定** — 4 参照側のうち **3 つは入っていた**、1 つ (`StrongForm`) は入らない |
| D2 | L3-1 無しではフルビルドと食い違う | 真 | **真** — 433 ページ中 **1 ページ**だけ食い違い、それが L2 の取りこぼした参照側 |
| D3 | L3-1 有りで byte 一致 | 真 | **真** — 433/433 一致 |
| D4 | 2 ラウンドで不動点 | 真 | **真** — 2 ラウンド目は stale 0 |
| D5 | L3-1 のコストは小さい (≲0.3 s) | 真 | **真** — 0.1604 + 0.0011 = **0.162 秒** |

**D1 がいちばん重要**: L2 は参照側を捕まえることが**あるが、当てにできない**。
4 つのうち 3 つは olean が実際に動いたので L2 が拾った。1 つは動かず、台帳側の
どんな規則でも届かない (= 増分 3 の P3)。**「L2 が参照側を拾うかどうかは Lean の
直列化の都合であって、ドキュメントの依存構造の性質ではない」** — 保証を述べられない層は
頼れる層ではない。

**D2 で腐っていたもの** (実測、`Huffman/StrongForm.html` の 76 行目):

```
増分:       .../Huffman/Length.html#InformationTheory.Shannon.Huffman.huffmanLength
フルビルド: .../Huffman/LengthCore.html#InformationTheory.Shannon.Huffman.huffmanLength
```

飾りの違いではなく**リンク切れ** — `Length.html` にそのアンカーはもう無い。
判断点 3 が予言し、端から端では示せていなかった失敗がここで実物として出た。

#### (c) コストは L3-1 本体ではなく「もう 1 ラウンド」【実測】

| | L3-1 無し | L3-1 有り |
|---|---:|---:|
| ラウンド | 1 | 2 |
| detect | 0.150 | 0.114 |
| extract | 3.199 | **6.170** |
| ownership | 0.000 | 0.265 |
| merge | 0.212 | 0.319 |
| impact | 0.159 | 0.158 |
| render | 0.225 | 0.212 |
| **合計** | **4.173** | **7.777** |

**L3-1 自体は 0.265 秒。正しさの代価 3.6 秒はほぼ全部「2 回目の抽出器起動」** —
1 モジュールの解析のために環境ロード約 3.1 秒を丸ごと払っている。
**これは増分 3 で優先度を下げた段階 6 (常駐) を押し戻す論拠**: ラウンドが増えるたびに
環境ロードが 1 回増え、**L3-1 はラウンドの追加を例外ではなく通常にする**。

#### (d) 実験の題材選びを 1 回間違えた (記録)

段階 5c の A (`GaussianPDFVarianceDerivative`) をそのまま使ったが、5c が「2 か所で参照して
いる」と書いた C (`FisherDeBruijnGaussian`) は**宣言 0 個のドキュメント専用モジュール**で、
参照は module docstring 内のバッククォートだった。**A の宣言を印字署名で参照する
モジュールはパッケージ内に 0 個** (実測)。olean を見る 5c ではこれで問題なかったが、
IR の `refs` を問う本節では**移動が原理的に観測できない**題材になっていた
(両方の木が byte 一致して何も言えなかった)。→ A は IR の census から選び直し
(`Huffman.Length`、参照側 4 モジュール)、`run.sh` は参照側 0 のモジュールでは走らない。

#### (e) 複製上の baseline を汚す罠 (今回の最大の落とし穴)

`cp -Rc` は**ビルド木ごと**複製するので、複製の olean は**元のパスで**作られている。
段階 5c が見つけた「olean にソースの絶対パスが埋まる」がここで効く —
**複製内で最初に再ビルドされたモジュールは、パス長の差だけで olean が変わる**
(実測: 5,968 → 6,048 B = +80、5c が記録した符号と一致)。
台帳はそれを「変更」と報告するので、**編集と無関係な理由で変更集合が汚れる。**

気づけた決め手は「**編集を戻してもう一度 check した**」こと — 移動の結果なら移動を
取り消せば消えるはずが、消えなかった。→ `rebuild-own.sh` で複製内の自パッケージ
431 モジュールを一度ビルドし直してからベースを取る。**規則として: 複製上の baseline は、
再ビルドされうるものを全部複製内で再ビルドしてからでないと有効でない。**

#### (f) 副産物 — 自パッケージのフルビルド【実測】

自パッケージの olean を消して Mathlib 暖機済みで再ビルド:
**624.88 秒 wall / 2,899 秒 user / 975 秒 sys**、431 モジュール、peak RSS 3.42 GB、
page fault 981,955 (Apple M1 / 16 GiB、Lake 既定の並列度)。

#### (g) この計測が答えていないこと

1. **移動は n=1**。別種の宣言 (instance / simp 補題 / `abbrev`) では確かめていない
   (増分 3 の宿題がそのまま残る)。
2. **3 ラウンド以上を要する変更を作っていない。** D4 が真だったのは「再抽出された参照側が
   さらに所有権を動かさなかった」ためで、**そうならない変更が存在しないことは示していない。**
   `--max-rounds` は 5 で、超えたら exit 5 で止まる。
3. **`byMovedElsewhere` の規則は 1 度も発火していない** (今回は全部 `lostOwner`)。
   冗長かどうかは未決。

### 段階 5 — 増分生成 / 増分 6「削除経路」 (2026-08-10)

台帳・IR・ページの 3 箇所から消す経路を実装し、複製上で**本物のモジュール削除 +
`lake build`** を回して、フルビルドとの byte 一致で判定した節。判断基準は**計測前に
commit** (`6baa32d` / stage5f README の F1〜F5)。出所は
`benchmarks/results/stage5f-deletion.txt`。削除したのは
`Shannon.Kolmogorov.OmegaNoncomputable` (52 宣言、root だけが import、参照側 0)
— IR の census から選んだ。

#### (a) 結果

| # | 命題 | 事前予測 | 結果 |
|---|---|---|---|
| F1 | `check` が `removed` に載せ、root を `changed` にする | 真 | **真** |
| F2 | 削除モジュールの全名を `lost` に畳み、stale は 0 | 真, 0 | **真** (52 名、stale 0、0.153 秒) |
| F3 | IR から索引エントリもモジュールファイルも消える | 真 | **真** (431=431、モジュール集合一致、依存写像 527=527 で一致) |
| F4 | ページが消え、孤児も空ディレクトリも残らない | 真 | **真** (ディレクトリ集合までフルビルドと一致) |
| F5 | 増分ページ木がフルビルドと byte 一致 | 真 | **否定** — 431 ページ中 **1 ページ**が食い違う |

**削除経路そのものは通った。** 3 箇所すべてがフルビルドと一致し、pruning のコストは
4.909 秒の実行のうち **0.066 秒**。

#### (b) F5 の正体は削除ではなく L3-2 (全域写像)【実測】

食い違ったのは削除と無関係な `Shannon/LZ78/GreedyLongestPrefix.html`。
**増分側にだけリンクが余分にある**:

```
増分:       <code>bAbsorbed = <a href=".../Mathlib/Data/Nat/Find.html#Nat.find">Nat.find</a></code>
フルビルド: <code>bAbsorbed = Nat.find</code>
```

機構を端から端まで追った:

1. 削除したモジュールは、パッケージ内で `Nat.find` を印字署名で参照していた**唯一**の
   モジュールだった (3 か所)。
2. だから `Nat.find` はパッケージの**全域写像 (名前 → 定義モジュール)** に入っていた。
   削除でこれが落ち、他 5 名と合わせて写像は **533 → 527** に減った。
3. `GreedyLongestPrefix` の **docstring** には `` `Nat.find` `` がバッククォートで出てくる。
   autolink はバッククォートのトークンをこの全域写像で解決するので、削除前はリンクになり
   削除後はならない。
4. そのページは再生成されない。自分の IR は変わっていないし、どの規則も指していない。
   **古いリンクがそのまま残る。**

判断点 3 が示した L3-2 より**鋭い**実例になっている:

- **失敗が双方向**。判断点 3 は「リンクが増えるべきなのに増えない」ページだった。
  ここは「**リンクが消えるべきなのに残る**」— しかも指す先のアンカーはもうどこにも無い。
- **影響先が任意に遠い**。LZ78 は Kolmogorov を import しないし、されないし、参照も共有
  しない。**繋いでいるのは全域写像だけ** — これが「import で閉じない」の意味。
- **差分が小さく、計算できる**。写像から抜けたのは 6 名。影響しうるページは
  「その 6 名を docstring に含むページ」で、これは**走査であって推測ではない** (→ 増分 7)。

**計測前に宣言した撤退ラインが「これは削除ではなく出力契約についての発見」と言っていた
とおりのものが出た。** F1〜F4 が削除経路であり、そこは成立している。

### 段階 5 — 増分生成 / 増分 7「全域索引と、全域写像の差分」 (2026-08-10)

判断点 3 が「未実装」に格上げした**全モジュールの関数である成果物**を実装し、同時に
それが持つ写像の**差分**を再生成集合の導出に使った節。判断基準は**計測前に commit**
(`7789711` / stage5g README の G1〜G5)。出所は `benchmarks/results/stage5g-global.txt`
(生ログ `stage5g-global.jsonl`) と `stage5f-deletion.txt`。

#### (a) 成果物と写像は同じデータの 2 つの顔

`declaration-data.bmp` が直列化しているのは「名前 → 定義モジュール」で、これは
docstring の autolink が引く写像そのもの。だから**成果物の差分がそのまま
「サイトのどこでリンクが変わりうるか」**になる (→ 増分 6 の L3-2)。

#### (b) 結果

| # | 命題 | 事前予測 | 結果 |
|---|---|---|---|
| G1 | `tactics.html` は自パッケージの IR から導けない | 真 | **真** (432 モジュールで tactic docstring **0**) |
| G2 | フル生成は 1 秒未満 | 真 | **真** — **0.139 秒** (読み 0.119 + 書き 0.020)、振れ 0.138〜0.143 |
| G3 | 増分生成の成果物はフル生成と byte 一致 | 真 | **真** (独立 2 回の生成が byte 一致。増分実行の成果物も 5e/5f の比較に含めて一致) |
| G4 | 増分更新は書く価値がある | **偽** | **偽で確定** |
| G5 | 全域写像の差分規則で 5f の F5 が閉じる | 真 | **真** — 1 ページ食い違い → **437/437 byte 一致** |

#### (c) 規模 — 38.6 MB を持ち込まないこと

| ファイル | バイト |
|---|---:|
| `declarations/declaration-data.bmp` | 1,216,017 |
| `declarations/name-map.json` | 602,729 |
| `navbar.html` | 57,949 |
| `tactics.html` / `references.html` / `references.bib` | 243 / 186 / 0 |
| **合計** | **1,877,124** |

**1.88 MB であって 38.6 MB ではない。** doc-gen4 の 37.5 MB は依存木全体
258,760 宣言を載せているから。こちらは**自パッケージ 4,750 宣言 + 依存側 533 名**。
**判断点 3 の 38.6 MB は doc-gen4 の出力の記述であって、こちらの数字として再利用しない。**

#### (d) G4 — 増分更新は書く価値が無い (予測どおり)

増分化で消せるのは IR 全読みの **0.119 秒**だけ。変更モジュールの読み・派生写像の再構築・
全成果物の書き出し (0.020 秒) は残る。増分実行 4.5〜8.0 秒に対し、この段全体が
**1.7〜3.1%**、消せる部分は **1.5〜2.6%**。

**同じ答えが 3 度目**: 台帳は `build` が `check` と同コストなので丸ごと作り直す、
依存スライスは `merge-ir.ts` が patch せず再計算する、そして全域成果物。
共通の原因は **IR 全読みが 0.119 秒**であること — §5.4 で「IR を読み直せる大きさに保つ」
と決めたことが、これらの増分版を毎回不要にしている。

#### (e) G5 — 差分規則は当たったが、そこに至るまでに 2 回間違えた

規則は効いた: 写像から 58 名が出入りし (5,283 → 5,225)、走査は**ちょうど 1 ページ**
(`LZ78.GreedyLongestPrefix`、`` `Nat.find` `` に言及) を名指しした。過剰近似なし。

間違いは 2 つとも同じ種類 — **形を読まずに仮定した**:

1. **写像を自パッケージの宣言だけで作った。** 効いていた `Nat.find` は**依存側**の名前で
   IR の依存スライスにいる。自宣言だけだと差分は 52 名で、そこに当該の名前が無く、
   **ページが明らかに壊れているのに「再生成 0 ページ」と報告した。**
2. **走査が code span を丸ごと照合していた。** `render.ts` の `autoLinkInline` は
   code span の**中身を空白で分割**してから各部分を名前として解決する。docstring は
   `` `bAbsorbed = Nat.find` `` なので、span 単位の照合では当たらない。同じ分割に直した。

**どちらも外からは同じ「0 ページ」に見えた。** フルビルドとのバイト比較が無ければ
どちらも見えないままだった — オラクルを外に置く理由を経験で言い直したことになる。

### 段階 6 — 常駐プロセス「1 プロセスで何回も抽出する」 (2026-08-10)

抽出器に `--serve` を足し、環境を 1 度だけ読んで stdin の要求ごとに抽出する形にして
測った節。判断基準は**計測前に commit** (stage5h README の H1〜H5)。
出所は `benchmarks/results/stage5h-resident.txt`。

#### (a) 結果

| # | 命題 | 事前予測 | 結果 |
|---|---|---|---|
| H1 | 2 回目以降は環境ロードを払わない | 真 | **真** — 6 要求すべてで `importModules` / `initSearchPath` が **0.0000 秒** |
| H2 | 常駐で作った IR は単発と byte 一致 | 真 | **真** — 6,021 モジュールの環境下でも全要求で一致 |
| H3 | 1 モジュール要求は単発より大幅に安い | 真, ≲0.6 秒 | **真。予測より 1 桁良い** — **0.035〜0.088 秒** 対 単発 1.72〜4.60 秒 |
| H4 | 汚染しない (繰り返し・交互でも同じ) | 真 | **真** — 6 要求で劣化なし |
| H5 | 5e の 2 ラウンドが環境ロード 1 回分安くなる | 真, ≈−3.2 秒 | **真** (≈−2.9 秒、**外挿**) |

| | 秒 |
|---|---:|
| 起動時の環境ロード (432 モジュールの import 一覧 → 6,021 モジュール) | **6.335** |
| 要求 1〜6 | 0.040 / 0.088 / 0.035 / 0.041 / 0.036 / 0.081 |
| 単発 StrongForm / Length / Asymptotic | 4.602 / 1.867 / 1.718 |
| うち `importModules` | 4.584 / 1.808 / 1.697 |

**単発実行はほぼ全部が環境ロード** — このモジュール群で 98〜99%。解析は 0.02〜0.06 秒。

#### (b) H2 が成立したことが前提条件だった

常駐の環境は **6,021 モジュール**を抱えている。単発は 1 モジュールの閉包しか持たない。
それでも IR は全要求で byte 一致した。**自明ではない** — pretty printer は環境に対して
名前を短縮するので、環境が大きいと印字が変わりうる。ここが崩れていたら常駐は
「速い同等物」ではなく「別の生成器」になっていた。

#### (c) 制約 — 常駐環境は再読み込みできない

Lean には、import 済み環境から 1 モジュールだけ差し替える手段が無い。だから
サーバの olean 観は import 時点で凍る。**編集したモジュールには誤りで、それ以外には正しい。**
これは L3-1 が作る切れ目とちょうど一致する:

- **ラウンド 1** は変更モジュールの再抽出 = olean が動いている → **常駐で答えてはいけない**。
- **ラウンド 2 以降**は olean が動いていないモジュール (L2 が見逃したのはまさにそれが理由)
  → **常駐で正しく答えられる。**

> **段階 6a で否定 (2026-08-10)。** 2 番目の箇条書きは**誤り**。モジュールの IR は
> 自分の olean の関数ではない — `Extract.lean:1352` が参照の**所有者を環境から**引き、
> **その所有者列を直すことが L3-1 がラウンド 2 を回す唯一の理由**である
> (`ownership.ts:8-20`)。編集前環境は古い所有者を返すので、ラウンド 2 を常駐で答えると
> **L3-1 が直そうとしたその誤りをそのまま再現する** — 実測で参照側 IR が 10,263 B
> 対 正解 10,267 B、所有者が `…Huffman.Length` (正解は `…Huffman.LengthCore`)、
> **439 ページ中 1 ページが誤り**。「それ以外には正しい」の「それ以外」は
> **編集の下流に無いもの全部**で、編集ループではそれは何も無い (→ 段階 6d の U1)。
> 安全なのは **`lake build` の後に立てた**サーバ。

#### (d) H5【外挿 → 段階 6a で否定】 — 5e の 2 ラウンドに当てると

| | 実測 | ラウンド 2 を常駐にした場合 |
|---|---:|---:|
| ラウンド 1 の抽出 (5 モジュール、新プロセス必須) | 3.199 | 3.199 |
| ラウンド 2 の抽出 (1 モジュール、olean 不変) | ~2.971 | **~0.04** |
| その他 | 1.819 | 1.819 |
| **合計** | **7.989** | **≈ 5.06** |

**≈ −2.9 秒。** これは 2 つの実測の合成であって、組み上げたパイプラインの計測ではない
(常駐サーバは `incremental.sh` に配線していない)。

> **段階 6a で組み上げた結果、この表は残らない。** この形 (`pre`) は **5.284 秒**に着地したので
> **時計は近かった**が、**出力が誤り**。到達可能で正しい数字は **6.168 秒**
> (`lake build` 後のサーバに全ラウンドを任せる) 対 現行 7.940 秒。
> この表が仮定する「ラウンド 2 だけ任せる」は、サーバ起動を run の中で払うと **8.788 秒で損**。

#### (e) この計測が言っていないこと

1. **起動は無料ではない — 6.335 秒。** 単発 1 モジュールの import (1.7 秒) より**高い**
   (パッケージ全体を読むため)。**常駐は「起動が安い」のではなく「何度も答える」ことで勝つ。**
   `lake build` のたびにサーバを再起動する設計は、1.7 秒を節約するために 6.3 秒を払う —
   **4 ラウンド未満では損。**
2. **本当に合うのは長寿命のプレビューモード** — サーバが複数の編集をまたいで生き、
   直前に編集したモジュール以外に答える形。**これは §8 の「プレビューモードを持つか」への
   論拠であって、当初の枠組みの段階 6 への論拠ではない。**
3. **6 要求はソークテストではない。** H4 は 6 回で成立しただけで、600 回は言えない。
4. **import 一覧の外のモジュールを要求した場合は未検証。**

### 段階 6a — 常駐をパイプラインに組む「配線して端から端で測る」 (2026-08-10)

段階 6 は `--serve` を**単体で**測っただけで、`incremental.sh` に入れていなかった。
その状態で書いた **7.989 → ≈5.06 秒【外挿】** を実測に置き換える節。
判断基準は計測前に commit (`19f2f54`)。

**計測条件。** Darwin 25.6.0 arm64 / Apple M1 / **16 GB** / `leanprover/lean4:v4.31.0`。
対象は `/Users/haruka/dev/lean-projects` の APFS 複製で、**自パッケージ 431 モジュールを
複製内で再ビルドしてから**使った (626.75 秒 wall / 2762.29 秒 user / 821.13 秒 sys、
peak RSS 3.42 GB、page faults 1,110,490)。**warm** — サーバを立てたのは再ビルドの数分後で、
自パッケージの olean は page cache にある。1 変種 6 run を**フェーズ内で交互配置**、run 1 破棄、
以下は中央値。生ログ `benchmarks/results/stage6a-resident-wiring.{txt,jsonl}` (30 run)。

**サーバは 2 台を逐次に立てた。** 抽出器の peak RSS が 3.3 GB で 16 GB 機なので、
2 台 + 単発プロセスの ~10 GB は paging を招き、それが timings に乗る。
`fresh` をフェーズごとに測り、**2 つの中央値の差 0.090 秒**が「2 フェーズは同じ計測」の根拠。

| | import list | 環境が映すもの | 立てた時点 |
|---|---|---|---|
| **P** | 移動前 | 移動**前**の olean | `setup-clone.sh move` の前 |
| **Q** | 移動後 | `lake build` **後**の olean | 移動のビルドの後 |

#### (a) W0 — 常駐サーバは自分が import した olean の書き換えを生き延びる【実測】

**生き延びた。** `lake build` の後も P は動いており、**ビルドが書き換えたモジュール**への要求に
**0.151 秒**で答えた。**ここまでが実測。**

**なぜ生き延びたかは推論で、実測ではない。** 有力なのは
「`importModules (leakEnv := true)` が必要なページを既に全部 fault 済みなので、
その後どこも読み直さず、in-place の truncate があっても気づかない」という説明。
(`lean` は `-o <path>.olean` で直接書くように見えるので、「rename で置き換わるから
古い inode が生きている」という筋は**おそらく機構ではない** — 当初ここにそう書いたが誤り。)
**この違いは効く**: 生存が「全部常駐済み」に依存するなら、遅延 fault するサーバ
(より大きな環境 / メモリ圧 / 一部しか触っていないモジュール) は **SIGBUS を食う可能性がある。未検証。**

疑いが無いのは帰結のほう: **長寿命サーバは派手に失敗しない。** 次項がその中身。

#### (b) W1 — 編集前の環境は L3-1 のラウンド 2 を誤答する【実測】

**予測どおり成立。** 差は 4 バイトで、正体は参照の所有者列:

```
InformationTheory.Shannon.Huffman.huffmanLength_optimal
  サーバ P の答え : ...Huffman.Length     :: ...Huffman.huffmanLength
  正解 (REFERENCE): ...Huffman.LengthCore :: ...Huffman.huffmanLength
```

参照側モジュールの IR が **10,263 B 対 正解 10,267 B** (`Length` は `LengthCore` より
4 バイト短い)。**常駐サーバは L3-1 が直すために存在するその古い組を返した。**
`Extract.lean:1352` が所有者を環境から引く (`env.getModuleIdxFor?`) ことの帰結で、
**「自分の olean が変わっていない」から「編集前の環境で正しく導ける」は導けない。**

#### (c) W2/W3 — 組み上がったサイトのバイト比較【実測】

判定オラクルは段階 5e/5f と同じ「フルビルドとの byte 一致」。**439 ページ**は
サイト全体 (モジュールページ 433 = `InformationTheory/` 配下 432 + `InformationTheory.html`、
+ `navbar.html` + `references.html` + `tactics.html` + `declaration-data.bmp` +
`name-map.json` + `references.bib`)。**段階 5e の「433」はモジュールページのみの数で、
矛盾ではない。**

| 変種 | 結果 |
|---|---|
| `fresh` (現行) | **439/439 byte 一致** |
| `pre` (ラウンド 2 を P に) | **1 ページ誤り** — `StrongForm.html`、参照側ページ。`--l3-1 off` と同じ壊れ方 |
| `post` (ラウンド 2 を Q に) | **439/439 byte 一致** |
| `postall` (全ラウンドを Q に) | **439/439 byte 一致** |

#### (d) W4 — 予測は当たったが、当たった変種が違った【実測】

| 変種 | 常駐が担う範囲 | 中央値 | [min–max] | うち extract |
|---|---|---:|---:|---:|
| `fresh` | なし | **7.850** | [7.765–7.901] | 5.872 |
| `pre` | ラウンド 2、編集前環境 | 5.284 | [5.245–5.320] | 3.335 |
| `fresh2` | なし (フェーズ 2 の対照) | **7.940** | [7.846–8.228] | 5.976 |
| `post` | ラウンド 2、ビルド後環境 | 5.302 | [5.253–5.317] | 3.352 |
| `postall` | **全ラウンド**、ビルド後環境 | **2.682** | [2.579–2.690] | 0.718 |

サーバ起動 (warm): **P 3.998 秒 / Q 3.486 秒**。
**段階 6 の 6.335 秒とは条件が違う** (あちらは元のツリー、こちらは再ビルド直後の複製)。
比較しない。

run 1 回ごとにサーバを立てる設計は、起動を run の中で払う:

| | パイプライン | + 起動 | `fresh2` 7.940 に対して |
|---|---:|---:|---|
| `post` | 5.302 | **8.788** | **0.85 秒の損** |
| `postall` | 2.682 | **6.168** | **1.77 秒の得 (−22%)** |

**分かれ目の理由。** 現行は**ラウンドごとに**環境ロードを払う (2 ラウンドで抽出 5.872 秒)。
ラウンド 2 だけ任せるとロード 1 回 (≈2.5 秒) は消えるが起動 3.486 秒が増えて損。
**両ラウンドを任せると 2 回分のロードが同じ 1 回の起動で消えて得** (抽出 5.872 → 0.718 秒)。
**損得の分岐点は 4 ラウンドではなく 2 ラウンド** — warm な olean では 1 ラウンドのロードが
2.5〜3 秒、起動が 3.5 秒なので。

#### (e) W5 — 到達可能な数字

- **5.06 秒には到達できない。** 前提の `pre` が誤答だから。**`pre` は 5.284 秒に着地した**ので、
  外挿の**時計は近かった**が**答えが違った**。同じ速さで正しさが違う、という形。
- **6.168 秒は到達可能で正しい** — `lake build` 後にサーバを 1 台立て、全ラウンドを任せる。
- **2.682 秒は起動を払わない場合だけ** = ビルドを挟まずに何度もパイプラインを回す用途
  (再レンダリング・問い合わせ) で、編集ループではない。

#### 判断

**段階 6 の枠組みを 3 点直した。**

1. **常駐サーバは自分が import した olean を書き換えるビルドをまたいで使ってはいけない。**
   死ぬからではなく (死なない)、**自信のある誤答をするから**。
2. **常駐の単位はセッションではなくパイプライン 1 回**で、抽出ラウンドが 2 回以上あるときだけ得。
3. `--serve-from` の既定を **2 → 1** に変えた。安全性はラウンド番号ではなく
   **サーバの olean 世代**が決めるので、編集前サーバならラウンド 2 も安全ではない。

**直した文書**: `approach.md` §7 段階 6 の行 / §8「常駐プロセスの寿命と安全性」(決着) /
§8「プレビューモードを持つか」/ `stage5h/README.md` の「rounds 2+ … serves them correctly」
(否定を本文に明記) と H5 の外挿表。

---

### 段階 6d — プレビューは常駐の受け皿になるか (2026-08-10)

段階 6 の結論「the real fit is a long-lived preview mode」が段階 6a の W1 と両立するかを
確かめる節。**プロセスを 1 つも起動せず、時計も使わない** — 問いは「どのバイトが存在するか」で
あって速さではないので、段階 6a が作った木のバイト比較だけで答える。
判断基準は計測前に commit。生ログ `benchmarks/results/stage6d-preview.txt`。

#### (a) U1 — 編集前サーバが「新しくて正しい」ページを出せるか【実測】

**出せない。集合は空。** P に L3-1 のラウンド 2 のモジュールを 1 件聞いた結果:

| | |
|---|---|
| 前回のビルドが持っていたものと byte 一致 | **する** |
| 事後の正解と一致 | **しない** |
| よって「正しく、かつ新しい」もの | **0 件** |

**編集前の環境は編集前の olean の関数**なので、そこから導けるものは前回すでに導かれている。
**プレビューの仕事は編集について何か言うこと**なので、常駐はその高速化にはならない。

#### (b) U2 — 1 回の編集の影響範囲【実測】

| | |
|---|---:|
| 編集が届いたモジュールページ | **433 中 6 = 1.39%** (変化 5 + 追加 1) |
| パイプラインの再生成集合 | **6 — 同一集合** |
| 変化した全域成果物 | 4 (`declaration-data.bmp` / `name-map.json` / `navbar.html` / `tactics.html`) |
| ディスク上で既に正しかったモジュールページ | **427** |

**全域成果物 4 件は L3-3 が毎回作り直す**ので、プレビューの意味では stale ではない。
モジュールページと混ぜると「プレビューが間違える割合」が 1.39% → 2.28% に膨らむので分けて数える。

#### (c) U3/U4 — 「未更新」表示の精度【実測】

- **stale は 1.39%** で、集合は**名指せる**。だから表示は**ページ単位**。
  サイト全体のバナーは**正しい 427 ページを無駄に警告する**。
- **ビルド完了前に 6 のうち 5 (83%) を正確に名指せる** — 台帳 (olean のハッシュ) だけで、
  Lean は要らない。残る 1 は L3-1 の参照側で、**原理的に事前には分からない**:
  自分の olean が変わらないからこそ stale なので、どんなハッシュも漏らさない。
- 保守的に補うなら「変更モジュールの宣言を IR で名指しているモジュールも印を付ける」
  = 参照側集合 (この変更で 4 モジュール)。IR にあるので Lean 不要。

#### 判断

**§8 のプレビューの選択肢が 3 つから 2 つに減った。** 常駐は落ちた。
残るのは (1) 構文ベースの近似 (未計測、コストは elaboration 無しの Lean パーサ) と
(2) 前回のページ + 未更新表示 (数字が付いた)。**(2) には数字があり (1) には無い。**

**直した文書**: `approach.md` §8「プレビューモードを持つか」/ §7 の段階 6 の総括。

---

### 段階 6b — revision をページのバイトから外す (2026-08-10)

§8 の「rev をページのバイトに埋めるか」を実装可能な形にして測った節。
判断基準は計測前に commit。生ログ `benchmarks/results/stage6b-revless.{txt,jsonl}` (12 run)。

**計測条件。** Darwin 25.6.0 arm64 / Apple M1 / 16 GB / `lean4:v4.31.0`。
複製の移動後 (433 モジュール) の状態。**IR は両契約で共用したので測定ループ内で Lean は動かない。**
1 契約 6 run を交互配置、run 1 破棄、以下は中央値。

| 契約 | `--source-url` に渡すもの |
|---|---|
| **A** (現行) | `https://github.com/FujiHaruka/information-theory/blob/<40 hex>` |
| **B** (提案) | `{{SOURCE_URL}}` (定数トークン) |

#### (a) 実装コストはゼロだった

`--source-url` は render.ts が IR 由来のパスと連結するだけの**前置文字列** (`render.ts:1250`)
なので、「バイトにプレースホルダを置く」は**その引数に定数を渡すこと**でしかない。
同時に台帳側も片付く — `renderKey` は渡された文字列をそのまま持つ (`ledger.ts:203-208`) ので、
**トークンにすれば rev は鍵から自動的に消える**。**変わるのは出力契約で、コードではない。**

#### (b) V0 — 空の再生成集合は「全部描く」を意味していた【実測】

`render.ts` は `--only` が無いと全モジュールを描く (`render.ts:1381`、実測 **433 ページ**)。
`incremental.sh` は再生成集合から `--only` を組んでいたので、**集合が空だと 0 個渡って
433 ページ全部が再生成されていた。** rev がバイトにある限りコミットは必ず `all` を立てるので、
**この経路には誰も来ていなかった。契約 B は毎回ここに来る。**
修正: 集合が空ならレンダラを呼ばない。**修正前なら V1 は「433 ページ」と読めて、
この段階は「何も買えない」ように見えていた。**

#### (c) V1〜V6【実測】

| # | 予測 | 結果 |
|---|---|---|
| V1 | 0 ページ | **成立** — B は **0** ページ、A は **433** ページ |
| V2 | rev が 0 回 | **成立** — B で **0** 回 (A は **4,993** 回) |
| V3 | 合計 ≲0.6 秒 | **成立** — 合計 **1.6227 → 0.6441 秒 (−60.3%)**、レンダリング段 **0.9017 → 0.0256 秒** |
| V4 | 逆置換で byte 一致 | **成立** — 439 ファイル、差 0 |
| V5 | ≈ −290 KB | **方向は成立、見積もりは外れた: −399,440 B (−1.27%)** |
| V6 | ≲0.3 秒 | **成立 — 0.1893 秒** (439 ファイル) |

**4,992 ではなく 4,993。** 前の数字は 432 モジュールの木で取ったもので、
こちらは移動後の 433 モジュールなのでページごとの nav リンクが 1 本多い。**両方正しい。**

**V5 の算術を直す。** 予測は 38% 外していた。実際の内訳:
前置 `https://…/blob/` + 40 hex = **94 B**、トークン `{{SOURCE_URL}}` = **14 B**、
差 **80 B** × **4,993 回** = **399,440 B** — 実測値とちょうど一致。

#### (d) 何が残るか

段別中央値 (`stage6b-revless.jsonl`、runs 2-6):

| 段 | A | B | B の合計に対する比 |
|---|---:|---:|---:|
| detect | 0.1105 | 0.1083 | 16.8% |
| rounds (extract / ownership / merge) | 0.0382 | 0.0365 | 5.7% |
| prune | 0.0278 | 0.0276 | 4.3% |
| **global (L3-3)** | 0.3136 | **0.2903** | **45.1%** |
| impact | 0.1630 | 0.1566 | 24.3% |
| **render** | **0.9017** | **0.0256** (skip) | 4.0% |
| **合計** | **1.6227** | **0.6441** | |

**残りの最大項は `global.ts` の 45.1%**、次が `impact.ts` の 24.3% (合わせて 69%)、
`detect` がさらに 17%。**当初ここに「B の 0.64 秒はほぼ全域成果物」と書いたが過大なので訂正した。**

**パイプラインでの L3-3 の費用は 0.29 秒で、増分 7 が測った 0.139 秒ではない** —
`incremental.sh` は `global.ts` を **2 回**呼ぶ (`build` と、L3-2 の入力になる `delta`)。
増分 7 は「増分化で消せるのは IR 全読み 0.119 秒だけ、それは実行の 1.5〜2.6%」を根拠に
不要と決めたが、**契約 B では同じ仕事が実行の 45%**。**別の分母での判断だったので、
継承ではなくやり直すべき。**

#### (e) 代償 (実測または特定済み)

1. **HTML が単体で完結しない** — 注入が走らなければリンク **4,993 本**が死ぬ。§8 が言う取引そのもの。
2. **置換で注入する版は 0.87 秒が丸ごと消えるわけではない** — **0.1893 秒**かかる
   (再生成 0.9017 秒より 4.8 倍安いが無料ではない)。**丸ごと消えるのは JS 注入版だけ。**
3. **`jump-src.js` が実行時に `.gh_link a` の `href` を読む**ので注入が先に走る必要がある — **未検証**
   (この段階はバイトと時間を測っただけでブラウザ挙動は測っていない)。
4. **`coverage.ts:512-513` の rev 正規化**が「rev だけの差」を前提にしているので、
   受け入れオラクルは注入後の木で回す必要がある — **未検証**。

#### 判断

**取り分は測れた。決定は製品判断として残す** — 「単体完結を失ってよいか」。
**直した文書**: `approach.md` §8 の当該行 (未計測 → 計測済み・決定は未) /
§5.5 の「常に `all`」に前提を付け、空集合の落とし穴を追記。

---

### 段階 6c — L3-1 の一般性と、ラウンド数の上限 (2026-08-10)

段階 5e が L3-1 を検証したのは**移動 1 例**だけで、動いた名前は「署名に出る普通の定理」
だった。残っていた 2 つ (他の宣言種別 / 3 ラウンド以上) を閉じる節。
判断基準は計測前に commit。判定は段階 5e/5f と同じ**フルビルドとの byte 一致** (439 ファイル)。
生ログ `benchmarks/results/stage6c-l31-generality.txt` (+ `-abbrev-strong.txt`)。

**計測条件。** Darwin 25.6.0 arm64 / Apple M1 / 16 GB / `lean4:v4.31.0`。
**シナリオごとに複製をリセットし、base を再抽出して台帳が不動点であることを確認してから**
移動を当てた (再ビルドした olean が前と byte 一致するとは仮定しない — 段階 5e が 1 run 失った罠)。

#### (a) N0 — 3 種のうち 2 種にしか witness が無い【実測】

| 種別 | witness |
|---|---|
| `instance` | **有** (9 instance を定義する `…Superposition.TimeShare`) |
| `@[simp]` | **無 — この対象では検証不能** |
| `abbrev` | **有** (`…Fano`、さらに強い witness で再走) |

**instance について census が対象全体で言っていること** — 候補行の 2 列目が結果そのもの:

```
…Superposition.TimeShare      9 instance(s), 0 of them named in a signature elsewhere, 2 referring module(s)
…OuterBoundUV.Region          7 instance(s), 0 of them named in a signature elsewhere, 8 referring module(s)
…OuterBoundUV.Bridge          5 instance(s), 0 of them named in a signature elsewhere, 9 referring module(s)
…TimeSharingConverse.Bridge   4 instance(s), 0 of them named in a signature elsewhere, 1 referring module(s)
…TimeBandLimiting.Operator    4 instance(s), 0 of them named in a signature elsewhere, 7 referring module(s)
```

**この対象で、他モジュールの印字署名に名前が出る instance は 1 つも無い。**
instance は typeclass 解決が見つけるもので、pretty printer は印字しない。
だから `refs` に載らず、**L3-1 は instance の移動を原理的に見られない。**

**`@[simp]` に witness が無い理由も同じ構造**:

| | |
|---|---:|
| 自パッケージ 432 ファイル中の `@[…simp…]` 出現 | **76** |
| うち宣言名まで解決できたもの | **73** |
| IR の宣言に接尾辞照合で対応付けられたもの | **70** |
| **うち他モジュールの印字署名に名前が出るもの** | **0** |

**`@[simp]` 補題は証明側の事実で、印字署名は型**だから。**「通った」ではなく「検証不能」**と
記録する — この対象は L3-1 が simp を扱えるかを示せない。示せたのは「この対象には L3-1 が
見る形で simp 補題を参照しているものが無い」こと。
なお **census の名前照合は最初まちがっていた** (ソース名は namespace 相対、IR 名は完全修飾)。
接尾辞照合に直して 70 件を同定し、**それでも参照は 0** — 結論はバグに依っていなかった
(直す前に別途確認した)。

#### (b) N1/N2 — instance の移動を救っているのは L3-1 ではない【実測】

| | `--l3-1 on` | `--l3-1 off` |
|---|---|---|
| 結果 | **439/439 byte 一致** | **439/439 byte 一致** |
| ラウンド / 変更 / stale | 1 / 4 / **0** | 1 / 4 / 0 |

L3-1 は**名前を 46 個 lost・46 個 gained と見ながら stale 0** を報告した。L2 が 4 モジュール
(参照側 2 つを含む) を全部拾っていた。**では何が救ったか** — instance 一覧は**ページのバイトに
無い**。`render.ts:1142` は空の `<details class="instances-for-list">` を出し、ブラウザが
`declarations/declaration-data.bmp` から埋める。そのファイルは `global.ts` が**毎回**
作り直す (L3-3)。**3 層でいちばん安い層が、L3-1 の仕事だと思われていたものをやっていた。**

#### (c) N4 — abbrev は L3-1 が必須で、5e より難しいケースだった【実測】

| | `--l3-1 on` | `--l3-1 off` |
|---|---|---|
| 結果 | **439/439 byte 一致** | **誤り — 3 ページが相違** |
| ラウンド / 変更 / stale / ページ | 2 / 2 / **3** / 5 | 1 / 2 / 0 / 2 |
| 誤ったページ | — | `Fano/Core.html` `Fano/DPI.html` `Fano/Measure.html` |

段階 5e の移動は stale **1**・誤り **1 ページ**だった。こちらは **3 と 3** で、
**L3-1 が作られた元のケースより強く効かせて、なお完全一致。**
**reducible な定義でも `(モジュール, 名前)` の鍵は壊れない** — delaborator は展開形ではなく
abbrev 自身の名前を印字する (少なくともここでは)。

#### (d) N5/N6 — 2 ラウンドは構造的な上限【実測】

ラウンド 2 に到達したのは abbrev だけで、その ownership 報告は **`lostNames 0, gainedNames 0`**。
これは構造的な議論が予測するとおり: **ラウンド 2 以降のモジュールは olean が動いていない**
(だから L2 が見逃した) ので、**宣言集合も動かない** (集合はそのモジュール自身の olean の
`constNames` から来る)。だから `lost`/`gained` に何も足さず、ループは止まる。
**偶然ではなく理由があって止まっている。**

**それでも `--max-rounds` は残す。** 議論には穴が 1 つある — 宣言集合は `constNames` を
`isBlackListed` で濾したもので、この述語は環境を参照する (`isAuxRecursor env` /
`isNoConfusion env` / `getStructureInfo? env parent` / `findDeclarationRanges?`、
`Extract.lean:168-189`)。**それを反転させる変更があればラウンド 3 が出る。**
今回のどの変更も反転させなかったが、それは定理ではない。

#### (e) 強い witness での追試 — 影響範囲 30 倍でも完全一致【実測】

初回の census は `abbrev` の候補を**並べ替えていなかった**ので、最初に来たもの
(`InformationTheory.Fano`、abbrev 1 個・参照側 2 モジュール) を選んでいた。直して、
この対象で最も強い witness (`…BroadcastChannel.Basic`、abbrev 4 個・**参照側 33 モジュール**)
で再走した。生ログ `benchmarks/results/stage6c-l31-generality-abbrev-strong.txt`。

| | `--l3-1 on` | `--l3-1 off` |
|---|---|---|
| 結果 | **439/439 byte 一致** | **誤り — 30 ページが相違** |
| ラウンド / 変更 / stale / globalStale / ページ | 2 / 5 / **30** / 1 / 36 | 1 / 5 / 0 / 1 / 6 |
| ラウンド 2 の ownership | `lostNames 0, gainedNames 0` | — |

**L3-1 が拾った 30 モジュールはすべて意味があった** — 無しにするとちょうどその 30 ページが誤る。
段階 5e の「stale 1 / 誤り 1 ページ」に対し **30 倍の影響範囲でなお完全一致**。
弱い witness に無かった 2 点: **L3-2 も発火した** (`globalStale 1`) ので L3-1 単独ではなく
**L3-1 と L3-2 の合成**を測っており、それでも byte 一致。そして **2 ラウンド上限が
ラウンド 2 に 30 モジュール入っても崩れない** — `lost 0 / gained 0` は小さいラウンドの
偶然ではなく、上限が規模に依存するならここで反例が出ていた。

#### 判断

**L3-1 の一般性は「移動する名前が印字署名に出るかどうか」で決まる**と確定した。
出る種別 (定理・`abbrev`) には必須、出ない種別 (`instance`、この対象の `@[simp]`) には無関係で、
**そちらを救っているのは L3-3 (全域成果物を毎回作り直す)**。
**3 層の分担は偶然ではなく、いちばん安い層が最も広い網だった。**

**直した文書**: `approach.md` §5.5 の L3-1 (種別ごとの効き方 + ラウンド上限とその穴) と
L3-3 (正しさにも効いていること)。

---

### CI 軸 — `ubuntu-latest` での `cache get` 直後の常駐率 (2026-08-10)

§3 が「**未着手の検証項目**。ここが埋まるまで CI 軸の床は確定しない」と書いていた 1 点。
macOS で出た否定 (常駐 4.7–20.2%) は**不利な側**なので移送できない、と §3 自身が予告していた。
**予告どおり移送できなかった。**

**計測条件。** `ubuntu-latest` (Linux 6.17.0-1020-azure x86_64 / ubuntu24 image 20260720.247.2)、
**2 コア / MemTotal 8,128,888 kB = 7.75 GiB / page size 4096**。
litedoc4 (private) の Actions で回し、対象は public な `FujiHaruka/information-theory` を
fresh に checkout。**計測対象リポジトリには何も書いていない。** 道具は
`benchmarks/tools/olean-residency.c` (mincore) を無改変で使用。
生ログ `benchmarks/results/ci-residency-ubuntu-latest.{txt,jsonl.gz}` (51,278 レコード)、
run `31395165118`。**n=1。**

#### (a) 数字【実測】

| | |
|---|---:|
| olean ファイル数 (`cache get` 前は **0**) | **51,278** |
| 見かけのバイト | 5,576,027,709 = 5.19 GiB |
| **常駐 (ファイルバイト)** | **3,480,558,540 = 62.42%** |
| 常駐 (ページ) | 873,492 / 1,399,761 = 62.40% |
| page cache バイト (常駐ページ × 4096) | 3,577,823,232 |

| /proc/meminfo | `cache get` 前 | 後 |
|---|---:|---:|
| MemFree | 4,651,300 kB | **469,972 kB** |
| MemAvailable | 6,914,548 kB | 7,152,096 kB |
| Cached | 2,411,392 kB | **6,500,556 kB** |

**+20 秒 / +40 秒のスナップショットは 1 回目と byte 一致** (`cmp` で確認)。
常駐は動かず、MemAvailable は 6.8 GiB のままなので**追い出す圧力が無かった**。

#### (b) macOS との対比

| | 常駐 | `importModules` |
|---|---:|---|
| macOS / M1 / 16 GiB【実測】 | **4.7–20.2%** | 14.96 / 15.66 秒 (warm 2.5 秒の 5.9 倍) |
| `ubuntu-latest` / 2 コア / 7.75 GiB【実測】 | **62.4%** | **未計測** |

**「展開直後だから warm」は macOS では偽、Linux では 62.4% の意味で真に近い。**
§3 の「不利な側で出た否定は移送できない」という留保は正しかった。

#### (c) この計測が言っていないこと

1. **`importModules` を測っていない。常駐率は代理指標。** macOS の数字には import 時間が
   付いていたが、こちらには無い — CI で抽出器をビルドするのは意図的に scope 外にした
   (安い問いの答えを高い問いに人質に取らせないため)。
2. **「どの 62.4%」かを言っていない。** cold な import が読むのは約 2.59 GB (macOS、実測)。
   常駐集合がその hot set を覆っているならほぼ warm、均等に散っているなら import が必要と
   するものの **約 37% は依然 fault する**。**mincore では抽出器の read set 無しに区別できない。**
3. **n=1**、ランナー 1 種類、image 1 種類。
4. **62.4% は特性値ではなく容量律速の可能性がある。** 7.75 GiB のランナー (既に 2.3 GiB が
   cached) に 5.19 GiB の olean を置き、cache は 6.20 GiB で終わっている = ほぼ上限。
   **より大きなランナーならもっと載る**かもしれず、そうなると**ランナーの大きさが定数ではなく
   変数**になる。

#### (d) 道具の欠陥を 1 件直した

`summarize-residency.ts` の "page-cache bytes" のラベルが **16384 をハードコード**していた
(macOS のページ長)。**値は正しい** — 探査側 (`olean-residency.c:148`) が実行時のページ長を
使っており、生 JSONL から再計算した implied page size は 4096 だった。**ラベルだけが
4 KiB ページのホストで 4 倍の誤りに読めた**ので直した。

#### 判断

**CI 軸の床は「確定した」ではなく「macOS の否定が Linux に移らないことが確定した」。**
残るのは上の (1) と (2) — **常駐率ではなく `importModules` を Linux で測る**必要がある。
それには CI で抽出器をビルドする段が要る。

**直した文書**: `approach.md` §3 の「未着手の検証項目」/ §8 の cold の行 /
`benchmarks/tools/summarize-residency.ts` のラベル。

### 段階 7 準備 — byte 再現率 63.6% の欠落を原因別に割った (2026-08-10)

段階 7 は「完成品の値を出す」— §6 の主要な数字から**出力範囲の差**という留保を外すこと。
着手前に**欠落 36.4 ポイントの内訳**を割った。**計画側の予測は外れていた** (下記「判断」)。

#### (a) 63.6% の正確な定義

**この数字は LCS でも差分バイト数でもない。** `experiments/stage4c/coverage.ts` は:

- 母数 = doc-gen4 が `.lake/build/doc/InformationTheory/` に出した **348 ページ / 22,028,728 B**
- 両サイドを doc-gen4 の構造 (`Output/Template.lean` / `Module.lean`) に沿って **17 領域**に切り、
  **領域はページを隙間なく覆う** (タイル検査に失敗すると report を出さずに throw する)
- 領域を key で突き合わせ、**byte 完全一致した領域のバイトだけ**を分子に積む。
  **1 文字違えば領域まるごと 0 点** (全か無か)

**63.587% = 14,007,303 / 22,028,728。ページ単位の一致は 34 / 348。**
無改変での再実行で stage4c の全項目を再現した【実測】(Deno 2.7.14、1.34 秒。
入力 IR は `/private/tmp/lean-doc-relay/w6a/base-ir`、ページは同 `base-pages`)。
ログ → `benchmarks/results/stage7-coverage-baseline.txt`。

#### (b) 欠落 8,021,425 B の原因別内訳【全部実測、未分類 0 B】

| 原因 | バイト | 母数比 | 領域数 | 直す側 |
|---|---:|---:|---:|---|
| **`splitWhitespaces`** (decl_header 6,982,052 / equations 269,624) | **7,251,676** | **32.919%** | 973 | **抽出 (schema 3)** |
| docstring の autolink 索引 | 401,987 | 1.825% | 208 | 抽出 or 全域索引 |
| メンバの binder / field docstring / `isDirect` | 135,078 | 0.613% | 33 | 抽出 |
| CommonMark 未実装 | 120,261 | 0.546% | 47 | 生成のみ |
| rev (設定値、gh_link 100,504 / nav_gh 8,268) | 108,772 | 0.494% | 540 | **修復不能** |
| `div.attributes` | 3,651 | 0.017% | 89 | 抽出 |
| 検索ページ・静的資産 | **0** | **0%** | — | **母数外** |
| **未分類** | **0** | **0%** | 0 | — |

14,007,303 + 8,021,425 = 22,028,728 で**完全に閉じる**。
内訳ツール → `benchmarks/tools/coverage-breakdown.ts` (セグメンタは無改変)、
ログ → `benchmarks/results/stage7-coverage-breakdown.txt`。

**到達上限は 99.506%。** doc-gen4 の参照ツリー自身が **2 つの git revision** を持つ
(305 ページが `573793b…`、43 ページが `5e38aec…`) ので、どんな `--source-url` を渡しても
348 ページ全部は一致しない。497/497 の gh_link 差が rev のみであることは検証済み【実測】。

ページ単位で「あと何を直せば一致するか」【実測】: 既に一致 34 /
**`splitWhitespaces` のみ 114** / autolink + ws 63 / CommonMark + ws 16 / rev のみ 14 /
autolink のみ 13 / CommonMark のみ 5。
→ **`splitWhitespaces` だけ直すと 34 → 148 ページ。CommonMark だけなら 34 → 39。**

#### (c) `splitWhitespaces` の 32.9% を「ページの 1/3 が再現不能」と読まない

実際に食い違っている文字は**全領域あわせて 1,976 文字**しかない【実測】。全か無かの帰属で
9 KB の `div.decl_header` が `\n` 1 つで丸ごと 0 点になっているだけ。
**この 1,976 文字を直すと 63.6% → 96.5% に跳ねる**ので、
**数字が動いたときに「レンダラが良くなった」と読ませない。出所を必ず併記する。**

原因は `experiments/stage4b/Extract.lean:304` の `wsTrim` が `(front, total, back)` を
**計算して捨てている**こと。「front/back で十分」は本ログの過去の主張だが**未検証**で、
**32.9 ポイントがこの 1 つの仮定に乗っている** — schema 3 の実装がその検証になる。

#### (d) autolink の欠落は自パッケージの問題ではなかった【実測】

生成側に出せていない 281 アンカー / 183 種のあて先: **`Mathlib` 246** / `Init` 23 /
`InformationTheory` 12。過剰にリンクした領域は **0**、数が同じで href だけ違う領域は 7。
→ 要るのは自パッケージの写像ではなく**依存クロージャ全体の名前 → モジュール索引**で、
**§8 の「依存パッケージの写像をどう配布するか」と同じ未決事項**。
`InformationTheory` 分 12 件だけは `global.ts` の写像で今すぐ閉じられる。

#### (e) `{{SOURCE_URL}}` は coverage の会計を静かに壊す【実測】

`coverage.ts:512` の revless 正規化は `/blob/[0-9a-f]{40}/` にしかマッチしないので、
段階 6b のプレースホルダを入れたページを採点すると:

| | 再現率 | 未分類 |
|---|---:|---:|
| rev 埋め込み | **63.6%** | **0 B / 0 領域** |
| `{{SOURCE_URL}}` | **60.5%** | **793,930 B / 3,825 領域** |

ログ → `benchmarks/results/stage7-coverage-sourceurl-token.txt`。
**「未分類 = 0」は会計が説明であることの唯一の検査**で、それが 3.6% に跳ねる。
**−3.1 ポイントはレンダラと無関係。採点は必ず rev 埋め込みのページに対して行う。**
段階 6b が「未検証」として残していた項目の 1 つがこれで閉じた。

**rev の 2 つの数を混ぜない** — 「rev がページ内 4,992〜4,993 か所に出る」(再生成の話) と
「rev 由来の不一致が 108,772 B / 540 領域」(採点の話) は別の数。後者が小さいのは
参照ツリーの多数派 rev を渡していて **43 モジュール分だけ**が食い違うため。

#### (f) 母数の外に「未知」が 1 つある

doc-gen4 の参照ツリーは **348 / 432 モジュール**しかない (フルビルド 42% 打ち切りの残骸)。
生成側は 432 ページ 29,671,173 B を書いているので、**7,642,445 B = 出力の 25.8% には
正解が存在しない**【実測】。doc-gen4 の抽出は 2.5 秒/モジュールなので
**84 モジュールの追補は約 210 秒【外挿】**でオラクルが 25.8% 広がる。

#### 判断

**計画側が外れていた。** `approach.md` §6.2 が挙げていた未実装リスト
(CommonMark・docstring の autolink 索引・`div.attributes`・メンバの binder・検索ページ・
静的資産) は、**欠落 36.4 ポイントのうち 3.0 ポイントしか説明していない**。
残る 32.9 ポイントはリストに載っていない `splitWhitespaces` で、しかも
**検索ページと静的資産は母数の外なので実装しても 0 ポイント**。
同じ文に**別の分母の話が混ざっていた**。

**優先順を「生成側を埋める」→「抽出側を埋める」に入れ替えた。**

**直した文書**: `approach.md` §6.2 (未実装リストと 63.6% の定義)、
本ログ冒頭の状態表 (段階 5・6 が完了済みなのに古いままだった)。

### 段階 7a — IR schema 3 で `splitWhitespaces` を閉じた (2026-08-10)

準備で割った最大項 (32.919 pt) を閉じにいった。**「front/back 幅で十分」という未検証の仮定に
32.9 ポイントが乗っていた**ので、実装がその仮定の検証を兼ねる。
実装 → `experiments/stage7a/`、条件と全数字 → `benchmarks/results/stage7a-summary.txt`。

#### (a) 何が起きていたか

doc-gen4 の `renderTagged` (`RenderedCode.lean:249`) は `.const` タグの本体を
`splitWhitespaces` (`Output/Base.lean:281`) に渡し、**前後の空白を切り落として
プレーンな空白として出し直す** (`"".pushn ' ' n`)。**幅は同じで文字が違う**。
実際の差分で確認した — 食い違う code unit は**全部が `\n` → `' '`** で、
**常に const アンカーのすぐ外側**にある。

`wsTrim` (`Extract.lean:304`) は `(front, total, back)` を既に計算しており、schema 2 が
**狭めた span だけを書いていた**ので、消費側はどの空白がタグの内側だったか判別できなかった。

#### (b) front/back で閉じた — 仮定が実測になった【実測】

**32.919 pt の原因が 0 B になり、再現バイトの増加が 7,251,676 = 原因が持っていた額と一致。
他のどの原因も 1 バイトも動いていない。**

| | schema 2 | schema 3 |
|---|---:|---:|
| 再現できたバイト | 14,007,303 | **21,258,979** |
| 再現率 | 63.587% | **96.506%** (+32.919 pt) |
| byte 完全一致ページ | 34 / 348 | **148 / 348** |
| `decl_header` 領域 | 2,705 / 3,477 | **3,477 / 3,477** |
| `equations` 領域 | 452 / 653 | **653 / 653** |

残る未再現は **769,749 B = 3.494%** (autolink 401,987 / メンバ 135,078 / CommonMark 120,261 /
rev 108,772 / attributes 3,651)。**うち rev の 0.494 pt は到達不能。**
ログ → `benchmarks/results/stage7a-coverage.txt`。
**オーケストレータが無改変の `coverage.ts` で独立に再実行して一致を確認した**
(21,258,979 + 769,749 = 22,028,728、byte 完全一致 148 ページ)。

**+32.9 pt を「レンダラが良くなった」と読まないこと。** 2 つのページ木は**長さがどこでも同一**で、
違っていたのは **2,725 UTF-16 code unit**、その全部が「改行がスペースに書き換わっていた」もの。
**その 2,725 文字が 7,251,676 B を 0 点から満点に動かした。**
領域単位の全か無かの帰属がそういう効き方をするというだけ。

#### (c) 推測では閉じられないことも測った【実測】

`experiments/stage7a/check-spans.ts`: 記録された 54,742 の run のうち非極大は **0**。一方で
**135,316** 箇所の const span の隣に「run が無いのに空白がある」側面があり、そこには
全 run を書き換える発見的手法が**誤って書き換える 8,403 単位**に対し**正しく直る 3,417 単位**
しかない。**`--ws-heuristic` の「407 件直して 100 件壊す」の定量形。**
ログ → `benchmarks/results/stage7a-checkspans.txt`。

#### (d) schema 3 の代償【実測、同一セッションの schema 2 対照、run 1 を捨てた runs 2-6 の median】

| | schema 2 | schema 3 | 差 |
|---|---:|---:|---:|
| IR バイト | 15,867,059 | **15,984,731** | +117,672 (+0.74%) |
| `writeIR` | 0.5300 s | **0.5927 s** | +0.0627 (+11.8%) |
| 抽出 `total` | 13.6362 s | **13.7084 s** | +0.0722 (+0.53%) |
| 読み出し (プロセス内) | 0.0981 s | **0.0981 s** | ±0 |
| 読み出し (別プロセス) | 0.1020 s | **0.1031 s** | +0.0011 |
| HTML 生成 | 0.7711 s | **0.7967 s** | +0.0256 (+3.3%) |

**`writeIR` の run レンジは重ならない** (0.5275–0.5514 対 0.5883–0.6064) ので差は本物。
**`total` のレンジは重なる** — **この計測が支持するのは「`writeIR` が 0.06 秒増えた」であって
「合計が増えた」ではない。** cold 側 (壁 29.90 s / CPU 15.60 s / page fault 117,095) は
`stage7a-tag-cold-*` に分けてあり **warm と混ぜない**。

**既存の 13.71 s / 0.549 s / 0.100 s は別セッションの値**なので比較に使わず、
schema 2 を同じセッションで測り直して対照にした。

#### (e) 落とし穴の再検査 — 3 点とも PASS

`benchmarks/results/stage7a-pitfall-recheck.txt`。(a) `--out` JSONL が `--pages` の有無で
byte 一致 (23,353,796 B / 4,560 行)、(b) `## decl_header path` の stats ブロックが文字一致、
(c) 2 回の `--pages` が `diff -r` で一致 (432 ファイル / 29,671,173 B —
**schema 2 の木と同じ総バイト**で、変わったのが文字だけであることの独立な確認)。

#### 判断

**閉じた。** 未検証だった「front/back で十分」は実測になった。
**新しい段階は新ディレクトリ** (`experiments/stage7a/`) に置き、stage4b / stage4c は
byte 無改変のまま — 既にコミット済みの schema 2 の列を再現可能に保つため。

**残る 3.494% のうち到達不能は rev の 0.494 pt。** doc-gen4 の参照ツリー自身が
**部分再ビルド**で、ソースリンク 3,285 本が rev `573793b2…`、540 本が `5e38aec…` を持つ。
どんなレンダラ構成でも両方には一致しないので、**これはオラクル側の天井であって
litedoc4 の欠陥ではない。残る 3.494% を作業リストとして読まないこと。**

### CI 軸 — Linux で `importModules` そのものを測った / **「CI の床は 13 秒」は成り立たなかった** (2026-08-10)

前回 (常駐率 62.4%) が残した 2 つ — 「時間を測っていない」「どの 62.4% か分からない」 — を
両方閉じた。run `31399169335` (39 分 18 秒、ユーザー承認済み)。
workflow → `.github/workflows/ci-import-modules.yml`、
生ログ → `benchmarks/results/ci-importmodules-linux-*`。

**測定点を動かした。** 実運用の CI は `cache get` → `lake build` → doc 生成の順に走るので、
doc 生成が見る page cache は **ビルド後**の状態であって `cache get` 直後ではない。
`cache get` 直後を測るのは**どのパイプラインも通らない瞬間**を測ることだった。両方取ってある。

#### (a) 条件

`ubuntu-latest` / **2 コア / 7.75 GiB (MemTotal 8,128,884 kB)** / page size 4096 /
kernel 6.17.0-1020-azure / image ubuntu24 20260720.247.2。
対象は `FujiHaruka/information-theory` を **`ca4fd931` に pin** (コミット済みの
`it-modules.txt` の 432 モジュールと一致させるため)。抽出器は stage4b を CI 上でビルド
(`import Lean` だけなので Mathlib を引かず、page cache をほとんど汚さない)。
**cold は `sudo drop_caches` で作った** — 同一機材・同一セッションで cold と warm を挟むため。

#### (b) 結果【全部実測】

| run | `importModules` | `analyze` | `total` | major fault | 壁時計 |
|---|---:|---:|---:|---:|---:|
| **ビルド直後** (CI が実際に払う) | **2.610 s** | 13.530 s | 16.172 s | **26** | 16.44 s |
| warm ×4 | 2.626 / 2.576 / 2.608 / 2.687 | 11.39 s | 13.80–14.03 s | **0** | 14.2 s |
| **cold ×2** (`drop_caches` 後) | **63.369 / 70.628 s** | 11.39 s | 74.78 / 82.01 s | **11,949** | 75.7 / 82.9 s |

macOS / M1 / 16 GiB の同一ワークロード対照: **warm 2.49–2.59 s / cold 14.96・15.66 s。**

**1. ビルドと同じジョブで doc を作るなら、CI は cold ではない。** `importModules` は
**2.610 秒 = macOS の warm と同じ**で、major fault は **26 回**。
**「CI の床は 13 秒」は、この配置では成り立たない。**

**2. 逆にビルドと分けると、Linux の cold は macOS の cold より桁が違って重い。**
**63.4〜70.6 秒 = macOS cold の 4.2〜4.7 倍。** major fault は逆に**少ない**
(11,949 対 macOS 121,386) ので、**1 fault あたりが高い** — 5.3 ms/fault。
ランナーのディスクがネットワーク越しであることと整合するが、**内訳は測っていない**。

**3. 「cold の追加コストは `importModules` に集中する」は Linux で実測になった。**
`analyze` は warm 11.39 s / cold 11.39 s / cold 11.36 s で**動かない**。
§6.3 がこの前提を「**未検証**なので大きい側に外れうる」としていた項が閉じた
(向きの確認。macOS の外挿値そのものが検証されたわけではない)。

#### (c) 「どの 62.4% か」は答えが出た — ただし問いの立て方が違っていた

| 時点 | olean 常駐 | 常駐バイト | 抽出器の major fault |
|---|---:|---:|---:|
| `cache get` 直後 | **60.4%** | 3.46 GB | (測っていない) |
| **`lake build` 後** | **40.3%** | **2.25 GB** | **26** |

**常駐率は下がったのに fault はほぼ 0。** つまり**割合ではなく「どのページか」が効いている** —
`lake build` は import が要るまさにそのページを読むので、生き残った 40.3% が hot set を
覆っている (cold の import が読むのは約 2.59 GB、macOS 実測)。
**`cache get` 直後の常駐率は、この問いに対しては代理指標ですらなかった。**

**60.4% は前回の 62.4% を再現した (n=2)。** 容量律速の疑いも再現 — `cache get` 直後の
`Cached` は 6,462,768 kB で MemTotal 8,128,884 kB に対しほぼ上限、`MemFree` は 504,916 kB。

#### (d) 説明できていないもの

**ビルド直後の run だけ `analyze` が 13.530 秒**で、warm の 11.39 秒より **+2.14 秒**遅い。
`importModules` ではないので上の結論は動かないが、**原因は分かっていない。**
有力な仮説は「`lake build` の dirty page の writeback が 2 コアとディスクを奪っている」だが
**未検証**。peak RSS は 3 run とも 4.0 GB で差が無く、minor fault もほぼ同じ (82,143 / 82,223)。

#### 判断

**CI 軸の床は「配置で決まる」。** 数字が 2 つあり、間は 24〜27 倍:

| CI の組み方 | `importModules` |
|---|---:|
| doc 生成を `lake build` と**同じジョブ**に置く | **2.61 s** |
| ビルド済み olean を別ジョブで復元して doc だけ作る | **63.4〜70.6 s** |

→ **設計上の推奨が 1 つ出た**: doc 生成はビルドと同じジョブに置く。ただしこれは
**先読み (§3) の価値を下げない** — 分けざるを得ない配置では cold の罰が macOS より重い。
**Linux での先読みは依然として未測定** (`MADV_WILLNEED` の同期・非同期が違うため
macOS の方式選択は移送できない)。**n=2、ランナー 1 種類。**

**直した文書**: `approach.md` §3 の CI の行 / §6.3 の cold の注 / §7 の CI 行 / §8 の cold の行。

### 段階 7b — 抽出側の未実装 3 つ (属性・instance 索引・メンバ) を埋めた (2026-08-11)

§6.1 の 13.71 秒に付いていた留保「**完成品の値として引用しない** (属性収集・instance 索引・
構造体メンバの binder が未実装)」を外すのが目的。3 つとも「doc-gen4 がやっていてこちらが
やっていない仕事」なので**時間は増える側**で、増える幅を測るのが半分。
実装 → `experiments/stage7b/` (IR schema 4)、数字の SoT → `benchmarks/results/stage7b-summary.txt`。

#### (a) 再現率 96.506% → **97.099%** (+0.593 pt)【実測】

| | schema 3 | schema 4 |
|---|---:|---:|
| 再現バイト | 21,258,979 | **21,389,581** / 22,028,728 |
| byte 完全一致ページ | 148 | **170** / 348 |
| `attributes` 領域 | 0 / 89 | **89 / 89** |
| `members` 領域 | 3 / 36 | **34 / 36** |

**オーケストレータが無改変の `coverage.ts` で独立に再実行して一致を確認**
(21,389,581 + 639,147 = 22,028,728、170 ページ)。

#### (b) 目標 97.136% に 0.037 pt 届かなかった — **目標値のほうが誤りだった**

残った 8,127 B は members の 2 領域。**両側から `<a …>` を剥がすとどちらも byte 一致する**
(アンカー数 24/25 と 13/16)。つまり原因は field docstring 中の **autolink 索引**で、
**メンバのデータ欠落は 0 B**。準備段階で立てた目標 +0.613 pt が、
**members 領域に紛れていた autolink 分を二重に数えていた。**
ログ → `benchmarks/results/stage7b-member-check.txt` §B。

**これはオラクルの原因ラベルの限界でもある** — `coverage.ts` は members 領域の不一致を
すべて「メンバのデータ欠落」に入れる (規則を書いた時点では他に原因が無かった)。
**オラクルは凍結でよいが、members が残ったときは毎回この分岐を書き添えないと
原因表が静かに嘘になる。**

したがって真の残りは **autolink 410,114 B (1.862%)** / CommonMark 120,261 B (0.546%) /
**rev 108,772 B (0.494%、到達不能)** = 639,147 B (2.901%)。**到達可能な残りは 2.407 pt。**

#### (c) 3 つの実装コスト【実測、phase timer、runs 2-6 の median】

| | 秒 | 規模 | IR バイト |
|---|---:|---|---:|
| 属性 `getAllAttributes` | **0.0199** | 4,750 呼び出し / 145 宣言が保持 | +3,188 |
| instance 索引 | **0.0020** | 91 instance | +10,532 |
| メンバ追加 (`getFieldOrigin` + doc) | **0.0014** | 156 field (うち inherited 4) | +23,181 |
| 合計 | **0.0233** (抽出の 0.17%) | | **+36,901** (+0.23%) |

メンバの binder は `ppSignature` が既に整形していたものを段階 7a が捨てていただけなので、
**増えるのは時間ではなく IR バイト**。

**ablation では `total` を分離できていない** — `--no-attrs` / `--no-inst-index` /
`--no-member-extra` を各 6 回、5 構成をラウンド内で回り持ちしても、差 0.14 秒は
run 間ばらつき ±0.15 秒と同じ大きさで実行順とも交絡する。
**分離できているのは phase timer とバイトだけ。**

#### (d) §6.1 用の数字【実測、中列と右列が同一セッションの対】

| | schema 3 (段階 7a・別セッション) | **schema 4** | schema 3 対照 (同セッション) |
|---|---:|---:|---:|
| 抽出 `total` | 13.7084 s | **14.0819 s** | 14.0467 s |
| うち `writeIR` | 0.5927 s | **0.6225 s** | 0.6072 s |
| IR バイト | 15,984,731 | **16,021,632** | 15,984,731 |
| 読み出し (プロセス内) | 0.0981 s | **0.0991 s** | 0.0982 s |
| HTML 生成 | 0.7967 s | **0.8044 s** | 0.8002 s |

**13.7084 → 14.0819 を「3 つの追加のコスト」と読まないこと。** 同一セッションの対照が
14.0467 なので**正味は +0.035 秒**で、残り 0.34 秒は**セッション間の差**。
段階 7a のセッションはこのセッションより約 2.5% 速かった。

#### (e) instance 索引は再現率を動かさなかった (想定どおり) — ただし出力は検証した

増えた 130,602 B は attributes 3,651 + members 126,951 で**ぴったり**説明でき、
instance 索引の寄与は **0 B**。ページのバイトに出ないものを「書いた」で済ませないため
`instance-check.ts` で doc-gen4 の `declaration-data.bmp` と**両方向**照合した:
doc-gen4 がページを出した 56 instance について **56/56 一致、食い違い 0**
(残る 35 は doc-gen4 が出していない 84 モジュール側なので対象外)。

#### (f) 落とし穴の検査

**割合検算**: 6 マーカー (`structure_field_info` 153 / うち `decl_args` 48 /
`structure_field_doc` 114 / direct 149 / inherited 4 / inherited+id 0) が
doc-gen4 と**ページごとに**全一致。**母数**: タイル検査が通り領域合計 = 22,028,728 B
(`div.attributes` の末尾 `\n` は要素の一部として出力)。
**`--pages` 3 点検査 3/3 PASS**、加えて `--tagged-code` を外した IR が
stage7a と byte 一致することも確認。

#### 判断

**§6.1 の留保は外せる。** 3 つとも実装済みで、うち 2 つ (属性・メンバ) は doc-gen4 の出力と
**byte 単位で一致**、instance 索引は別経路で 56/56 一致。
**完成出力での warm 抽出は 14.08 秒、HTML 生成 0.80 秒、合計 14.89 秒**【実測】。

**直した文書**: `approach.md` §5.4 (IR) / §6.1 (表と留保) / §6.2 (再現率) / §6.3 (合計) /
§6.4 (倍率) / §7。

### CI 軸 — Linux での先読み / **`importModules` は −76%、壁時計は −4%** (2026-08-11)

§8 に残っていた最後の打ち手 (a)。run `31403402584` (成功)。
workflow → `.github/workflows/ci-import-prefetch.yml`、
生ログ → `benchmarks/results/ci-prefetch-linux-*`。
変種の定義は `benchmarks/tools/measure-prefetch.sh` から写した (evictor だけ
macOS の `olean-evict` → Linux の `sudo drop_caches` に差し替え)。
先読み集合は **`.lean` ヘッダから作った import closure、17,199 ファイル / 4.14 GB**
(スクリプトが動いたので blind fallback ではない)。

#### (a) 結果【全部実測、各変種 n=2】

| 変種 | `importModules` | major fault | 先読み自体 | **壁時計** |
|---|---:|---:|---:|---:|
| baseline (cold) | **20.390 / 21.144 s** | 30,424 | — | **46.27 / 44.80 s** |
| overlapped madvise | **5.190 / 5.193 s** | 4,945 | 19.44 s | 44.45 / 43.99 s |
| overlapped read | **4.796 / 4.865 s** | 4,301 / 4,372 | 20.96 / 20.67 s | 45.30 / 45.41 s |
| sequential madvise | 5.093 / 5.179 s | 4,945 | 18.84 s | 44.75 / 44.27 s |

**`importModules` は −75〜77%** (macOS の −49.0% より大きい)。**しかし壁時計は −2〜4%。**

#### (b) なぜ壁時計が動かないか — 帯域律速で、集合が大きすぎる

抽出器のタイマーの**外側** (= `lake env` 起動 + バイナリのページイン) を引くと出る:

| | 壁時計 | 抽出器 `total` | **外側** |
|---|---:|---:|---:|
| baseline | 46.27 s | 39.89 s | **6.38 s** |
| overlapped madvise | 44.45 s | 22.17 s | **22.28 s** |
| sequential madvise | 44.75 s | 22.46 s | **22.29 s** |

**`importModules` で削った 15.6 秒が、そのまま外側で戻ってくる。**
**sequential と overlapped が同じ**なので、これはスケジューリングの競合ではなく**帯域**の問題:

- ランナーのディスクは **197〜220 MB/s**【実測】。macOS の **2,395 MB/s** の **1/11**。
- **先読み集合は 4.14 GB だが、import が実際に必要とするのは約 2.5 GB。**
  差の 1.6 GB は、この帯域では **約 8 秒の純粋な無駄**。
- **正直さ検査がそれを裏付けた**: 先読みだけして何もしない状態での常駐は
  **2.48〜2.52 GB (42.7〜43.4%)** — **読んだ 4.14 GB のうち約 1.6 GB は追い出されている。**
  「速く終わったのは cache が捨てたから」という失敗モードを mincore が実際に捕まえた。

→ **macOS で closure 集合を選べたのは、そこでは先読みが 1.75 秒でほぼ無料だったから。**
帯域が 1/11 の機材では**同じ集合が主コストになる**。Linux で効かせるなら、
closure ではなく**実際に読まれる集合まで絞る**必要がある (macOS 実測で closure は
必要バイトの 98.80% を覆うが、**逆に必要の 1.65 倍を読んでいる**)。

#### (c) **前回の cold と 3 倍食い違った — ランナー実体の差**

| | `importModules` (cold) | major fault | fault あたり |
|---|---:|---:|---:|
| run `31399169335` | **63.369 / 70.628 s** | 11,949 | 5.3 ms / 188 KB |
| run `31403402584` | **20.390 / 21.144 s** | 30,424 | 0.67 ms / 82 KB |

**同じ `ubuntu-latest`・同じワークロード・同じ抽出器で 3.3 倍違う。** fault 数が 2.5 倍多く
1 件あたりが 8 倍速いので、**readahead の窓とストレージ実体が違う**とみられる (未検証)。
→ **`ubuntu-latest` の cold は 20〜71 秒のレンジで扱う。** 各実体では n=2 だが**実体は n=2**。
**前の節で「63〜71 秒」と書いたのは 1 実体の値だった** — approach.md を直した。

#### 判断

**先読みは Linux では「時間を移すだけ」で、壁時計としては成立しない** (この集合・この帯域では)。
**否定されたのは打ち手そのものではなく「closure 集合をそのまま読む」形。**
`importModules` が −76% になることは、集合を絞れば効く余地があることを示している
(**未検証**)。ただし §3 の第一の打ち手 (doc 生成をビルドと同じジョブに置く = 2.61 秒) が
効く配置では、そもそも先読みは要らない。

**直した文書**: `approach.md` §3 (CI の行・cold のレンジ・先読みの節) / §6.3 / §8。

### 段階 7c — autolink 索引と CommonMark / **到達上限 99.506% に到達** (2026-08-11)

生成側に残っていた到達可能分 2.407 pt を全部閉じた。実装 → `experiments/stage7c/`、
数字の SoT → `benchmarks/results/stage7c-summary.txt`。

#### (a) 再現率 97.099% → **99.506%**【実測】

| | stage7b (同一セッション対照) | **stage7c** |
|---|---:|---:|
| 再現できたバイト | 21,389,581 | **21,919,956** |
| 再現率 | 97.099% | **99.506%** = 到達上限 |
| byte 完全一致ページ | 170 / 348 | **304 / 348** |
| 生成側が出したバイト | 22,002,135 | **22,028,728** (doc-gen4 と同数) |

| 原因 | 7b | **7c** | 領域 |
|---|---:|---:|---:|
| autolink 索引 | 410,114 | **0** | 210 → 0 |
| CommonMark | 120,261 | **0** | 47 → 0 |
| rev (到達不能) | 108,772 | 108,772 | 540 |

**残りは rev だけ。** オーケストレータが無改変の `coverage.ts` で独立に再実行して一致を確認
(21,919,956 + 108,772 = 22,028,728、304 ページ、切れなかったページ 0)。

**ラベルの中身を確認した** (段階 7b の教訓): `rev-check.ts` で 540 領域を全部読み、
`/blob/<40hex>/` を含む href 3,825 本のうち不一致 540 本は**全部 rev だけの差**、
それ以外 0。ページ単位でも 348/348 が「rev を伏せれば一致」。

#### (b) **オラクルの盲点を 1 つ見つけた**

`coverage.ts` は領域を宣言**名**で対応付けるので、**同じ集合の並べ替えを一致と数える。**
rev を伏せても byte 一致しないページが 2 枚ある (`Polymatroid.Basic` /
`EntropyPower.Ext`、doc-gen4 の**不安定 qsort** が同着をどう並べるか)。
**stage7b のページでも同じ 2 枚**なので 7c が作った差ではないが、
**99.506% はこの 2 ページを含んだ数字**である。

#### (c) autolink 索引の実サイズ — §5.3 の 647 倍の幅が埋まった【実測】

| | |
|---|---|
| 実サイズ | **8,508,273 B** / gzip **1,332,647 B** (JSON 形式なら 9,990,592 B) |
| 作る時間 | **0.4152 s** |
| レンダラが読む時間 | **0.0764 s** (プロセス起動ごと) |

宣言 258,760 / モジュール 5,775。`kind` を落としモジュール名を 1 回だけ書く形。
§5.3 の「**下限 53 KB / 上限 34.3 MB**」に対し、**下限の 160 倍・上限の 1/4** に落ちた。

**前提「上流の公開ドキュメントサイトから同じファイルが手に入る」は未検証。**
ローカルの doc-gen4 出力から作ったのは計測のためで、配布方式を決めたわけではない (→ §8)。

索引以外に 2 つ必要だった: (a) `isNameLit` が `'` `!` `?` を落としていた
(Lean の `isIdRest` は含む、9 アンカー)、(b) モジュール内フォールバックの探索順が IR 順
だった (正しくは宣言範囲順、6 アンカー)。**過剰アンカーは 4 → 0**、署名側は不変
(`--out` JSONL が stage7b と byte 一致)。

#### (d) CommonMark は規則 7 つ【ablation で実測】

| 外した規則 | 落ちるバイト | 壊れる領域 |
|---|---:|---:|
| lazy continuation | 49,663 | 23 |
| 項目境界 (マーカーは項目を閉じる) | 46,595 | 18 |
| loose list (`<li><p>`) | 43,822 | 11 |
| indented code block | 39,712 | 13 |
| nested list | 25,116 | 7 |
| 順序付きが段落を割れるのは `1.` だけ | 5,663 | 2 |
| `_` の強調 (md4c flanking) | 532 | 1 |

合計が 47 領域にならないのは 1 領域が複数規則を要求するため。**外部依存ゼロ。**
tables / strikethrough / task list / hard break は**この対象に 0 個**なので実装していない。

#### (e) §6.6 の「+0.36 秒」は **外れた。向きごと。**【実測】

同一セッション・同一 IR・warm、11 ラウンド round-robin の runs 2-11 median:

| | stage7b | **stage7c** | 索引なし |
|---|---:|---:|---:|
| docstring 描画 | 0.1809 | **0.1616 (0.89×)** | 0.1595 |
| linkIndex 読み込み | — | 0.0764 | — |
| 合計 | 0.8204 | **0.8852** | 0.8063 |

予測は「docstring 項が 3 倍 → +0.36 秒 → 合計 1.2 秒」。実測は
**docstring 項が −10.7%** (本物の CommonMark のほうが素朴な近似より**速い**。範囲は完全に分離)、
**合計 +0.0648 秒で、その増分は全部 autolink 索引の読み込み。CommonMark 単体の寄与は負。**
**§6.3 の結論は動かない** (warm 合計 14.89 → 14.97 秒)。
**代償は時間よりメモリ: peak RSS 222 → 288 MB (+29.6%)。**

#### (f) 検査

タイル検査 22,028,728 B / 切れなかったページ 0、`--pages` 3 点検査 **3/3 PASS**、
`decl_header` JSONL が stage7b と byte 一致 (23,353,796 B / 4,560 行)、
check-spans OK、member-check 6/6 で未帰属 0 B、instance-check 56/56 双方向。

#### 判断

**「出力範囲の差」という留保が消えた。** 到達可能なものは全部 doc-gen4 と byte 一致し、
残る 0.494% は**オラクル側の天井** (参照ツリーが 2 revision を持つ)。
§6 の倍率に付いていた「作業範囲が違う」という注意書きは、**これで不要になった**
(残るのは静的資産が母数の外にあることだけ)。

**直した文書**: `approach.md` §5.3 (写像の実サイズ) / §6.2 / §6.3 / §6.4 (倍率) /
§6.6 (外挿の否定) / §7 / §8 (写像の配布)。

### CI 軸 — 先読みの集合を絞る / **前節の結論を訂正する** (2026-08-11)

前節 (run `31403402584`) は「Linux では先読みは壁時計を動かさない (−4%)」と結論した。
**その結論は 1 つのランナー実体でしか成り立たない。** run `31408219046` は**遅い側の実体**に
当たり、逆の答えが出た。生ログ → `benchmarks/results/ci-prefetch-narrow-*`。

#### (a) 遅い実体では先読みは効く【実測、n=2】

`ubuntu-latest` / 2 コア / 7.75 GiB。**この実体の baseline は前々節 (run `31399169335`) と
同じ fault 数 11,949 で再現**しているので、2 種類の実体の片方である。

| 変種 | `importModules` | major fault | 先読み | **壁時計** |
|---|---:|---:|---:|---:|
| baseline (cold) | 63.899 / 64.127 s | 11,949 | — | **97.30 / 100.96 s** |
| **closure** (17,199 ファイル / 4.14 GB) | **19.409 / 12.439 s** | **2,132** | 21.7 s | **74.62 / 55.96 s** |
| 残留集合 (10,439 ファイル / 2.53 GB) | 49.908 / 29.329 s | 4,245 | 13.7 / 14.4 s | 104.40 / 89.69 s |

**closure 先読みは壁時計を −23〜−44% 動かした。** 前節の速い実体では −4% だった。

| 実体 | baseline の `importModules` | closure 先読みの壁時計 |
|---|---:|---|
| 速い側 (fault 30,424) | 20.4 / 21.1 s | **−4%** = 効かない |
| 遅い側 (fault 11,949) | 63.9 / 64.1 s | **−23〜−44%** = 効く |

**筋が通る**: 遅い実体は 1 fault が 5.3 ms かかるので、直列な demand fault を 16 並列の
読みに置き換える価値が大きい。速い実体は 0.67 ms/fault で、**closure 4.14 GB に対し
import が要るのは約 2.5 GB** なので、余計に読む 1.6 GB が節約を食い潰す。
→ **「Linux では成立しない」は誤り。「速い実体では成立しない」が正しい。**
**ランナー実体は選べない**ので、先読みは**期待値としては効くが保証はない**打ち手になる。

#### (b) 「集合を絞れば効く」は **この実験では検証できていない**

絞った集合のほうが**遅かった** (import 49.9/29.3 対 closure の 19.4/12.4、fault 4,245 対 2,132)。
ただし**これは仮説の否定にならない** — **導出方法に欠陥があった。**

集合は「cold な import を走らせた**後に**まだ常駐していたファイル」から作った。
これは**読まれた集合ではなく、読まれて残った集合**で、import の途中で追い出されたぶんを
落としている。実際 fault が closure の 2 倍出ているので、**必要なものを取りこぼしている。**
**名前を `oracle` としたのは誤りで、正しくは `residual`。**

→ **「実際に読まれる集合まで絞れば効くか」は依然として未検証。**
正しく測るには読み取りを追跡する必要がある (`fanotify` / eBPF、あるいは import 中に
定期スナップショットを取って和を取る)。**やっていない。**

#### (c) この計測の弱さを明記する

- **n=2 で、変種内のばらつきが大きい** (closure 19.4 対 12.4 秒 = 1.6 倍、
  residual 49.9 対 29.3 秒)。**レンジで読むべきで、中央値を代表値にしてはいけない。**
- **VM の `drop_caches` はゲストのページキャッシュしか落とさない。** ホスト側のキャッシュは
  残りうるので、後の run ほど暖まっている可能性がある (baseline は速くならなかったので
  強い証拠は無い。**未検証**)。
- 導出用の 1 回目の cold は **89.237 秒**で、この実体の cold レンジは **63.9〜89.2 秒**。
  **`ubuntu-latest` 全体では 20.4〜89.2 秒。**

#### 判断

**先読みは「効くこともある」打ち手に格下げ。** 第一の打ち手は依然として**配置**
(doc 生成を `lake build` と同じジョブに置く = 2.61 秒、→ §3)。そちらが使える配置なら
先読みの成否は問題にならない。**残る未検証は「読まれる集合まで正しく絞ったらどうか」。**

**直した文書**: `approach.md` §3 の先読みの節 (前節の結論を訂正) / §8。

### 段階 7d — 意味解析 10.58 秒を割り、並列化した (2026-08-11)

§8 の「次に効くのはここしかない」という項目。実装 → `experiments/stage7d/`、
数字の SoT → `benchmarks/results/stage7d-summary.txt`。

#### (a) `ppUs` の内訳【実測、同一セッション warm、rounds 2-7 median】

`analyze` 10.7475 秒 / `ppUs + eqUs` 10.4811 秒に対して:

| 段 | 秒 | pp+eq 比 |
|---|---:|---:|
| **delaboration** (`Expr` → `Syntax`) | **5.7966** | **55.3%** |
| formatting (`Syntax` → `Format`) | 1.6144 | 15.4% |
| タグ付け (位置つきスパン収集) | 1.3262 | 12.7% |
| parenthesize | 0.9160 | 8.7% |
| layout (`Format` → `String`) | 0.3028 | 2.9% |
| equation 生成 | 0.2934 | 2.8% |
| sanitize | 0.0840 | 0.8% |
| **計上できた合計** | **10.3441** | **98.7%** |

**「pretty printer のコスト」の正体はレイアウト (2.9%) ではなく delaborate。**
delaboration : formatting = **3.02 : 1**。

**分布はほぼ一様なので、外れ値を潰す筋は無い**: 上位 1% (48 件) の取り分は **6.97%**、
上位 10% で 32.9%、中央値 1,597 µs / 平均 2,246 µs。種別の取り分も件数比でほぼ決まる
(theorem は件数 78.8% / 時間 81.5%)。**blacklisted 4,074 件は analyze の 0.21%** で取り分なし。
**analyze の 0.318 秒 (3.0%) はどのカウンタにも入っていない** — そこは割れていない。

#### (b) 並列化は成立した — **3.23 倍、IR は byte 一致**【実測】

`--jobs N` (ストライド分割 + 候補添字で書き戻し)。warm median:

| | stage7c 対照 | `j1` | **`j4`** | `j8` | `j16` |
|---|---:|---:|---:|---:|---:|
| `analyze` | 10.7855 | 10.7890 | **3.3365 (3.23×)** | 3.5316 | 3.4569 |
| `total` | 14.0166 | 14.2137 | **6.5037 (2.19×)** | 6.6658 | 6.6654 |

**出力は 1 バイトも変わっていない。** `--jobs` 1/2/4/8/16 の IR が stage7c と `diff -r` で無差、
独立 2 回の `--jobs 8` 同士も無差、`--jobs 8` の IR から描いたページは
**99.506% / 完全一致 304/348** で stage7c と同一。
**オーケストレータが独立に確認した**: `/usr/bin/diff -r` で `ir-7c == ir-j4 == ir-j8 == ir-j1`、
追試で analyze 10.81〜10.99 (j1) → 3.22〜3.24 (j4)、warm の total 13.29 → 5.77 秒。

**成立する理由**: 宣言ごとに新しい `Core.State` を作って捨てるので宣言間に可変状態が無く、
`leakEnv := true` が環境を persistent に印付けするので RC 競合も無い。

**頭打ちは 4 スレッド付近**で、理由は 2 つとも実測: M1 の性能コアが 4 本、かつ
**instructions +1.0% に対し cycles +66%** (仕事が増えたのではなくストール)。

**`--jobs > 1` の `ppUs` は壁時計でも CPU 時間でもない** (スレッドごと経過時間の和。
j16 で 48.5 秒 > user+sys 28.2 秒)。**壁時計はフェーズの `us` だけで読む。**

#### (c) equation を切る筋は採らない【実測】

`--jobs 1` では −1.18 秒 (8.6%) だが、**並列化すると −0.23 秒 (3.4%) に縮む**。
代償は再現率 **99.506% → 97.107% (−2.400 pt)**、完全一致ページ **304 → 133**。**割に合わない。**

#### (d) §6.1 を更新できる形【実測】

| | stage7c 対照 | `--jobs 1` | **`--jobs 4`** |
|---|---:|---:|---:|
| 環境ロード | 2.5825 | 2.7797 | **2.5255** |
| 意味解析 | **10.7855** | 10.7890 | **3.3365** |
| 書き込み | 0.6133 | 0.6208 | **0.6131** |
| **合計** | **14.0166** | 14.2137 | **6.5037** |
| 意味解析の割合 | 77.0% | 75.9% | **51.3%** |

**「75% は本質的なコスト」は `--jobs 1` の話だった。** `--jobs 4` では 51.3% に落ち、
**最大項は `importModules` (38.8%) に移る。**
なお **CPU 時間としての仕事は減っていない** (instructions +1.0%) — 減ったのは壁時計。

#### (e) 分離できなかったもの

- **計装コスト**: 計装ありのほうが 0.04〜0.21 秒**速い**という結果で、範囲がほぼ完全に重なる。
  **この分解能 (±0.2 秒) では見えない**、という意味であって「0 秒」ではない。
- **`--jobs 4` 対 `8` 対 `16`**: j4 3.19-3.64 と j8 3.44-3.67 が重なる。
  言えるのは「2 → 4 で伸び、4 を超えると伸びない」まで。
- **ラウンド群をまたいだ差**: 同じ構成が別のラウンド群で 0.126 秒ずれる。

#### 判断

**§8 の「意味解析を減らせるか」は「壁時計としては減らせる、CPU 時間としては減らない」で決着。**
筋は**並列化**であって、算法の改良でも生成物の削減でもなかった。
**そしてこれは §5.1 の副次効果 (メモリ律速が消える) の取り分**でもある —
litedoc4 は 1 環境の中で 4 スレッドに割るので追加メモリがほぼ要らない。
doc-gen4 の並列はプロセス並列で 1 プロセスあたり 2〜3 GB の環境を持つため、
8 並列で RSS 21.7 GB に達してスワップする (実測、§5.1)。**同じ「4 並列」でも意味が違う。**

**倍率の扱いに注意**: §6.4(a) の 76 倍は**どちらも 1 スレッドの比**。
`--jobs 4` の 6.50 秒を分子 1,076 秒と割るのは**条件を揃えていない**ので、
そうしたければ doc-gen4 側の並列壁時計 (メモリ律速がかかる) が要る。**測っていない。**

**直した文書**: `approach.md` §5.1 / §6.1 / §6.3 / §6.4 / §6.6 / §7 / §8。

---

### 段階 7g — 常駐を `--jobs` 前提で本配線し、600 要求まで回した (2026-08-11)

§8 に残っていた常駐の 2 項 (「本配線」と「6 要求は 600 要求について何も言わない」) を
1 つの実験で閉じた。実装 → `experiments/stage7g/`、数字の SoT →
`benchmarks/results/stage7g-{main,y0-gate,soak}.txt` と同名の `.jsonl`。

**なぜ配線し直したか。** 段階 6a の常駐は `stage4b/build/extract` (IR schema 3・`--jobs` なし) を
指していた。**つまり §8 の「常駐は決着」の数字は現行の抽出器で取られていない。**
7g は `serve-ctl.sh` / `extract-once.sh` / `incremental.sh` を stage7d の抽出器 (schema 4 +
`--jobs`) と `stage7d/render.ts` の上に載せ直し、常駐を **1 パイプライン 1 台・`lake build` の
後に起動・全ラウンドを委譲・`trap` で必ず停止**に自動化した。**起動と停止は計測する壁時計の
内側**に置いてある (それが「1 パイプライン 1 台」の正直なコスト)。
`stage5/*.ts` (ledger / ownership / merge-ir / global / impact / prune-pages) は**無改変で再利用**。

条件: Darwin 25.6.0 arm64 / Apple M1 / 16 GB、`leanprover/lean4:v4.31.0`。
対象は `/private/tmp/lean-doc-relay/clone` (計測対象の APFS clone、自パッケージを in-place で
再ビルド済み)。移動は段階 6a と同じ `Huffman.Length → Huffman.LengthCore`。
**すべて同一セッションの warm**、増分は 7 run × 4 変種を回り持ちで run 1 破棄、
フル抽出は 5 round × 4 変種を round 1 破棄。cold は測っていない。

#### (a) 門番 — 常駐 × 並列は 1 バイトも変えない【実測】

26 モジュール (432 の 17 個おき) を 5 構成で抽出し `/usr/bin/diff -r`:
`単発 j1 == 単発 j4 == serve j1 == serve j4 == 同一サーバへの独立 2 要求目`、**5 組すべて無差**。
抽出器が報告する 6 カウンタ (considered 505 / produced 265 / blacklisted 240 / equations 40 /
refOccurrences 8,445 / spans 22,972) も 5 構成で完全一致。
**抽出器の Lean 改修は要らなかった** — `serve` は `run reqCfg (some env)` を呼ぶだけなので
`cfg.jobs` がそのまま効く。サーバ起動は j1 3.514 秒 / j4 3.584 秒 で、
**`--jobs` は `importModules` に触らない**ことの確認にもなっている。

#### (b) 増分 2 ラウンドのケース【実測、7 run 中 6 の median】

**4 変種すべて、組み上がったサイトは REFERENCE と 439/439 byte 一致。**

| 変種 | 合計 | [min–max] | extract | サーバ起動 |
|---|---:|---:|---:|---:|
| `fresh-j1` | **7.968** | [7.928–8.151] | 5.967 | — |
| `fresh-j4` | **7.894** | [7.825–7.950] | 5.895 | — |
| `res-j1` | **6.668** | [6.585–7.109] | 0.731 | 3.705 |
| `res-j4` | **6.509** | [6.457–6.571] | 0.604 | 3.693 |

**常駐の取り分は `--jobs` でほぼ動かない**: −1.300 秒 (−16.3%、分母 `fresh-j1`) /
−1.385 秒 (−17.5%、分母 `fresh-j4`)。**予測どおりで、機構まで分離できた** —
このケースで再抽出するのは round 1 が 5 モジュール / round 2 が 1 モジュールなので
`analyze` は合計 **0.121 → 0.049 秒**しかなく、`--jobs 4` の取り分 (合計で −0.074 秒 = −0.9%)
はその差 0.072 秒でほぼ説明が付く。**効く場所が小さいだけで、並列化が効かないのではない。**
常駐が消しているのは**ラウンドごとの `importModules`** (1.819 + 1.766 = 3.585 秒) と、
extract 5.967 秒との差 2.38 秒 = プロセス起動 2 回分で、その代わりに起動 3.70 秒を 1 回払う。

#### (c) フル 432 モジュール再抽出【実測、5 round 中 4 の median】

IR は毎回 REFERENCE と byte 一致。

| | fresh | resident (起動抜き) | 差 | 起動込み |
|---|---:|---:|---:|---:|
| `j1` | 14.931 | 11.439 | **−3.491 秒 (−23.4%、分母 fresh-j1)** | 15.042 = **+0.7%** |
| `j4` | 7.507 | 4.257 | **−3.250 秒 (−43.3%、分母 fresh-j4)** | 8.066 = **+7.4%** |

**絶対値はほぼ同じ (≈3.3〜3.5 秒) で、相対値だけが倍近くになる。** 消えるのは
`importModules` (2.479 / 2.485 秒) とプロセス起動で、**どちらも並列化されない** —
`--jobs 4` は分母だけを縮めるので取り分の比率が上がる。
**起動を払うなら fresh より遅い** (+0.7% / +7.4%)。つまりフル抽出で常駐が得なのは
**ビルドを挟まずに何度も投げる用途だけ**で、これは段階 6a の 2.682 秒と同じ条件の話。
待機中のサーバ RSS は `ps` で **0.57〜0.62 GB**。

#### (d) ソーク 600 要求【実測、`--jobs 4`、432 モジュールを 1 回 import】

要求は 1〜3 モジュールをパッケージ内で巡回。**600/600 成功、非ゼロ応答 0。**

- **IR**: #1 / #300 / #600 に同一の 5 モジュール canary を投げて `/usr/bin/diff -r` で**無差**。
- **RSS**: 25 標本すべて **0.55 GB**、first→last **+0.0 MB (+0.00%)**。
- **速度**: 前 100 対 後 100 は **交絡している** — 巡回式 `start=(i*7) mod M, k=1+(i mod 3)` なので
  前後で聞くモジュールが違い、宣言数が時間を決める。そこで交絡のない対照を 2 つ取った:
  (1) **同一要求の再投入** (周期 432 なので `i` と `i+432` は同一集合) n=166 で
  中央値 **−0.0006 秒 (−1.9%)**、遅くなったのは 48/166 組。
  (2) **canary** #1 0.0740 → #300 0.0652 → #600 0.0657 秒 — **#1 が最も遅い**。

**閉じたのは「600 要求 / 約 1 分 / 同一 olean 世代」の範囲。**
ビルドをまたぐ寿命は (b) の (a) 節と同じく「使ってはいけない」のまま。

#### (e) 段階 6a の 7.940 / 6.168 秒と並べてよいか — **並べない**

抽出器が stage4b (schema 3) → stage7d (schema 4)、レンダラが `stage4c/render.ts` →
`stage7d/render.ts` に変わっており、`fresh` 同士でも同じ仕事ではない。
**7g の `fresh-j1` 7.968 秒が新しい基準。** 6a の `fresh2` 7.940 秒とたまたま近いが、
同一セッションで両方を測っていないので「schema 4 は速度中立」とは言えない。

#### (f) 訂正 — 常駐 1 台は 3.3 GB を占有しない

`experiments/stage6a/README.md` と本ログの段階 6a は「抽出器の peak RSS は 3.27〜3.37 GB
なので常駐 2 台 + 単発で ~10 GB」と書いている。**3.27〜3.37 GB は要求処理中のピーク**であって、
待機中の常駐は `ps` で **0.55〜0.62 GB**【7g、実測】。運用判断 (要求が重なればピークが乗るので
2 台同時は避ける) は覆らないが、**「常駐 1 台が常時 3.3 GB を占める」と読める書き方は正確でない。**
プレビュー常駐を検討するときに効く区別。

#### (g) 分離できなかったもの

- **`fresh-j1` 対 `fresh-j4` の合計**: [7.928–8.151] と [7.825–7.950] が重なる。
  **「`--jobs 4` は増分を速くしない」を合計だけで主張してはいけない** — 上の (b) は
  `analyze` 0.072 秒 = 合計差 0.074 秒という機構で立てている。
- **`res-j4` の `analyze` が fresh より 0.18 秒遅い** (3.395 対 3.215、フル抽出)。
  +7.4% の内訳のどこまでがこれかは分けていない。
- **常駐の損益分岐ラウンド数を `--jobs 4` で測っていない** (6a の「2 ラウンド」は j1 相当の話)。
- **計測ハーネスの往復**が `res-j4` 6.509 秒に ≈0.55 秒乗っている (ソークの python ドライバ
  では同じ往復が 0.005 秒)。差し引いた ≈5.96 秒は【外挿】であって主張には使っていない。

#### 判断

**§8 の常駐の項は閉じた。** 本配線は成立し (439/439 byte 一致)、寿命の内側では 600 要求に耐える。
**設計上の含意は 1 つ**: 常駐が消すのは `importModules` とプロセス起動という**並列化されない項**
なので、意味解析を速くするほど常駐の相対的な価値は上がる。§5.1 の 2 本の柱 (1 環境 + 長寿命) は
**独立ではなく、互いの取り分を増やす**関係にある。

**直した文書**: `approach.md` §5.1 / §7 / §8。

---

### 段階 7e-rev — rev 非埋め込みの「未検証」2 点を潰した (2026-08-11)

§8「revision をページのバイトに埋めるか」の行に残っていた **(未検証) 2 つ**を閉じにいった。
実装 → `experiments/stage7e-rev/`、数字の SoT → `benchmarks/results/stage7e-rev-summary.txt`
と `stage7e-rev-coverage-{rev,ph,inj}.txt`。

条件: 同一 IR (`w7d/ir-j4`)・同一 link-index (stage7c)・同一レンダラ (`stage7d/render.ts`、無改変)
から `--source-url` の引数だけを変えて 2 木を描き、`{{SOURCE_URL}}` 版の写しに段階 6b と同じ
置換注入を当てた。`experiments/stage4c/coverage.ts` は**1 行も変えていない**。同一セッション warm。

#### (a) オラクルは注入後なら成立する — **閉じた**【実測】

| ページ集合 | 再現率 | 未再現 | byte 完全一致ページ |
|---|---:|---:|---:|
| (i) 具体 rev | **99.5062%** | 108,772 B / 540 領域 | **304 / 348** |
| (ii) `{{SOURCE_URL}}` | 96.3959% | 793,930 B / 3,825 領域 | **0 / 348** |
| (ii-注入後) | **99.5062%** | 108,772 B / 540 領域 | **304 / 348** |

**(i) と (ii-注入後) は `/usr/bin/diff -r` で全木 byte 一致** (432 ファイル、differing / missing /
extra すべて 0。合計 29,740,469 B が一致)。**オーケストレータが独立に `/usr/bin/diff -r` を回して
exit 0 を確認し、再現バイト 21,919,956 / 未再現 108,772 が (i) と (ii-注入後) で同値であることも確認した。**

ずれるのは (ii) だけで **−3.1103 pt**。原因は `coverage.ts:512` の revless 正規化が
`/blob/[0-9a-f]{40}/` にしかマッチしないことで、**`gh_link` / `nav_gh` の再現率が「落ちる」のではなく
きっかり 0.0%** になる (3,477 領域中 1 つも一致しない)。(ii) の未再現 793,930 B / 3,825 領域は
段階 7 準備の baseline と完全一致し、−3.1 pt が**レンダラ非依存**であることの裏も取れた。

**注入コスト**: 段階 6b と同じ python3 実装、`cp -R` は時計の外、5 回で
**中央値 0.1552 秒 / 432 ファイル**。user+sys 0.09 秒に対し real 0.17〜0.20 秒で **I/O 律速**。
**段階 6b の 0.1893 秒とは並べない** — あちらは 439 ファイル (433 モジュール + 全域成果物) の木で、
同一セッションで測り直していないので 2 つの差は分離できていない。

#### (b) `jump-src.js` の順序制約 — **方式を分けないと答えが出ない問いだった**

`/Users/haruka/dev/lean-projects/.lake/packages/doc-gen4/static/jump-src.js` は全 10 行:
読むのは `#<declId>` 直下の `.gh_link a` の **`getAttribute("href")`** (`:5-7`) で、
`data-*` でも `a.href` プロパティでもないので**属性の生文字列**が返る。読むのは
**DOMContentLoaded ハンドラ内** (`:1`)、click 時ではなく、しかも **`?jump=src` 付き & hash 非空の
ときだけ** (`:3-4`)。`static/` 以下 15 ファイルを grep して `gh_link` に触るのは
`jump-src.js:6` と `style.css:790` (見た目) だけと確認した。

したがって順序制約は**注入方式で変わる**:

- **配信時に文字列置換で注入する版 — 制約は無い**【実測】。HTML が届く前に置換が終わっている。
  裏取りは全木 byte 一致 + 注入後の `getAttribute("href")` が具体 rev であること。
  **§8 の「注入が先に走らないといけない」はこの版には当てはまらなかった。**
- **JS 実行時注入版 — 制約はある**【コードからの言明。**ブラウザ未検証**】。正確には「注入が先」ではなく
  **「`.gh_link a[href]` の書き換え完了が、`jump-src.js:1` の DOMContentLoaded ハンドラの実行より前」**。
  parse 完了より前である必要は無い。満たし方は仕様上 2 通りあるが**どちらも実行していない**。
  壊れ方は「全ページ」ではなく **`?jump=src` のディープリンクだけ**。

**DOM シムで確かめたこと (ブラウザではない)**: `jump-src.js` のバイトそのものを実在の生成ページの
パース結果 (`jsr:@b-fuze/deno-dom`) に対して評価し、`location.replace` に渡る値を捕まえた。
(ii) では `{{SOURCE_URL}}/…#L44-L52` がそのまま渡り、ページ相対に解決すると
`…/InformationTheory/%7B%7BSOURCE_URL%7D%7D/…` = **同一オリジンの 404**。
**死に方が「無反応」ではなく「404」だと分かった**のがこの節の副産物。

**死ぬリンクの本数**【実測】: 432 モジュールの木で `{{SOURCE_URL}}` の出現 **4,992**
(gh_link 4,560 + gh_nav_link 432)、432/432 ファイルに含まれる。木サイズ差 399,360 B = 80 B × 4,992。
§8 の 4,993 は段階 6b の移動後 433 モジュール (4,560 + 433) の数字で、**矛盾ではない**。

#### (c) docs の書き方の問題として直したもの

- **「注入が先に走らないといけない」は 2 つの注入方式を同じ括弧に入れていた。**
  配信時置換版には制約が無く、代償リストの中で**唯一「実測で消える」項目**だった。
- **「リンク 4,993 本」「0.1893 秒」はどちらも木の規模に依存する数字なのに、規模が書かれていなかった。**
  同じ行の前半が「4,992〜4,993 か所」と幅で書いているので、読み手は片方を誤りだと思う。規模を添えた。

#### 判断

**§8 の revision 行から「未検証」が 1 つに減った** (残るのは JS 注入版の順序、ブラウザ未検証)。
**決定そのものは依然ユーザーの製品判断**で、この節は取り分と代償を測っただけ。

**残る危険**: `coverage.ts:512` の revless 正規化が `/blob/[0-9a-f]{40}/` のハードコードなので、
**将来 `--source-url` にタグ名やブランチ名を渡す実装が出た瞬間に同じ壊れ方をする** —
正規化がマッチせず未分類が跳ね、**採点は静かに下がる**。オラクル側の前提として書き留めておく。

**直した文書**: `approach.md` §8。

---

### 段階 7e — 構文ベースのプレビュー近似を測った (2026-08-11)

`approach.md` §8「プレビューモードを持つか」に残っていた**唯一の未計測の分岐**、
選択肢 (1)「構文ベースの近似」。§8 はコストを「elaboration 無しの Lean パーサ」と
書いていたが、**それは未計測の仮定だった**。実験は `experiments/stage7e/`。

計測条件: Darwin 25.6.0 arm64 / Apple M1 / 16 GB、`leanprover/lean4:v4.31.0`、
`LEAN_NUM_THREADS` unset、**シングルスレッド**、warm (同一ログ内で 壁 24.17s ≒
CPU 24.07s)、peak RSS 3.26 GB、major fault 384。
生ログ `benchmarks/results/stage7e-{parse,cost,2pass,ab,decl}-*`
(`stage7e-rev-*` は別の問いなので混ぜない)。

#### (a) gate — **パーサは環境に依存する。しかも「import だけでは足りない」側**【実測】

432 モジュールを構成ごとにパースしたときのエラー:

| 構成 | エラーの出たモジュール | エラー総数 | 構文木ノード数 |
|---|---:|---:|---:|
| `--env init` (`Init` のみ = §8 が想定した「パーサだけ」) | **404 / 432** | 4,678 | 641,813 |
| `--env mathlib` | 215 / 432 | 989 | 3,140,109 |
| `--env full` (対象 432 = 常駐サーバが持つ環境) | 215 / 432 | 989 | 3,140,799 |
| `--env full --open <122 名前空間>` | 86 / 432 | 326 | 3,739,218 |
| 同上 − `ContDiff` | 5 / 432 | 9 | 4,008,495 |
| **`--two-pass` (ファイル別活性化)** | **0 / 432** | **0** | **4,010,184** |

**同じバイト列から出る構文木がノード数で 6.2 倍動く。**「パースできた / できない」の
二値ではなく、**環境が薄いほど木が浅くなる**。エラー数だけ見ていると気づかない。

- `--env mathlib` と `--env full` は**エラー数が完全一致** — このプロジェクトの構文は
  全部 `scoped` なので、**自分のモジュールを import してもパーサには何も足されない**。
- `≐` (`InformationTheory/Asymptotic.lean:58:33`)、`ℝ≥0∞`、`H(μ; X)` は
  **`--env full` でも落ちる**。直るのは `Lean.activateScoped` を足したときだけ。
- `--env init` の p50 4.33 ms を「速い」と読まない。**ノードが 6.2 分の 1 しか
  出ていない = 仕事量が少ないだけ**で、トークンテーブルの大きさによる純粋な減速とは
  **分離できていない**。

#### (b) 活性化集合はファイルごとに正確でないといけない【実測】

122 名前空間を一律に活性化すると **161 本が直る代わりに 32 本が壊れる**。

- 壊れた 32 本のうち **81 本ぶんの犯人は `ContDiff` 1 個** — Mathlib の
  `scoped[ContDiff] notation3 "ω"` が `ω` をトークンにし、`ω` は対象プロジェクトの
  標本点の標準変数名。`open scoped ContDiff` しているのは 432 本中 **1 本だけ**。
- 残り 5 本も同型 (`Real` の `π` が `(π := β)` の名前付き引数を壊す)。

パーサ自身が解けるのは `open X in <cmd>` だけで、**単独の `open X` / `open scoped X` /
`namespace` / `section` は後続コマンドに elaboration 経由でしか効かない**
(`ParserModuleContext.currNamespace` は常に空のまま)。

#### (c) 2 パス (ファイル別活性化) は成立し、**環境は使い回せる**【実測】

パス 1 (活性化なし) でそのモジュール自身の構文木から `open` / `namespace` を集め、
それだけを `activateScoped` して同じモジュールを再パースする。**0 / 432 エラー**、
5 プロセス × 5 ラウンド + 逆順 + 追加 2 回、**全ラウンドで 0**。3 パス目は不要
(`wanted grew 0 / 432`)。活性化する名前空間はモジュールあたり p50 15 / max 31。

**環境を捨てるコストは 0。** `Environment` は値で、`importModules (leakEnv := true)` の
環境は persistent なので `activateScoped` は元を書き換えず新しい値を返す。
**`importModules` の 2.6 秒はプロセスにつき 1 回**。根拠は 4 つとも独立:

- パス 1 のエラーが**常に 215 / 989** — (a) の `--env full` と完全一致。基底環境が
  汚れていれば `ContDiff` を処理した後のモジュールが壊れるはずで、壊れない。
- **逆順でも 215 / 989** — 順序の副作用ではない。
- **`--accum` (わざと引き継ぐ) は壊れる** — パス 2 が **52 / 432 (196 エラー)**。
  `ω` 汚染は実在する、という対照実験。
- **peak RSS が動かない** — 1 パス 3.266 GB / 2 パス 3.266 GB (同一 A/B 組)。
  2,160 回派生させても 3.24〜3.28 GB。派生環境は溜まらない。

**コスト**【実測、warm、同一セッションで 1 パスと交互に測った 3 組】:

| | 1 パス | 2 パス (パス1 + 走査 + 活性化 + パス2 + 走査) |
|---|---|---|
| **1 モジュール p50** | **6.78〜7.09 ms** | **15.4〜16.3 ms** |
| p90 / max | 23.2〜24.6 / 54〜57 ms | 50.3〜53.1 / 112〜118 ms |
| 432 本合計 | 4.29〜4.38 s | 9.54〜9.89 s |
| errModules | 215 / 432 | **0 / 432** |

内訳 (p50): パス1 6.80 / 走査 0.18 / **`activateScoped` 0.91** / パス2 7.54 /
走査 0.24 ms。**2.2〜2.3 倍**で支配項は「2 回パースすること」自体、`activateScoped` は 3.8%。

**撤退ラインは踏まなかった** — 「1 モジュールが秒オーダーなら構文近似の利点が消える」に対し
実測は **15.4〜16.3 ms = 環境ロード 2.6 秒の 0.6%**。
**§8 の「安い」は*環境の中にいれば*正しく、環境を立てる側が 160〜170 倍。**

**この計測は 5 プロセス連続では取れなかった** — `importModules` が 2.6 → 12.9 秒に
悪化していく (3.26 GB の RSS を持つプロセスを 16 GB 機で連続して立てるので page cache が
落ちる)。**A/B を交互に回し、`importModules` が 2.60〜2.73 秒だった 3 組だけを使った。**
一続きの 5 プロセスの数字を warm として並べていたら、2 パスの取り分は嘘になっていた。

#### (d) 宣言名は **95.7% 取れて偽陽性 0**【実測】

突き合わせ先は独立な出所である既存の IR (`w7d/ir-j4`、432 本、**4,750 宣言**)。
段階 1 の 5,123 は粗いフィルタ後の数 (→ 本ログ 段階 2)。

構文木から 5,253、うち `private` 707 を落として **4,546**。
**一致 4,546 = IR の 95.7% / 構文側の 100.0%、偽陽性 0、取りこぼし 204 (4.3%)。**
宣言集合が完全一致するモジュールが **369 / 432**。

取りこぼし 204 は**全部 elaboration が作るもの**: structure のフィールド射影 152 /
コンストラクタ `.mk` 37 / `notation` が作る `«term…»` 6 / 無名 instance 3 (名前を
elaborator が合成するので**構文からは原理的に作れない**) / その他 6。
**equation lemma は 0 件** — この IR では別宣言ではなく `.equations` に入るため。
**1 パスと 2 パスで宣言名は byte 一致** — 名前を拾うだけなら活性化は要らない。

#### (e) 署名は **byte 一致 7.5%**。ここが「プレビュー」を否定する【実測】

署名の定義: `<binders> " : " <type>`。IR 側は `.binders` 連結 + `.type`、
構文側は同じ 2 領域の生ソース。**素朴なテキスト比較であって `stage4c/coverage.ts` の
採点ではない** (受け入れオラクルは 1 行も触っていない)。

| 正規化 | 一致 (4,546 中) |
|---|---:|
| byte 一致 | **340 (7.5%)** |
| + 空白・改行を潰す | 350 (7.7%) |
| + 名前修飾を落とす | 418 (9.2%) |
| + IR 側にしか無い先頭 binder を無視 (型は一致必須) | 1,301 (28.6%) |

外れ方 (1 件 1 分類): binder と型の両方 32.1% / 型のみ 26.2% /
**IR がファイルに無い binder を足している 19.4%** / binder のみ 12.4% / byte 一致 7.5% /
修飾だけ 1.5% / 定義側で型を書いていない 0.7% / 空白だけ 0.2%。
中身の多くは「間違い」ではなく別レンダリング (`fun T ↦` ↔ `fun (T : ℝ) =>`、
`𝓝 0` ↔ `nhds 0`、`(· ≤ ·)` ↔ `fun (x1 x2 : ℝ) => x1 ≤ x2`)。

**本物の情報欠落が 1 つある**: binder group は IR 48,499 に対しソース **25,263 (52.1%)**。
**1,692 宣言 (37.2%) で 14,225 の binder group** (型クラス仮定込み) が**ソースに現れない** —
`variable` ブロックが別の場所にあるため。**整形差ではなく、そのファイルに書かれていない**。

**活性化は署名にだけ効く**: 型領域が途中で切れる宣言が 1 パス **347 (7.6%)** →
2 パス **32 (0.7%)**。宣言名 (d) は 1 パスでも同じなので、
**「何があるか」だけなら活性化なし、「どういう型か」を出すなら活性化が要る。**

#### (f) 分離できなかったもの

- **`variable` の復元可能性** — 消えた 14,225 binder group の出どころは構文木に見えている
  (`variable` 970 / `omit` 953 / `include` 25 コマンド) が、**どれが実際に使われるかは
  elaboration の問題**。過剰近似の当たり率は未計測。
- **2 パスを 1.5 パスにできるか** — 支配項は「2 回パース」。ヘッダ再利用や
  `open` 以前の再利用は試していない。
- **`open X in` の位置依存** — 活性化集合をファイル単位の和で近似した。この対象では
  `ContDiff` を開くファイルが 1 本だけなので害が出なかっただけで、
  **位置ごとの活性化は未実装・未計測**。
- **真の cold な `importModules`** — passwordless sudo が無く page cache を落とせない。
- **並列パース** — 全部シングルスレッド。

#### 判断

**§8 の「プレビューモードを持つか」から未計測が消えた。** 選択肢 (1) は成立するが、
§8 の書きぶりは 2 か所で誤っていた:

1. **「コストは elaboration 無しの Lean パーサ」は前提が欠けていた。** パーサは環境に
   依存し、**常駐環境の中にいること**に加えて**対象ファイル自身の `open` / `namespace` を
   解いていること**が要る。安いのは*その中にいれば*の話。
2. **「近似」の中身は署名ではなく宣言名だった。** 宣言名は 95.7% 偽陽性 0 で取れるが、
   署名は byte 一致 7.5% で、**37.2% の宣言は型クラス仮定がソースに書かれていない**。
   **これは「プレビュー」ではなく「アウトライン」**で、doc-gen4 出力の代用にはならない。

**選択肢 (1) と (2) は排他ではない** — (2)「前回のページを出して『未更新』と示す」の上に
(1) の宣言名リスト (15 ms) を重ねられる。**決定はユーザーの製品判断**で、この節は
取り分と限界を測っただけ。

**直した文書**: `approach.md` §7 の段階 7 行、§8 の「プレビューモードを持つか」行。

---

### 段階 7h — L3-3 (全域成果物) を増分化した (2026-08-11)

段階 7 の残り 1 項。`experiments/stage5/global.ts` は全パッケージに依存する 6 成果物
(`declaration-data.bmp` / `navbar.html` / `tactics.html` / `references.html` /
`references.bib` / `name-map.json`) を毎回ゼロから作り直していた。
`approach.md` §8 上は**既に決着済みの行**なので、これは最適化であって未解決の問いではない。
**だから最初の成果物は実装ではなく「天井を正しく測ること」**。実験は `experiments/stage7h/`。

計測条件: Darwin 25.6.0 arm64 / Apple M1 / 16 GB、`leanprover/lean4:v4.31.0`、deno 2.7.14、
抽出器 `stage7d` (schema 4)、レンダラ `stage7d/render.ts`、配線 `stage7g`、
対象は `/Users/haruka/dev/lean-projects` の APFS 複製、移動は 6a/7g と同じもの、
`--mode self`、`--jobs 4`、**warm** (同一セッション、複製をビルドし直した直後)。
生ログ `benchmarks/results/stage7h-{main,probe,oracle}.txt` / `stage7h-{incremental,reads}.jsonl`。

#### (a) 天井 — `globalSeconds` を部品に割った【実測、9 rep 中央値】

| 部品 | 秒 | 判定 |
|---|---:|---|
| deno 起動 (`build` プロセス) | 0.0717 | **床** (増分化後も 1 プロセスは要る) |
| `build`: 433 モジュールの IR 全読み | 0.1339 | **消せる** |
| `build`: 6 成果物の導出 + 書き出し | 0.0241 | 消せない (各成果物は集合全体の関数) |
| deno 起動 (`delta` プロセス) | 0.0570 | **消せる** (1 プロセスに統合) |
| `delta`: 2 つの name-map の差分 | 0.0066 | 消せない |
| `delta`: 433 モジュールの IR 全走査 | 0.1276 | **消せる** |
| **合計** | **0.4210** | **消せる 0.3186 / 床 0.1024** |

`deno run` 自体の床は 0.0557 秒 (空スクリプト)。名前が 1 つも動かない run では
`delta` が走査を自分でスキップするので、そのケースの取り分は 0.128 秒小さい。

**増分 7 の「消せるのは IR 全読みの 0.119 秒だけ = 増分実行の 1.5〜2.6%」は外れていた** —
消えるのは全読み 1 回ぶんではなく、**`build` の全読み + `delta` の全走査 + プロセス 1 つ**。

#### (b) 診断 — このパイプラインは IR を 7 回全読みしていた【実測】

`count-reads.ts` が `Deno.readTextFile` を包んで**実スクリプトを走らせて**数えた:

| step | 現行 | 新版 |
|---|---:|---:|
| ownership-r1 / merge-r1 / ownership-r2 / merge-r2 | 437 / 433 / 2 / 433 | 同じ |
| global | 433 | **6** |
| global-delta | 433 | — (統合) |
| impact / render | 433 / 433 | 同じ |
| **合計** | **3,037 読み (全読み 7 回)** | **2,177 (全読み 5 回)** |

**L3-3 が消すのは 7 回のうち 2 回で、残る 5 回のほうが大きい。**
この段階の取り分を「パイプラインの固定費を消した」と読むのは誇大。

#### (c) 実装 — バイトを作るコードを触らない

- 導出に要る**モジュール単位の事実だけ**をキャッシュに置く (imports / tactic 数 /
  (name, kind) / instance の型頭定数 / docstring の autolink トークン)。**842,210 B** で済む
  (IR は 16 MB)。置き場所は**サイトの外**の `--state` ディレクトリ。
- **導出コードは stage5 のまま**で、毎回 433 モジュール全部を回す。増分になったのは
  **「読む」ところだけ**。**byte 一致を保つ最も安い方法はバイトを作るコードを触らないこと。**
- 有効性の判定は `index.json` の **`contentHash`** — **呼び手が渡す changed-set ではない**ので、
  呼び手が間違えてもキャッシュは腐らない。
- `build` と `delta` は同じ事実を要るので **1 プロセスに統合**した。`delta` の述語は無改変。

#### (d) オラクル — 先に決めてから書いた【実測、全 PASS】

1. **6 成果物が from-scratch 版と byte 一致** — base / rerun / modified / **removed** /
   **added** / restored / **stale-state** の 7 状態で確認 (`stage7h-oracle.txt`)。
   キャッシュは順に 0/433・433/0・432/1・432/0・432/1・431/2・0/433 hit/miss。
2. **サイト全体が byte 一致** — 7g と同じ 439 ファイルの木を REFERENCE と突き合わせ、
   **4 構成すべて missing 0 / extra 0 / differing 0**。
3. **step 6 が step 7 に渡す `--print-set` が old/new で一致** — 全域写像の差分は L3-2 の
   入力なので、ここがずれると再生成集合が静かに変わる。実測では 27 名移動 → 0 ページで一致。
   非ゼロ経路は A/B では踏まないので、**名前 40 件を動かした合成ケース**で
   old/new が同じ 106 モジュールを返すことを別途確認した。

#### (e) 結果【実測、同一セッション、7 run × 4 構成を交互、run 1 破棄】

`globalSeconds` の中央値:

| 構成 | 現行 | 新版 | 差 |
|---|---:|---:|---:|
| fresh | 0.4527 | **0.1257** | **−0.3270** |
| resident | 0.4470 | **0.1461** | **−0.3010** |

内訳: `readSeconds` 0.1525 → 0.0098、スクリプト内部 total 0.1785 → 0.0449
(新版のこの値は `delta` 込み、旧版は含まない)。キャッシュ 427 hit / 6 miss。

**スケール** (5 rep 中央値、無効化件数 0 / 6 / 25 / 100 / 433):
0.107 / 0.103 / 0.110 / 0.142 / 0.234 秒。対照 (stage5 `build`) 0.241 秒。
**全件ミスでも劣化しない。**

#### (f) 総時間には帰属できない — これがこの段階の一番大事な留保

同じペアで **`extract` + `serveStart` だけが 0.80 秒 (fresh) / 0.37 秒 (resident) 動いている** —
**効果の 2.5 倍 / 1.2 倍**。**引用してよいのは step 6 の行だけで、`total` の行ではない。**
分母つきで言えるのは「削減 0.327 秒 = 総時間 11.13 秒の **2.94%**」
「同 0.301 秒 = 19.18 秒の **1.57%**」まで。

**このセッションは 7g より重い** (`serveStart` 15.0 秒 ↔ 7g 3.71 秒) ので、
**7g の総時間 (6.5〜8.0 秒) と並べていない。**

**最初の A/B は位置と処置がエイリアスしていた** — 構成の順序が固定で、
**常に先頭の構成が直前の 3 GB 常駐サーバによるページキャッシュ退避を被っていた**
(page fault 29,779 ↔ 2,013、3.3 秒差)。ラウンドの偶奇で順序を反転して測り直した。
**時間だけ見ていると気づかない壊れ方**で、7d の「並列化したら IR の byte 一致を必ず確認する」と
同じ種類の罠 (計測の構造が結果を作る)。

#### (g) 分離できなかったもの

- **総時間への効果** — Lean 側の分散が効果より大きい。上の通り帰属していない。
- **`delta` JSON の `witnesses`** はモジュールごとに別のトークンを挙げうる
  (キャッシュはトークンをソート・重複除去して持つため)。パイプラインが消費する
  `--print-set` は構成上同一で、実測でも同一。
- **パイプライン内での「state が存在しない初回」** は測っていない
  (probe の misses=433 点 = 0.234 秒がその形)。
- **cold** — このセッションでは取っていない。

#### 判断

**段階 7 の残り 2 項が両方閉じた。** L3-3 は増分化され、6 成果物もサイト 439 ファイルも
byte 一致のまま **step 6 が 0.45 → 0.13 秒**。

**予測が外れた行を直した** — 増分 7 の「増分化する価値なし」は**別の分母での判断**で、
実際の天井は 2.5 倍以上あった。`approach.md` §5.5-3 をその通りに書き換えた。

**次に効くのは L3-3 ではない** — 残る IR 全読み 5 回 (ownership・merge ×2・impact・render)
は L3-3 の天井より大きく、**同じキャッシュの筋がそのまま使える**。ただしこれは
§8 の未解決の問いではなく**新しい最適化項目**なので、着手はユーザーの優先度判断。

**直した文書**: `approach.md` §5.5-3、§7 の段階 7 行、§8 の増分行。

---

### Mathlib 規模の実測と、外側の実装言語の決定 (2026-08-11)

検証段階ではなく**実装フェーズに入る前の測定**。目的は 2 つ — (1)「Mathlib のビルド速度」という
問いに答える、(2) 外側 (Lean 以外) の実装言語を決める材料を出す。
**数字と条件の SoT は [`benchmarks/results/mathlib-scale-summary.txt`](../benchmarks/results/mathlib-scale-summary.txt)**、
生ログは `mathlib-scale-extract.jsonl`。

**このリポジトリで初めて `lean-projects` 以外を対象にした。** 対象は
`.lake/packages/mathlib` の **8,169 モジュール** (ソースの glob)。**InformationTheory の数字と
並べない** — ワークロードもモジュール数も宣言密度も違う。条件: M1 / 物理 16 GB / Lean 4.31.0 /
Mathlib v4.31.0 / 抽出器 `stage7d` (IR schema 4) / `--jobs 4` / Deno 2.7.14。

#### (1) Mathlib 全体の抽出は完走する。ただし warm が収束しなかった

| run | `importModules` | `analyze` | `writeIR` | プロセス内計 | 壁時計 | CPU (u+s) | peak RSS | page fault |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 (cold) | 64.01 | 247.50 | 31.14 | 343.72 | 347.26 | 1,025.6 | 3.48 GB | 886,261 |
| 2 | 29.05 | 263.71 | 31.20 | 324.41 | 329.77 | 1,094.7 | 5.60 GB | 520,122 |
| 3 | 24.86 | 278.08 | 31.95 | 335.32 | 341.79 | 1,173.0 | 5.32 GB | 430,235 |
| 4 | 23.47 | **325.51** | 33.02 | 382.50 | 388.27 | **1,344.6** | 5.47 GB | 474,789 |

すべて【実測】。宣言は 557,366 検討 → **311,415 生成** (blacklist 245,951、**失敗 0**)、
equations 58,426、参照 4,559,774 箇所、環境の closure 10,484 モジュール、IR **547 MB / 8,169 ファイル**。

**`analyze` の CPU 時間が run ごとに単調増加している** (247.50 → 325.51 秒、総 CPU 1,025.6 →
1,344.6 秒 = **+31%**)。同じ仕事なので**計測側の異常**で、疑いは**サーマルスロットリング**
(M1 で `--jobs 4` の 6 分級を 4 回連続) か**メモリ圧力** (run 2 以降 peak RSS 5.3〜5.6 GB、
物理 16 GB)。run 1 だけ RSS が低いのは、cold では olean の mmap ページが読み込まれる端から
回収され、warm では page cache に載ったまま計上されるため、と考えると筋が通る。
**このセッションから warm の数字は引用できない。** 支持されるのは 3 つだけ:

- **完走する** (4 回とも exit 0、`analyze:failed` = 0)
- 壁時計 **330〜390 秒 = 5.5〜6.5 分** (`--jobs 4`)
- `importModules` は cold **64 秒** → warm **23〜29 秒** (**run 4 でもまだ下がっている**)

**doc-gen4 との比較で書けるのは CPU 時間の 1 本だけ。** doc-gen4 の約 32,600 秒 CPU は
**外挿** (8,600 モジュール、42% で打ち切り) で、8,169 に揃えると 30,966 秒。対して実測
1,025.6 秒 = **約 30 倍**。**分子は外挿のまま。壁時計の倍率は書けない** (doc-gen4 側の並列壁時計
が無い)。**`approach.md` §6.4(a) の 76 倍とは別の主張** — あちらは 432 モジュールを 1 スレッド
同士で比べた値。

**この節のいちばん重要な発見は数字ではなく機材の限界** — **6 分級の計測をこの機材で連続させると
信頼できない**。今後 Mathlib 規模を測るなら run 間に冷却を挟むか、対象を分割する。

#### (2) 外側 (Deno) の IR 全読みは規模にほぼ線形

`Deno.readTextFile` + `JSON.parse` を全モジュールに 1 回【実測、warm、n=3 中央値】:

| | ファイル | バイト | 宣言 | 1 回の全読み |
|---|---:|---:|---:|---:|
| InformationTheory | 433 | 15.7 MB | 4,750 | **0.105 s** |
| Mathlib | 8,169 | 542.3 MB | 311,415 | **3.18 s** |

cold の 1 回目は 0.172 / 3.909 s。**バイト 34.6 倍に対して時間 30.3 倍。**
7h が測った `global.readSeconds` 0.1525 s (433 モジュール) とも整合する。

**含意**: 増分パイプラインは 7h の後も全読みを **5 回**残しているので、Mathlib 規模では
**約 15.9 秒**。Lean 側は常駐なら 1 モジュール再抽出 0.707 秒で**規模に依存しない**ため、
**大きなパッケージの増分では外側が支配的になる**。**ただし言語を変えても解決しない** — 仮に
JSON パースが 5 倍速くても 3.2 秒残るのに対し、**7h と同じ `contentHash` キャッシュを当てれば
ほぼ 0 になる。桁が違う。**

#### (3) 検索索引 — `docLink` は 100% 導出可能で、自パッケージなら gzip 45.8 KB

doc-gen4 の `declaration-data.bmp` (Mathlib 込み 258,760 宣言 / 37.48 MB) の内訳【実測】:
`docLink` **21.17 MB (56%)** / 宣言名 8.34 MB / `kind` 4.21 MB / instances・modules 3.21 MB
(**検索は読まない**)。

**`docLink` は (モジュール, 名前) から 258,760 / 258,760 = 100.0000% 復元できる (反例 0)**【実測】。
モジュール名は 5,775 種、`kind` は **9 種**しかないのに文字列で 258,760 回埋まっている。
配列 + 整数 ID に組み替えた索引:

| | raw | gzip-6 |
|---|---:|---:|
| Mathlib 込み — doc-gen4 現行 | 37.48 MB | 3.17 MB |
| Mathlib 込み — 導出形式 | 10.29 MB | 1.48 MB |
| **自パッケージのみ — doc-gen4 互換** | 1,216,017 B | 87,460 B |
| **自パッケージのみ — 導出形式** | **361,466 B** | **45,780 B** |

**引用は gzip 側で。** raw の 3.64 倍は誇大で、`docLink` の反復は gzip が既に潰しているため
実際は **2.14 倍**。機能は落ちない — `search.js` の `getMatches` が読むのは
`name` / `kind` / `docLink` の 3 つだけ。

**罠**: litedoc4 の kind 語彙は doc-gen4 と違う (`definition`/`constructor` 対 `def`/`ctor`)。
doc-gen4 の `search.html` を流用すると**種別フィルタが黙って全部外れる**。静的資産は byte 再現率
の母数外なので、この経路は誰も踏んでいない。

#### (4) Mathlib 対象では `render` が通らない

`declNameToLink: no defining module for Setoid.iseqv (doc-gen4 would panic here)` で
**767 / 8,169 ページで停止**【実測】。`Setoid.iseqv` は Lean core の宣言で、Mathlib の IR に
定義モジュールが無い。**§5.3「依存は外部参照」の裏返し** — Mathlib を対象にするならその依存
(Lean core / Batteries / Aesop …) の写像が要る。InformationTheory では doc-gen4 の bmp から
作っていた部分が、Mathlib 対象では存在しない。**§8「依存パッケージの写像をどう配布するか」が
そのまま出た形。**

#### 判断

- **外側の実装言語は Rust に決定。ただし選定理由は速度ではない** — (2) が示すとおり外側の
  速度差は言語ではなく **IR 全読みの回数**で決まる。決め手は配布と、対外的に「高速を謳うなら
  Rust」という期待に合わせる製品判断。技術的な利得は **md4c (C) を FFI で直接リンクできる**こと
  (TypeScript ではバイト一致する CommonMark 実装が無く自前のサブセットを書いていた)。
- **rev はバイトから外す / 検索は組み込み・自パッケージのみ / プレビューは持たない**。
- **`approach.md` を直した箇所**: 冒頭の状態 / §5.5 の「常に `all`」(前提を外した) と空の
  再生成集合 (**rev を外した今、常時通る経路になった**) / §5.6 に決定 3 つ / §6.5 を 7h の段別
  内訳に置換 / §6.6 の検索行 / §7 に**段階 8** (rev 注入のブラウザ実測) を新設 / §8 から決着した
  4 行を削除 / §9 にプレビューを追加。

---

### 段階 8 — rev 注入の順序制約を本物のブラウザで閉じた (2026-08-11)

§7 の最後の検証段階。7e-rev が残した **B-3 (b)「JS で実行時に注入する版の順序制約」だけ**が
対象で、(a) 配信時置換版は 7e-rev で閉じている。**判断基準は計測前に commit した**
(`f60edde` / `experiments/stage8/README.md` の予測 V1〜V7)。数字と条件の SoT →
`benchmarks/results/stage8-summary.txt`、生ログ → `stage8-nav.json` ほか。

条件: MacBookAir10,1 / M1 / 16 GiB / macOS 26.6 / **Chrome 151.0.7922.76 (headless)** /
deno 2.7.14。CDP を deno の WebSocket から直叩き (npm/node は壊れているので不使用)。
木は `w7e/pages-ph` (432 ファイル) の写しをローカル HTTP で配信 — **`file://` では測っていない**。
**外部への実リクエストは 0 件** (215 件を遮断し `Network.loadingFailed` で全件裏取り。
オーケストレータが独立に再集計して未中断 0 を確認した)。**Lean は 1 度も走らせていない。**

#### 結論 — 満たし方は 3 通りあり、外すのは 1 通りだけ【実測】

判定は見た目ではなく **`Page.frameRequestedNavigation(scriptInitiated)` の URL**
(= `location.replace()` の引数、フラグメント込み)。10 ケース × 5 回、**5 回とも同一結果**。

| 構成 | 注入の置き方 | `location.replace` に渡る URL |
|---|---|---|
| B0 | 注入なし | `…/%7B%7BSOURCE_URL%7D%7D/…` = **同一オリジンの 404** |
| M1 | module、`jump-src.js` の**前**、即時 | 具体 rev の GitHub URL |
| M2 | **同期**、`jump-src.js` の**前**、リスナ内 | 同上 |
| **M3** | module、`jump-src.js` の**後**、**リスナ内** | **B0 と同じ 404** |
| m3b | module、`jump-src.js` の**後**、即時 | 具体 rev の GitHub URL |

**制約は「タグの位置」ではなく「リスナ登録順」**と分離できた (m3b が通ることが分離の要)。
M3 が壊れた理由は 3 通り潰してある: `source-url.js` は 200 で配信され `rewrote 10/10`、
`document.addEventListener` を包んで登録順を記録 (M2 は注入が seq=1、M3 は seq=2)、
`?jump=src` を外すと M3 でも 5 回とも `matched 10 / rewritten 10`。
**注入は健全で、外れているのはタイミングだけ。** 404 はサーバ側ログが一次証拠 (b0 / m3 各 5 本) で、
7e-rev の DOM シム結論がブラウザで裏取りできた。
**rev は 432 ページの HTML に 1 バイトも入っていない** (5 構成すべてで出現 0、供給元は
全ページ共通の外部 1 ファイルのみ) — これが供給元の制約を満たしていることの裏取り。

**`location.replace` を JS で包む案は採れない**【実測】 — `replace` は Location の own property で
`configurable=false`、`Object.defineProperty` が `TypeError`。判定手段の選択がここで決まった。

#### 予測が外れた点 3 つ

1. **「1 ページ最大 73 か所」という分母が誤りだった**【実測】。書き換えコストは**本数に比例しない** —
   47 本のページ (0.66 ms) が 73 本のページ (0.33 ms) の 2 倍かかる。**要素数に対しては 5 点すべて単調**で、
   最も高いのは本数最大のページではなく**バイト最大のページ** (47 本 / 555,904 B / 13,562 要素) の
   **0.80 ms** (観測最大 0.90 ms)。`querySelectorAll` が文書全体を歩くためと見られるが、
   **5 点では要素数と本数を分離できていない**。V5「1 ms のオーダー」は成立するが、
   **大きく下回るのではなく 1 ms の直下**。p50 のページは 0.10 ms。
   なお `performance.now()` の分解能は **0.1 ms** (Chrome の時刻粗視化) で単発値はこの粒度に
   量子化されるので、償却 400 パスを併記してある。
2. **「全ページ共通なので 2 ページ目以降はキャッシュに載る」は無条件には成立しない**【実測】。
   鮮度ヘッダが無ければ Chrome は 10 ナビゲーションで `source-url.js` を **10 回**取りに来る
   (サーバ総リクエスト 142)。`Cache-Control: max-age=3600` なら **1 回** (総 47)。
   **配信側の要件**として残り、かつ rev 更新時に古い `source-url.js` を掴ませない設計が要る。
   判定は**サーバ側ログでしかできない** — レンダラ側の `Network.responseReceived` は
   キャッシュヒットでも発火するので、どちらの条件でも 10 件出る。
3. **module 版は parse をブロックしないが、DOMContentLoaded は同期版と同じだけ遅れる**
   【実測、予測に無かった】。`source-url.js` を 400 ms 遅らせると parse 完了 (`domInteractive`) が
   押し出されるのは M2 だけ (511.9 ms 対 B0 の 77.4 ms) だが、**DCL 開始は M1 / M3 も 440 ms 台**。
   「module にすれば往復のコストが消える」わけではない。遅延なしでは 4 構成の差は
   **ノイズに埋もれて分離できない** (中央値 40〜79 ms、8 回の範囲が互いに重なる)。

#### 判断

**段階 8 は閉じた。§7 の検証段階はこれで全て完了。** ただし §5.6 の決定の実装形には条件が付く:

- 採れる形は **3 通り** (M1 / M2 / m3b)。外すのは「リスナ内で注入し、かつ登録が
  `jump-src.js` より後」のときだけ
- **配信は鮮度ヘッダを付ける** — 付けないと全ページで `source-url.js` を取りに行く
- 壊れ方は `?jump=src` 付きのディープリンクに限られる (V6: 5 構成 × 5 回で `location.replace` 0 件)

**測っていないもの**: Chrome 151 以外のエンジン (Safari / Firefox)。仕様上は同じはずだが
**それは仮定であって実測ではない**。そして**配信時置換版 (7e-rev で実測、注入 0.1552 秒 /
432 ファイル) との製品上の選択はまだ決めていない** — この段階は JS 注入版が**成立する**ことを
示しただけで、どちらを採るかは別の判断。

> **その判断は 2026-08-11 に出た: 製品は配信時 (ビルド時) 置換を採る**
> (→ 実装側の決定 3)。0.1552 秒で制約ゼロの側に対し、
> JS 注入は順序制約・鮮度ヘッダ・Chrome のみ実測の 3 つを負う。**この段階の数字は
> 選択肢の保存として残す** — 静的ホスティングを使えない配信形態が出たら成立する形がある。

**直した文書**: `approach.md` の冒頭状態 / §5.6 の出力契約 (未検証 → 閉じた、配信要件を追加) /
§7 の段階 8 行 (✅、および**「4,992 か所」の分母混同を訂正**) / この文書の冒頭の状態表。

---

## 実装フェーズの実測

検証段階ではないが、実装計画が引用する数字なのでここに置く。

### 依存写像の配布経路 — 公開サイトは使えない (2026-08-11)

**計測条件**: ネットワーク越しの HTTP HEAD / GET のみ。Lean も lake も走らせていない。

| 問い | 実測 |
|---|---|
| 公開サイトに `declaration-data.bmp` はあるか | **ある** — `https://leanprover-community.github.io/mathlib4_docs/declarations/declaration-data.bmp` が **HTTP 200 / `image/bmp` / 66,418,003 B / `last-modified: Tue, 11 Aug 2026 01:10:04 GMT`**。**ルート直下は 404** (`declarations/` の下にある) |
| 公開サイトのバージョン | **Lean 4.33.0** (`index.html` 本文)、**mathlib `c3a9a08f698aab9788592d1efa32383bdda23396`** (ページ内 blob URL から) |
| 対象リポジトリのバージョン | **Lean v4.31.0** (`lean-toolchain`)、**mathlib `fabf563a7c95a166b8d7b6efca11c8b4dc9d911f`** (`lake-manifest.json`) |

**判断**: **公開サイト経路は使えない。** **2 マイナー版ずれている**うえ、下流プロジェクトは
古い Mathlib に固定される。公開サイトがバージョン別アーカイブを持たない**【仮定・未確認】**以上、
対応する写像は原理的に取れない。→ **写像は生成側で作る** (`approach.md` §8 を更新した)。

**未検証**: 環境走査で作った写像が bmp 由来 (258,760 宣言、段階 7c) と**同じ結果を出すか** —
実装計画の V1 で測る。**否定されたら M5 の設計をやり直す。**

> **この節の【仮定】は 2026-08-15 に潰した** → 次節。「バージョン別アーカイブは無い」は**実測**に
> なり、同時に**写像を生成側で作っても版ズレは残る**ことが分かった。

### 公開ドキュメントは master の 1 本しか無い — 外部リンクの版ズレ (2026-08-15)

**計測条件**: ネットワーク越しの HTTP GET と GitHub API のみ。Lean も lake も走らせていない。
再現は `benchmarks/tools/measure-docsite-drift.sh`、ログは
`benchmarks/results/docsite-drift-2026-08-15-{env,hosting,modules,decls}.txt`。
サイト側は `last-modified: Sat, 15 Aug 2026 09:00:33 GMT` の版 = **Lean 4.34.0-rc1 /
mathlib `971f540b`**、対象は **Lean v4.31.0 / mathlib `fabf563a`** で **3 マイナー版差**。
**同一条件で 3 回連続実行し、3 回とも同じ数字** (サイトの再デプロイは 8 時間ごとなので
計測窓の中では動かない)。

#### (a) 版別アーカイブは存在しない — 【仮定】が【実測】になった

| 問い | 実測 |
|---|---|
| 版別の URL はあるか | **無い** — `/v4.31.0/` `/4.31.0/` `/v4.33.0/` `/4.33.0/` `/versions/` `/archive/` は**すべて 404** |
| サイトは何から作られるか | `leanprover-community/mathlib4_docs` の `.github/workflows/docs.yaml` — **cron `0 */8 * * *`** で mathlib4 を `actions/checkout` (**`ref:` の指定が 1 行も無い** = default branch `master`)、`upload-pages-artifact` → **`deploy-pages@v5`** で Pages を**まるごと差し替え** |
| 過去の版は git に残るか | **残らない** — リポジトリ本体は **28 KB / `.github` と `lakefile.toml` のみ**。生成物は commit されないので履歴から取り出すこともできない |
| リリース資産にあるか | **無い** — `v4.34.0-rc1` / `v4.33.0` / `v4.33.0-rc2` いずれも **assets=0** |
| 他にホストしている所はあるか | **無い**。Reservoir はパッケージの版一覧を持つが docs は持たない (ページ上の documentation リンクは README 由来の外部リンク)。版付きなのは**散文の Lean Language Reference** (`lean-lang.org/doc/reference/4.31.0/` は 200、`4.99.0` は 404) だけで、これは doc-gen4 の API ドキュメントではない |

#### (b) 実害 — ピン固定のプロジェクトから見たリンクの生存率【実測、サンプル】

正は**ローカルのピン版** (`.lake/packages/mathlib` のソース木と、`.lake/build/doc-data` の
`declaration-data-*.bmp`)。飛び先は公開サイト。

| 層 | 母数 | 壊れ | 率 | 失敗の質 |
|---|---|---|---|---|
| モジュール (ページの存在) | 8,169 中 **205 本を抽出** (40 本に 1 本) | **2** | **0.98%** | **ハード 404**。`Mathlib/CategoryTheory/Category/Grpd.html`、`Mathlib/CategoryTheory/Subpresheaf/Equalizer.html` |
| 宣言 (ページ内アンカー) | 生存 203 本のうちローカル doc-data がある **71 モジュール / 3,862 宣言** | **48** | **1.24%** | **ソフト失敗** — ページは 200 で返るので**ページ先頭に着地する**。壊れたことに気づけない |

宣言側の内訳は 9 モジュールに偏っている。`Mathlib.Data.Set.Dissipate` は **16/16 全滅**
(モジュールごと中身が移った)、`Mathlib.Analysis.Fourier.FourierTransformDeriv` は
`Real.fourierIntegral_*` 系が 13 本、`Mathlib.Analysis.InnerProductSpace.PiL2` は
`Matrix.toEuclideanLin*` 系が 8 本。**リネーム・deprecated 削除・別モジュールへの移動**の混合。

**外挿**: 索引の 255,205 名にこの率をかけると **3,000 本規模**が壊れる【外挿。母数はサンプル
3,862 宣言 = 索引の 1.5%。偏りの補正はしていない】。しかも**サイトは master 追従・こちらは
ピン固定なので、率は時間とともに単調に増える**。

#### 計測でやりかけた誤り (記録として残す)

最初の抽出は bmp の href から `#` 以降をそのまま宣言名として数えたため、**ソースの行アンカー
(`#L26-L28`) を宣言名に混ぜた**。行アンカーは doc ページに `id` を持たないので全部「消滅」に
数えられ、**欠損率 50.97%** という数字が出た。ラベルの問題ではなく**母数の汚染**。
`^L[0-9]+(-L[0-9]+)?$` を除いて 1.24%。**不利な方向に出たから気づけた** — 逆向きに出ていたら
見逃していた種類の取り違えなので、スクリプト側に除外を固定した。

**判断**: **外部参照方式そのものは倒れていない** (§5.3 の写像は自分の版に対して正しい)。
倒れたのは「**写像が正しければリンクも正しい**」という暗黙の前提のほうで、**写像の正しさと
飛び先の生存は別問題**だと分離できた。

**決着した (2026-08-16、M7)** — 依存へのリンクは公開 doc サイトではなく**そのパッケージの
版固定 GitHub blob URL** を指すようにした。**この節の 0.98% / 1.24% は両方 0 になった**:
自サイトが emit した依存 URL の**全数**で **ファイル 484/484 が 200、行アンカー 738/738 が有効**
【実測、M7】。**ゲート A はこの変更で保留に入った** (doc-gen4 とは
バイトで一致しなくなる。互換はもう追わない → 実装計画 §1)。

### md4c の所在と doc-gen4 の使い方 (2026-08-11)

**計測条件**: ソースを読んだだけ (実行していない)。対象は `lean-projects/.lake/packages/`。

| 事実 | 出所 |
|---|---|
| md4c の C ソースは**対象環境に vendored されている** | `MD4Lean/md4c/` に `md4c.c` `md4c.h` `entity.c` `entity.h` `md4c-html.c` `md4c-html.h` |
| MD4Lean は 3 本ともビルドする | `MD4Lean/lakefile.lean:10` の `srcNames := #["entity", "md4c", "md4c-html"]` |
| doc-gen4 が渡すフラグ | `MD_DIALECT_GITHUB ||| MD_FLAG_LATEXMATHSPANS ||| MD_FLAG_NOHTML` (`doc-gen4/DocGen4/Output/DocString.lean:393`) |
| doc-gen4 は md4c の **HTML レンダラを使わない** | `MD4Lean.parse` で AST を取り `renderBlock` / `renderText` で自前に組む (`DocString.lean:202-393`) |
| entity は**生のまま通る** | `DocString.lean:211` — `\| .entity s => return #[Html.raw s]` |
| MD4Lean の wrapper は AST を**正規化しない** | `MD4Lean/wrapper/wrapper.c:157-191` — push API を Lean の ADT に 1:1 で写すのみ |
| `md4c.c` は `entity.h` を include しない | `md4c.c:26-32` は `md4c.h` と標準ヘッダだけ |

**判断**: Rust 側は **`md4c.c` / `md4c.h` だけ vendor すれば足りる**。同じフラグで叩けば
**AST は定義上同じ**になるので、approach.md §5.6 の「本物のパーサに置き換えられる」は成立する。
ただし**効くのは自前サブセット 594 行のほうだけ**で、autolink 解決系 153 行
(doc-gen4 の `Output/DocString.lean` 相当) は書き直しが残る → 実装計画 §7。

---

### CI 軸 — テンプレートを実走させた / **ランナーの実体が変わっていた** (2026-08-16)

**この節の数字は、過去 4 本の CI 実測と同じ機材で取られていない。** 先にそれを書く。

#### ランナーが 2 コア / 7.75 GiB → **4 コア / 16 GiB** になっている【実測】

| | 過去 4 run (2026-08-10〜11) | 今回 (2026-08-16) |
|---|---|---|
| image | `ubuntu24 20260720.247.2` | **`ubuntu24 20260810.271.1`** |
| kernel | `6.17.0-1020-azure` | `6.17.0-1022-azure` |
| `nproc` | **2** | **4** |
| `MemTotal` | 8,128,884 kB (7.75 GiB) | **16,373,448 kB (15.6 GiB)** |
| CPU | 記録なし | **AMD EPYC 7763** (2 コア / 2 スレッド、`sha_ni` + `avx2`) |
| ディスク | 記録なし | Virtual Disk 150G、`/dev/sda1`、`ROTA 1` |

生ログ `benchmarks/results/ci-template-env.txt`。**したがって
「同じジョブ 2.610 秒 / 別ジョブ 20.4〜89.2 秒」を今回以降の数字と直接並べてはいけない** —
メモリが 2 倍になったので page cache に入る olean の量が変わり、これは**まさに
あの倍率を決めていた変数**。approach.md §3 の数字は 2 コア / 7.75 GiB のものとして読む。

**副作用が 1 つある**: approach.md §8 (c)「完全版の抽出器でも同じ差が出るか (作業集合が
16 GiB に収まらず macOS では測れなかった)」の**前提が変わった** — ランナーの RAM が
macOS 機と同じ 15.6 GiB になったので、この問いは**ランナー上で測れる**。

#### テンプレートは実走して緑になった【実測】

`.github/workflow-templates/litedoc4-docs.yml` は 2026-08-16 まで冒頭に
`THIS FILE HAS NEVER BEEN RUN` と書いてあった。`.github/workflows/ci-template.yml` が
**その手順を逐語で写して**実走し、[run 31955883894](https://github.com/FujiHaruka/litedoc4/actions/runs/31955883894)
で全ステップ success。**対象パッケージはジョブ内で `tools/make-target2.sh` が生成する**
(litedoc4 に Lean パッケージが無く、外部への新規リポジトリ作成はしないため)。

| phase | 秒 |
|---|---:|
| `lake exe cache get` | 5.312 (生成側で既に取っているので実質 no-op) |
| `lake build` (13 モジュール) | 4.945 |
| 抽出器のビルド | 15.882 |
| `cargo build --release` | 23.932 |
| **`litedoc4 build`** | **2.510** (extract 1.730 / render 0.021 / global 0.002) |
| 合計 | 52.647、site 23 ファイル |

**未検証として残るのは 2 つだけ**: `push:` トリガと、利用者リポジトリ自身の checkout。

#### 落ちたことのほうが収穫だった — `--deps fetch` が暴いた欠陥【実測】

1 回目の実走 ([run 31955697828](https://github.com/FujiHaruka/litedoc4/actions/runs/31955697828))
は `error: dependency '«doc-gen4»' not in manifest` で落ちた。`tools/make-target2.sh` の
`[[require]]` が **loogle / mathlib / doc-gen4 の 3 本ハードコード**で、計測対象が
2026-08-16 (`c4f6af29`) に開発ツールを条件付き依存へ移して manifest から外した時点で
無効になっていた。

**構造として気づけなかった**: macOS 側の `--deps clone` は `.lake/packages` を APFS
clonefile で借りるだけで、**Lake に解決を一度も頼まない**。lakefile が manifest と
食い違っていても、`cp -Rc` は成功する。**「manifest と一致すること」は要件だったのに、
一致は願望でハードコードは事実だった** — いまは manifest から導出し、mathlib 1 本だけ
require する (これらのソースが import する唯一の依存)。`lake build` 132 ジョブ成功。

**target2 の HEAD は `18a02d58` → `0a072ea2` に動いた** (原因 2 つ、→ 結果ログ「E2」)。

**直したうえで E2 を実走し直した — `tools/target2-gate.sh all` は全段 PASS**【実測
2026-08-16、Apple M1 / 16 GB / Lean v4.31.0 / Mathlib `fabf563a` / target2 HEAD
`0a072ea2` / `--jobs 4`】: gate4-site 24 ファイル identical、gate4-ir 16 ファイル
identical、ledger と link index も identical。**require を 3 本から 1 本に減らしても
E2 の判定は動かない** (これらのソースは loogle も doc-gen4 も import していなかった)。

---

### CI 軸 — 配置の A/B を製品でランナー実体に当てた / **「8〜34 倍」は再現しなかった** (2026-08-16)

**§3 の中心的な主張が、現在のランナーでは成り立たない。** 差は 8〜34 倍ではなく
**1.08〜1.58 倍**だった。ただし**機構のほうは生きている** — 対照でページキャッシュを
落とすと **5.2〜11.9 倍**になる。壊れたのは機構ではなく「別ジョブは cold で始まる」という前提。

計測: [run 31956756819](https://github.com/FujiHaruka/litedoc4/actions/runs/31956756819)、
`.github/workflows/ci-placement.yml`。対象 `FujiHaruka/information-theory` `c4f6af29`
(422 モジュール)、`ubuntu24 20260810.271.1` / 4 vCPU / 15.6 GiB / AMD EPYC 7763、
`--jobs 4`。**測っているのはプロトタイプではなく製品** — `tools/ci-build.sh` →
`litedoc4 build` の端から端で、環境ロードは常駐抽出器の `ready <ns>` 行から取る
(§8 (c)「完全版の抽出器でも同じ差が出るか」への答えでもある)。
生ログ `benchmarks/results/ci-placement-5-{summary.txt,phases.jsonl}`。

#### 3 つの腕

| 腕 | 何をしたジョブか |
|---|---|
| `same` | `cache get` → `lake build` → `litedoc4 build`。1 ランナー / 1 page cache |
| `split` | **別のランナー**。`cache get` で Mathlib を取り、対象自身の olean と抽出器を `same` から**バイトとして復元**して `--no-lake-build` |
| `cold` | `same` と**同じランナー**で `drop_caches` 後。配置ではなく**陽性対照** |

**対照を置いた理由**: 最初の run (n=1) が 1.06 倍で返ってきたから。**「差が無い」は、
差が出るはずの条件で差が出ることを示さない限り読めない** — 計測器が鈍いのか主張が偽なのかを
分けられない。`cold` がその条件で、対照が効かなければこの run の数字は全部無効になる。

#### 結果 — 環境ロード (r1 = 各腕の最初の 1 本。以降はその機材の warm floor)

| pair | `same` | `split` | split/same | `cold` | cold/same |
|---|---:|---:|---:|---:|---:|
| 1 | 2.593 s | 2.816 s | **1.09x** | 14.338 s | 5.53x |
| 2 | 2.415 s | 2.605 s | **1.08x** | 14.289 s | 5.92x |
| 3 | 2.592 s | 2.890 s | **1.11x** | 13.482 s | 5.20x |
| 4 | 1.599 s | 2.532 s | **1.58x** | 19.019 s | 11.89x |
| 5 | 2.393 s | 2.730 s | **1.14x** | 14.031 s | 5.86x |

major fault: `same` **126〜199** / `split` **1〜318** / `cold` **14,450〜37,240**。
**端から端 (`litedoc4 build` 全体) では差はさらに消える** — split/same は **0.83〜1.33 倍**で、
5 対のうち 3 対は split のほうが速い。**50 本すべてが byte 一致の site を出した**
(digest 1 種)。

#### なぜ再現しないのか — 「別ジョブ = cold」が前提として壊れた【実測】

`split` のジョブは olean を**自分で書いている**: `lake exe cache get` は Mathlib の
olean をダウンロードして展開し、対象自身の olean は artifact から展開する。
**書いたページは page cache に載る。** 旧ランナーは 7.75 GiB でそれを保持できず、
新ランナーは 15.6 GiB で保持できる。**メモリ 2 倍が、まさにあの倍率を決めていた変数だった。**

したがって:

- **機構の主張 (「効くのは olean が page cache にあるかどうか」) は否定されていない** —
  `cold` の 5.2〜11.9 倍がそれを示す。
- **系の主張 (「別ジョブに割ると 8〜34 倍払う」) は現在のランナーでは偽。**
- **配置を同じジョブに保つ理由は残るが、根拠が変わる** — 「別ジョブが遅いと観測されたから」
  ではなく、「**cold の罰則が 5〜12 倍あり、別ジョブが cold になるかどうかはランナーの
  RAM と作業集合の比で決まる**から」。RAM の小さいランナーや、この対象 (422 モジュール /
  peak RSS 約 4.0 GB) より大きいパッケージでは前提が戻りうる。**測ったのは 1 ワークロード。**

#### ランナー実体は 2 つのまま。ただし別れ方が変わった【実測】

`cold` の fault あたり時間で割れる:

| | pair 1 / 2 / 3 / 5 | **pair 4** |
|---|---|---|
| major fault | 36,844〜37,240 | **14,451** |
| fault あたり | 0.362〜0.388 ms | **1.316 ms** |
| `same` の import | 2.393〜2.593 s | **1.599 s (最速)** |
| CPU 較正 (1 GiB sha256 の最小) | 0.870〜0.930 s | **0.830 s (最速)** |

**旧ランナーで見えた形と同じ** (少ない fault × 高い単価 ↔ 多い fault × 低い単価) だが、
**単価差は 5.3 ms ↔ 0.67 ms から 1.32 ms ↔ 0.38 ms に縮んだ**。今回は**実体を名指しできる
フィールドを記録している** (`benchmarks/tools/record-runner.sh` が CPU モデル / `lscpu` /
`read_ahead_kb` / `lsblk` / CPU 較正を出す) — 過去 4 run はこれが無く、実体の差は
「説明したい数字」からしか推定できなかった。

**CPU 較正は 10 ランナーで 0.830〜0.991 s = 1.19 倍**に収まった。旧ランナーで見えた
**2.19 倍の CPU ばらつきは今回は出ていない**ので、腕どうしの比較を交絡していない。

---

### 再抽出の単位 — 「宣言単位の再解析キャッシュ」は増分では効かない (2026-08-17)

**きっかけ**: CI 実測 (上節) で床が I/O から CPU に移り、ランナー上の `litedoc4 build`
11.5〜20.7 s のうち **extract が 10.0〜18.2 s** を占めた。`approach.md` §6.1 末尾が名指しする
「変更のない宣言を再解析しない」を次の目標に据えた。**実装に入る前に母数を測った。**

#### 母数 — 既存ログのモジュール単位への再集計【実測の再集計】

出所は `benchmarks/results/stage7d-decl-profile.jsonl.gz` (段階 7d、4,750 宣言 /
406 モジュール、合計 analyze 10.6924 s)。**新しい計測ではなく、同じログを別の軸で割った値**:

| | 宣言数 | analyze 時間 |
|---|---:|---:|
| mean | 11.7 | 0.0263 s |
| median | 8 | 0.0178 s |
| p90 | 25 | 0.0562 s |
| p99 | 49 | 0.1240 s |
| max | 72 | 0.2033 s |

#### 判断 — 否定【実測】

1 モジュール変更で再解析されるのは **median 8 宣言 / 17.8 ms**、最悪のモジュールでも **203 ms**。
増分ビルドの実測総時間は **11.739 s (fresh) / 18.096 s (resident)** (段階 7h) なので、
**宣言単位のキャッシュが 100% 当たっても中央値で 0.15%、最良でも 1.7% しか減らない。**
→ **増分シナリオでは宣言単位のキャッシュは作らない。**

#### 10〜18 s の帰属先は増分ではなくフル抽出だった

CI の extract 10〜18 s は 432 モジュール / 4,750 宣言の**全解析**である。増分機構は実装済み
だが、**CI では一度も発火していない** — `.github/workflows/ci-template.yml` がキャッシュ
しているのは Mathlib の olean・抽出器バイナリ・Rust ビルドの 3 つだけで、**IR ディレクトリと
`ledger.json` は run 跨ぎで保存していない**。毎 run が初回ビルドなので L2 (モジュール単位
ハッシュ) は比較相手を持たない。

→ **「回数を減らす」の単位は、いまのところ宣言ではなくモジュール。** 次に決めるべきは
「モジュール単位のキャッシュが CI 跨ぎで当たるか」で、それは**再ビルドした olean が
byte 一致するか**に還元される (L2 の既定鍵は olean バイトの SHA-256 —
`crates/litedoc4-incr/src/ledger.rs`、`build.rs:808`)。設計と分岐は
再抽出回数の調査。

### IR キャッシュを CI 跨ぎで効かせた / **律速は宣言でも抽出でもなかった** (2026-08-17)

前節の続き。段 A (olean は再ビルドで byte 一致するか) と段 B (CI に配線して実走) を回した。
設計と分岐は再抽出回数の調査。

#### 段 A — olean は再ビルドで byte 一致する【実測、n=9】

計測器 `benchmarks/tools/olean-determinism.sh`、ログ
`benchmarks/results/olean-determinism-2026-08-17.txt` / `-large.txt`。対象は葉モジュール
9 本 (olean **3,680 B 〜 1,231,808 B**)、機材は M1 (Darwin arm64)。

| 判定 | 内容 | 結果 |
|---|---|---|
| DETERMINISTIC | 消して再ビルドしても同じバイト (h1 == h0) | **9/9 PASS** |
| SENSITIVE | 宣言を 1 本足すとバイトが動く (h2 != h1) — **陽性対照** | **9/9 PASS** |
| REPRODUCIBLE | 編集を戻すと元に戻る (h3 == h1) | **9/9 PASS** |

sha256 (olean のバイト、台帳の既定) と lake (`<file>.olean.hash`) の両方で同じ判定。
**陽性対照が全部通っているので「バイトが動かなかった」は「再ビルドされていなかった」ではない。**

#### 段 B-1 — CI 跨ぎで当たった。**Linux 上の決定性も同時に取れた**【実測、n=2 run】

`.github/workflow-templates/litedoc4-docs.yml` に 4 つ目のキャッシュ (`docs-out` =
IR + 台帳 + state + site) を足し、検証ワークフロー `ci-template.yml` を同一 commit で 2 回
`workflow_dispatch` した。**母数は 13 モジュールの生成パッケージ** (この検証は 422 モジュールの
本番対象ではない)。

| | run 1 (31959354738) | run 2 (31959511320) |
|---|---|---|
| `docs-out` キャッシュ | miss | **cache-hit true** |
| `litedoc4 build` | full **1.6741 s** | incremental **0.0163 s** |
| 再抽出モジュール | **13** | **0** |
| 再描画ページ | 13 | **0** |
| 抽出器プロセス | 1 | **0** |
| `ci-build.sh` の `docs` 段 | 2.230 s | 0.634 s |
| `lake-build` 段 | 5.056 s **ran** | 4.894 s **ran** |
| ジョブ合計 | 12.427 s | 10.790 s |

**`lake build` は run 2 でも実走している** (パッケージの `.lake/build` はキャッシュしていない)。
それでも台帳は 13 本すべてのハッシュに一致した — **これが Linux ランナー上の olean 決定性の
実測**であり、段 A の macOS の結果を移送せずに済んだ。

**ただしジョブ全体は 12.43 → 10.79 s (1.15 倍) にしかならない。** この母数では
`cache-get` 5.1〜5.2 s と `lake build` 4.9〜5.1 s が支配的で、doc は元から少数派。
**倍率を doc 段だけで書かないこと。**

#### 段 B-2 — 本番対象 (422 モジュール) をローカルで A/B【実測、同一セッション・warm】

M1、`--jobs 4`、対象 `/Users/haruka/dev/lean-projects` (`c4f6af29`)。同一 `--out` を
使い回して測った。**壁時計は page cache で動くので、判定は決定的な整数のほうで読む。**

| | フル生成 | 無変更の増分 | **1 モジュール変更** |
|---|---:|---:|---:|
| 合計 | **9.8848 s** | **0.3048〜0.7984 s** (n=6、中央値 0.313) | **6.1998〜8.2803 s** (n=3) |
| 再抽出モジュール | 422 | **0** | **1** |
| 再描画ページ | 422 | **0** | **422** |
| 抽出器プロセス | 1 | **0** | 1 |
| IR 全読み | 2.00 pass | 1.00 pass | 4.00 pass |

**フル生成 9.8848 s は `ready` (環境ロード) 3.016 s の warm な回。** 同セッションの 1 回目は
`ready` 15.999 s / 合計 24.1956 s だった — **cold と warm を混ぜないこと。**

#### 判断 — **効いたのは無変更のときだけ。本番の押下は 1.35 倍**【実測】

- **無変更**: 9.8848 → 0.313 s = **31.6 倍**。抽出器プロセスは 0 で、Lean は 1 回も起動しない。
- **1 モジュール変更**: 9.8848 → 7.3118 s = **1.35 倍**。**ここが本番の CI 押下の姿。**

#### なぜ 1.35 倍しか出ないか — 2 つの全環境成果物【実測、内訳】

1 モジュール変更 (合計 7.3118 s) の内訳。出所は抽出器自身の位相ログと `work/serve.out`:

| | 秒 | 何をしているか |
|---|---:|---|
| `ready` (環境ロード) | **3.329** | 6,011 モジュールの import。**変更量と無関係** |
| 抽出器の要求 | **1.484** | うち **`linkIndex` 1.461 s (98.5%)**、**`analyze` 0.0072 s (0.5%)** |
| 残り (merge / global / impact / render 422 ページ / 台帳) | ≈ 1.5 | |

- **`linkIndex`** は依存写像で、**490,287 定数を走査して 255,810 宣言 / 10,448,303 B を
  5,684 モジュール分書き直す**。1 モジュールしか変わらなくても**毎回まるごと作り直す**。
- **その 10 MB の全体ダイジェストが `renderKey` に入っている**ので、宣言が 1 本増えただけで
  鍵が動き、**422 ページ全部が再描画される**。出力はそれを名指しする:

  ```
  global  delta: 2 name(s) moved in or out of the map (5111 -> 5113) -> 0 page(s) to re-render
    render-all linkIndex: the dependency map moved during this run (f45fc90ff9b465ca -> b60740ec8f2ff605)
  impact  mode all -> 422 page(s) (422 from the changed set, 0 from the global map)
  ```

  **全域写像の差分 (L3-2) は正しく 0 ページと答えている。** 過剰描画を起こしているのは
  L3-2 ではなく、**`linkIndex` をファイル全体のダイジェストで鍵にしていること**。
  L3-2 が全域写像に対してやったこと (「差分に出た名前を含むページだけ足す」) を、
  依存写像に対してはやっていない。

- **宣言の意味解析は 0.0072 s = 0.1%。** 前節の否定 (母数から) と**別経路で同じ結論**に着いた。
  **「変更のない宣言を再解析しない」は、実測 2 通りで律速ではない。**

#### 計測でやりかけた誤り (記録として残す)

`served 1 module(s) from the resident environment in 5.764s` を要求単体の時間と読み、
「1 モジュールの要求 (5.764 s) は 422 モジュール全部 (5.704 s) とほぼ同じ」と結論しかけた。
**`Resident::extract` の計時は `self.server()?` を含む** (`crates/litedoc4/src/resident.rs:244`) —
サーバは遅延起動なので初回要求には**環境ロードが入る**し、末尾の `fold_timings` も入る。
実際の要求時間はサーバ自身が返す `ok 0 1484368209` = **1.484 s**。
**外側の計時器と内側の計時器で名前が同じでも範囲が違う。**

#### 依存写像の差分は **267,504 行中 2 行**【実測】

`link-index.lidx` を 1 モジュール変更の前後で保存して `/usr/bin/diff` を取った
(生ログ `benchmarks/results/linkindex-delta-2026-08-17.txt`)。
267,506 → 267,504 行、**差分 2 行 (0.0007%)** — モジュール見出し 1 行と、増やした宣言 1 行。
**残りは byte 一致。** それでも 10,448,303 B をまるごと書き直し (1.461 s)、
全体ダイジェストが動くので 422/422 ページを再描画している。
**内訳では自パッケージの 4,750 宣言に対し、依存側が 251,060 宣言 (98%) を占める** —
そちらが動くのは manifest / toolchain が動いたときだけで、それは `extractKey` の担当。

#### 次にやること

**回数を減らす対象は宣言ではなく、この 2 つの全環境成果物。** → 再抽出回数の調査 段 C。

### 依存写像の**自パッケージぶんは描画バイトに 0 効果**【実測 2026-08-17】

生ログ `benchmarks/results/lidx-own-half-2026-08-17.txt`。
切り分けは `tools/slice-link-index.py`、描画は `litedoc4 site`
(同一 IR = 422 モジュール 4,584 宣言、同一 `--source-url` / `--root`)。

| `.lidx` | 中身 | サイト 429 ファイルとの byte 比較 |
|---|---|---:|
| full | 10,448,228 B / 255,809 宣言 / 5,683 群 / 6,011 `@` 行 | — |
| **deps** | 自パッケージの宣言群 396 と 4,584 行を除去 (10,098,730 B) | **差分 0** |
| **deps-only** | さらに `@` 節の自分ぶん 422 行も除去 (10,076,330 B) | **差分 0** |
| local | 自パッケージぶんだけ (371,905 B) — **陽性対照** | **差分 408** |

**陽性対照が 408/429 で落ちているので、比較器が差を見ていることは確認済み。**

理由はコードにある: `NameIndex::module_of` は **IR 由来の `known` を先に見る**
(`crates/litedoc4-render/src/autolink.rs:258`)。`range_of` は全リンクで引かれるが
(`autolink.rs:332`)、出力に出るのは `ExternalLinks` が**依存パッケージ**の URL を
解決したときだけ (`crates/litedoc4-render/src/external.rs:146-162`)。
自パッケージ内リンクでは引いた range は捨てられる。

→ **前節が名指しした「毎回動く 3.6%」は、そもそも誰も読んでいない。**
`renderKey` の鍵の粒度を変える (段 C の旧 C1) 必要はなく、**地図に書かなければよい**。
段 C は「割る」から「書かない」に置き換わった (→ 再抽出回数の調査 §6)。

### 1 モジュール変更の warm ベースライン (leg 4 セッション)【実測 2026-08-17】

生ログ `benchmarks/results/g3-baseline-onemod-2026-08-17.txt`。
同一セッション連続 10 回 (1 宣言を足す / 戻すの交互、各回 `lake build` 込み)、
`--jobs 4`、対象 422 モジュール。cold/warm の判定は `ready`。

| | 値 |
|---|---|
| `ready` (環境ロード) | 2.44〜3.29 s = **warm** |
| 1 モジュール変更の総時間 | **5.897〜6.662 s、中央値 約 6.24 s** (収束後 9 回) |
| 抽出器の `linkIndex` 位相 | 1.204〜1.698 s、中央値 **約 1.23 s** |
| 再抽出 / 再描画 / IR 全読み / 抽出器プロセス | **1 / 422 / 4.00 pass / 1** |

**leg 3 の 7.3118 s と直接は比べない — 暖機状態が違う。**
そして **leg 3 が段 C に置いた撤退ライン「6.5 s を切らなければ否定」は破棄した**:
このセッションの warm ベースラインが既に閾値の下にいる。**壁時計に閾値を置いたのが誤り**で、
判定は決定的な整数 (再描画ページ数 / `linkIndex` 位相) に移した。

### 段 C の結果 — **再描画 422 → 1、壁時計は動かない**【実測 2026-08-17】

生ログ `benchmarks/results/g3-stage-c-2026-08-17.txt` (before は 1 つ上の節)。
実装は commit `54239be` — 抽出器の `--link-index-omit <modules.txt>` で
自パッケージのモジュールの宣言群を地図に書かない。`@` 節は全部残す。

| | before | after |
|---|---:|---:|
| **再描画ページ数** | **422 / 422** | **1 / 422** |
| `link-index.lidx` の移動 | 2 行動く | **バイト不動** (sha256 `a259847bd2b7a2bf`) |
| 地図の宣言数 | 255,809 | 251,225 (自パッケージ 4,584 を除いた) |
| 地図のバイト数 | 10,448,228 | **10,098,730** |
| IR 全読み回数 | 4.00 pass | **4.00 pass** |
| 壁時計 (warm、中央値) | 6.24 s | **6.23 s** |

**オラクル**: 増分ツリーは**同じ IR の全描画とバイト一致** (差分 0)。
陽性対照 (編集前 IR との比較) は差分 4 で、比較器が差を見ていることを確認済み。
地図の 10,098,730 B は**切り分け実験の `deps` 変種と同一バイト数** — 別経路で同じ地図に着いた。

**予測が 2 つ外れた。結果が SoT。**

1. **IR 全読みは 4.00 pass のまま。** 4 pass は描画の枚数ではなく、増分機構そのもの
   (ownership / merge / global) が読んでいる。再描画を 1 枚にしても IR は 4 周読む。
2. **壁時計は動かない。** 422 ページの描画はこの対象で約 0.3〜0.5 s しかなく、
   6.2 s の内訳は環境ロード 2.7〜3.4 s と依存写像の書き出し 1.2〜1.8 s。
   **段 C の取り分は決定的な整数と「地図が動かなくなったこと」であって、秒ではない。**

**ゲート**: `tools/e2e-micro.sh` の GATE 6 (1 モジュール編集)。
**段 C を外した状態で exit 1、入れた状態で exit 0** を両方実走して確認した。

### 段 D の結果 — **抽出要求 2.09 s → 0.022 s、1 モジュール変更が −21.8%**【実測 2026-08-17】

生ログ `benchmarks/results/g3-stage-d-2026-08-17.txt`。実装は commit `c8c0fae`。
抽出器が `<地図>.key` の sidecar を持ち、**呼び手のトークン + `#lidx2` マーカー +
`@` 節が現環境と一致**したら 490,287 定数の走査ごと飛ばす。

| | baseline | 段 C | 段 D |
|---|---:|---:|---:|
| 抽出器が走査した定数 | 490,287 | 490,286 | **0** |
| 抽出器が書いた地図のバイト | 10,448,228 | 10,098,730 | **0** |
| 抽出器の `linkIndex` 位相 | 1.23 s | 1.53 s | **0.006 s** |
| 抽出要求の合計 | 2.09 s | 2.09 s | **0.022 s** |
| 再描画ページ数 | 422 | 1 | 1 |
| 1 モジュール変更 (warm 中央値、9 回) | 6.244 s | 6.228 s | **4.880 s** |

**baseline → 段 D で −1.364 s (−21.8%)。差は `linkIndex` 位相の減り (1.23 → 0.006 s) と
ほぼ一致**していて、辻褄が合う。オラクル (増分ツリー == 同じ IR の全描画) は差分 0 のまま、
陽性対照は 4。**フル生成では地図が無いので必ず書く — 段 D が効くのは増分だけ。**

**トークンに何を入れるかで 1 度誤った**【記録として残す】。最初の実装は `extractKey` の
5 値すべてを入れていたが、そのうち `irSchemaVersion` / `irGenerator` は
**IR を説明する値で地図とは無関係**、しかも後者は「ディスク上の IR を書いたのは誰か」の事実で
更新してはいけない値 (実装計画 §6)。入れていた副作用として **初回ビルド直後の 1 回だけ、
理由なく地図を書き直していた** (初回は `<ir>/index.json` がまだ無いので鍵が 3 値になる)。
トークンは `leanToolchain` / `manifestSha256` / `extractor` の 3 値 + omit 一覧のバイトに絞った。

**再利用の判定は 3 つ**で、1 つは後から足した: トークン (olean と omit 一覧) /
`@` 節 (モジュール集合) / **`#lidx2` マーカー (この地図自身の形式)**。
3 つ目が無いと、形式を変えても `EXTRACTOR_ID` を bump し忘れれば旧形式の地図を再利用しうる。

**ゲート**: GATE 6 に「抽出器が地図を書き直さず再利用したと言っているか」を追加。
`--link-index-key` を渡さない状態で **exit 1**、渡す状態で **exit 0** を両方実走。

### CI は段 C / 段 D を一度も走らせていなかった【実測 2026-08-17】

run `31962696064` / `31962863538`。`ci-template.yml` が作る package は run ごとに
決定的に生成されるので、2 run 目以降は**必ず無変更**
(`extract 0 / render 0 / requests 0`、`incremental in 0.0164 s`)。
**段 C/D が効くのは 1 モジュール変更のときだけ**なので、CI が見ていたのは
「無変更なら何もしない」だけ。**回帰は捕まえるが、取り分は測れない。**

計測用ワークフローに「1 モジュール編集してもう一度回す」段を足して塞いだ
(commit `e13897e`)。判定は `tools/onemod-gate.sh` の 1 本で、e2e と CI が同じものを呼ぶ。
**利用者に配る雛形は変えていない。** 生ログ `benchmarks/results/g3-ci-2026-08-17.txt`。

#### そのゲートが初回に落ちて出したもの — **段 C を跨ぐアップグレードは 1 回だけ全再描画する**

run `31963079828`【実測 2026-08-17】。新しい段が**最初の実走で落ちた**。原因は欠陥ではなく、
**キャッシュから復元された `docs-out` の地図が段 C 以前の形式だった**こと:

```
render-all linkIndex: the dependency map moved during this run (5b607769821c6e58 -> c03219d3c5f163be)
impact  mode all -> 13 page(s) (13 from the changed set, 0 from the global map)
work    extract 1 / render 13 / requests 1
```

古い地図は自パッケージの宣言群を持ち `.key` sidecar も無いので、
**最初の抽出でいまの形に書き直され、ダイジェストが動き、全ページが再描画される。**
**これは一度きりの移行コストで、利用者も同じものを払う** — 段 C を含む litedoc4 に
上げた後の最初の増分ビルドは、そのパッケージのページを全部書き直す。

ゲート側は正しかった (定常状態を問うている) ので、**直したのはハーネスの方**:
CI は 2 回編集して**2 回目**を測り、1 回目のログも残して移行が見えるようにした
(commit `611c4f3`)。**「ゲートを緩める」ではなく「定常状態に到達してから測る」に倒した。**

#### 2 度目に落ちて出したもの — **CI は段 C/D 以前の `litedoc4` を走らせていた**【実測 2026-08-17】

run `31963305864`。2 回目の編集でも地図が動き、`impact mode all -> 13 page(s)`。
抽出器は**その run で新しく建てられている** (`extractor 15.815 built`) のに、
`serve.out` に `omitted` も `reused` も出ない — つまり `--link-index-omit` も
`--link-index-key` も渡されていない。**渡す側の Rust が古かった。**

原因はキャッシュキーと `tools/ci-build.sh` の組み合わせ:

- ワークフローの Rust キャッシュは `cargo-${{ runner.os }}-${{ hashFiles('litedoc4/Cargo.lock') }}`。
  **ソースが動いても `Cargo.lock` は動かない**ので、前 run の `target/` がそのまま復元される。
- `ci-build.sh` は `if [ -x "$LITEDOC4_BIN" ]` で**バイナリがあれば cargo を呼ばなかった**。

結果、**ランナーは「今の抽出器 + 別 commit の litedoc4」で走っていた。**
`cargo` を必ず呼ぶように直した (commit `7859e5c`)。新鮮なツリーなら約 0.2 s。
抽出器側も `-nt` でソースの方が新しければ建て直すようにした
(こちらはキャッシュキーが `Extract.lean` を含むので CI では起きないが、手で叩くときに起きる)。

**leg 3 の CI 数字 (無変更 31.6 倍 / 再抽出 13 → 0) は影響を受けていない** —
`d5af924`〜`4590030` は `crates/` も `Cargo.lock` も触っていない【確認済】。

**この形は今日 3 度目**: e2e が `.lake/e2e-extract` のバイナリを無条件に使い回していた件、
`ci-build.sh` の 2 件。**「あるものを使う」は「今のソースのものを使う」ではない。**
3 件とも**出力は正常に見えていた**。捕まえたのは、そのどれでもなく
**1 モジュール変更という別の質問を投げるゲート**。

#### 3 度目で緑 — **Linux ランナー上で段 C / 段 D が効いた**【実測 2026-08-17】

run `31963581512`。生ログ `benchmarks/results/g3-ci-2026-08-17.txt`。

| | round 1 (形式移行) | round 2 (定常状態、ゲートが読む方) |
|---|---|---|
| impact | `mode all -> 13 page(s)` | **`mode self -> 1 page(s)`** |
| 再描画 | 13 / 13 | **1 / 13** |
| 抽出器の `linkIndex` | 書き直し | **0.001 s reused** |
| 増分ビルド | 1.1925 s | **0.9290 s** |

**−22%。M1 実測の −21.8% と一致する。** ただしこの package は 13 モジュールなので、
**倍率が対象の規模に依存しないという主張にはなっていない。**

### v0.1 のゲート B は段 C / 段 D の後も通る【実測 2026-08-17】

`tools/target2-gate.sh all` → **all checks passed**。
生ログ `benchmarks/results/g3-gate-b-2026-08-17.txt`。

| | |
|---|---|
| gate 1 一発・地図を渡さない | PASS (`litedoc4 site` の出力と byte 一致) |
| gate 2 決定性 | PASS (site も IR も) |
| gate 3 無変更 | PASS (23 ファイル、1 バイトも動かない) |
| gate 4 実変更 (モジュール移動) | PASS (site / IR / 台帳 / 地図がフル生成と byte 一致) |

**gate 4 で `impact mode all -> 14 page(s)` になるのは正しい。** モジュールが 1 本増えると
`env.header.moduleNames` が伸び、`.lidx` の `@` 節が変わり、地図のバイトが動く。
**段 C が安くするのは「既存モジュールの中身の編集」だけで、モジュールの増減は
全ページ再描画のまま** — `@` 節を全部残す選択の帰結で、設計どおり。

**主張しないこと**: 実在の公開パッケージでの実走。第 2 の対象は合成なので
**こちらが知らない形では驚かせてくれない** (実装計画 §1 の決定 2026-08-15)。
**未検証項目として残す。**

### 段 D 後の秒の全額割り当て【実測 2026-08-17】

生ログ `benchmarks/results/g3-attribution-2026-08-17.txt`。**新しいコードは要らなかった** —
`litedoc4 build --timings` は 4 段しか出さないが、`litedoc4 incremental --timings` は 10 段出す。
`incremental` は台帳を書かないので同じ 1 モジュール変更を何度でも測り直せる。

| 段 | 秒 | 比例するもの |
|---|---:|---|
| Lean の環境ロード (`ready`) | **2.363** | 既知の床 |
| `Server::start` の `lake env` + exec + 171 MB ページイン | **約 1.14** | **何にも比例しない** |
| `lake env lean --githash` (**clock の外**) | **0.763** | **何にも比例しない** |
| detect: olean 228 MB の SHA-256 | 0.463 | パッケージ規模 |
| ownership | 0.309 | パッケージ規模 |
| render (1 ページ) | 0.132 | 変更規模 |
| impact | **0.078** | パッケージ規模だが安い |
| merge / global / 抽出器の要求 | 0.069 / 0.023 / 0.022 | |
| **合計 5.359** | | wall 5.33 と一致 — **全額説明がついた** |

**次に大きいのは `lake env` を 2 回叩いていること (合計約 1.90 s)。**
床 2.363 s を除いた残り約 3.0 s の 63%。
**`impact` は 0.078 s しかない** — IR を 422 件読んでいるのは事実だが秒としては効いていない。
**「4 周読んでいる」を秒の問題として扱う根拠は無い** (測る前は最有力候補だった)。

### 段 E — 核の githash は `lake env` 抜きで聞ける【実測 2026-08-17】

生ログ `benchmarks/results/g3-stage-e-2026-08-17.txt`。実装は commit `04fb36b`。
`resolve_external_links` の `lake env lean --githash` を `lean --githash` に替えた。
elan の `lean` shim は `lake env lean` が最終的に呼ぶのと同じ shim で、
同じ `lean-toolchain` を作業ディレクトリから解決する。

| | 対象 (lean-projects) | 第 2 の対象 |
|---|---|---|
| `lake env lean --githash` | `68218e87…` **0.84 s** | `68218e87…` **0.98 s** |
| `lean --githash` | `68218e87…` **0.03 s** | `68218e87…` **0.03 s** |

**陽性対照**: `lean-toolchain` の無いディレクトリでは shim が
`no default toolchain configured` で失敗する — 一致は「たまたま同じ既定」ではなく
**同じ toolchain を同じ経路で解決している**こと。

第 2 の対象で end-to-end: **wall 中央値 2.87 → 1.94 s (−0.94 s)**、site 23 ファイルと
`.lidx` はバイト一致。**`build full in` は動かない** — この呼び出しが `build` 自身の
clock の**外**にあるという上の割り当てがそのまま裏付けられた。
**内側の秒だけ見ていたら、この改善は 1 度も見えなかった。**

対象 (422 モジュール) でも 1 モジュール変更の交互 A/B を回した (順番を入れ替えて 4 往復)。
`outside` = wall − `build … in X s` = 置き換えた呼び出し + 引数処理:

| | before (段 D) | after (段 E) |
|---|---|---|
| **outside 中央値** | **0.780 s** (4 回とも 0.756〜0.814) | **0.0345 s** (4 回とも 0.033〜0.038) |
| wall 中央値 | 5.96 s | 4.59 s |

**取り分は outside の −0.745 s。ここは暖機の影響を受けない** — 同じ子プロセスを起こすか
どうかの差で、両者とも毎 run 同じ仕事をする。**wall の差 1.37 s をそのまま引用しない**:
inside が before で 3.97〜6.25 s と散っており (page cache)、その散らばりが乗っている。

**この計測は 1 度落ちている**【記録】 — ディスクが満杯になり、**中断された `lake build` が
対象の olean を 1 つ欠落させた**上に、シェルコマンド自体が動かなくなった
(ハーネスが出力ファイルを作れない)。空きを作って `lake build InformationTheory` で復旧
(3,769 jobs)、ソースツリー clean を確認してから測り直した。
規則は CLAUDE.md「ベンチマーク」に追加。

### 段 F — 台帳の olean ハッシュが 1 本ずつだった【実測 2026-08-17】

生ログ `benchmarks/results/g3-stage-f-2026-08-17.txt`。
`detect` は 422 モジュールの olean **228,448,584 B** を読んでハッシュするのに
`concurrency: 1` が直書きされていた。**理由は無く、測られてもいなかった**
(コメントは「台帳のバイトはこれに依存しない (M3-a 実測)、速度は依存する、フラグは後で」)。

| concurrency | hashSeconds |
|---:|---:|
| 1 | **0.500** |
| 2 | **0.068** |
| 4 | 0.037 |
| 8 | 0.029 |

**1 → 2 で 7.4 倍**は 2 スレッドの CPU では説明がつかない — 仕事は mmap した olean を
読むことなので、2 本目は**演算ではなく page fault の待ちを隠している**。

**バイト同一性を主張そのもので検査した**: `ledger build --concurrency 1` と `8` の出力は
**IDENTICAL (139,190 B)**、hashSeconds は 0.436 → 0.027 s (16 倍)。

end-to-end (`incremental --timings` の `detectSeconds`): **0.463 → 0.042 s (−0.42 s)**、
1 モジュール変更 約 4.6 s の **9%**。既定は `available_parallelism()` を [1, 8] に丸める。

**ゲート**: `cargo test` 346/0/21、clippy 無警告、e2e 緑、`target2-gate.sh all` =
all checks passed。CI (run `31978156708`) も緑で、定常状態は
`impact mode self -> 1 page(s)` / `linkIndex 0.001s reused` / `incremental in 1.1884 s`。

### 「1 モジュール変更の IR 全読みは 3.00 回」は模擬の値だった【2026-08-17 に判明】

`docs/approach.md` と実装ログが持っていた **3.00 回** (1,268 / 422、
実測 2026-08-16) は **`ledger touch` で台帳だけ古くした模擬**の値。
**本物のソース編集 (宣言を 1 本足す) では 4.00 回** (1,690 / 422)【実測 2026-08-17、
今日の全 run で一貫】。

**差はちょうど 422。正体は `ownership` の base 側全走査**で、これは
**全域写像に名前が出入りしたときだけ**走る (`watching`)。`ledger touch` は IR を
1 バイトも動かさないので名前が 1 つも動かず、この 1 周がまるごと出ない。

**模擬と本物で 1 パス違う。** approach.md 自身が「シナリオを言わずに N 回と書かない」と
書いている、その実例が自分の数字に出ていた。approach.md 側に但し書きを入れた。

---

### CI の cargo キャッシュが CI を 2 倍遅くしていた【実測 2026-08-19】

生ログ: `benchmarks/results/ci-cache-2026-08-19.txt`。

`ci.yml` の Rust キャッシュは `restore-keys: cargo-${{ runner.os }}-` を持ち、
path に `target` を丸ごと入れていた。この組み合わせは**単調増加する** — 新しい
`Cargo.lock` は前世代の `target/` を復元し、その上に建て、和集合を保存するため。
**72 MB → 357 → 560 → 884 → 1208 → 1530 → 1854 → 2177 MB を 3 日で**、
9 世代が同時に生きて **8.47 GiB**。リポジトリ全体は **9.88 GiB = 10 GiB 上限の
98.8%** で、GitHub は超過分を LRU で**黙って**追い出す (エラーは出ず、
キャッシュミス率だけが上がる)。

**症状は「遅い」ではなく「復元がビルドより長い」**:

| run | 復元 | Format+Clippy+Rustdoc+Test |
|---|---|---|
| 32149030757 | **63 s** | 14 s |
| 32204121981 | 33 s | 40 s |

`Cargo.lock` は **34 パッケージ** (うち 6 は自前クレート。外部は serde /
serde_json / sha2 / indexmap / libc とその支持クレートだけで、tokio も clap も
regex も無い)。**この木では 2.2 GB を復元するより全部建て直す方が速い。**

同じキーを **3 ジョブが共有していた** — `ci.yml` の `test` (`--all-targets`) と
`e2e-micro` (1 バイナリ)、`ci-template.yml` (別パスの `target`)。
**先に保存したジョブが、他のジョブが復元する中身を決めていた。**

直したのは 4 点: `restore-keys` の削除 / キーにジョブ名 / キーに
`rust-toolchain.toml` / `CARGO_INCREMENTAL: 0` (手元で
`target/debug/incremental` が 7.2 GB 中 **3.5 GB**。CI は復元したキャッシュから
1 回建てて捨てるので読み返されない)。結果 **160 MB (-93%)、復元 3-9 s、
リポジトリ全体 1.23 GiB**。

**直した後、キャッシュ自体の効果は測れない**【実測、warm/cold n=4 ずつ、
ソースは全 run 同一】: `test` は平均が一致 (61.0 s / 61.0 s)。復元に払う 5.25 s と
ビルドで返る 3.00 s が相殺する。**ノイズ床は対照群が与えた** — cargo キャッシュに
一切触れない `supply-chain` ジョブが warm/cold で **3.4 s** 違う。3.5 s 未満は
この計測では検出できず、キャッシュの効果はその下。ラチェットの 63 s は
ノイズ床のはるか上だったので、そちらだけが実在の問題だった。

**キャッシュは残す。** 「効果が測れない」は「外すと速くなる」ではない。
ひっくり返す条件は生ログ §5 に置いた (依存が増える / 復元が 10 s を超える)。

**`CARGO_INCREMENTAL: 0` は dev profile を建てる 4 本にだけ置いた** —
`--release` は cargo が既定で incremental を切るので、残り 5 本では
**変数が何もしない**。効かない場所に「3.5 GB 減る」と書いたコメントを
残す方が、書かないより悪い。

---

### M10 step E — what leaves unasked when `crates/` goes (2026-09-02)

The 21 `#[ignore]`d tests of `tools/corpus-tests.txt` all live under `crates/`. Every one was
read and matched against the checks that survive. **7 have no surviving home, not the 2 an
earlier leg recorded** (measured 2026-09-02, by reading the 21 bodies; the earlier number came
from reading two of them). The other 14 lose nothing: **10 die with an input** that only the
removed prototype could write (`tag experiments-frozen`, or a `/private/tmp` tree the sweeps
emptied), and **4 are asked again** — by `purelean-render-gate.sh` items 2/4/7, by
`build-gate.sh`'s `gate1`/`gate4`, and by two Lean tests
(`Litedoc4Test.theCacheSequenceAgreesWithAFromScratchBuild`,
`Litedoc4Test.theTwoUrlsThePlanQuotesComeOutOfTheMap`).

**Two were placed.** `base_ir::reads_every_module_of_the_target_package` and
`base_ir::astral_binders_slice_correctly` are now `tools/base-ir-gate.sh` (`manual`). Its
expectations are not frozen: the extractor's index is compared against the extractor's own module
files, and `litedoc4 global` is compared against that enumeration — the constraint the Rust
header carried, that the expected values must never come from the reader. 1,674,710 checks over
the 422-module target, 3.7 s (measured 2026-09-02).

**The Rust exact counts were already dead when they were carried across.** They sat behind a
guard that fired only for a tree with `moduleCount` 432 and `declarationCount` 4,750; the target
has been 422 modules since 2026-08-31, so both tests had been running their structural half alone
and `astral_binders_slice_correctly` had been asserting nothing at all.

**Five were not placed, and they are a list of defects not yet known rather than work remaining:**

| what it asks | what it needs | how fragile |
|---|---|---|
| `pages::pages_carry_the_doc_gen4_trees_declarations` — the rendered pages carry doc-gen4's own anchor set, order, module docstrings and source files | the target's `.lake/build/doc` (6,080 pages, present 2026-09-02) + an IR | **doc-gen4 is not in the target's manifest** (10 packages, none of them doc-gen4), so `lake update` there destroys the oracle for good |
| `packages::every_root_matches_doc_gen4s_own_blob_urls` — every resolved root's URL is the one doc-gen4 wrote | same tree + `lake --githash` | same. `Lake`'s `src/lake` prefix is checked by nothing else in the tree |
| `packages::every_lidx_entry_matches_doc_gen4s_declaration_urls` — every `.lidx` name's source URL is doc-gen4's | same tree + a `.lidx` + a ~41 MB TSV from `benchmarks/tools/extract-decl-source-urls.sh` | same |
| `global::corpus_facts_match_the_prototype` — the facts serialise to the prototype's bytes | an IR only; **the expectation is committed** (`fixtures/global/global-expected.json`) | not fragile — rebuildable whenever |
| `merge::the_corpus_matches_the_prototype` — 9 merge rounds over the real base IR | a base IR + `tools/merge-reference.sh`, which survives; **expectation committed** (`fixtures/incr/merge-expected.json`) | not fragile |

**These three doc-gen4 comparisons are the only checks in the tree that hold litedoc4 against an
implementation nobody here wrote.** Everything else is self-consistency, an invariant, or a
fixture minted from this repository's own output.

**`tools/corpus-gate.sh` cannot survive its subject.** With the 21 rows deleted it **exits 1
printing nothing** — `entries`' `grep -v` finds no line and `set -e` kills the command
substitution (measured 2026-09-02, by stubbing `listed` to answer as cargo would with no test
targets); with the rows kept it prints 21 `in the inventory but not ignored` lines and is red for
ever. There is no third arrangement in which it asks a question, so it goes with `crates/`,
together with `tools/corpus-tests.txt` and its `ci.yml` step.

**And it already has the shape it would end in.** On this machine `--verify-list` **exits 101
printing nothing at all** (measured 2026-09-02): `listed()` runs `cargo test --workspace --no-run`
with `2>/dev/null`, and that command dies in `crates/litedoc4-render`'s `build.rs`, which runs
npm — `node` on PATH here is a 2023 build the kernel SIGKILLs. CI is green because the job has
`setup-node`. So the one gate that would be left holding an empty inventory is already a gate
whose output and exit code disagree, which is the shape this repository keeps being bitten by.

**`fixtures/render/**`, `fixtures/global/**` and `fixtures/incr/**` lose their last reader**;
only `fixtures/md/fuzz/` keeps one (`fuzz/fuzz_targets/docstring.rs`). Two of them are the
committed expectations of the two rebuildable questions above, so they are not decoration yet —
but they are data with no consumer until something asks again.

## 書き方

段階ごとに 1 節を足す。各節に必ず入れるもの:

- 日付と、そのときの計測条件 (機材 / 各種バージョン / 並列度 / 暖機状態)
- 数字とラベル、対応するログのパス
- **判断** — 次に進んでよいか。否定されたなら、計画のどこを見直すか
- 予測と食い違った場合は、`approach.md` のどの行を直したか
